{
  int size=8 + REFERENCE_SIZE * 2;
  for (  int rels : new int[]{nrOut,nrIn,nrLoop}) {
    if (rels > 0) {
      size+=withObjectOverhead(withReference(withArrayOverhead(4 * (rels + 1))));
    }
  }
  if (nrLoop > 0) {
    size+=REFERENCE_SIZE;
  }
  return withObjectOverhead(size);
}
