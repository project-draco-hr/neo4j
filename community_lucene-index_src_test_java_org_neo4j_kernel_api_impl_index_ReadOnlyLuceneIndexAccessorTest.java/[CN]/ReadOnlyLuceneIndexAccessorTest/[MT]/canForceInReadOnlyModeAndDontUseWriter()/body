{
  try (LuceneIndexAccessor luceneIndexAccessor=getNonUniqueLuceneIndexAccessor(directoryFactory,indexDirectory,documentStructure,true,writerFactory)){
    verifyNoMoreInteractions(writerFactory);
    luceneIndexAccessor.force();
    verifyNoMoreInteractions(writerFactory);
  }
 }
