{
  StringBuffer buf=new StringBuffer();
  buf.append("NodeRecord[").append(getId()).append(",").append(inUse()).append(",").append(nextRel).append(",").append(nextProp).append("]");
  return buf.toString();
}
