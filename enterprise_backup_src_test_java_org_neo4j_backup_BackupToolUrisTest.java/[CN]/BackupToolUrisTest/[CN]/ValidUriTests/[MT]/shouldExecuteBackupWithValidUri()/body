{
  String[] args=new String[]{"-from",uri,"-to","/var/backup/graph"};
  newBackupTool().run(args);
  verify(backupService).doIncrementalBackupOrFallbackToFull(eq(host),eq(port),eq("/var/backup/graph"),eq(true),any(Config.class));
}
