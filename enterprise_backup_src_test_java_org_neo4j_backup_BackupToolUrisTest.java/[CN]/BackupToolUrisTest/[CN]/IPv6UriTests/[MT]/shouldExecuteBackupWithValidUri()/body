{
  String[] args=new String[]{"-host",host,"-port",String.valueOf(port),"-to","/var/backup/graph"};
  newBackupTool().run(args);
  verify(backupService).doIncrementalBackupOrFallbackToFull(eq(host),eq(port),eq("/var/backup/graph"),eq(true),any(Config.class),eq(BackupClient.BIG_READ_TIMEOUT));
}
