{
  final StartupHealthCheck parentCheck=super.createHealthCheck();
  return new StartupHealthCheck(new Neo4jHAPropertiesMustExistRule()){
    @Override public boolean run(){
      return parentCheck.run() && super.run();
    }
  }
;
}
