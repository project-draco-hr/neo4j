{
  String indexName="kvnode";
  helper.createNodeIndex(indexName);
  DocsGenerator.create("Delete node index").expectedStatus(Response.Status.NO_CONTENT).delete(functionalTestHelper.indexNodeUri(indexName));
}
