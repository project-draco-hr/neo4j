{
  String indexName="empty-index";
  helper.createNodeIndex(indexName);
  ClientResponse response=CLIENT.resource(functionalTestHelper.indexNodeUri(indexName,"non-existent-key","non-existent-value")).accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  assertEquals(200,response.getStatus());
  response.close();
}
