{
  gen.create().payload("{\"name\":\"fulltext\", \"config\":{\"type\":\"fulltext\",\"provider\":\"lucene\"}}").expectedStatus(Response.Status.CREATED).expectedHeader("Location").post(functionalTestHelper.nodeIndexUri());
  assertEquals(1,helper.getNodeIndexes().length);
  assertEquals("fulltext",helper.getNodeIndexes()[0]);
}
