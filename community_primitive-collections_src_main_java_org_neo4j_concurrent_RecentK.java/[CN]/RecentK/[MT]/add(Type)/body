{
  Slot slot=index.get(item);
  if (slot != null) {
    markAsMostRecent(slot);
  }
 else   if (index.size() >= maxItems) {
    removeLeastRecent();
    addNewItem(item);
  }
 else {
    addNewItem(item);
  }
}
