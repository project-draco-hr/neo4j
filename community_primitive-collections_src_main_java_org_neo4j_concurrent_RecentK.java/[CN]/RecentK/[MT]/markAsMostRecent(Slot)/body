{
  if (slot == head) {
    return;
  }
  if (slot == tail) {
    tail=slot.prev;
    slot.prev.next=slot.next;
  }
 else {
    slot.prev.next=slot.next;
    slot.next.prev=slot.prev;
  }
  slot.prev=null;
  slot.next=head;
  head=slot;
}
