{
  raft.handle(appendEntriesRequest().from(otherMember).leaderTerm(0).leader(myself).prevLogIndex(-1).prevLogTerm(-1).logEntry(new RaftLogEntry(0,data)).build());
  verify(outbound).send(same(otherMember),eq(appendEntriesResponse().from(myself).term(0).success().build()));
}
