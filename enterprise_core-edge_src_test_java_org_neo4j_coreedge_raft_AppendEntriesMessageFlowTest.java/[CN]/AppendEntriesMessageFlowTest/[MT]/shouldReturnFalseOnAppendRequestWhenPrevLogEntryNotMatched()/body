{
  raft.handle(appendEntriesRequest().from(otherMember).leaderTerm(0).leader(myself).prevLogIndex(0).prevLogTerm(0).logEntry(new RaftLogEntry(0,data)).build());
  verify(outbound).send(same(otherMember),eq(appendEntriesResponse().from(myself).term(0).failure().build()));
}
