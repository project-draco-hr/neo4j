{
  return new Predicate<CommittedTransactionRepresentation>(){
    @Override public boolean accept(    CommittedTransactionRepresentation transaction){
      return transaction.getCommitEntry().getTxId() <= upToAndIncludingTxId;
    }
  }
;
}
