{
  for (  Record<K,V> bucket : buckets) {
    while (bucket != null) {
      if (bucket.value == value || bucket.value.equals(value)) {
        return true;
      }
      bucket=bucket.next;
    }
  }
  return false;
}
