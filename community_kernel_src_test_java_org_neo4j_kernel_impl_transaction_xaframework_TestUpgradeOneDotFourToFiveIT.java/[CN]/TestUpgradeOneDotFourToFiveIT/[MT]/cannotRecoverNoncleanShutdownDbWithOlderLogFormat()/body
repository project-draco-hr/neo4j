{
  copyRecursively(new File(TestUpgradeOneDotFourToFiveIT.class.getResource("non-clean-1.4.2-db/neostore").getFile()).getParentFile(),PATH);
  XaLogicalLog log=new XaLogicalLog(resourceFile(),null,null,null,defaultLogBufferFactory(),defaultFileSystemAbstraction(),DEV_NULL,LogPruneStrategies.NO_PRUNING,TransactionStateFactory.NO_STATE_FACTORY);
  log.open();
  fail("Shouldn't be able to start");
}
