{
  for (  StackTraceElement stack : getAllStackTraces().get(currentThread())) {
    if (stack.getClassName().equals(cls.getName()) && stack.getMethodName().equals(method))     return true;
  }
  return false;
}
