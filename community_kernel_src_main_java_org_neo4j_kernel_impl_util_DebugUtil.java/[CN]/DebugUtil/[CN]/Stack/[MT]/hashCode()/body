{
  int hashCode=stackTrace.getMessage() == null || !considerMessage ? 31 : stackTrace.getMessage().hashCode();
  for (  StackTraceElement element : stackTrace.getStackTrace())   hashCode=hashCode * 9 + element.hashCode();
  return hashCode;
}
