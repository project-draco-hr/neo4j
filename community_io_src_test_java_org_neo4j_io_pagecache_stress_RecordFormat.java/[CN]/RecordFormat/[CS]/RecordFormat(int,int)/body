{
  this.numberOfThreads=numberOfThreads;
  this.cachePageSize=cachePageSize;
  this.fieldSize=Long.BYTES;
  this.checksumFieldOffset=numberOfThreads * fieldSize;
  this.recordSize=checksumFieldOffset + fieldSize;
}
