{
  IndexHits<Node> hits=indexService.getNodes(key,value);
  try {
    for (    Node hit : hits) {
      if (hit.equals(node)) {
        return true;
      }
    }
    return false;
  }
  finally {
    hits.close();
  }
}
