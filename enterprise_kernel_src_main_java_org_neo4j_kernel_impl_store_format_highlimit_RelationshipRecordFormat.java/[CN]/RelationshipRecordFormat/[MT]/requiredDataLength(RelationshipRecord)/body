{
  return Short.BYTES + length(record.getNextProp(),NULL) + length(record.getFirstNode())+ length(record.getSecondNode())+ length(record.getFirstPrevRel())+ length(record.getFirstNextRel(),NULL)+ length(record.getSecondPrevRel())+ length(record.getSecondNextRel(),NULL);
}
