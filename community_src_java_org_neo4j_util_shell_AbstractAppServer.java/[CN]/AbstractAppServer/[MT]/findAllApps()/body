{
  Collection<? extends Class<?>> apps=ClassLister.listClassesExtendingOrImplementing(App.class,this.packages);
  Set<String> set=new TreeSet<String>();
  for (  Class<?> app : apps) {
    set.add(app.getSimpleName().toLowerCase());
  }
  return set;
}
