{
  Class<? extends App> app=this.apps.get(command);
  if (app == null) {
    return null;
  }
  App result=null;
  try {
    result=app.newInstance();
    ((AbstractApp)result).setServer(this);
  }
 catch (  Exception e) {
  }
  return result;
}
