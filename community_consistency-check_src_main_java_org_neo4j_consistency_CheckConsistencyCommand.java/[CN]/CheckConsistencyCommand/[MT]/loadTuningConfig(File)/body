{
  if (tuningConfigFile == null) {
    return new HashMap<>();
  }
  try {
    return MapUtil.load(tuningConfigFile);
  }
 catch (  IOException e) {
    throw new IllegalArgumentException(String.format("Could not read configuration file [%s]",tuningConfigFile),e);
  }
}
