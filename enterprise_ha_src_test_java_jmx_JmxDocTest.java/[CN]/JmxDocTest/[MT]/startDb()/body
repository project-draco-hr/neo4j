{
  File storeDir=dir.graphDbDir(true);
  CreateEmptyDb.at(storeDir);
  db=new HighlyAvailableGraphDatabaseFactory().newHighlyAvailableDatabaseBuilder(storeDir.getAbsolutePath()).setConfig(ClusterSettings.server_id,"1").setConfig("jmx.port","9913").newGraphDatabase();
}
