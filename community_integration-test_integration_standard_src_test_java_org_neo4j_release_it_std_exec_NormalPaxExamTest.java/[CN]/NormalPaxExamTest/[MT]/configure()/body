{
  return options(systemProperty("hello.name").value("world"),provision(mavenBundle().groupId("org.ops4j.pax.url").artifactId("pax-url-mvn").version("1.1.2")),vmOption("-d64 -server -Xms1G -Xmx1G -verbose:gc"),felix());
}
