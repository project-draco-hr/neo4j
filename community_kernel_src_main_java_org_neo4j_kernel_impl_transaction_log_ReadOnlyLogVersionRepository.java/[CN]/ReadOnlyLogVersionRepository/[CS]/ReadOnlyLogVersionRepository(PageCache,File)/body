{
  File neoStore=new File(storeDir,NeoStore.DEFAULT_NAME);
  this.logVersion=NeoStore.isStorePresent(pageCache,storeDir) ? NeoStore.getRecord(pageCache,neoStore,NeoStore.Position.LOG_VERSION) : 0;
}
