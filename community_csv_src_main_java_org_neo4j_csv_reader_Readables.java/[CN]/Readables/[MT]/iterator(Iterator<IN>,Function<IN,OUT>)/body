{
  return new Iterator<OUT>(){
    @Override public boolean hasNext(){
      return items.hasNext();
    }
    @Override public OUT next(){
      return converter.apply(items.next());
    }
    @Override public void remove(){
      items.remove();
    }
  }
;
}
