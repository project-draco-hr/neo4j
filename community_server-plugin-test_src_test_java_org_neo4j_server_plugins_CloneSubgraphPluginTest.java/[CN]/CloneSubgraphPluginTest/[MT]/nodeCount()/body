{
  Transaction tx=server.getDatabase().getGraph().beginTx();
  try {
    int count=0;
    for (    @SuppressWarnings("unused") Node node : GlobalGraphOperations.at(server.getDatabase().getGraph()).getAllNodes()) {
      count++;
    }
    return count;
  }
  finally {
    tx.finish();
  }
}
