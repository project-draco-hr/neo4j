{
  super.setUp();
  Logger log=Logger.getLogger("org.neo4j.impl.persistence.PersistenceLayerMonitor");
  level1=log.getLevel();
  log.setLevel(Level.OFF);
  log=Logger.getLogger("org.neo4j.impl.core.NeoConstraintsListener");
  level2=log.getLevel();
  log.setLevel(Level.OFF);
}
