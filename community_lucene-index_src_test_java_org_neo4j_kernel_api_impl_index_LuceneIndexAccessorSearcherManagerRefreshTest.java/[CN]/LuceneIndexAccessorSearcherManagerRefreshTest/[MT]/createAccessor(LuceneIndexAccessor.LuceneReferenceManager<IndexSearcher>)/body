{
  PartitionedIndexStorage indexStorage=new PartitionedIndexStorage(mock(DirectoryFactory.class),mock(FileSystemAbstraction.class),dir,1);
  return new LuceneIndexAccessor(structure,writer,manager,indexStorage,42){
  }
;
}
