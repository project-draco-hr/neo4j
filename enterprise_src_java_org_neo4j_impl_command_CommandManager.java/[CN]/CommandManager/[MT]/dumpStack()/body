{
  System.out.print("Commands in stack: ");
  java.util.Iterator itr=commandStack.keySet().iterator();
  if (!itr.hasNext()) {
    System.out.println("NONE");
  }
 else {
    System.out.println();
  }
  while (itr.hasNext()) {
    Thread thread=(Thread)itr.next();
    System.out.println("" + thread + "->"+ commandStack.get(thread));
  }
  System.out.println("TransactionCache size: " + TransactionCache.size());
}
