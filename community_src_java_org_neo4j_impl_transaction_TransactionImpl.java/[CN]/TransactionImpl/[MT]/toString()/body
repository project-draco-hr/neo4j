{
  StringBuffer txString=new StringBuffer("Transaction[Status=" + TxManager.getManager().getTxStatusAsString(status) + ",ResourceList=");
  Iterator itr=resourceList.iterator();
  while (itr.hasNext()) {
    txString.append(itr.next().toString());
    if (itr.hasNext()) {
      txString.append(",");
    }
  }
  return txString.toString();
}
