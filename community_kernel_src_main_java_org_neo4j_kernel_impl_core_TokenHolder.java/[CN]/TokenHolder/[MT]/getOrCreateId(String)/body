{
  Integer id=nameToId.get(name);
  if (id != null) {
    return id;
  }
  try {
    id=createToken(name);
    return id;
  }
 catch (  Throwable e) {
    throw new TransactionFailureException("Could not create token",e);
  }
}
