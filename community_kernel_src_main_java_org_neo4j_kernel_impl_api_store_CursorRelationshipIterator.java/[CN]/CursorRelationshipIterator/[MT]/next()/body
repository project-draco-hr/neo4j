{
  if (hasNext) {
    try {
      id=cursor.getId();
      type=cursor.getType();
      startNode=cursor.getStartNode();
      endNode=cursor.getEndNode();
      return id;
    }
  finally {
      hasNext=nextCursor();
    }
  }
 else {
    throw new NoSuchElementException();
  }
}
