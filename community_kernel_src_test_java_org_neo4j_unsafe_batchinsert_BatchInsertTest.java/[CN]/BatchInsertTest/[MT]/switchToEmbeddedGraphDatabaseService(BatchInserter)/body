{
  inserter.shutdown();
  TestGraphDatabaseFactory factory=new TestGraphDatabaseFactory();
  factory.setFileSystem(fs.get());
  return factory.newImpermanentDatabaseBuilder(inserter.getStoreDir()).setConfig(configuration()).newGraphDatabase();
}
