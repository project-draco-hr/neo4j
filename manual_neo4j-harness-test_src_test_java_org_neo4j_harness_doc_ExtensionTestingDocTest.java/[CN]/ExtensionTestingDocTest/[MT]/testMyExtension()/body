{
  try (ServerControls server=TestServerBuilders.newInProcessBuilder().withExtension("/myExtension",MyUnmanagedExtension.class).newServer()){
    HTTP.Response response=HTTP.GET(server.httpURI().resolve("myExtension").toString());
    assertEquals(200,response.status());
  }
 }
