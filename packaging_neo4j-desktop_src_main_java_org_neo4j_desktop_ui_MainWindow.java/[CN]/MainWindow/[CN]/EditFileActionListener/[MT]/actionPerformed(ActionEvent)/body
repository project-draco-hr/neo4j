{
  File file=getFile();
  if (null == file) {
    showMessageDialog(frame,"Did not find location of .vmoptions file","Error",ERROR_MESSAGE);
  }
  try {
    ensureFileAndParentDirectoriesExists(file);
    environment.editFile(file);
  }
 catch (  IOException e) {
    e.printStackTrace();
    showMessageDialog(frame,format("Couldn't open %s, please open the file manually",file.getAbsolutePath()),"Error",ERROR_MESSAGE);
  }
}
