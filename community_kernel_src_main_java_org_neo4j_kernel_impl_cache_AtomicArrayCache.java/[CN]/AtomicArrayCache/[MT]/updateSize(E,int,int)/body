{
  int pos=getPosition(obj);
  E oldObj=cache.get(pos);
  if (oldObj != obj) {
    return;
  }
  long size=currentSize.addAndGet((sizeAfter - sizeBefore));
  if (size > maxSize) {
    purgeFrom(pos);
  }
}
