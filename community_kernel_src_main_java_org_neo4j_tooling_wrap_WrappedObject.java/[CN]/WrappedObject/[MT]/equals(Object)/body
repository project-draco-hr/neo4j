{
  if (this == obj)   return true;
  if (getClass().isInstance(obj)) {
    WrappedObject<?> other=(WrappedObject<?>)obj;
    if (wrapped == null ? other.wrapped == null : wrapped.equals(other.wrapped))     return graphdb.equals(other.graphdb);
  }
  return false;
}
