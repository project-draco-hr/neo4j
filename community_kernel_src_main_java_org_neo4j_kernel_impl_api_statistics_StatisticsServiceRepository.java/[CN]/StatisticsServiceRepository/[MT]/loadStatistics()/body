{
  SamplingStatisticsService runtime=SamplingStatisticsService.load(this.fs,statisticsFile(),store,scheduler);
  if (config.get(statistics_enabled)) {
    return runtime;
  }
 else {
    return new StaleStatisticsService(runtime);
  }
}
