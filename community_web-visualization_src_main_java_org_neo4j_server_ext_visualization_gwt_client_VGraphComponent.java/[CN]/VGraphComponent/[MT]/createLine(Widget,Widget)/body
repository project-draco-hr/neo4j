{
  Line line=new Line(getCenterX(node1),getCenterY(node1),getCenterX(node2),getCenterY(node2));
  canvas.add(line);
  mapRelationship(linesFrom,node1,line);
  mapRelationship(linesTo,node2,line);
  return line;
}
