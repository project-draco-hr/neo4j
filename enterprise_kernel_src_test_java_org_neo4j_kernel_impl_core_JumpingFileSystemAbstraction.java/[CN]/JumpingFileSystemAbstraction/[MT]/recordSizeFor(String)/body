{
  if (fileName.endsWith("nodestore.db")) {
    return 9;
  }
 else   if (fileName.endsWith("relationshipstore.db")) {
    return 33;
  }
 else   if (fileName.endsWith("propertystore.db.strings") || fileName.endsWith("propertystore.db.arrays")) {
    return 133;
  }
 else   if (fileName.endsWith("propertystore.db")) {
    return 25;
  }
  throw new IllegalArgumentException(fileName);
}
