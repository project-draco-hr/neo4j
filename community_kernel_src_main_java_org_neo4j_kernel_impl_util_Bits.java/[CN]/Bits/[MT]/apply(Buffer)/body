{
  int readPositionBefore=readPosition;
  readPosition=0;
  try {
    int rest=numberOfBytes;
    while (rest-- > 0) {
      buffer.put(getByte());
    }
    return this;
  }
  finally {
    readPosition=readPositionBefore;
  }
}
