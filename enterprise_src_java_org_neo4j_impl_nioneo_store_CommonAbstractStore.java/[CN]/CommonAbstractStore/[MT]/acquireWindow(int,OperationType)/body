{
  if (!isInRecoveryMode() && position > idGenerator.getHighId()) {
    throw new IOException("Illegal position[" + position + "] high id["+ idGenerator.getHighId()+ "]");
  }
  return windowPool.acquire(position,type);
}
