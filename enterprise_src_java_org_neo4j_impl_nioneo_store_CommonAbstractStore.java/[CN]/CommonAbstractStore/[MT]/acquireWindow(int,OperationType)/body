{
  if (!isInRecoveryMode() && (position > idGenerator.getHighId() || !storeOk)) {
    throw new StoreFailureException("Position[" + position + "] requested for operation is high id["+ idGenerator.getHighId()+ "] or store is flagged as dirty["+ storeOk+ "]");
  }
  return windowPool.acquire(position,type);
}
