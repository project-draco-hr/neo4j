{
  ClientResponse response=Client.create().resource(server.baseUri()).type(MediaType.APPLICATION_JSON).accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  assertThat(response.getStatus(),is(not(404)));
}
