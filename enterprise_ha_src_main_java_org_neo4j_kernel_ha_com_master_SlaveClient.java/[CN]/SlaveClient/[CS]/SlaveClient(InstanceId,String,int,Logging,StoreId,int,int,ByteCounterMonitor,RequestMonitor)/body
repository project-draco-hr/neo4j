{
  super(hostNameOrIp,port,logging,storeId,Protocol.DEFAULT_FRAME_LENGTH,new ProtocolVersion(SlaveServer.APPLICATION_PROTOCOL_VERSION,INTERNAL_PROTOCOL_VERSION),HaSettings.read_timeout.apply(Functions.<String,String>nullFunction()),maxConcurrentChannels,chunkSize,byteCounterMonitor,requestMonitor);
  this.machineId=machineId;
}
