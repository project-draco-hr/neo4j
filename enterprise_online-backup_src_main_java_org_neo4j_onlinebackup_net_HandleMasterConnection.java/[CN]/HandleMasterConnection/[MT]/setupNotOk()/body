{
  if (retries > 20) {
    close();
  }
  if (!acquireWriteBuffer()) {
    retries++;
    return false;
  }
  buffer.put(HeaderConstants.NOT_OK);
  buffer.flip();
  log("Setup not ok");
  setStatus(Status.SEND_NOT_OK);
  retries=0;
  return true;
}
