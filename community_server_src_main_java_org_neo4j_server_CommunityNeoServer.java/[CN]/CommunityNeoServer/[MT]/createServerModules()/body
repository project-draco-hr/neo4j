{
  Logging logging=dependencies.logging();
  return Arrays.asList(new DBMSModule(webServer),new RESTApiModule(webServer,database,configurator.configuration(),logging),new ManagementApiModule(webServer,configurator.configuration()),new ThirdPartyJAXRSModule(webServer,configurator.configuration(),logging,this),new WebAdminModule(webServer),new Neo4jBrowserModule(webServer),new AuthorizationModule(webServer,authManager,configurator.configuration(),logging),new SecurityRulesModule(webServer,configurator.configuration(),logging));
}
