{
  ClientResponse response=DocsGenerator.create("Create node with properties").payload("{\"foo\" : \"bar\"}").expectedStatus(Response.Status.CREATED).expectedHeader("Location").expectedHeader("Content-Length").post(functionalTestHelper.nodeUri()).response();
  assertTrue(response.getLocation().toString().matches(NODE_URI_PATTERN));
}
