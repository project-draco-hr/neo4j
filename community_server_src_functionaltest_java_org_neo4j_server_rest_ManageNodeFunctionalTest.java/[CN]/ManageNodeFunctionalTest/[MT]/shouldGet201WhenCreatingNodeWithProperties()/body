{
  ClientResponse response=doc.doRequest("Create node with properties","POST",functionalTestHelper.nodeUri(),"{\"foo\" : \"bar\"}",Response.Status.CREATED,"Location");
  assertNotNull(response.getHeaders().get("Content-Length"));
  assertTrue(response.getLocation().toString().matches(NODE_URI_PATTERN));
  assertProperNodeRepresentation(JsonHelper.jsonToMap(doc.data.entity));
}
