{
  ClientResponse response=DocsGenerator.create("Create a node").expectedStatus(Response.Status.CREATED).expectedHeader("Location").post(functionalTestHelper.nodeUri()).response();
  assertTrue(response.getLocation().toString().matches(NODE_URI_PATTERN));
}
