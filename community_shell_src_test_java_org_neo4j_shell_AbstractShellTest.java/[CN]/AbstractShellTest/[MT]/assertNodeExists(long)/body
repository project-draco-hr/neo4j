{
  Transaction transaction=db.beginTx();
  try {
    db.getNodeById(id);
  }
 catch (  NotFoundException e) {
    fail("Node " + id + " should exist");
  }
 finally {
    transaction.finish();
  }
}
