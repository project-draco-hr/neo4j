{
  this.keySize=keySize;
  this.valueSize=valueSize;
  Rotation rotation=getClass().getAnnotation(Rotation.class);
  this.format=new Format(headerFields);
  this.state=getClass().getAnnotation(State.class).value().initialState(rotation.value().create(fs,pages,format,base,rotation.parameters()),format);
}
