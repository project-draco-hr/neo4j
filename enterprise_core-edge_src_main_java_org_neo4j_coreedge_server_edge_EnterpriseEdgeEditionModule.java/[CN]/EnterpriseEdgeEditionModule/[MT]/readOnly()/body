{
  return new CommitProcessFactory(){
    @Override public TransactionCommitProcess create(    TransactionAppender appender,    StorageEngine storageEngine,    IndexUpdatesValidator indexUpdatesValidator,    Config config){
      return new ReadOnlyTransactionCommitProcess();
    }
  }
;
}
