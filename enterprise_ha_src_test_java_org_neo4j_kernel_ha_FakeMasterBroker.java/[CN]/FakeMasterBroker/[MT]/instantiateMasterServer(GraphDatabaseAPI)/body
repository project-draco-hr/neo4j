{
  return new MasterServer(new MasterImpl(graphDb,config.getInteger(HaSettings.lock_read_timeout)),Protocol.PORT,graphDb.getMessageLog(),config.getInteger(HaSettings.max_concurrent_channels_per_slave),config.getInteger(HaSettings.lock_read_timeout),TxChecksumVerifier.ALWAYS_MATCH);
}
