{
  return new SegmentedRaftLog(resourceManager.fileSystem(),resourceManager.testPath(),rotateAtSize,new DummyRaftableContentSerializer(),NullLogProvider.getInstance(),raft_log_pruning_strategy.getDefaultValue(),0,new FakeClock());
}
