{
  final Map<IdType,Long> highIds=new HashMap<>();
  NeoStore neoStore=db.getDependencyResolver().resolveDependency(DataSourceManager.class).getDataSource().getNeoStore();
  neoStore.visitStore(new Visitor<CommonAbstractStore,RuntimeException>(){
    @Override public boolean visit(    CommonAbstractStore store) throws RuntimeException {
      highIds.put(store.getIdType(),store.getHighId());
      return true;
    }
  }
);
  return highIds;
}
