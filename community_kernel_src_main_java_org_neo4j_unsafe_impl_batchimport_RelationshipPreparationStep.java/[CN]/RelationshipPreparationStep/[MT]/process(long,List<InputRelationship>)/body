{
  long[] ids=new long[batch.size() * 2];
  int index=0;
  for (  InputRelationship batchRelationship : batch) {
    ids[index++]=idMapper.get(batchRelationship.startNode());
    ids[index++]=idMapper.get(batchRelationship.endNode());
  }
  return Pair.of(batch,ids);
}
