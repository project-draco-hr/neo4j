{
  AdvertisedSocketAddress catchUpAddress=discoveryService.currentTopology().coreAddresses(memberId).getCatchupServer();
  CatchUpChannel channel=idleChannels.remove(catchUpAddress);
  if (channel == null) {
    channel=new CatchUpChannel(catchUpAddress);
  }
  activeChannels.add(channel);
  return channel;
}
