{
  if (((loader == null) && !canRewriteBootstrap) || isBlackListed(className)) {
    System.err.println("Skipping: " + className);
    return false;
  }
  System.err.println("Rewriting: " + className);
  return true;
}
