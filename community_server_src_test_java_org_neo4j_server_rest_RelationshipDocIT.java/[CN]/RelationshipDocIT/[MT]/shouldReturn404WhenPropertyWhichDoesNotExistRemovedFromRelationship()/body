{
  data.get();
  Relationship loves=getFirstRelationshipFromRomeoNode();
  gen().description(startGraph("remove non-existent property from relationship")).noGraph().expectedStatus(Status.NOT_FOUND.getStatusCode()).delete(getPropertiesUri(loves) + "/non-existent").entity();
}
