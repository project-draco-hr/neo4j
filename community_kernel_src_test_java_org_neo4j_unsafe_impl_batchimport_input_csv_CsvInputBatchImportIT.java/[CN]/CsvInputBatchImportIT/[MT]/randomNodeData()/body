{
  List<InputNode> nodes=new ArrayList<>();
  for (int i=0; i < 300; i++) {
    Object[] properties=new Object[]{"name","Node " + i};
    nodes.add(new InputNode(UUID.randomUUID().toString(),properties,null,randomLabels(random),null));
  }
  return nodes;
}
