{
  super.dump(logger);
  logger.log("What have I done up until now?");
  for (  String op : state.completedOperations)   logger.log(op);
  logger.log("Doing right now:");
  logger.log(state.doing);
}
