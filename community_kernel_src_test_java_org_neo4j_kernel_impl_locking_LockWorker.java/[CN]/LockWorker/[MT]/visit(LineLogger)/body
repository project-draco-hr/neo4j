{
  boolean result=super.visit(logger);
  logger.logLine("What have I done up until now?");
  for (  String op : state.completedOperations)   logger.logLine(op);
  logger.logLine("Doing right now:");
  logger.logLine(state.doing);
  return result;
}
