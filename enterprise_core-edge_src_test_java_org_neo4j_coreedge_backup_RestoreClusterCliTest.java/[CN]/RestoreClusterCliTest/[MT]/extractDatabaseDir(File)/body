{
  Config config=new ConfigLoader(RestoreExistingClusterCli.settings()).loadConfig(Optional.of(rootNewDatabaseDir),Optional.of(new File(rootNewDatabaseDir,"neo4j.conf")));
  return config.get(DatabaseManagementSystemSettings.database_path);
}
