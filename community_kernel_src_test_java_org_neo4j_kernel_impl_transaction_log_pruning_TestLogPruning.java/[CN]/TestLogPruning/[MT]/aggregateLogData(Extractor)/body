{
  int total=0;
  for (long i=files.getHighestLogVersion(); i >= 0; i--) {
    if (files.versionExists(i)) {
      total+=extractor.extract(i);
    }
 else {
      break;
    }
  }
  return total;
}
