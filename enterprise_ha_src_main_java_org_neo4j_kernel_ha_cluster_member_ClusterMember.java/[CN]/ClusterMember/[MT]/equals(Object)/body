{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ClusterMember that=(ClusterMember)o;
  if (!clusterUri.equals(that.clusterUri)) {
    return false;
  }
  return true;
}
