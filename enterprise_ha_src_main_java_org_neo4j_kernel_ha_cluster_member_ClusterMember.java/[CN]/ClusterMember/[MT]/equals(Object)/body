{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ClusterMember that=(ClusterMember)o;
  if (!memberId.equals(that.memberId)) {
    return false;
  }
  return true;
}
