{
  return new DataFactory(){
    @Override public CharSeeker create(    Configuration config){
      return charSeeker(multipleFiles(files),DEFAULT_BUFFER_SIZE,true,config.quotationCharacter());
    }
  }
;
}
