{
  pageCache=pageCacheRule.getPageCache(fsRule.get());
  DefaultIdGeneratorFactory idGeneratorFactory=new DefaultIdGeneratorFactory();
  storeFactory=new StoreFactory(storeDir,new Config(),idGeneratorFactory,pageCache,fsRule.get(),NullLogProvider.getInstance(),new Monitors());
}
