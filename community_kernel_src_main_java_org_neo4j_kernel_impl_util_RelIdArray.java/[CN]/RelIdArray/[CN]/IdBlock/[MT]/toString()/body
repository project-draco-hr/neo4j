{
  StringBuilder builder=new StringBuilder("[");
  int len=length();
  for (int i=0; i < len; i++) {
    if (i > 0) {
      builder.append(",");
      if (i % 10 == 0) {
        builder.append("\n");
      }
    }
    builder.append(get(i));
  }
  return builder.append("]").toString();
}
