{
  int finalSize=newCapacity + 1;
  int[] newIds=new int[finalSize];
  byte[] newHighBits=new byte[finalSize];
  arraycopy(ids,0,newIds,0,length + 1);
  arraycopy(highBits,0,newHighBits,0,length + 1);
  ids=newIds;
  highBits=newHighBits;
}
