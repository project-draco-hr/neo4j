{
  super(type);
  emptyIterator=new RelIdIteratorImpl(this,new DirectionWrapper[0]){
    @Override public boolean hasNext(){
      return false;
    }
    @Override protected boolean nextBlock(){
      return false;
    }
    public void doAnotherRound(){
    }
    public boolean isPlacebo(){
      return true;
    }
  }
;
}
