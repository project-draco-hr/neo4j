{
  int result=0;
  for (  DirectionWrapper direction : dir.allDirections()) {
    IdBlock block=direction.getBlock(this);
    if (block != null) {
      result+=block.length();
    }
  }
  return result;
}
