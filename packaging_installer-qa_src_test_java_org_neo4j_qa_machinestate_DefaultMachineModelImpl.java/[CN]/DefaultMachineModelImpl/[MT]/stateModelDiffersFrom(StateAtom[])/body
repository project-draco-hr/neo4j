{
  if (stateModifications != null) {
    for (    StateAtom atom : stateModifications) {
      if (!state.contains(atom) || state.get(atom).value() != atom.value()) {
        return true;
      }
    }
  }
  return false;
}
