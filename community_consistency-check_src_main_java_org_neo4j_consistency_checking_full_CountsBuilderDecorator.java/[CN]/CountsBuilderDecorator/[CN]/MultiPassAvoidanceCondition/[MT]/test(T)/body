{
  if (done) {
    return false;
  }
  if (record.getId() == terminationId) {
    done=true;
    return true;
  }
  if (record.getId() == 0) {
    if (started) {
      done=true;
      return false;
    }
 else {
      started=true;
    }
  }
  return true;
}
