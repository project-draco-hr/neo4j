{
  if (args.length != 1) {
    System.out.println("usage: DumpClusterState <graph.db>");
  }
  DumpClusterState dumpTool=new DumpClusterState(new DefaultFileSystemAbstraction(),new File(args[0],CLUSTER_STATE_DIRECTORY_NAME),System.out);
  dumpTool.dump();
}
