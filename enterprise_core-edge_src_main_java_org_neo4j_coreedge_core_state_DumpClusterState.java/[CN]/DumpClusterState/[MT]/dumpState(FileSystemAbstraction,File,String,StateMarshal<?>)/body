{
  DurableStateStorage<?> storage=new DurableStateStorage<>(fs,baseDir,name,marshal,1024,null,NullLogProvider.getInstance());
  System.out.println(name + ": " + storage.getInitialState());
  storage.shutdown();
}
