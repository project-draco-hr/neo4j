{
  server=Undertow.builder().addHttpListener(port,host).setHandler(path().addExactPath("/session/",new SessionCollectionHandler(sessionRegistry)).addPrefixPath("/session/",new SessionInstanceHandler(sessionRegistry,log))).build();
}
