{
  beforeOperation();
  beforeReadOperation();
  IndexDescriptor result=schemaOperations.indexesGetForLabelAndPropertyKey(labelId,propertyKey);
  afterReadOperation();
  afterOperation();
  return result;
}
