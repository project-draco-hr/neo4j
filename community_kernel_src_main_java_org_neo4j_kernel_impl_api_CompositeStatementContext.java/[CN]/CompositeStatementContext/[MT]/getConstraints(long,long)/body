{
  beforeOperation();
  beforeReadOperation();
  Iterator<UniquenessConstraint> result=schemaOperations.getConstraints(labelId,propertyKeyId);
  afterReadOperation();
  afterOperation();
  return result;
}
