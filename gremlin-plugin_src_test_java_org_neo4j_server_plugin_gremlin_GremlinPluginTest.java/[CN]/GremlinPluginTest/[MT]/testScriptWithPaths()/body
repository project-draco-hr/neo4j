{
  Representation result=GremlinPluginTest.executeTestScript("" + "g.loadGraphML(\"" + exampleURI + "\");"+ "g.v(1).out().name.paths",null);
  Assert.assertTrue(json.format(result).contains("[v[1], v[2], vadas]"));
}
