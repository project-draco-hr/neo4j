{
  Buffer buffer=window.getOffsettedBuffer(id);
  long read=buffer.getLong();
  Long existingValue=values.get(id);
  if (existingValue != null)   Assert.assertEquals(existingValue.longValue(),read);
}
