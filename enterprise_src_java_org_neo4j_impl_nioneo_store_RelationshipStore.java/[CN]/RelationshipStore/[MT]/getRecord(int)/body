{
  PersistenceWindow window=acquireWindow(id,OperationType.READ);
  try {
    RelationshipRecord record=getRecord(id,window.getBuffer(),false);
    return record;
  }
  finally {
    releaseWindow(window);
  }
}
