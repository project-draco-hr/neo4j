{
  return new SlaveTxIdGenerator(config.get(ClusterSettings.server_id).toIntegerIndex(),master.cement(),HighAvailabilityModeSwitcher.getServerId(serverHaUri).toIntegerIndex(),requestContextFactory);
}
