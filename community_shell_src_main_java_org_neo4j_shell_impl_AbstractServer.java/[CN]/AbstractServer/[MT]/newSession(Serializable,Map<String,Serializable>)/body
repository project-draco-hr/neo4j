{
  Session session=new Session(id);
  initialPopulateSession(session);
  for (  Map.Entry<String,Serializable> entry : initialSession.entrySet())   session.set(entry.getKey(),entry.getValue());
  return session;
}
