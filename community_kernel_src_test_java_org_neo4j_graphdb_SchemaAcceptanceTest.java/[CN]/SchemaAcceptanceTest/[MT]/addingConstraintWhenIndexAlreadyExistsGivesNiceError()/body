{
  createIndex(label,propertyKey);
  try {
    createConstraint(label,propertyKey);
    fail("Expected exception to be thrown");
  }
 catch (  ConstraintViolationException e) {
    assertEquals(e.getMessage(),"Can not create an constraint on :MY_LABEL(my_property_key). An index " + "already exists for this combination. Drop the index and create the constraint again.");
  }
}
