{
switch (entry.type()) {
case PROPERTY:
    if (value != null && value.getClass().isArray() && Array.getLength(value) == 0) {
      break;
    }
  addProperty(entry.name(),value);
break;
case IGNORE:
break;
default :
break;
}
}
