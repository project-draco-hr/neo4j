{
  List<PropertyRecord> records=ReadTransaction.getPropertyRecordChain(getPropertyStore(),nextProp);
  if (records == null) {
    return null;
  }
  if (records.size() > 0) {
    addPropertyRecord(records.get(0));
  }
  return ReadTransaction.propertyChainToMap(records);
}
