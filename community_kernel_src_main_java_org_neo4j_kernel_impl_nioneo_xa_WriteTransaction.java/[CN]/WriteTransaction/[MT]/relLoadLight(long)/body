{
  RelationshipRecord relRecord=getRelationshipRecord(id);
  if (relRecord != null) {
    return relRecord;
  }
  relRecord=getRelationshipStore().getLightRel(id);
  if (relRecord != null) {
    return relRecord;
  }
  return null;
}
