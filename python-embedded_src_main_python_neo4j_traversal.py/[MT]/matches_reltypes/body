def matches_reltypes(rel, reltypes):
    if (len(reltypes) == 0):
        return True
    expected = rel.getType().name()
    for reltype in reltypes:
        if (expected == reltype):
            return True
    return False
