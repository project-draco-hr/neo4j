{
  for (  EnterpriseDriver d : drivers)   d.up();
  setupZookeeperCluster();
  setupHighAvailabilityCluster();
  assertClusterWorks();
  drivers[0].reboot();
  assertClusterWorks();
  assertHABackupWorks();
}
