{
  Config config=new ConfigLoader(settings()).loadConfig(Optional.of(rootNewDatabaseDir),Optional.of(new File(rootNewDatabaseDir,"neo4j.conf")),NullLog.getInstance());
  return config.get(DatabaseManagementSystemSettings.database_path);
}
