{
  IndexStorageFactory storageFactory=new IndexStorageFactory(directoryFactory,new EphemeralFileSystemAbstraction(),indexDirectory);
  IndexStorage indexStorage=storageFactory.indexStorageOf(1);
  indexStorage.prepareIndexStorage();
  return indexStorage;
}
