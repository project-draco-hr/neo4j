{
  if (arguments == null || arguments.length == 0) {
    log(format);
    return;
  }
  PrintWriter writer;
synchronized (lock) {
    writer=writerSupplier.get();
    writeLog(writer,format,arguments);
  }
  maybeFlush(writer);
}
