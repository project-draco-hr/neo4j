{
  this.receiver=new TestMessageSource();
  this.sender=new TestMessageSender();
  stateMachineExecutor=new DelayedDirectExecutor();
  server=factory.newProtocolServer(instanceId,timeoutStrategy,receiver,sender,acceptorInstanceStore,electionCredentialsProvider,stateMachineExecutor);
  server.listeningAt(serverUri);
}
