{
  assertTrue(data.get().size() == 2);
  String response=gen.get().expectedStatus(Status.OK.getStatusCode()).payload("{\"script\":\"g.v(4).outE.inV\"}").payloadType(MediaType.APPLICATION_JSON_TYPE).post("http://localhost:7474/db/data/ext/GremlinPlugin/graphdb/execute_script").entity();
  System.out.println(response);
  assertTrue(response.contains("you"));
}
