{
  Traverser traverser=new TraversalDescriptionImpl().depthFirst().traverse(referenceNode());
  int i=0;
  for (  Position pos : traverser) {
    assertEquals(expectedDepth(i++),pos.depth());
  }
  assertEquals(13,i);
}
