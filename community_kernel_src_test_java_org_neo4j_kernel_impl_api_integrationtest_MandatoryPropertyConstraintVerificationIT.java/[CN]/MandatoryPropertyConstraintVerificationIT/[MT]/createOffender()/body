{
  return new ThrowingFunction<Void,Void,RuntimeException>(){
    @Override public Void apply(    Void aVoid) throws RuntimeException {
      try (Transaction tx=db.beginTx()){
        createOffender(db,KEY);
        tx.success();
      }
       return null;
    }
  }
;
}
