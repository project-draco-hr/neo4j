{
  if (!bridge.hasTransactionContextForThread()) {
    return;
  }
  if (nested) {
    txCtx.finish();
    return;
  }
  bridge.closeAnyActiveContext(txCtx);
  txCtx.finish();
  bridge.clearThisThread();
}
