{
  try {
    printGreeting(args);
    ShellClient client=ShellLobby.newClient(getPort(args),getShellName(args));
    setSessionVariablesFromArgs(client,args);
    client.grabPrompt();
  }
 catch (  Exception e) {
    System.err.println("Can't start client shell: " + e);
    System.exit(1);
  }
}
