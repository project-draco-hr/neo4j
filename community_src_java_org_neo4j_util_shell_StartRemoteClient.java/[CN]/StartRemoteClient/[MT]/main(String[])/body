{
  try {
    printGreeting(args);
    ShellServer server=ShellLobby.getInstance().findRemoteServer(getPort(args),getShellName(args));
    ShellLobby.getInstance().startClient(server);
  }
 catch (  Exception e) {
    System.err.println("Can't start client shell: " + e);
  }
}
