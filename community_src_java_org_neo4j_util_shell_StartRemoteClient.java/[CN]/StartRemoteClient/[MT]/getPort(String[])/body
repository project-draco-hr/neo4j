{
  try {
    String arg=getArg(args,0);
    return arg != null ? Integer.parseInt(arg) : AbstractServer.DEFAULT_PORT;
  }
 catch (  ArrayIndexOutOfBoundsException e) {
    return AbstractServer.DEFAULT_PORT;
  }
}
