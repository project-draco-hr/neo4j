{
  if (current == null) {
    return false;
  }
 else   if (current.hasNext()) {
    return true;
  }
 else   if (relations.hasNext()) {
    current=relations.next();
    return true;
  }
 else {
    current=null;
    return false;
  }
}
