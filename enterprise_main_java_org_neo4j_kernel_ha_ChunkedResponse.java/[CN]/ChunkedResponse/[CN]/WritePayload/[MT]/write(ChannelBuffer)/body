{
  int position=buffer.writerIndex();
  buffer.writeShort(0);
  serializer.write(response.response(),buffer);
  buffer.setShort(position,buffer.writerIndex() - position - 2);
  (state=next).write(buffer);
}
