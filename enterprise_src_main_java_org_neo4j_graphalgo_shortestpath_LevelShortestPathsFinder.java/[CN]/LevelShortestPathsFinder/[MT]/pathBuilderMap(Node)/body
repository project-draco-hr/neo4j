{
  Map<Node,List<PathImpl.Builder>> result=new HashMap<Node,List<PathImpl.Builder>>();
  result.put(node,new LinkedList<PathImpl.Builder>(Arrays.asList(new PathImpl.Builder(node))));
  return result;
}
