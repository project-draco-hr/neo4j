{
  if (!transaction.mode().allowsReads()) {
    throw new AuthorizationViolationException(String.format("Read operations are not allowed for `%s` transactions.",transaction.mode().name()));
  }
  return facade;
}
