{
  if (!transaction.mode().allowsReads()) {
    throw new AuthorizationViolationException(String.format("Read operations are not allowed for '%s'.",transaction.mode().name()));
  }
  return facade;
}
