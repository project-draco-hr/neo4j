{
  if (!file.exists()) {
    return;
  }
  if (file.isDirectory()) {
    for (    File child : nonNull(file.listFiles())) {
      deleteFileOrDirectory(child);
    }
  }
 else {
    assertTrue("delete " + file,file.delete());
  }
}
