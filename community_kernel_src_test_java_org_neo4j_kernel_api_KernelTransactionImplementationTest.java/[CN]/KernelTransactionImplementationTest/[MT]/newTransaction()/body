{
  KernelTransactionImplementation transaction=new KernelTransactionImplementation(null,null,new NoOpClient(),hooks,null,headerInformationFactory,commitProcess,transactionMonitor,legacyIndexState,pool,clock,TransactionTracer.NULL,storageEngine);
  transaction.initialize(0);
  return transaction;
}
