{
  logger.info("Recovery complete, " + "all transactions have been resolved");
  logger.info("Rebuilding id generators as needed. " + "This can take a while for large stores...");
  neoStore.makeStoreOk();
  logger.info("Rebuild of id generators complete.");
}
