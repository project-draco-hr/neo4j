{
  try {
    FileUtils.copyFile(new File(workingDirectory,StringLogger.DEFAULT_NAME),new File(backupDirectory,StringLogger.DEFAULT_NAME));
  }
 catch (  IOException e) {
    throw new UnableToUpgradeException(e);
  }
}
