{
  RaftState<RaftTestMember> state=raftState().myself(myself).term(0).build();
  Follower follower=new Follower();
  Outcome<RaftTestMember> outcome=follower.handle(new RaftMessages.Heartbeat<>(myself,1,1,1,storeId.storeId()),state,log(),storeId);
  assertTrue(outcome.electionTimeoutRenewed());
}
