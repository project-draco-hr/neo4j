{
  RaftState<RaftTestMember> state=raftState().build();
  Follower follower=new Follower();
  Outcome<RaftTestMember> outcome=follower.handle(voteRequest().from(member1).term(HIGHEST_TERM).lastLogIndex(0).lastLogTerm(-1).build(),state,log());
  assertEquals(HIGHEST_TERM,outcome.getTerm());
}
