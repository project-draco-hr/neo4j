{
  RaftState<RaftTestMember> state=raftState().myself(myself).term(2).build();
  Follower follower=new Follower();
  Outcome<RaftTestMember> outcome=follower.handle(new RaftMessages.Heartbeat<>(myself,1,1,1),state,log());
  assertFalse(outcome.renewElectionTimeout);
}
