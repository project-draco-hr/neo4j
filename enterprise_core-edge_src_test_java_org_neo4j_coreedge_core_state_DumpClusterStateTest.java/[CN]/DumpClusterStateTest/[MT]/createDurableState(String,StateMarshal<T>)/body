{
  DurableStateStorage<T> storage=new DurableStateStorage<>(fsa.get(),clusterStateDirectory,name,marshal,1024,null,NullLogProvider.getInstance());
  try (Lifespan ignored=new Lifespan(storage)){
  }
 }
