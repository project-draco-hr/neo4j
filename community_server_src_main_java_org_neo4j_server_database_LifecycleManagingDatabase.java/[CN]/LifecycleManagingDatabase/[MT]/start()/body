{
  try {
    this.graph=dbFactory.newGraphDatabase(dbConfig,logbackOrDefaultToClassic(),factoryState.getKernelExtension(),factoryState.getCacheProviders(),factoryState.getTransactionInterceptorProviders());
    isRunning=true;
    log.info("Successfully started database");
  }
 catch (  Exception e) {
    log.error("Failed to start database.",e);
    throw e;
  }
}
