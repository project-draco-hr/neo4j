{
  try {
    this.graph=dbFactory.newGraphDatabase(dbConfig,loggingProvider,factoryState.getKernelExtension(),factoryState.getCacheProviders(),factoryState.getTransactionInterceptorProviders());
    this.executionEngine=new ExecutionEngine(graph);
    isRunning=true;
    log.info("Successfully started database");
  }
 catch (  Exception e) {
    log.error("Failed to start database.",e);
    throw e;
  }
}
