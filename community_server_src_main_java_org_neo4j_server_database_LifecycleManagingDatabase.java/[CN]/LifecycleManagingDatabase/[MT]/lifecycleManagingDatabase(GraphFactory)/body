{
  return new Factory(){
    @Override public Database newDatabase(    Config config,    Iterable<KernelExtensionFactory<?>> kernelExtensions){
      return new LifecycleManagingDatabase(config,graphDbFactory,kernelExtensions);
    }
  }
;
}
