{
  return new Factory(){
    @Override public Database newDatabase(    Config config,    Dependencies dependencies){
      return new LifecycleManagingDatabase(config,graphDbFactory,dependencies);
    }
  }
;
}
