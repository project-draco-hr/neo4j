{
  try {
    if (graph != null) {
      graph.shutdown();
      isRunning=false;
      graph=null;
      log.log("Successfully stopped database");
    }
  }
 catch (  Exception e) {
    log.error("Database did not stop cleanly. Reason [%s]",e.getMessage());
    throw e;
  }
}
