{
  persistenceManager=new PersistenceManager(transactionManager);
  ResourceBroker broker=persistenceManager.getResourceBroker();
  persistenceMonitor=new PersistenceLayerMonitor(eventManager,broker);
}
