{
  buffer.makeReadyForTransfer();
  try {
    if (getBlockSize() != getFileChannel().transferTo(((long)id) * getBlockSize(),getBlockSize(),buffer.getFileChannel())) {
      return false;
    }
    return true;
  }
 catch (  IOException e) {
    throw new StoreFailureException("Failed to transfer data to read from buffer",e);
  }
}
