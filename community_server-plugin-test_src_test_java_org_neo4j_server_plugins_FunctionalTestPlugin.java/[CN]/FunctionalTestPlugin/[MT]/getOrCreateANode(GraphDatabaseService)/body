{
  try (Transaction tx=db.beginTx()){
    Node node;
    try {
      node=db.getNodeById(0l);
    }
 catch (    NotFoundException e) {
      node=db.createNode();
    }
    tx.success();
    return node;
  }
 }
