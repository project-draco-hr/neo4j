{
  optional=id;
  if (id == null) {
    return start;
  }
  Transaction tx=start.getGraphDatabase().beginTx();
  try {
    Node node=start.getGraphDatabase().getNodeById(id);
    tx.success();
    return node;
  }
  finally {
    tx.finish();
  }
}
