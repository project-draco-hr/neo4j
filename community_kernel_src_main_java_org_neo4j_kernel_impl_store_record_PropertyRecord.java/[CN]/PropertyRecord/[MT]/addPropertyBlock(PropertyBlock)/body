{
  if (size() + block.getSize() > PropertyType.getPayloadSize()) {
    malformedMessage="Exceeded capacity of PropertyRecord[" + getId() + "]. "+ "PropertyRecord size is "+ size()+ ". "+ "The added block of type "+ block.forceGetType()+ " has size "+ block.getSize();
  }
  blockRecords[blockRecordsCursor++]=block;
}
