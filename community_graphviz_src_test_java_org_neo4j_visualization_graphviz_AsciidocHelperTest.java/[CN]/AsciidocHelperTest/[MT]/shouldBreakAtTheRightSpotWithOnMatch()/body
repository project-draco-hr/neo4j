{
  String cypher="merge (a)\non match set a.foo = 2";
  String snippet=AsciidocHelper.createCypherSnippet(cypher);
  assertEquals("[source,cypher]\n" + "----\n" + "MERGE (a)\n"+ "ON MATCH SET a.foo = 2\n"+ "----\n",snippet);
}
