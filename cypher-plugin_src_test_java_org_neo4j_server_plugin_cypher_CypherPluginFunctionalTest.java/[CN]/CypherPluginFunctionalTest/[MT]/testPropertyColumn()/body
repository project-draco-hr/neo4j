{
  String script="start x  = (" + data.get().get("I").getId() + ") match (x) --> (n) return n.name?, n.age?";
  String response=gen.get().expectedStatus(Status.OK.getStatusCode()).payload("{\"query\": \"" + script + "\"}").description(formatCypher(script)).post(ENDPOINT).entity();
  assertTrue(response.contains("you"));
  assertTrue(response.contains("him"));
  assertTrue(!response.contains("\"x\""));
}
