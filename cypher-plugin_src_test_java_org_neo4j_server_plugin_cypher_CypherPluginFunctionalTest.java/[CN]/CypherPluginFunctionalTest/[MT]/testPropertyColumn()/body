{
  String response=gen.get().expectedStatus(Status.OK.getStatusCode()).payload("{\"query\": \"start x  = (" + data.get().get("I").getId() + ") match (x) -- (n) return n, n.name?\"}").post(ENDPOINT).entity();
  assertTrue(response.contains("you"));
  assertTrue(response.contains("him"));
}
