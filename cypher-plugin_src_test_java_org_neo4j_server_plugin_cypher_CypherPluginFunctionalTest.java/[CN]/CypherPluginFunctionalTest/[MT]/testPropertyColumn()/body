{
  String response=gen.get().expectedStatus(Status.OK.getStatusCode()).payload("query=" + URLEncoder.encode("start n  = (" + data.get().get("I").getId() + ") return n.name","UTF-8")).payloadType(MediaType.APPLICATION_FORM_URLENCODED_TYPE).post(ENDPOINT).entity();
  System.out.println(response);
  assertTrue(response.contains("I"));
}
