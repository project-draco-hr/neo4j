{
  if (key == null)   throw new IllegalArgumentException("null provider key prohibited");
  if (key.length() == 0)   throw new IllegalArgumentException("empty provider key prohibited");
  if (version == null)   throw new IllegalArgumentException("null provider version prohibited");
  this.key=key;
  this.version=version;
}
