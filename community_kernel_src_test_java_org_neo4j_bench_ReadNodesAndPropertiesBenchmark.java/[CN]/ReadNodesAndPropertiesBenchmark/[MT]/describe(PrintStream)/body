{
  out.println("name=" + ReadNodesAndPropertiesBenchmark.class.getSimpleName());
  out.println("description=In a background thread, " + "creates single nodes (2 string properties on each). " + "In the main thread, read all nodes, and their properties. "+ "Reports number of nodes 'fully read', including reading their properties.");
}
