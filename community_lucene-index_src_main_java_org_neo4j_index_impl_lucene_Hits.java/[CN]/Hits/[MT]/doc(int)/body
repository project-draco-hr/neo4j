{
  HitDoc hitDoc=hitDoc(n);
  remove(hitDoc);
  addToFront(hitDoc);
  if (numDocs > MAX_CACHED_DOCS) {
    HitDoc oldLast=last;
    remove(last);
    oldLast.doc=null;
  }
  if (hitDoc.doc == null) {
    hitDoc.doc=searcher.doc(hitDoc.id);
  }
  return hitDoc.doc;
}
