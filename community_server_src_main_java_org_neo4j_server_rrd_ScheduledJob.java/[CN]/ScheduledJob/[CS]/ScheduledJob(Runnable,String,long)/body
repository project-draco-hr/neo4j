{
  timer=new Timer(name);
  TimerTask runJob=new TimerTask(){
    public void run(){
      try {
        job.run();
      }
 catch (      Exception e) {
        logger.warn(e);
      }
    }
  }
;
  timer.scheduleAtFixedRate(runJob,0,period);
}
