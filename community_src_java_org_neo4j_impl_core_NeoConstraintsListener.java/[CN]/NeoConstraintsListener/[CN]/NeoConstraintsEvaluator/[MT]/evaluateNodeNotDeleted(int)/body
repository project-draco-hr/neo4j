{
  if (deletedNodes != null && deletedNodes.containsKey(nodeId)) {
    throw new IllegalStateException("Property operation on node[" + nodeId + "] illegal since it has already been deleted "+ "(in this tx)");
  }
}
