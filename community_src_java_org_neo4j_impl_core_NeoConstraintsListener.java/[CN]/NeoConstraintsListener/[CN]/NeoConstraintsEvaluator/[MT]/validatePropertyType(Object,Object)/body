{
  if (prop instanceof String || prop instanceof Boolean || prop instanceof Byte|| prop instanceof Integer|| prop instanceof Long|| prop instanceof Float|| prop instanceof Double|| prop instanceof Character) {
    return true;
  }
  if (prop.getClass().isArray()) {
    return validateArrayType(prop);
  }
  log.severe("Illegal property type added to[" + entity + "]");
  return false;
}
