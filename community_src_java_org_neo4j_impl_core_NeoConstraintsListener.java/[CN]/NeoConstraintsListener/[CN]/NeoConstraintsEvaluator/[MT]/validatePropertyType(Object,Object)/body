{
  if (prop instanceof String || prop instanceof Boolean || prop instanceof Byte|| prop instanceof Integer|| prop instanceof Long|| prop instanceof Float|| prop instanceof Double) {
    return true;
  }
  log.severe("Illegal property type added to[" + entity + "]");
  return false;
}
