{
  server=server().withHttpsEnabled().usingDatabaseDir(folder.getRoot().getAbsolutePath()).build();
  server.start();
  assertThat(server.getHttpsEnabled(),is(true));
  trustAllSslCerts();
  assertThat(GET(server.httpsUri().toASCIIString()).status(),is(200));
}
