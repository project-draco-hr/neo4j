{
  return new IOCursor<LogEntry>(){
    private int pos=0;
    @Override public LogEntry get(){
      return entries[pos++];
    }
    @Override public boolean next() throws IOException {
      return pos < entries.length;
    }
    @Override public void close() throws IOException {
    }
  }
;
}
