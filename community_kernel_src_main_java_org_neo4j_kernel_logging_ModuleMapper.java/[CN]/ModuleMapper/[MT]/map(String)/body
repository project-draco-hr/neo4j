{
  for (  Map.Entry<String,String> moduleEntry : modules.entrySet()) {
    String[] packages=moduleEntry.getValue().split(",");
    for (    String aPackage : packages) {
      if (logger.startsWith(aPackage))       return moduleEntry.getKey();
    }
  }
  return logger;
}
