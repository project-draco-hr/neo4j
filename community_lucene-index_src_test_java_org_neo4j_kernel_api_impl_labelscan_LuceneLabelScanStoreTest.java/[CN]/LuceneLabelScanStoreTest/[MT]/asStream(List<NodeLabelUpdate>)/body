{
  return new FullStoreChangeStream(){
    @Override public long applyTo(    LabelScanWriter writer) throws IOException {
      long count=0;
      for (      NodeLabelUpdate update : existingData) {
        writer.write(update);
        count++;
      }
      return count;
    }
  }
;
}
