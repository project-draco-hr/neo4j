{
  super("Node counts",config,false);
  add(new ReadNodeRecordsStep(control(),config.batchSize(),config.movingAverageSize(),nodeStore));
  add(new ProcessNodeCountsDataStep(control(),cache,config.workAheadSize(),config.movingAverageSize(),nodeStore,highLabelId,countsUpdater));
}
