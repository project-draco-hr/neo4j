{
  resume();
  try {
    server.removeIndexNode(connection.graphDb,indexId,nodeId,key,value);
  }
 catch (  Exception ex) {
    return response().buildErrorResponse(ex);
  }
  return response().buildVoidResponse();
}
