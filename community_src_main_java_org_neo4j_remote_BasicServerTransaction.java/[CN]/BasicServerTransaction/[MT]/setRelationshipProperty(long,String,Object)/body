{
  resume();
  Object old;
  try {
    old=server.setRelationshipProperty(connection.neo,relationshipId,key,value);
  }
 catch (  RuntimeException ex) {
    return response().buildErrorResponse(ex);
  }
  return response().buildPropertyResponse(old);
}
