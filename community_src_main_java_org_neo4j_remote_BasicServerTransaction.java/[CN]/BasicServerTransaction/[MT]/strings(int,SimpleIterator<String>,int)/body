{
  String[] result=consume(token,strIter,iterator,batchSize).toArray(new String[0]);
  if (iterator.hasNext()) {
    return response().buildPartialStringResponse(token,result);
  }
 else {
    return response().buildFinalStringResponse(result);
  }
}
