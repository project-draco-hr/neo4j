def short_name(self):
    ' Return the text following the last dash in the section name.'
    i = self.name.rfind('-')
    if (i == (-1)):
        return self.name
    else:
        return self.name[(i + 1):]
