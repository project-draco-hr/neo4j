def short_name(self):
    ' Return the text following the first dash in the section name.'
    i = self.defname.find('-')
    if (i == (-1)):
        return self.defname
    else:
        return self.defname[(i + 1):]
