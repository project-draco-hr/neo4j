def char_decode(s):
    if char_encoding():
        try:
            return s.decode(char_encoding())
        except Exception:
            raise EAsciiDoc, ('\'%s\' codec can\'t decode "%s"' % (char_encoding(), s))
    else:
        return s
