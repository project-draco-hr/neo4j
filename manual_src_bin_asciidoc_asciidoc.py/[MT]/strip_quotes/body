def strip_quotes(s):
    'Trim white space and, if necessary, quote characters from s.'
    s = s.strip()
    if ((len(s) >= 3) and (s[0] == '"') and (s[(-1)] == '"')):
        s = s[1:(-1)]
    return s
