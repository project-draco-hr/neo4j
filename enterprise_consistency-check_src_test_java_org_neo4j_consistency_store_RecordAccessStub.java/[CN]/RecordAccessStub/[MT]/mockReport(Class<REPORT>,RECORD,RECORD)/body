{
  REPORT report=mock(reportClass);
  doAnswer(new DeferredReferenceDispatch(report,oldRecord,newRecord)).when(report).forReference(any(RecordReference.class),any(ComparativeRecordChecker.class));
  return report;
}
