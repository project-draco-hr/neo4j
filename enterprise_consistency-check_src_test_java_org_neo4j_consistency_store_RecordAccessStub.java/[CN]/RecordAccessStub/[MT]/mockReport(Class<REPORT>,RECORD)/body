{
  REPORT report=mock(reportClass);
  doAnswer(new DeferredReferenceDispatch(report,record)).when(report).forReference(any(RecordReference.class),any(ComparativeRecordChecker.class));
  return report;
}
