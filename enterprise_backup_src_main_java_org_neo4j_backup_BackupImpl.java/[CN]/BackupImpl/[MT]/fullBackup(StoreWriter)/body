{
  RequestContext context=ServerUtil.rotateLogsAndStreamStoreFiles(spi.getStoreDir(),xaDataSourceManager,kpeg,logger,false,writer,new DefaultFileSystemAbstraction());
  writer.done();
  return packResponse(context);
}
