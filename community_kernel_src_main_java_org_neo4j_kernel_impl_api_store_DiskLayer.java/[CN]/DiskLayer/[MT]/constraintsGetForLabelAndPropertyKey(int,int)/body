{
  return schemaStorage.schemaRules(RULE_TO_CONSTRAINT,PropertyConstraintRule.class,labelId,new Predicate<PropertyConstraintRule>(){
    @Override public boolean test(    PropertyConstraintRule rule){
      return rule.containsPropertyKeyId(propertyKeyId);
    }
  }
);
}
