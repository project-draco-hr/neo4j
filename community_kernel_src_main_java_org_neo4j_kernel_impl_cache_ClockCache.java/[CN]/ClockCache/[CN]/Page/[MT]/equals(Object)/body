{
  if (obj == null) {
    return false;
  }
  if (!(obj instanceof Page)) {
    return false;
  }
  Page<?> other=(Page<?>)obj;
  if (value == null) {
    return other.value == null;
  }
  return value.equals(other.value);
}
