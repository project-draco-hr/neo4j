{
  SessionStateMachine machine=new SessionStateMachine(mock(GraphDatabaseService.class),mock(ThreadToStatementContextBridge.class),mock(StatementRunner.class),StringLogger.DEV_NULL);
  assertThat(machine.state(),equalTo(SessionStateMachine.State.IDLE));
}
