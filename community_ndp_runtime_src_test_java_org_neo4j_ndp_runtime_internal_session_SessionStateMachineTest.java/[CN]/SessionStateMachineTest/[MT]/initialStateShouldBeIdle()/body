{
  SessionStateMachine machine=new SessionStateMachine(mock(GraphDatabaseService.class),mock(ThreadToStatementContextBridge.class),mock(StatementRunner.class),NullLogService.getInstance());
  assertThat(machine.state(),equalTo(SessionStateMachine.State.IDLE));
}
