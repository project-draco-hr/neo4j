{
  return new CountsTracker(the.logger(),the.fileSystem(),the.pageCache(),new Config(),the.testPath()).setInitializer(new DataInitializer<CountsAccessor.Updater>(){
    @Override public void initialize(    CountsAccessor.Updater updater){
    }
    @Override public long initialVersion(){
      return FileVersion.INITIAL_TX_ID;
    }
  }
);
}
