{
  StoreCopyServer streamer=new StoreCopyServer(transactionIdStore,neoStoreDataSource,storeFlusher,fileSystem,storeDir,monitors.newMonitor(StoreCopyServer.Monitor.class));
  return streamer.flushStoresAndStreamStoreFiles(writer,false);
}
