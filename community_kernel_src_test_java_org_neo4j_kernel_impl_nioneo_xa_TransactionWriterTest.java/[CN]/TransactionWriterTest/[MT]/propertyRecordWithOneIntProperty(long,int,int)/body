{
  PropertyRecord record=new PropertyRecord(id);
  record.setInUse(true);
  PropertyBlock block=new PropertyBlock();
  block.setSingleBlock(keyId | (((long)PropertyType.INT.intValue()) << 24) | (value << 28));
  record.addPropertyBlock(block);
  return record;
}
