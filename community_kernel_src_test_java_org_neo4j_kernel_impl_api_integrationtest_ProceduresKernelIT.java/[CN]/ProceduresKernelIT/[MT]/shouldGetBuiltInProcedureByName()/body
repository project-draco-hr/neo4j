{
  ProcedureSignature found=readOperationsInNewTransaction().procedureGet(procedureName("sys","db","labels"));
  assertThat(found,equalTo(procedureSignature(procedureName("sys","db","labels")).out("label",Neo4jTypes.NTString).build()));
}
