{
  kernel.registerProcedure(procedure);
  kernel.registerProcedure(procedure(procedureSignature("example","exampleProc2").out("name",NTString).build()));
  kernel.registerProcedure(procedure(procedureSignature("example","exampleProc3").out("name",NTString).build()));
  List<ProcedureSignature> signatures=Iterables.asList(readOperationsInNewTransaction().proceduresGetAll());
  assertThat(signatures,hasItems(procedure.signature(),procedureSignature("example","exampleProc2").out("name",NTString).build(),procedureSignature("example","exampleProc3").out("name",NTString).build()));
}
