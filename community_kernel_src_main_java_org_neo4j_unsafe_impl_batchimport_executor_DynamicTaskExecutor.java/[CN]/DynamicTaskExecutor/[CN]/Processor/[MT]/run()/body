{
  while (!shutDown) {
    Callable<?> task=queue.poll();
    if (task != null) {
      try {
        task.call();
      }
 catch (      Throwable e) {
        panic=e;
        shutdown(false);
        throw launderedException(e);
      }
    }
 else {
      parkAWhile();
    }
  }
}
