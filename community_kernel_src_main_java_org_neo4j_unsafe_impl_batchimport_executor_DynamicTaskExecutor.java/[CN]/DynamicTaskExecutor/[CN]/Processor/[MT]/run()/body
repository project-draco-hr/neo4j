{
  while (!shutDown) {
    Callable<?> task=queue.poll();
    if (task != null) {
      try {
        task.call();
      }
 catch (      Throwable e) {
        shutdown0(false,e);
        throw launderedException(e);
      }
    }
 else {
      parkAWhile();
    }
  }
}
