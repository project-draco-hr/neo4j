{
  Thread thread=Thread.currentThread();
  if (txThreadMap.containsKey(thread)) {
    int status=((TransactionImpl)txThreadMap.get(thread)).getStatus();
    return status;
  }
  return Status.STATUS_NO_TRANSACTION;
}
