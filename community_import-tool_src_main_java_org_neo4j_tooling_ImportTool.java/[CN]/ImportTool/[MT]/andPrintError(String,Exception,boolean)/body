{
  if (e.getClass().equals(DuplicateInputIdException.class)) {
    System.err.println("Duplicate input ids that would otherwise clash can be put into separate id space," + " read more about how to use id spaces in the manual:" + " http://neo4j.com/docs/" + ComponentVersion.getKernel().getVersion() + "/import-tool-header-format.html#_id_spaces");
  }
  System.err.println(typeOfError + ": " + e.getMessage());
  if (stackTrace) {
    e.printStackTrace(System.err);
  }
  System.err.println();
  Thread.currentThread().setUncaughtExceptionHandler(new UncaughtExceptionHandler(){
    @Override public void uncaughtException(    Thread t,    Throwable e){
    }
  }
);
  return launderedException(e);
}
