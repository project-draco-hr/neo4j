{
  Style style=widget.getElement().getStyle();
  style.setLeft(limit(0,newX,parent.getOffsetWidth() - widget.getOffsetWidth()),Unit.PX);
  style.setTop(limit(0,newY,parent.getOffsetHeight() - widget.getOffsetHeight()),Unit.PX);
  updateRelationships();
}
