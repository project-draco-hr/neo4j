{
  Iterator<? extends X> iter=i.iterator();
  if (iter.hasNext()) {
    X result=iter.next();
    if (iter.hasNext())     throw new IllegalArgumentException("More than one element in iterable");
    return result;
  }
 else {
    throw new IllegalArgumentException("No elements in iterable");
  }
}
