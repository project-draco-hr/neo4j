{
  ServerProcess server=new ServerProcess();
  try {
    server.startup(Pair.of(path,"true"));
  }
 catch (  Throwable e) {
    throw new RuntimeException(e);
  }
  server.awaitStarted();
  return server;
}
