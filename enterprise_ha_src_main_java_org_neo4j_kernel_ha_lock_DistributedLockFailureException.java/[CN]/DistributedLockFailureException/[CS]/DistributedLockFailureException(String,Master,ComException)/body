{
  super(message + " (for master instance " + master+ "). The most common causes of this exception are "+ "network failures, or master-switches where the failing transaction was started before the last "+ "master election.",cause);
}
