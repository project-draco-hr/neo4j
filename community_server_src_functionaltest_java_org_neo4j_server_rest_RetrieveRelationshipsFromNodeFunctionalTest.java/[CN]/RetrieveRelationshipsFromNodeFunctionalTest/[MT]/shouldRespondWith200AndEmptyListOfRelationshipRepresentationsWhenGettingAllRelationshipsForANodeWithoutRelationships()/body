{
  String entity=DocsGenerator.create("Get relationships on a node without relationships").get(functionalTestHelper.nodeUri() + "/" + nodeWithoutRelationships+ "/relationships"+ "/all").entity();
  verifyRelReps(0,entity);
}
