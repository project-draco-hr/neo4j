{
  String entity=DocsGenerator.create("Get typed relationships","Note that the \"+&+\" escaped for example when using " + "http://curl.haxx.se/[cURL] from the terminal.").get(functionalTestHelper.nodeUri() + "/" + nodeWithRelationships+ "/relationships"+ "/all/LIKES&HATES").entity();
  verifyRelReps(3,entity);
}
