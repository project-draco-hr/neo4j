{
  int tolerance=100;
  String message=String.format("Expected number of entries to not differ by more than %d (expected: %d actual: %d)",tolerance,expected,actual);
  assertTrue(message,Math.abs(expected - actual) <= tolerance);
}
