{
  if (desc.getId() != null) {
    removeJobDescription(desc.getId());
    if (idCount < desc.getId()) {
      idCount=desc.getId();
    }
    BackupManager.INSTANCE.getLog().logInfo(new Date(),desc,"Settings changed.");
  }
 else {
    desc.setId(++idCount);
  }
  descs.add(desc);
  persist();
}
