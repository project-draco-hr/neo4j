{
  ReplicatedTokenHolder<Token,LabelTokenRecord> tokenHolder=new ReplicatedLabelTokenHolder(null,null,dependencies,TIMEOUT_MILLIS,NullLogProvider.getInstance());
  try {
    tokenHolder.start();
    fail("Should have thrown exception");
  }
 catch (  IllegalStateException e) {
  }
}
