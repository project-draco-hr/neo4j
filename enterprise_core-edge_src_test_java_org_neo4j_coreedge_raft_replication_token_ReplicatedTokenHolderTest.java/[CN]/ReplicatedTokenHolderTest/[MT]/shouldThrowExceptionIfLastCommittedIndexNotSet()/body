{
  ReplicatedTokenHolder<Token,LabelTokenRecord> tokenHolder=new ReplicatedLabelTokenHolder(null,null,dependencies);
  try {
    tokenHolder.start();
    fail("Should have thrown exception");
  }
 catch (  IllegalStateException e) {
  }
}
