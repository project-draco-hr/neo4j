{
  ReplicatedTokenHolder<Token,LabelTokenRecord> tokenHolder=new ReplicatedLabelTokenHolder(null,null,dependencies,TIMEOUT_MILLIS);
  try {
    tokenHolder.start();
    fail("Should have thrown exception");
  }
 catch (  IllegalStateException e) {
  }
}
