{
  if (iAmMaster()) {
  }
 else {
    ResponseReceiver receiver=null;
    this.localGraph=new EmbeddedGraphDbImpl("storeDir",new HashMap<String,String>(),this,new SlaveLockManager.SlaveLockManagerFactory(broker,receiver),new SlaveIdGenerator.SlaveIdGeneratorFactory(broker,receiver));
  }
}
