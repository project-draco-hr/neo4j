{
  File file=new File(storeDir,"i-am-master");
  if (isMaster) {
    try {
      file.createNewFile();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
 else {
    file.delete();
  }
}
