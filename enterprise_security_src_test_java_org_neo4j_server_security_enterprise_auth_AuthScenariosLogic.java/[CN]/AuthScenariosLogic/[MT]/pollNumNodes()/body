{
  Transaction tx=neo.getGraph().beginTx();
  Statement statement=neo.getGraph().getDependencyResolver().resolveDependency(ThreadToStatementContextBridge.class).get();
  long nodeCount=statement.readOperations().countsForNode(-1);
  tx.success();
  tx.close();
  return nodeCount;
}
