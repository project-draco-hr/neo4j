{
  pageCache=new MuninnPageCache(swapperFactory,calculateMaxPages(config),calculatePageSize(config),monitor);
  pageEvictionJobHandle=scheduler.schedule(JobScheduler.Group.pageCacheEviction,pageCache);
}
