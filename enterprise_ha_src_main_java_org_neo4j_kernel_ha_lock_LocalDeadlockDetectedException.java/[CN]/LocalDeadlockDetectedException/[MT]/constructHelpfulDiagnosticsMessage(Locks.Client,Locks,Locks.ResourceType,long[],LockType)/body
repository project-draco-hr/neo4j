{
  StringBuilder builder=new StringBuilder(format("%s tried to apply local %s lock on %s(%s) after acquired on master. Currently these locks exist:%n",client,type,resourceType,Arrays.toString(resourceIds)));
  lockManager.dumpLocks(new StringBuilderStringLogger(builder));
  return builder.toString();
}
