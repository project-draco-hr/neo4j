{
  if (retries > 20) {
    close();
  }
  log("Send not ok");
  connection.write();
  if (!buffer.hasRemaining()) {
    buffer.clear();
    releaseWriteBuffer();
    setStatus(Status.GET_MESSAGE);
    return true;
  }
  retries++;
  return false;
}
