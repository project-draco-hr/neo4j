{
  return new BaseMatcher<MemberIsAvailable>(){
    @Override public boolean matches(    Object item){
      MemberIsAvailable input=(MemberIsAvailable)item;
      return nullSafeEquals(input.getClusterUri(),expected.getClusterUri()) && nullSafeEquals(input.getRole(),expected.getRole()) && nullSafeEquals(input.getRoleUri(),expected.getRoleUri());
    }
    @Override public void describeTo(    Description description){
    }
  }
;
}
