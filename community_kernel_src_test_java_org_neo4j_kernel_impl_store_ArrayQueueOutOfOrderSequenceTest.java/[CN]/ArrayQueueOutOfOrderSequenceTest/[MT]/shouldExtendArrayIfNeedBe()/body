{
  OutOfOrderSequence sequence=new ArrayQueueOutOfOrderSequence(0L,5);
  offer(sequence,3L,0);
  offer(sequence,2L,1);
  offer(sequence,5L,2);
  offer(sequence,4L,3);
  offer(sequence,6L,4);
  offer(sequence,1L,5);
  assertGet(sequence,6L,4);
}
