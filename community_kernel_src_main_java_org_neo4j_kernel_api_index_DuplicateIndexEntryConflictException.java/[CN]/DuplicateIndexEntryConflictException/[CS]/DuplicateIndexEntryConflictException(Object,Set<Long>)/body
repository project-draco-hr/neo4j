{
  super(String.format("Attempting to set same property value %s on nodes with ids %s " + "disallowed by unique index.",propertyValue,conflictingNodeIds));
  this.propertyValue=propertyValue;
  this.conflictingNodeIds=conflictingNodeIds;
}
