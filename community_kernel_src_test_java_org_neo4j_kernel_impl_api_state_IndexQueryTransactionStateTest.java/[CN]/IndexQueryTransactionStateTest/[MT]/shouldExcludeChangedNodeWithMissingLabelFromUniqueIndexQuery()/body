{
  when(store.nodeGetUniqueFromIndexSeek(state,indexDescriptor,value)).thenReturn(asPrimitiveResourceIterator());
  state.txState().nodeDoReplaceProperty(1l,Property.noNodeProperty(1l,propertyKeyId),Property.intProperty(propertyKeyId,10));
  long result=txContext.nodeGetUniqueFromIndexSeek(state,indexDescriptor,value);
  assertNoSuchNode(result);
}
