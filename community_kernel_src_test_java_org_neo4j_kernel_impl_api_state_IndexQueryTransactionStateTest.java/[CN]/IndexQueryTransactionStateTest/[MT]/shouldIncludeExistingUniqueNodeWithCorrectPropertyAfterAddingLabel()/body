{
  when(store.nodeGetFromUniqueIndexSeek(state,indexDescriptor,value)).thenReturn(asPrimitiveResourceIterator());
  when(statement.acquireSingleNodeCursor(2l)).thenReturn(asNodeCursor(2l,asPropertyCursor(stringProperty(propertyKeyId,value)),LabelCursor.EMPTY));
  txContext.nodeAddLabel(state,2l,labelId);
  long result=txContext.nodeGetFromUniqueIndexSeek(state,indexDescriptor,value);
  assertThat(result,equalTo(2l));
}
