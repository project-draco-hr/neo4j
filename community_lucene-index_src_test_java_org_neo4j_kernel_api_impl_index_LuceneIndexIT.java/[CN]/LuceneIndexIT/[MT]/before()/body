{
  LuceneIndex index=LuceneIndexBuilder.create().withIndexRootFolder(testDir.directory()).withIndexIdentifier("testIndex").build();
  index.prepare();
  index.open();
  accessor=new LuceneIndexAccessor(index);
}
