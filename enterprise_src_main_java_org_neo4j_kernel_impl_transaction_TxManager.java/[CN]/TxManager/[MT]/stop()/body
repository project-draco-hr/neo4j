{
  StringLogger.close(txLogDir + "/messages.log");
  if (txLog != null) {
    try {
      txLog.close();
    }
 catch (    IOException e) {
      log.warning("Unable to close tx log[" + txLog.getName() + "]"+ ", "+ e);
    }
  }
}
