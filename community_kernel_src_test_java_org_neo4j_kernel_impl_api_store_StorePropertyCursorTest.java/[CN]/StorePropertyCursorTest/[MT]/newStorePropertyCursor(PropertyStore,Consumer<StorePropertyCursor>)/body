{
  DynamicStringStore s=propertyStore.getStringStore();
  DynamicArrayStore a=propertyStore.getArrayStore();
  RecordCursor<DynamicRecord> sc=s.newRecordCursor(s.newRecord());
  RecordCursor<DynamicRecord> ac=a.newRecordCursor(a.newRecord());
  return new StorePropertyCursor(propertyStore.newRecordCursor(propertyStore.newRecord()).acquire(0,NORMAL),propertyStore.getStringStore().newRecordCursor(propertyStore.getStringStore().newRecord()).acquire(0,NORMAL),propertyStore.getArrayStore().newRecordCursor(propertyStore.getArrayStore().newRecord()).acquire(0,NORMAL),cache);
}
