{
  for (  HighAvailabilityMemberListener listener : listeners) {
    listener.masterIsAvailable(new HighAvailabilityMemberChangeEvent(HighAvailabilityMemberState.TO_MASTER,HighAvailabilityMemberState.MASTER,myId,uri));
  }
}
