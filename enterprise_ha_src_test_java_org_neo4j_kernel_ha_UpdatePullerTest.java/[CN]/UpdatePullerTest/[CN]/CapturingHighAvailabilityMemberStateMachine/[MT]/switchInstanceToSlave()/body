{
  for (  HighAvailabilityMemberListener listener : listeners) {
    listener.slaveIsAvailable(new HighAvailabilityMemberChangeEvent(HighAvailabilityMemberState.TO_SLAVE,HighAvailabilityMemberState.SLAVE,myId,uri));
  }
}
