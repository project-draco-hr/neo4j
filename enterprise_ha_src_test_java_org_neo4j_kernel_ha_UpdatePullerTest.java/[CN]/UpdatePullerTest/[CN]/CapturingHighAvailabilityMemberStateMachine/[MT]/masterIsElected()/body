{
  for (  HighAvailabilityMemberListener listener : listeners) {
    listener.masterIsElected(new HighAvailabilityMemberChangeEvent(HighAvailabilityMemberState.PENDING,HighAvailabilityMemberState.TO_MASTER,myId,uri));
  }
}
