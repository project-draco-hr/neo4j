{
  final UpdatePullerClient puller=new UpdatePullerClient(1,scheduler,NullLogProvider.getInstance(),updatePuller,availabilityGuard);
  puller.init();
  assertNotNull(scheduler.getJob());
  scheduler.runJob();
  verifyZeroInteractions(lastUpdateTime,availabilityGuard);
}
