{
  final UpdatePuller puller=new UpdatePuller(stateMachine,master,requestContextFactory,availabilityGuard,lastUpdateTime,config,scheduler,stringLogger,resolver);
  puller.init();
  assertNotNull(scheduler.getJob());
  scheduler.runJob();
  verifyZeroInteractions(lastUpdateTime,availabilityGuard);
}
