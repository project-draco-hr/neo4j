{
  if (pool == null) {
    return;
  }
  Iterator<Reader> itr=pool.iterator();
  while (itr.hasNext()) {
    Reader reader=itr.next();
    if (reader.version() == version) {
      dispose(reader);
      itr.remove();
    }
  }
}
