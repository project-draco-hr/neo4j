{
  resume();
  try {
    server.removeIndexNode(connection.neo,indexId,nodeId,key,value);
  }
 catch (  Exception ex) {
    return response().buildErrorResponse(ex);
  }
  return response().buildVoidResponse();
}
