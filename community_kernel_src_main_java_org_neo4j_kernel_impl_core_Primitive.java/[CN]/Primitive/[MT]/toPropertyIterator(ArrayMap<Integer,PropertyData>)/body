{
  return map(new Function<PropertyData,SafeProperty>(){
    @Override public SafeProperty apply(    PropertyData from){
      return Property.property(from.getIndex(),from.getValue());
    }
  }
,loadedProperties.values()).iterator();
}
