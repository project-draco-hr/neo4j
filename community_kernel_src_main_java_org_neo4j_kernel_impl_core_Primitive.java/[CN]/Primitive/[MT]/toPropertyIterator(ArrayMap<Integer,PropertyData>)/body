{
  return map(new Function<PropertyData,Property>(){
    @Override public Property apply(    PropertyData from){
      return Property.property(from.getIndex(),from.getValue());
    }
  }
,loadedProperties.values()).iterator();
}
