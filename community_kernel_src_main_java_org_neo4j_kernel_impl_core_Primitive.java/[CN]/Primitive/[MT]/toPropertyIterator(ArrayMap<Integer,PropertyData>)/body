{
  return map(new Function<PropertyData,DefinedProperty>(){
    @Override public DefinedProperty apply(    PropertyData from){
      return Property.property(from.getIndex(),from.getValue());
    }
  }
,loadedProperties.values()).iterator();
}
