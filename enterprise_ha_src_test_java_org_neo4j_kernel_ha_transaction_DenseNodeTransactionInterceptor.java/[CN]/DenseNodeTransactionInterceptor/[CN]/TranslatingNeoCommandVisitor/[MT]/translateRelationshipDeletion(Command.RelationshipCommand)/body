{
  RelationshipDeleter deleter=new RelationshipDeleter(new RelationshipLocker(){
    @Override public void getWriteLock(    long relId) throws AcquireLockTimeoutException {
      return;
    }
  }
,new RelationshipGroupGetter(neoStore.getRelationshipGroupStore()),new PropertyDeleter(neoStore.getPropertyStore(),new PropertyTraverser()));
  RelationshipRecord record=command.getRecord();
  deleter.relDelete(record.getId(),recordChangeSet);
}
