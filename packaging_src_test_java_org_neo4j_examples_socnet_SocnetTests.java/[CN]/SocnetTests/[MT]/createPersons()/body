{
  Transaction tx=graphDb.beginTx();
  try {
    for (int i=0; i < nrOfPersons; i++) {
      personRepository.createPerson("person#" + i);
    }
    tx.success();
  }
  finally {
    tx.finish();
  }
}
