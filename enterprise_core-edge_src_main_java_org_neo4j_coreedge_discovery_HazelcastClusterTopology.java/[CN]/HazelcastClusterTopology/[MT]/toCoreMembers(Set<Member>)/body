{
  HashSet<CoreMember> coreMembers=new HashSet<>();
  for (  Member member : members) {
    coreMembers.add(new CoreMember(address(member.getStringAttribute(TRANSACTION_SERVER)),address(member.getStringAttribute(RAFT_SERVER))));
  }
  return coreMembers;
}
