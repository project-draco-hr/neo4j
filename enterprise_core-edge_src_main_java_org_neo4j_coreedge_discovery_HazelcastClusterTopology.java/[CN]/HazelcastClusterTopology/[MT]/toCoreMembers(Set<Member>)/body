{
  HashSet<CoreMember> coreMembers=new HashSet<>();
  for (  Member member : members) {
    coreMembers.add(new CoreMember(new AdvertisedSocketAddress(member.getStringAttribute(TRANSACTION_SERVER)),new AdvertisedSocketAddress(member.getStringAttribute(RAFT_SERVER)),new AdvertisedSocketAddress(member.getStringAttribute(BOLT_SERVER))));
  }
  return coreMembers;
}
