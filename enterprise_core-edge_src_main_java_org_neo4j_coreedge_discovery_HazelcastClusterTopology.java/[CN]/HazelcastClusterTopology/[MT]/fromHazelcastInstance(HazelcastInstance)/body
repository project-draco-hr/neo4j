{
  Set<Member> coreMembers=hazelcastInstance.getCluster().getMembers();
  return new ClusterTopology(canBeBootstrapped(coreMembers),toCoreMembers(coreMembers),toBoltMembers(coreMembers),edgeMembers(hazelcastInstance));
}
