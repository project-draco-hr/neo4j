{
  Set<BoltAddress> coreMembers=new HashSet<>();
  for (  Member member : members) {
    if (member.getStringAttribute(BOLT_SERVER) != null) {
      coreMembers.add(new BoltAddress(new AdvertisedSocketAddress(member.getStringAttribute(BOLT_SERVER))));
    }
  }
  return coreMembers;
}
