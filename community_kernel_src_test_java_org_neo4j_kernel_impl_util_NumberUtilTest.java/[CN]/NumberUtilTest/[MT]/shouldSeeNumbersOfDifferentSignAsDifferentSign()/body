{
  Random random=new Random();
  for (int i=0; i < 100; i++) {
    assertFalse(haveSameSign(abs(random.nextLong()),-abs(random.nextLong())));
    assertFalse(haveSameSign(-abs(random.nextLong()),abs(random.nextLong())));
  }
}
