{
  File directory=new File(PHYSICAL_LOG_DIRECTORY_NAME);
  FileSystemAbstraction fileSystem=fsRule.get();
  fileSystem.mkdir(directory);
  return life.add(new SegmentedRaftLog(fileSystem,directory,1024,new DummyRaftableContentSerializer(),getInstance(),"1 entries",8,Clocks.fakeClock(),new OnDemandJobScheduler()));
}
