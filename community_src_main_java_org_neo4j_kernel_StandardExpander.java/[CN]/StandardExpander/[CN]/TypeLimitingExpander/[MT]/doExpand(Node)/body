{
  return new FilteringIterator<Relationship>(start.getRelationships().iterator()){
    @Override protected boolean passes(    Relationship item){
      Direction dir=exclusion.get(item.getType().name());
      return !matchDirection(dir,start,item);
    }
  }
;
}
