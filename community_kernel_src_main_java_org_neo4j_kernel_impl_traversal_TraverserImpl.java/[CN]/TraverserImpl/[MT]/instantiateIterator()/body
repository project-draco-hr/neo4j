{
  TraverserIterator iterator=new TraverserIterator(description.uniqueness.create(description.uniquenessParameter),description.expander,description.branchOrdering,description.evaluator,startNodes,description.initialState);
  return description.sorting != null ? new SortingTraverserIterator(this,iterator) : iterator;
}
