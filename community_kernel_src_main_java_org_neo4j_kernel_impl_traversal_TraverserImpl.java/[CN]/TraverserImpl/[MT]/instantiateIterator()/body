{
  TraverserIterator iterator=new TraverserIterator(description.uniqueness.create(description.uniquenessParameter),description.expander,description.branchOrdering,description.evaluator,startNodes,description.initialState,description.uniqueness);
  return description.sorting != null ? new SortingTraverserIterator(this,iterator) : iterator;
}
