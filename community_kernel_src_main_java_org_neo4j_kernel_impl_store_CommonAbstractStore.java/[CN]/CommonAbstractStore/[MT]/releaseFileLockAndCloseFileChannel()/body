{
  try {
    if (fileLock != null) {
      fileLock.release();
    }
    if (fileChannel != null) {
      fileChannel.close();
    }
  }
 catch (  IOException e) {
    log.warn("Could not close [" + storageFileName + "]",e);
  }
  fileChannel=null;
}
