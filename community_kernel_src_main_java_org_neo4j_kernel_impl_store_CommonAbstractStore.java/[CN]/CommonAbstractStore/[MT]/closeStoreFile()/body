{
  try {
    storeFile.close();
    if (idGenerator != null) {
      if (contains(openOptions,DELETE_ON_CLOSE)) {
        idGenerator.delete();
      }
 else {
        idGenerator.close();
      }
    }
  }
  finally {
    storeFile=null;
  }
}
