{
  return new CommittedTransactionRepresentation(new LogEntryStart(id,id,id,id - 1,new byte[]{},LogPosition.UNSPECIFIED),new PhysicalTransactionRepresentation(asList(nodeCommand())),new OnePhaseCommit(id,id));
}
