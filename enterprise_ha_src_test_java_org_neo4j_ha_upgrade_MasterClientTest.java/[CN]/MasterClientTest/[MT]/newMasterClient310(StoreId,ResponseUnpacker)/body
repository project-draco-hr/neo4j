{
  return life.add(new MasterClient310(MASTER_SERVER_HOST,MASTER_SERVER_PORT,null,NullLogProvider.getInstance(),storeId,TIMEOUT,TIMEOUT,1,CHUNK_SIZE,responseUnpacker,monitors.newMonitor(ByteCounterMonitor.class,MasterClient310.class),monitors.newMonitor(RequestMonitor.class,MasterClient310.class),logEntryReader));
}
