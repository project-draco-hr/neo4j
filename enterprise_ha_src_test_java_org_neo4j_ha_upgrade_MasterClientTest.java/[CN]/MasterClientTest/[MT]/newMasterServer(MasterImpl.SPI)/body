{
  MasterImpl master=new MasterImpl(masterImplSPI,mock(Monitor.class),mock(Logging.class),masterConfig());
  return new MasterServer(master,mock(Logging.class,RETURNS_MOCKS),masterServerConfiguration(),mock(TxChecksumVerifier.class),new Monitors());
}
