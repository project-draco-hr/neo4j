{
  return new MasterClient214(MASTER_SERVER_HOST,MASTER_SERVER_PORT,mock(Logging.class,RETURNS_MOCKS),storeId,TIMEOUT,TIMEOUT,1,CHUNK_SIZE,monitors.newMonitor(ByteCounterMonitor.class,MasterClient214.class),monitors.newMonitor(RequestMonitor.class,MasterClient214.class));
}
