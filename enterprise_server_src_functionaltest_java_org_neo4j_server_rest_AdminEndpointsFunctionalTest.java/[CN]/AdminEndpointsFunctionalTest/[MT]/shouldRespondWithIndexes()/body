{
  ClientResponse response=Client.create().resource(functionalTestHelper.managementUri() + "properties/neo4j-servers").accept(MediaType.APPLICATION_JSON_TYPE).get(ClientResponse.class);
  assertEquals(200,response.getStatus());
}
