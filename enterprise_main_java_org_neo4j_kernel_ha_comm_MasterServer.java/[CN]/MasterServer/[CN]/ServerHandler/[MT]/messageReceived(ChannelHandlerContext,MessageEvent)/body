{
  try {
    MasterInvoker invoker=(MasterInvoker)event.getMessage();
    Response<DataWriter> result=invoker.invoke(realMaster);
    event.getChannel().write(new ChunkedResponse(result));
  }
 catch (  Exception e) {
    e.printStackTrace();
    throw e;
  }
}
