{
  NodeOrRelationship result=null;
  if (typedId.isNode()) {
    result=NodeOrRelationship.wrap(server.getNeo().getNodeById(typedId.getId()));
  }
 else {
    result=NodeOrRelationship.wrap(server.getNeo().getRelationshipById(typedId.getId()));
  }
  return result;
}
