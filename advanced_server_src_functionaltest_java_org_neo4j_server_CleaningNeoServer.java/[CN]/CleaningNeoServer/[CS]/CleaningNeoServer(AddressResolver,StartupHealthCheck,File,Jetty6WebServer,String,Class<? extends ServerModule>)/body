{
  super(ServerTestUtils.EMBEDDED_GRAPH_DATABASE_FACTORY,addressResolver,startupHealthCheck,configFile,jetty6WebServer,serverModulesOrDefault(serverModules));
  this.configFile=configFile;
  this.dir=dir;
  if (lastStarted != null) {
    try {
      throw lastStarted;
    }
  finally {
      lastStarted=null;
    }
  }
}
