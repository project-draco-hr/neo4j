{
  pageCache=pageCacheRule.getPageCache(fs);
  storeFactory=new StoreFactory(storeDir.directory(),config,new DefaultIdGeneratorFactory(fs),pageCache,fs,NullLogProvider.getInstance(),new Monitors());
  upgradableDatabase=new UpgradableDatabase(new StoreVersionCheck(pageCache));
}
