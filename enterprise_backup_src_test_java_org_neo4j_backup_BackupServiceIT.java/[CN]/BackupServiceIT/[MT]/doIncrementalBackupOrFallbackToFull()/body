{
  BackupService backupService=backupService();
  backupService.doIncrementalBackupOrFallbackToFull(BACKUP_HOST,backupPort,backupDir,ConsistencyCheck.NONE,Config.empty(),BackupClient.BIG_READ_TIMEOUT,false);
}
