{
  BackupService backupService=backupService();
  backupService.doIncrementalBackupOrFallbackToFull(BACKUP_HOST,backupPort,backupDir,ConsistencyCheck.NONE,new Config(),BackupClient.BIG_READ_TIMEOUT,false);
}
