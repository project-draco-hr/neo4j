{
  String encoded=ArrayEncoder.encode(new String[]{"This string is long enough for BASE64 to emit a line break, making the encoding platform dependant."});
  assertTrue("The encoding is platform dependant",encoded.contains(System.lineSeparator()));
}
