{
  if (position > getCachePageSize()) {
    String msg="Position " + position + " is greater than the upper "+ "page size bound of "+ (getCachePageSize());
    throw new IndexOutOfBoundsException(msg);
  }
}
