{
  if (position > cachePageSize) {
    String msg="Position " + position + " is greater than the upper "+ "page size bound of "+ cachePageSize;
    throw new IndexOutOfBoundsException(msg);
  }
  return true;
}
