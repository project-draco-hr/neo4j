{
  return format("MuninnPage@%x[%s -> %x, filePageId = %s%s, swapper = %s, usage counter = %s, %s]",hashCode(),getCachePageId(),pointer,filePageId,(isDirty() ? ", dirty" : ""),swapper,getUsageCounter(),super.toString());
}
