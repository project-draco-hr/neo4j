{
  if (bufferProxy == null) {
    pointer=UnsafeUtil.malloc(cachePageSize);
    try {
      bufferProxy=(ByteBuffer)directBufferConstructor.newInstance(pointer,cachePageSize);
    }
 catch (    Exception e) {
      throw new AssertionError(e);
    }
  }
}
