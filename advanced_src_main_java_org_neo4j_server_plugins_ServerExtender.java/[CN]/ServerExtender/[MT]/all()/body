{
  return new NestingIterable<PluginPoint,Map<String,PluginPoint>>(extensions.values()){
    @Override protected Iterator<PluginPoint> createNestedIterator(    Map<String,PluginPoint> item){
      return item.values().iterator();
    }
  }
;
}
