{
  if (extensions.get(extension.name()) != null) {
    throw new IllegalArgumentException("This extension already has an extension point with the name \"" + extension.name() + "\"");
  }
  extensions.put(extension.name(),extension);
}
