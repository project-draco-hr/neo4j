{
  if (retries > 20) {
    close();
  }
  if (!acquireWriteBuffer()) {
    retries++;
    return false;
  }
  buffer.put(HeaderConstants.OK);
  buffer.flip();
  log("Setup ok");
  setStatus(Status.SEND_OK);
  retries=0;
  return true;
}
