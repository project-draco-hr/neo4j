{
  Store<TestRecord,TestCursor> store=new StandardStore<>(new TestHeaderlessStoreFormat(),path,new TestStoreIdGenerator(),cache,fs,NullLogProvider.getInstance());
  store.init();
  store.start();
  return store;
}
