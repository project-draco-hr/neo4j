{
  Store<TestRecord,TestCursor> store=new StandardStore<>(new TestHeaderlessStoreFormat(),path,new TestStoreIdGenerator(),cache,fs,StringLogger.DEV_NULL);
  store.init();
  store.start();
  return store;
}
