{
  try (KernelTransaction transaction=kernel.newTransaction(KernelTransaction.Type.implicit,AccessMode.Static.FULL)){
    long defaultTimeout=Config.defaults().get(GraphDatabaseSettings.transaction_timeout);
    assertEquals("By default should start transaction with default timeout.",defaultTimeout,transaction.timeout());
  }
 }
