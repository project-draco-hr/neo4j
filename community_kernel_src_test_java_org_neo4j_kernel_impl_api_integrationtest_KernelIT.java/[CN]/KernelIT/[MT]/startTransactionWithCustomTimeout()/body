{
  long transactionTimeout=12345L;
  try (KernelTransaction transaction=kernel.newTransaction(KernelTransaction.Type.explicit,AccessMode.Static.READ,transactionTimeout)){
    assertEquals("Transaction should have custom timeout.",transactionTimeout,transaction.timeout());
  }
 }
