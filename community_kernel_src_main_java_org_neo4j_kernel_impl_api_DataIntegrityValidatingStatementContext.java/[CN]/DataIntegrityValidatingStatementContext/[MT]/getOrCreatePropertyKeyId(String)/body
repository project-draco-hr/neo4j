{
  if (propertyKey == null) {
    throw new DataIntegrityKernelException(String.format("Null is not a valid property name. Only non-null strings are allowed."));
  }
  return delegate.getOrCreatePropertyKeyId(propertyKey);
}
