{
  super.setUpGlobal();
  RandomGraph graph1;
  SmallWorldGraph graph2;
  PreferentialAttachment graph3;
  graph=new PreferentialAttachment(neo,new GraphStore(neo),MyRelTypes.R1,10,3);
  numberOfRuns=100;
}
