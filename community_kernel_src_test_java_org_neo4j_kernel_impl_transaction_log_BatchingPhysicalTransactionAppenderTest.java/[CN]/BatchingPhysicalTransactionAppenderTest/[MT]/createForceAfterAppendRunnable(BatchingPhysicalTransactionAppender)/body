{
  return new Runnable(){
    @Override public void run(){
      try {
        appender.forceAfterAppend();
      }
 catch (      IOException e) {
        throw new RuntimeException(e);
      }
    }
  }
;
}
