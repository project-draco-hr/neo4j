{
  int offset=(int)(id - position) * recordSize;
  try {
    buffer.setOffset(offset);
    return buffer;
  }
 catch (  IllegalArgumentException e) {
    throw new IllegalArgumentException("Unable to set offset. id: " + id + ", position: "+ position+ ", recordSize: "+ recordSize,e);
  }
}
