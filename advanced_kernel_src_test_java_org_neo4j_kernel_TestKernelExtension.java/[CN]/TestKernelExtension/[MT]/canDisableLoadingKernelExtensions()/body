{
  EmbeddedGraphDatabase graphdb=graphdb("graphdb",false);
  try {
    assertFalse("Extensions were loaded despite configured not to",new DummyExtension().isLoaded(getExtensions(graphdb)));
  }
  finally {
    graphdb.shutdown();
  }
}
