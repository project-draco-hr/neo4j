{
  newTransaction();
  statement.uniqueIndexCreate(labelId,propertyKey);
  commit();
  try {
    newTransaction();
    statement.indexCreate(labelId,propertyKey);
    commit();
    fail("expected exception");
  }
 catch (  SchemaKernelException e) {
    assertEquals(format("Unable to add index on [label: %s, %s] : Already constrained " + "CONSTRAINT ON ( n:label[%s] ) ASSERT n.property[%s] IS UNIQUE.",labelId,propertyKey,labelId,propertyKey),e.getMessage());
  }
}
