{
  try {
    model.openBrowser(link.getText());
  }
 catch (  IOException|URISyntaxException e) {
    e.printStackTrace(System.out);
    showMessageDialog(link,format("Couldn't open the browser: %s",e.getMessage()),"Error",ERROR_MESSAGE);
  }
}
