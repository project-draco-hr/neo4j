{
  if (propertyKeysToInclude.size() > 0) {
    handlePropertiesDefaultInclude(getRemovedPropertiesOnCommit(data),getAssignedPropertiesOnCommit(data));
  }
 else {
    handlePropertiesDefaultIgnore(getRemovedPropertiesOnCommit(data),getAssignedPropertiesOnCommit(data));
  }
  return null;
}
