{
  Collection<DynamicRecord> records=nodeStore.allocateRecordsForDynamicLabels(nodeId,longs,IteratorUtil.<DynamicRecord>emptyIterator());
  nodeStore.updateDynamicLabelRecords(records);
  return records;
}
