{
  ClientRequest request;
  try {
    if (payload != null) {
      request=ClientRequest.create().type(payloadType).accept(accept).entity(payload).build(new URI(uri),method);
    }
 else {
      request=ClientRequest.create().accept(accept).build(new URI(uri),method);
    }
  }
 catch (  URISyntaxException e) {
    System.out.println("URI syntax exception: '" + uri + "'");
    throw new RuntimeException(e);
  }
  return retrieveResponse(title,description,uri,responseCode,accept,headerFields,request);
}
