{
  Collection<Object[]> mappings=new ArrayList<>();
  for (  DocTable.Row row : DocsRepository.docs().read("dev/serialization.asciidoc","#bolt-type-system-mapping",DocTable.table).get(0)) {
    mappings.add(new Object[]{row.get(0)});
  }
  return mappings;
}
