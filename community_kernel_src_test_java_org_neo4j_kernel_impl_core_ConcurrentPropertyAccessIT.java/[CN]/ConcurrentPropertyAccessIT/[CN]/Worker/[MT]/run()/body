{
  while (true) {
    try (Transaction tx=db.beginTx()){
      doSomething();
      tx.success();
    }
 catch (    Throwable t) {
      t.printStackTrace(System.err);
      System.err.flush();
    }
  }
}
