{
  Node[] nodes=new Node[100];
  Transaction tx=db.beginTx();
  try {
    for (int i=0; i < nodes.length; i++) {
      nodes[i]=db.createNode();
    }
    tx.success();
  }
  finally {
    tx.finish();
  }
  return nodes;
}
