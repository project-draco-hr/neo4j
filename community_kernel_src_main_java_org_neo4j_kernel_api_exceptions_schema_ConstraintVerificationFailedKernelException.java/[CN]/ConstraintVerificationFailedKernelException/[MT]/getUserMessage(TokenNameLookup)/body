{
  StringBuilder message=new StringBuilder();
  for (  Evidence evidenceItem : evidence()) {
    message.append(evidenceItem.message(tokenNameLookup.labelGetName(constraint.label()),tokenNameLookup.propertyKeyGetName(constraint.propertyKeyId())));
  }
  return message.toString();
}
