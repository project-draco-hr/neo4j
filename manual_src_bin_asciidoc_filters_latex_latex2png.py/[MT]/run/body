def run(cmd):
    global verbose
    if verbose:
        cmd += ' 1>&2'
    else:
        cmd += (' 2>%s 1>&2' % os.devnull)
    print_verbose(('executing: %s' % cmd))
    if os.system(cmd):
        raise EApp, ('failed command: %s' % cmd)
