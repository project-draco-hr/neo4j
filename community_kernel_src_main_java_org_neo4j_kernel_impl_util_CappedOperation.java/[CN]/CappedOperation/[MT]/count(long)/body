{
  return new Switch<T>(){
    private long count;
    @Override public boolean accept(    T item){
      return ++count >= maxCount;
    }
    @Override public void reset(){
      count=0;
    }
  }
;
}
