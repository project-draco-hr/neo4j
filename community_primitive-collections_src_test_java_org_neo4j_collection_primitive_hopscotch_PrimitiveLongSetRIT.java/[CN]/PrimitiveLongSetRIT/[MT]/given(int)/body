{
  return new Printable(){
    @Override public void print(    LinePrinter out){
      out.println(PrimitiveLongSet.class.getSimpleName() + " set = new " + PrimitiveLongHashSet.class.getSimpleName()+ "( "+ h+ " );");
    }
  }
;
}
