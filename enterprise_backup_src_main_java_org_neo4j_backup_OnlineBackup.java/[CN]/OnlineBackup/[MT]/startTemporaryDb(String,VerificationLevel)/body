{
  if (verification != VerificationLevel.NONE)   return new EmbeddedGraphDatabase(targetDirectory,MapUtil.stringMap(Config.LOG_DESERIALIZER_IMPLEMENTATION,verification.deserializerName));
 else   return new EmbeddedGraphDatabase(targetDirectory);
}
