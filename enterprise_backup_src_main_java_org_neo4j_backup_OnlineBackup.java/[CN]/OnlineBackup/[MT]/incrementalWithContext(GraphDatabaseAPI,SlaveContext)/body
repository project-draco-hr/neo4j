{
  BackupClient client=new BackupClient(hostNameOrIp,port,targetDb.getMessageLog(),targetDb.getStoreId());
  try {
    unpackResponse(client.incrementalBackup(context),targetDb,new ProgressTxHandler());
    trimLogicalLogCount(targetDb);
  }
  finally {
    client.shutdown();
  }
  return this;
}
