{
  if (verification)   return new EmbeddedGraphDatabase(targetDirectory,MapUtil.stringMap(Config.LOG_DESERIALIZER_IMPLEMENTATION,VerifyingLogDeserializerProvider.NAME));
 else   return new EmbeddedGraphDatabase(targetDirectory);
}
