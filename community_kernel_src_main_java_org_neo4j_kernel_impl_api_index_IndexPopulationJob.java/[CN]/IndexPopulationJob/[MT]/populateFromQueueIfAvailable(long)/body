{
  int n=0;
  for (NodePropertyUpdate update=queue.poll(); update != null; update=queue.poll(), n++) {
    boolean hasNotYetBeenIndexed=update.getNodeId() <= maxIndexedId;
    if (hasNotYetBeenIndexed)     update.apply(n,indexManipulator);
  }
}
