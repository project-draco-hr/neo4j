{
  storeScan=storeView.visitNodesWithPropertyAndLabel(labelId,propertyKeyId,new Visitor<Pair<Long,Object>>(){
    @Override public boolean visit(    Pair<Long,Object> element){
      populator.add(element.first(),element.other());
      populateFromQueueIfAvailable(element.first());
      return false;
    }
  }
);
  storeScan.run();
}
