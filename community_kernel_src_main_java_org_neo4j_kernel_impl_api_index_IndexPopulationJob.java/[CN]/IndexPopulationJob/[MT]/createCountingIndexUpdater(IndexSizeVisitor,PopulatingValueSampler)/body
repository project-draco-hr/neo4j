{
  final IndexUpdater countingIndexUpdater=new CountingIndexUpdater(MAX_TX_ID,populator.newPopulatingUpdater(storeView),countVisitor);
  return new SamplingIndexUpdater(countingIndexUpdater,populatingSampler);
}
