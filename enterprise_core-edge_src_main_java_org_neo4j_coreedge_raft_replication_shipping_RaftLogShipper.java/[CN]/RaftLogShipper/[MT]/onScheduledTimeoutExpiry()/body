{
  if (timedOut()) {
    onTimeout();
    return;
  }
  if (timeoutAbsoluteMillis <= 0) {
    return;
  }
  long timeLeft=timeoutAbsoluteMillis - clock.millis();
  if (timeLeft > 0) {
    scheduleTimeout(timeLeft);
  }
 else {
    onTimeout();
  }
}
