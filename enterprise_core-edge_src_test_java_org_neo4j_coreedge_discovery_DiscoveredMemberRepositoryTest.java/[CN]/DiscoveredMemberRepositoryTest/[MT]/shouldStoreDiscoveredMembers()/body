{
  DiscoveredMemberRepository discoveredMemberRepositoryA=new DiscoveredMemberRepository(testDirectory.directory(),fileSystem,NullLogProvider.getInstance());
  Set<AdvertisedSocketAddress> members=set(new AdvertisedSocketAddress("localhost:5003"),new AdvertisedSocketAddress("localhost:5004"),new AdvertisedSocketAddress("localhost:5005"));
  discoveredMemberRepositoryA.store(members);
  DiscoveredMemberRepository discoveredMemberRepositoryB=new DiscoveredMemberRepository(testDirectory.directory(),fileSystem,NullLogProvider.getInstance());
  assertEquals(members,discoveredMemberRepositoryB.previouslyDiscoveredMembers());
}
