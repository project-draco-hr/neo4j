{
  EphemeralFileSystemAbstraction fileSystem=new EphemeralFileSystemAbstraction();
  indexStorage=new PartitionedIndexStorage(directoryFactory,fileSystem,new File("/target/whatever"),INDEX_ID);
  DeferredConstraintVerificationUniqueLuceneIndexPopulator populator=new DeferredConstraintVerificationUniqueLuceneIndexPopulator(new LuceneIndex(indexStorage),descriptor);
  populator.create();
  return populator;
}
