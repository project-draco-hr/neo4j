{
  DeferredConstraintVerificationUniqueLuceneIndexPopulator populator=new DeferredConstraintVerificationUniqueLuceneIndexPopulator(new LuceneDocumentStructure(),NullLogProvider.getInstance(),IndexWriterFactories.tracking(),searcherManagerFactory,directoryFactory,indexDirectory,false,failureStorage,INDEX_ID,descriptor);
  populator.create();
  return populator;
}
