{
  indexDirectory=new File("target/whatever");
  documentLogic=new LuceneDocumentStructure();
  propertyAccessor=mock(PropertyAccessor.class);
  populator=new DeferredConstraintVerificationUniqueLuceneIndexPopulator(documentLogic,tracking(),new IndexWriterStatus(),directoryFactory,indexDirectory,failureStorage,indexId,descriptor);
  populator.create();
}
