{
  if (!mandatoryRelPropertyConstraints.add(constraint)) {
    throw new IllegalArgumentException(format("Duplicated mandatory relationship property constraint %s for %s",constraint,userDescription));
  }
}
