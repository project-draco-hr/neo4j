{
  if (!mandatoryNodePropertyConstraints.add(constraint)) {
    throw new IllegalArgumentException(format("Duplicated mandatory node property constraint %s for %s",constraint,userDescription));
  }
}
