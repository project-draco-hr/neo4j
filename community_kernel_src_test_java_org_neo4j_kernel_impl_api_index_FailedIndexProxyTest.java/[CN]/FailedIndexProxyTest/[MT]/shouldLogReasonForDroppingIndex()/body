{
  StringLogger log=mock(StringLogger.class);
  new FailedIndexProxy(new IndexDescriptor(0,0),config,new SchemaIndexProvider.Descriptor("foo","bar"),"foo",mock(IndexPopulator.class),IndexPopulationFailure.failure("it broke"),indexCountsRemover,log).drop();
  verify(log).info("FailedIndexProxy#drop index on foo dropped due to:\nit broke");
}
