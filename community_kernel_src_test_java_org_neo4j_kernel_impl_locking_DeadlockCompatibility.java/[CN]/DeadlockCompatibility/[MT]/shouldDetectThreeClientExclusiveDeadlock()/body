{
  assertDetectsDeadlock(acquireExclusive(clientA,NODE,1l),acquireExclusive(clientB,NODE,2l),acquireExclusive(clientC,NODE,3l),acquireExclusive(clientB,NODE,1l),acquireExclusive(clientC,NODE,2l),acquireExclusive(clientA,NODE,3l));
}
