{
  expectedException.expect(UnsupportedOperationException.class);
  expectedException.expectMessage("Too many tokens. Creation of more then " + numberOfSupportedTokens + " "+ "tokens is not supported.");
}
