{
  CursorRef ref=freelist.get();
  if (ref == null) {
    ref=new CursorRef();
    freelist.set(ref);
  }
  StandardPageCursor cursor=ref.cursor;
  if (cursor == null) {
    return new StandardPageCursor(this);
  }
  ref.cursor=cursor.nextFree;
  ref.counter--;
  return cursor;
}
