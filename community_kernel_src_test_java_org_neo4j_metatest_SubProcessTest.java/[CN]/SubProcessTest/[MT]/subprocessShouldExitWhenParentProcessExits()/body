{
  CallbackImpl callback=new CallbackImpl();
  Object proc=new ParentProcess().start(callback);
  assertTrue("Subprocess didn't exit properly",callback.isCalled(10,SECONDS));
  SubProcess.kill(proc);
}
