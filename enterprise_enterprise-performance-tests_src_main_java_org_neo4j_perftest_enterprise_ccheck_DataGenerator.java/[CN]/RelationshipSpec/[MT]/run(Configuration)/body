{
  DataGenerator generator=new DataGenerator(configuration);
  BatchInserter batchInserter=new BatchInserterImpl(configuration.get(store_dir),generator.batchInserterConfig());
  try {
    generator.generateData(batchInserter);
  }
  finally {
    batchInserter.shutdown();
  }
}
