{
  Neo4jTransactionalContext transactionalContext=new Neo4jTransactionalContext(databaseQueryService,transaction,statement,"",Collections.emptyMap(),propertyContainerLocker);
  transactionalContext.check();
  verify(guard).check(statement);
}
