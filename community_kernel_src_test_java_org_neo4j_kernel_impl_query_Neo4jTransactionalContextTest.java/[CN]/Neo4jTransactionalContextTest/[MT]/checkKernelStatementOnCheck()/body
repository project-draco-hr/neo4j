{
  Neo4jTransactionalContext transactionalContext=new Neo4jTransactionalContext(databaseQueryService,transaction,statement,propertyContainerLocker);
  transactionalContext.check();
  verify(guard).check(statement);
}
