{
  Statement statement=statementSupplier.get();
  GraphDatabaseQueryService queryService=queryServiceSupplier.get();
  statement.queryRegistration().registerExecutingQuery(query);
  return new Neo4jTransactionalContext(queryService,transaction,transaction.transactionType(),transaction.mode(),statement,query,locker,txBridge,dbmsOpsFactory,guard,this);
}
