{
  CacheTask action=new CacheTask.CacheNextRel(CheckStage.Stage3_NS_NextRel,cacheAccess,new IterableWrapper<NodeRecord,Delta<NodeRecord>>(nodes.values()){
    @Override protected NodeRecord underlyingObjectToObject(    Delta<NodeRecord> node){
      return node.newRecord;
    }
  }
);
  action.run();
}
