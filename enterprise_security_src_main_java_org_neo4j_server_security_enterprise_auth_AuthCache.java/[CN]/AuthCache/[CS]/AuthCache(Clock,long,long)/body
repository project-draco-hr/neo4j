{
  this.clock=clock;
  this.TTL=ttl;
  this.lastValidation=clock.millis();
  this.executorService=Executors.newSingleThreadExecutor();
  cacheMap=new ConcurrentLinkedHashMap.Builder<K,Pair<Long,V>>().maximumWeightedCapacity(maxCapacity).build();
}
