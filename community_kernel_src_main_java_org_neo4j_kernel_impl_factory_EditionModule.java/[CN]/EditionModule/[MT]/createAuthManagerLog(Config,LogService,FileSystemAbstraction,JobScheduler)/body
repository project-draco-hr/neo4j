{
  Log authManagerLog;
  try {
    authManagerLog=authManagerLog(config,fileSystem,jobScheduler);
  }
 catch (  IOException ioe) {
    logging.getInternalLog(GraphDatabaseFacadeFactory.class).warn("Unable to create log for auth-manager. Auth logging turned off.");
    authManagerLog=NullLog.getInstance();
  }
  return authManagerLog;
}
