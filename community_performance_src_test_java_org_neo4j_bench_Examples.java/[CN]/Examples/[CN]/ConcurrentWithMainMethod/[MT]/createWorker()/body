{
  return new BenchWorker(){
    @Override public WorkerMetric[] doWork(    WorkerContext ctx){
      return new WorkerMetric[]{new WorkerMetric(WorkerMetric.TOTAL,"A result that is summed up for all workers",3.0,OPERATION.per(SECOND)),new WorkerMetric(WorkerMetric.AVERAGE,"A result that is reported as the average reported",3.0,SECOND.per(OPERATION))};
    }
  }
;
}
