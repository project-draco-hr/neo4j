{
  RecordChange<KEY,RECORD,ADDITIONAL> result=recordChanges.get(key);
  if (result == null) {
    result=new RecordChange<KEY,RECORD,ADDITIONAL>(recordChanges,key,loader.load(key,additionalData),loader,manageBeforeState,false);
  }
  return result;
}
