{
  RecordChange<KEY,RECORD,ADDITIONAL> result=recordChanges.get(key);
  if (result == null) {
    result=new RecordChange<>(recordChanges,changeCounter,key,loader.load(key,additionalData),loader,manageBeforeState,false,additionalData);
  }
  return result;
}
