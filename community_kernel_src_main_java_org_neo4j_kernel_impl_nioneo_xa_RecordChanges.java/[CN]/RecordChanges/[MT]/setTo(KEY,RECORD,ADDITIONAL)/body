{
  RecordChange<KEY,RECORD,ADDITIONAL> recordChange=new RecordChange<>(recordChanges,key,newRecord,loader,manageBeforeState,false,additionalData);
  recordChanges.put(key,recordChange);
  recordChange.forChangingData();
}
