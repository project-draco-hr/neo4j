{
  HaRequestType18 specificType=(HaRequestType18)type;
  if (specificType.isLock())   return lockReadTimeout;
  if (specificType == HaRequestType18.COPY_STORE)   return readTimeout * 2;
  return readTimeout;
}
