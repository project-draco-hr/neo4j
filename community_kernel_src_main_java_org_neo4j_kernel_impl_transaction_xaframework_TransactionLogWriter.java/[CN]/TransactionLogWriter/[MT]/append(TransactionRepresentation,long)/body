{
  writer.writeStartEntry(transaction.getMasterId(),transaction.getAuthorId(),transaction.getTimeWritten(),transaction.getLatestCommittedTxWhenStarted(),transaction.additionalHeader());
  writer.serialize(transaction);
  writer.writeCommitEntry(transactionId,transaction.getTimeWritten());
}
