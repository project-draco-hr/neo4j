{
  super.setUpGlobal();
  RandomGraph graph1;
  SmallWorldGraph graph2;
  PreferentialAttachment graph3;
  graph3=new PreferentialAttachment(neo,new GraphStore(neo),MyRelTypes.R1,250000,4);
  graph=graph3;
  numberOfRuns=100;
}
