{
  if (failing) {
    throw new ComException("Told to fail");
  }
  calledWithTxId.add(txId);
  return new Response<>(null,new StoreId(),Iterables.<CommittedTransactionRepresentation>empty(),ResourceReleaser.NO_OP);
}
