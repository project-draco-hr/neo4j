{
  Collection<FakeSlave> slaves=new ArrayList<FakeSlave>();
  for (int i=0; i < count; i++)   slaves.add(new FakeSlave(i < failingSlaves.length ? failingSlaves[i] : false,i));
  return slaves.toArray(new FakeSlave[slaves.size()]);
}
