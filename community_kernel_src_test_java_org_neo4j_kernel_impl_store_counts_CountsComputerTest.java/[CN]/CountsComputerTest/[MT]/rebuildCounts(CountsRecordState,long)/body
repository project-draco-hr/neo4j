{
  CountsTracker tracker=new CountsTracker(StringLogger.DEV_NULL,fs,pageCache,new File(dir,COUNTS_STORE_BASE),BASE_TX_ID);
  countsState.accept(new CountsAccessor.Initializer(tracker));
  tracker.rotate(lastCommittedTransactionId);
  tracker.close();
}
