{
  fs.deleteFile(alphaStoreFile());
  fs.deleteFile(betaStoreFile());
  CountsTracker.createEmptyCountsStore(pageCache,new File(dir,COUNTS_STORE_BASE),buildTypeDescriptorAndVersion(CountsTracker.STORE_DESCRIPTOR));
}
