{
  fs.deleteFile(alphaStoreFile());
  fs.deleteFile(betaStoreFile());
  CountsTracker.createEmptyCountsStore(pageCache,new File(dir,countsStoreBase),buildTypeDescriptorAndVersion(CountsTracker.STORE_DESCRIPTOR));
}
