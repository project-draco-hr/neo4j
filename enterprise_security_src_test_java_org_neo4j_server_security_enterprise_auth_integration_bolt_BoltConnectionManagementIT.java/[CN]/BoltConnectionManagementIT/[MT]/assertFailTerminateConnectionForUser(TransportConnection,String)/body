{
  client.send(TransportTestUtil.chunk(run("CALL dbms.security.terminateConnectionsForUser( '" + username + "' ) YIELD username, connectionCount"),pullAll()));
  assertThat(client,eventuallyReceives(msgFailure(Status.Security.Forbidden,AuthProcedures.PERMISSION_DENIED),msgIgnored()));
  client.send(TransportTestUtil.chunk(ResetMessage.reset()));
  assertThat(client,eventuallyReceives(msgSuccess()));
}
