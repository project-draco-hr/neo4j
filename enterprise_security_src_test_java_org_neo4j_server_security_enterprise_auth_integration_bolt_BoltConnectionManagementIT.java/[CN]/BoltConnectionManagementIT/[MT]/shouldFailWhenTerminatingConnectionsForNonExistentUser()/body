{
  admin.send(TransportTestUtil.chunk(run("CALL dbms.terminateConnectionsForUser( 'NonExistentUser' ) YIELD username, connectionCount"),pullAll()));
  assertThat(admin,eventuallyReceives(msgFailure(Status.General.InvalidArguments,"User 'NonExistentUser' does not exist.")));
}
