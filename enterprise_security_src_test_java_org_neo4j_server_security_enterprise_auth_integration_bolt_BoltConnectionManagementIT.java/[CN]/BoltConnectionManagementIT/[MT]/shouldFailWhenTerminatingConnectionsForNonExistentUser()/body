{
  admin.send(TransportTestUtil.chunk(run("CALL dbms.security.terminateConnectionsForUser( 'NonExistentUser' ) YIELD username, connectionCount"),pullAll()));
  assertThat(admin,eventuallyReceives(msgFailure(Status.Security.InvalidArguments,"User 'NonExistentUser' does not exist.")));
}
