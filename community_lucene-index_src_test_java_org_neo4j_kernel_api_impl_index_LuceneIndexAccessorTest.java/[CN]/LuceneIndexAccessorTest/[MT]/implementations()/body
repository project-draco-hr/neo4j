{
  final File dir=new File("dir");
  final LuceneDocumentStructure documentLogic=new LuceneDocumentStructure();
  return Arrays.asList(arg(new IOFunction<DirectoryFactory,LuceneIndexAccessor>(){
    @Override public LuceneIndexAccessor apply(    DirectoryFactory dirFactory) throws IOException {
      LuceneIndex index=LuceneIndexBuilder.create().withFileSystem(new EphemeralFileSystemAbstraction()).withDirectoryFactory(dirFactory).withIndexRootFolder(dir).withIndexIdentifier("1").build();
      index.prepare();
      index.open();
      return new LuceneIndexAccessor(index);
    }
  }
),arg(new IOFunction<DirectoryFactory,LuceneIndexAccessor>(){
    @Override public LuceneIndexAccessor apply(    DirectoryFactory dirFactory) throws IOException {
      LuceneIndex index=LuceneIndexBuilder.create().uniqueIndex().withFileSystem(new EphemeralFileSystemAbstraction()).withDirectoryFactory(dirFactory).withIndexRootFolder(dir).withIndexIdentifier("testIndex").build();
      index.prepare();
      index.open();
      return new LuceneIndexAccessor(index);
    }
  }
));
}
