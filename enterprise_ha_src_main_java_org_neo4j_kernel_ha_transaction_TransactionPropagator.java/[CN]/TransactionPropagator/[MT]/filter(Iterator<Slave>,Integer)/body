{
  return externalAuthorServerId == null ? slaves : new FilteringIterator<>(slaves,new Predicate<Slave>(){
    @Override public boolean test(    Slave item){
      return item.getServerId() != externalAuthorServerId;
    }
  }
);
}
