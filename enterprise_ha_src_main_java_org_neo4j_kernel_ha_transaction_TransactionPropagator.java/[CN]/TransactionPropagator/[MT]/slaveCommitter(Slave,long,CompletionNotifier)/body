{
  return new Callable<Void>(){
    @Override public Void call(){
      try {
        slave.pullUpdates(txId);
        return null;
      }
  finally {
        notifier.completed();
      }
    }
  }
;
}
