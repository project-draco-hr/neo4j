{
  List<KernelExtensionFactory<?>> extensions=Arrays.<KernelExtensionFactory<?>>asList(provider,new InMemoryLabelScanStoreExtension());
  return BatchInserters.inserter("neo-batch-db",fs.get(),stringMap(),extensions);
}
