{
  return newGraphDatabase(new Predicate<TestGraphDatabaseFactory>(){
    @Override public boolean accept(    TestGraphDatabaseFactory item){
      item.setKernelExtensions(extensions);
      return true;
    }
  }
);
}
