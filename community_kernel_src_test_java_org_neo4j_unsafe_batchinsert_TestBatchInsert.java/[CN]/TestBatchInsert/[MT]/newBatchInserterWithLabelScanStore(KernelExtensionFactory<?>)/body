{
  List<KernelExtensionFactory<?>> extensions=Arrays.<KernelExtensionFactory<?>>asList(new InMemoryIndexProviderFactory(),provider);
  return BatchInserters.inserter("neo-batch-db",fs.get(),stringMap(),extensions);
}
