{
  String script="meaning_of_life";
  String payload="{\"script\":\"" + script + "\","+ "\"params\":{\"meaning_of_life\" : 42.0}}";
  description(formatGroovy(script));
  String response=gen().expectedStatus(OK.getStatusCode()).payload(JSONPrettifier.parse(payload)).payloadType(MediaType.APPLICATION_JSON_TYPE).post(ENDPOINT).entity();
  assertTrue(response.contains("42.0"));
}
