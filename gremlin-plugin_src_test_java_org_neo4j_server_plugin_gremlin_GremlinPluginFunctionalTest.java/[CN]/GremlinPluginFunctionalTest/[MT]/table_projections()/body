{
  data.get();
  String script="g.v(%Root%).out('AllFriends').as('Friend').ifThenElse" + "{it.out('HasPet').hasNext()}" + "{it.out('HasPet')}"+ "{it}"+ ".as('Pet').out('HasCareTaker').as('CareTaker').table(new Table()){it['name']}{it['name']}{it['name']}.cap";
  String response=doRestCall(script,OK);
  System.out.println(response);
}
