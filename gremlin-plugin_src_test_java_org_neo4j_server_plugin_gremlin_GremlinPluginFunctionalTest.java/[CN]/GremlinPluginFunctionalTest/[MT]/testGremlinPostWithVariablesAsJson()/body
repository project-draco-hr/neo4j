{
  final String script="g.v(me).out";
  final String params="{ \"me\" : " + data.get().get("I").getId() + " }";
  final String payload=String.format("{ \"script\" : \"%s\", \"params\" : %s }",script,params);
  String response=gen.get().expectedStatus(Status.OK.getStatusCode()).payload(payload).payloadType(MediaType.APPLICATION_JSON_TYPE).post(ENDPOINT).entity();
  assertTrue(response.contains("you"));
}
