{
  data.get();
  String response=gen().expectedStatus(Status.BAD_REQUEST.getStatusCode()).payload("{\"script\":\"g.addVertex([name:{}])\"}").post(ENDPOINT).entity();
  assertTrue(response.contains("BadInputException"));
}
