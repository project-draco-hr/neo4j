{
  String script="i = g.v(" + data.get().get("I").getId() + ");i.out";
  String response=gen.get().expectedStatus(Status.OK.getStatusCode()).description(formatGroovy(script)).payload("script=" + URLEncoder.encode(script,"UTF-8")).payloadType(MediaType.APPLICATION_FORM_URLENCODED_TYPE).post(ENDPOINT).entity();
  assertTrue(response.contains("you"));
}
