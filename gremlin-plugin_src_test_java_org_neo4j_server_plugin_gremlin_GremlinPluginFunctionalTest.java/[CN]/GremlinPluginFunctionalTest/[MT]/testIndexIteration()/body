{
  data.get();
  String script="g.idx('node_auto_index')[[name:'I']]";
  gen().expectedStatus(OK.getStatusCode()).description(formatGroovy(script));
  String response=doRestCall(script,OK);
  assertTrue(response.contains("I"));
}
