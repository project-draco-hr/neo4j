{
  data.get().clear();
  String script="" + "g.clear();" + "g.loadGraphML('https://raw.github.com/neo4j/gremlin-plugin/master/src/data/graphml1.xml');"+ "g.idx('node_auto_index')[[name:'you']];";
  String response=doRestCall(script,OK);
  assertTrue(response.contains("you"));
}
