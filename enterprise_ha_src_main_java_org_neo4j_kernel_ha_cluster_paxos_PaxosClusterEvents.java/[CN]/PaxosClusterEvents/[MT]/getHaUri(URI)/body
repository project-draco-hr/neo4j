{
  try {
    String host=getConfiguredHaAddress(clusterUri);
    return new URI("ha",null,host,portOf(config.getHaServer()),null,"serverId=" + config.getServerId(),null);
  }
 catch (  URISyntaxException e) {
    throw new RuntimeException(e);
  }
}
