{
  ClientResponse response=Client.create().resource(functionalTestHelper.indexNodeUri()).accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  Assert.assertEquals(204,response.getStatus());
}
