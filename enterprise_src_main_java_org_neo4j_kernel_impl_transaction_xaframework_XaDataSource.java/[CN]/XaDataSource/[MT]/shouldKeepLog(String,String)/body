{
  if (config != null) {
    StringTokenizer tok=new StringTokenizer(config,",");
    while (tok.hasMoreTokens()) {
      String element=tok.nextToken().trim();
      if (element.startsWith(resourceName) && element.endsWith("=true")) {
        return true;
      }
    }
  }
  return false;
}
