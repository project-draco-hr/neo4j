{
  return entities(new ThrowingSupplier<InputIterator<InputRelationship>,IOException>(){
    @Override public InputIterator<InputRelationship> get() throws IOException {
      return new InputRelationshipReader(channel(RELATIONSHIPS,subType,"r"),channel(RELATIONSHIPS_HEADER,subType,"r"),bufferSize,deleteAction(deleteAfterUse,RELATIONSHIPS,RELATIONSHIPS_HEADER,subType),config.maxNumberOfProcessors());
    }
  }
);
}
