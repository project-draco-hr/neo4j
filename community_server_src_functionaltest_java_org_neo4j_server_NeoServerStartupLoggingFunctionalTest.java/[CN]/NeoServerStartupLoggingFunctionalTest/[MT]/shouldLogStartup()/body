{
  assertThat(appender.toString().length(),is(greaterThan(0)));
  ClientResponse response=NON_REDIRECTING_CLIENT.resource("http://localhost:" + server.getWebServerPort() + "/").get(ClientResponse.class);
  assertThat(response.getStatus(),is(greaterThan(199)));
}
