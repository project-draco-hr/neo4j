{
  RelationshipType result=this.getNeoServer().getNeo().getRelationshipType(name);
  if (result == null) {
    throw new RuntimeException("No relationship type '" + name + "' found");
  }
  return result;
}
