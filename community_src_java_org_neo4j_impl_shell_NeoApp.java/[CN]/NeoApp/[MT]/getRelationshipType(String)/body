{
  RelationshipType result=this.getNeoServer().getNeo().getRelationshipTypeByName(name);
  if (result == null) {
    throw new RuntimeException("No relationship type '" + name + "' found");
  }
  return result;
}
