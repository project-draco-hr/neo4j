{
  if (this.engine == null) {
synchronized (this) {
      if (this.engine == null) {
        this.engine=new ServerExecutionEngine(getServer().getDb(),getCypherLogger());
      }
    }
  }
  return this.engine;
}
