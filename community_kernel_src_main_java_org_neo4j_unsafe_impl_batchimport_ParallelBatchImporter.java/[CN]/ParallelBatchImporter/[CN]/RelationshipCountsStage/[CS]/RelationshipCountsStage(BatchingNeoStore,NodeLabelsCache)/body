{
  super("Relationship counts",config);
  add(new RelationshipCountsStep(control(),config,neoStore.getRelationshipStore(),cache,neoStore.getLabelRepository().getHighId(),neoStore.getRelationshipTypeRepository().getHighId(),neoStore.getCountsStore()));
}
