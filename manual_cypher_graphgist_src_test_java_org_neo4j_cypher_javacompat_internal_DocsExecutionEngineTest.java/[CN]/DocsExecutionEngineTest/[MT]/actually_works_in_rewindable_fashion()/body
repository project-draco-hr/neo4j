{
  InternalExecutionResult result=engine.internalProfile("CREATE (n:Person {name:'Adam'}) RETURN n",Collections.emptyMap(),createSession().get(TransactionalContext.METADATA_KEY));
  String dump=result.dumpToString();
  assertThat(dump,containsString("1 row"));
  assertThat(result.javaIterator().hasNext(),equalTo(true));
}
