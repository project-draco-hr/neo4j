{
  fillBufferIfWeHaveExhaustedIt();
  int ch=buffer[bufferPos++];
  if (skippedChars > 0) {
    repositionChar(bufferPos - 1,skippedChars);
  }
  if (ch == EOF_CHAR) {
    eof=true;
  }
  return ch;
}
