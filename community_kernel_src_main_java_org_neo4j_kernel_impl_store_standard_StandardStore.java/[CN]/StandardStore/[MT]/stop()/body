{
  if (file != null) {
    file=null;
    pageCache.unmap(dbFileName);
  }
  if (channel != null) {
    openCloseLogic.closeStore(channel,idGenerator.highestIdInUse());
    channel.close();
    channel=null;
  }
}
