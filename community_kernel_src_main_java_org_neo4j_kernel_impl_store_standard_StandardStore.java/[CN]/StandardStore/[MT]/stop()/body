{
  if (file != null) {
    file.close();
    file=null;
  }
  if (channel != null) {
    openCloseLogic.closeStore(channel,idGenerator.highestIdInUse());
    channel.close();
    channel=null;
  }
}
