{
  GraphDatabaseService db=new TestGraphDatabaseFactory().newEmbeddedDatabase(Cluster.edgeServerStoreDirectory(dir,1966));
  try (Transaction tx=db.beginTx()){
    db.createNode();
    tx.success();
  }
   db.shutdown();
}
