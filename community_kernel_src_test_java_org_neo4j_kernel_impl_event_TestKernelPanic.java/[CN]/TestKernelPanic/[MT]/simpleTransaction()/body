{
  InMemoryLogBuffer buffer=new InMemoryLogBuffer();
  TransactionWriter writer=new TransactionWriter(buffer,1,-1);
  writer.start(-1,-1,0);
  writer.add(new NodeRecord(0,Record.NO_NEXT_RELATIONSHIP.intValue(),Record.NO_NEXT_PROPERTY.intValue()),new NodeRecord(0,Record.NO_NEXT_RELATIONSHIP.intValue(),Record.NO_NEXT_PROPERTY.intValue()));
  writer.commit(false,2);
  writer.done();
  return buffer;
}
