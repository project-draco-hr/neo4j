{
  int count=0;
  while (!taskCompleted) {
    if (count > MAX_WAIT_LOOPS) {
      throw new RuntimeException("Task timed out");
    }
    try {
      wait(25);
    }
 catch (    InterruptedException e) {
    }
    count++;
  }
}
