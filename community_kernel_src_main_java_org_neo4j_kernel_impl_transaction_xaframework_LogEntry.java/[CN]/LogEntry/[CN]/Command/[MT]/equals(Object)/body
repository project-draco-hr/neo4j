{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Command command1=(Command)o;
  if (!command.equals(command1.command)) {
    return false;
  }
  return true;
}
