{
  if (!register(new XaMonitor.MXBeanImplementation(instanceId,datasourceMananger))) {
    if (!register(new XaMonitor.XaManager(instanceId,datasourceMananger)))     failedToRegister("XaManagerMBean");
  }
}
