{
  if (schemaReadDelegate.procedureGet(state,signature.name()) != null) {
    throw new ProcedureConstraintViolation("%s cannot be created because there is already a procedure with the same " + "name in the graph.",signature.toString());
  }
  schemaWriteDelegate.procedureCreate(state,signature,language,code);
}
