{
  File emptyPropertyFile=PropertyFileBuilder.builder(folder.getRoot()).build();
  DatabaseTuningPropertyFileBuilder.builder(folder.getRoot()).withKernelId("fromdefaultlocation").build();
  Config config=loadConfig(null,emptyPropertyFile,log);
  assertEquals("fromdefaultlocation",config.get(GraphDatabaseSettings.forced_kernel_id));
}
