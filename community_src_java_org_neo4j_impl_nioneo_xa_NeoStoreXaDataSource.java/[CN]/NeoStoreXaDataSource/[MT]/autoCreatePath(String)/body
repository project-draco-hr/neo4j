{
  String dirs=store.substring(0,store.lastIndexOf('/'));
  File directories=new File(dirs);
  if (!directories.exists()) {
    if (!directories.mkdirs()) {
      throw new IOException("Unable to create directory paht[" + dirs + "] for Neo store.");
    }
  }
}
