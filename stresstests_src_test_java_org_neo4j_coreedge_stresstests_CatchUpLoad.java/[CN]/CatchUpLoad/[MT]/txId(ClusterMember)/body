{
  GraphDatabaseAPI database=leader.database();
  if (database == null || !database.isAvailable(100)) {
    return -1L;
  }
  return database.getDependencyResolver().resolveDependency(TransactionIdStore.class).getLastClosedTransactionId();
}
