{
  try {
    registry.create(database);
  }
 catch (  NoSuchDatabaseProviderException e) {
    log.error(String.format("Unable to start database '%s', because there is no database provider called " + "'%s', which this database has been configured to use.",database.key(),e.provider()));
  }
}
