{
  LogDeserializer deserializer=new LogDeserializer(monitor,sharedBuffer,commandReaderFactory.newInstance(sharedBuffer));
  consumer.init(targetLog,targetLogVersion);
  Cursor<LogEntry,IOException> cursor=deserializer.cursor(sourceLog);
  while (cursor.next(consumer))   ;
}
