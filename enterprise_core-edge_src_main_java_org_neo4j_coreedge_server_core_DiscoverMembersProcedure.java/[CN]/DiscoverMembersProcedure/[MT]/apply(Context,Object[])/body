{
  Set<AdvertisedSocketAddress> addresses=findAddresses().limit(noOfAddressesToReturn(input)).collect(toSet());
  log.info("Discovery members: %s",addresses.stream().collect(toSet()));
  return wrapUpAddresses(addresses);
}
