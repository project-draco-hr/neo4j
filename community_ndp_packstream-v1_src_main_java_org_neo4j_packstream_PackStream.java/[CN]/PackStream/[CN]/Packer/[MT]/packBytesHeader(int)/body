{
  if (size <= Byte.MAX_VALUE) {
    out.ensure(2).put(BYTES_8).put((byte)size);
  }
 else   if (size <= Short.MAX_VALUE) {
    out.ensure(3).put(BYTES_16).putShort((short)size);
  }
 else {
    out.ensure(5).put(BYTES_32).putInt(size);
  }
}
