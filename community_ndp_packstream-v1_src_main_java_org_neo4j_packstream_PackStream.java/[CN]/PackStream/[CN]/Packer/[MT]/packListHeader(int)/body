{
  if (size < 0x10) {
    out.ensure(1).put((byte)(TINY_LIST | size));
  }
 else   if (size <= Byte.MAX_VALUE) {
    out.ensure(2).put(LIST_8).put((byte)size);
  }
 else   if (size <= Short.MAX_VALUE) {
    out.ensure(3).put(LIST_16).putShort((short)size);
  }
 else {
    out.ensure(5).put(LIST_32).putInt(size);
  }
}
