{
  timer.schedule(new TimerTask(){
    @Override public void run(){
      Transaction tx=db.beginTx();
      try {
        db.createNode();
        tx.success();
      }
  finally {
        tx.finish();
      }
    }
  }
,10,200);
}
