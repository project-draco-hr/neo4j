{
  out.println("name=" + ReadNodesBenchmark.class.getSimpleName());
  out.println("description=In a background thread, " + "creates individual unconnected nodes. " + "In the main thread, read all nodes. "+ "Reports number of nodes read.");
}
