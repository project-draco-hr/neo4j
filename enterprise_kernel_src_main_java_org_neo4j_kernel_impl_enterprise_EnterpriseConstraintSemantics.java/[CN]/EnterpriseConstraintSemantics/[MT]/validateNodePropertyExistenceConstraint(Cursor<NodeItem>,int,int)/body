{
  while (allNodes.next()) {
    NodeItem node=allNodes.get();
    if (!node.hasProperty(propertyKey)) {
      throw createConstraintFailure(new NodePropertyExistenceConstraintVerificationFailedKernelException(new NodePropertyExistenceConstraint(label,propertyKey),node.id()));
    }
  }
}
