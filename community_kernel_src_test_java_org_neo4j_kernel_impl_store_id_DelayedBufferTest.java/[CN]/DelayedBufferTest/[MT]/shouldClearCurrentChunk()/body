{
  Consumer<long[]> consumer=mock(Consumer.class);
  DelayedBuffer<Long> buffer=new DelayedBuffer<>(singleton(0L),Predicates.<Long>alwaysTrue(),10,consumer);
  buffer.offer(0);
  buffer.offer(1);
  buffer.offer(2);
  buffer.clear();
  buffer.maintenance();
  verifyNoMoreInteractions(consumer);
}
