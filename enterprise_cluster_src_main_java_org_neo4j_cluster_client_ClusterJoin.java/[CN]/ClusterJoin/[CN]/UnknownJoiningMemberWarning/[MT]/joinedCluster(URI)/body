{
  for (  HostnamePort host : initialHosts) {
    if (host.matches(member)) {
      return;
    }
  }
  logger.warn("Member " + member + " joined cluster but was not part of initial hosts ("+ initialHosts+ ")");
}
