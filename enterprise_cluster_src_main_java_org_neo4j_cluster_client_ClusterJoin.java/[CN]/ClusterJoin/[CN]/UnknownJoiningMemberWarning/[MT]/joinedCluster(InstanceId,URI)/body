{
  for (  HostnamePort host : initialHosts) {
    if (host.matches(uri)) {
      return;
    }
  }
  logger.info("Member " + member + " joined cluster but was not part of initial hosts ("+ initialHosts+ ")");
}
