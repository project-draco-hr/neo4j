{
  try {
    if (host.startsWith(":"))     return InetAddress.getLocalHost().getHostAddress() + host;
    return host;
  }
 catch (  UnknownHostException e) {
    throw new RuntimeException(e);
  }
}
