{
  db=new GraphDatabaseFactory().newEmbeddedDatabaseBuilder(PATH).setConfig(OnlineBackupSettings.online_backup_enabled,GraphDatabaseSetting.TRUE).setConfig(OnlineBackupSettings.online_backup_port,backupPort).newGraphDatabase();
  createSomeData(db);
}
