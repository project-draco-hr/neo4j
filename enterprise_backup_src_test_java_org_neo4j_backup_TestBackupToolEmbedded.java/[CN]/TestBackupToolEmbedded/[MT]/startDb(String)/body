{
  db=new EmbeddedGraphDatabase(PATH,stringMap("online_backup_enabled","true","online_backup_port",backupPort)){
    @Override protected StringLogger createStringLogger(){
      return StringLogger.SYSTEM;
    }
  }
;
  createSomeData(db);
}
