{
  if (!(options instanceof String)) {
    return null;
  }
  String config=(String)options;
  if (!"true".equalsIgnoreCase(config)) {
    return null;
  }
  return new VerifyingTransactionInterceptor((NeoStoreXaDataSource)ds,false);
}
