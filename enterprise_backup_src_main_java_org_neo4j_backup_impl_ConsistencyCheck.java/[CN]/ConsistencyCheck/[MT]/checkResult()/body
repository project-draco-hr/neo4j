{
  if (brokenNodes != 0 || brokenRels != 0 || brokenProps != 0 || brokenStrings != 0 || brokenArrays != 0) {
    throw new AssertionError(String.format("Store level inconsistency found in %d nodes, %d relationships, %d properties, %d strings, %d arrays",brokenNodes,brokenRels,brokenProps,brokenStrings,brokenArrays));
  }
}
