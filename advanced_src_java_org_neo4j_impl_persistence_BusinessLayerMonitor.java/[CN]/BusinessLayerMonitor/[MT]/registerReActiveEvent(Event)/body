{
  try {
    EventManager.getManager().registerReActiveEventListener(this,event);
  }
 catch (  EventListenerAlreadyRegisteredException elare) {
    log.warning("Failed to register ourselves as a listener on " + event + " events because we are already registered.");
  }
}
