{
  try {
    return String.format("%s (%s) %s %s (%s)",property.value(),property.value().getClass().getSimpleName(),shouldMatch ? "==" : "!=",value,value.getClass().getSimpleName());
  }
 catch (  PropertyNotFoundException e) {
    throw new RuntimeException(e);
  }
}
