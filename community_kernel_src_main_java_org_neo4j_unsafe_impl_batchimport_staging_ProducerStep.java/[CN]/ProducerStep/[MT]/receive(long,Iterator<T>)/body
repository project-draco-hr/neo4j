{
  new Thread(){
    @Override public void run(){
      assertHealthy();
      try {
        process(input);
        endOfUpstream();
      }
 catch (      Throwable e) {
        issuePanic(e);
      }
    }
  }
.start();
  return 0;
}
