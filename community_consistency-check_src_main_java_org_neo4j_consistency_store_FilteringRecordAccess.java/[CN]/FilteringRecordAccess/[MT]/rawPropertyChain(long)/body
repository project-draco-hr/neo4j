{
  return new FilteringIterator<>(super.rawPropertyChain(firstId),new Predicate<PropertyRecord>(){
    @Override public boolean accept(    PropertyRecord item){
      return shouldCheck(item.getId(),MultiPassStore.PROPERTIES);
    }
  }
);
}
