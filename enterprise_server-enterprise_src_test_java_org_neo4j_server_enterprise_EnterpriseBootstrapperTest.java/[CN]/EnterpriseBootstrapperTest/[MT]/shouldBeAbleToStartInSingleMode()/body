{
  int resultCode=start(bootstrapper,commandLineConfig("-c",configOption(EnterpriseServerSettings.mode.name(),"SINGLE")));
  assertEquals(Bootstrapper.OK,resultCode);
  assertNotNull(bootstrapper.getServer());
}
