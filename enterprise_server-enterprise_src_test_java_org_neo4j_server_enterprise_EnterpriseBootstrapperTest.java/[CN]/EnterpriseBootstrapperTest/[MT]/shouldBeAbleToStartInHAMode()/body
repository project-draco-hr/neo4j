{
  int resultCode=start(bootstrapper,commandLineConfig("-c",configOption(EnterpriseServerSettings.mode.name(),"HA"),"-c",configOption(ClusterSettings.server_id.name(),"1"),"-c",configOption(ClusterSettings.initial_hosts.name(),"127.0.0.1:5001")));
  assertEquals(Bootstrapper.OK,resultCode);
  assertNotNull(bootstrapper.getServer());
}
