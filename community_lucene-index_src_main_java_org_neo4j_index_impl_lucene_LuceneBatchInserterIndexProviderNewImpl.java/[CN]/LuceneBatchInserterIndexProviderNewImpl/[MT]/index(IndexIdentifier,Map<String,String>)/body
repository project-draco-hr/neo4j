{
  LuceneBatchInserterIndex index=indexes.get(identifier);
  if (index == null) {
    index=new LuceneBatchInserterIndex(inserter.getStoreDir(),identifier,config);
    indexes.put(identifier,index);
  }
  return index;
}
