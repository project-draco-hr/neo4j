{
  return filter(new Predicate<SchemaRule>(){
    @Override public boolean test(    SchemaRule schemaRule){
      return schemaRule.getKind() != SchemaRule.Kind.RELATIONSHIP_PROPERTY_EXISTENCE_CONSTRAINT && schemaRule.getLabel() == label;
    }
  }
,schemaRules());
}
