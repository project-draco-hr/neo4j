{
  return filter(new Predicate<SchemaRule>(){
    @Override public boolean test(    SchemaRule schemaRule){
      return schemaRule.getKind() != SchemaRule.Kind.MANDATORY_RELATIONSHIP_PROPERTY_CONSTRAINT && schemaRule.getLabel() == label;
    }
  }
,schemaRules());
}
