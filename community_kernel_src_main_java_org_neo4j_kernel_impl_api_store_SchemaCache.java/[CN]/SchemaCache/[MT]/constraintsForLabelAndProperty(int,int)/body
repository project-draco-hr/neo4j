{
  return filter(new Predicate<UniquenessConstraint>(){
    @Override public boolean accept(    UniquenessConstraint item){
      return item.label() == label && item.propertyKeyId() == property;
    }
  }
,constraints.iterator());
}
