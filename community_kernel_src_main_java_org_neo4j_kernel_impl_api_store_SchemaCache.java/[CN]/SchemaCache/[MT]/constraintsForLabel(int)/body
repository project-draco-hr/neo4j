{
  return filter(new Predicate<NodePropertyConstraint>(){
    @Override public boolean test(    NodePropertyConstraint constraint){
      return constraint.label() == label;
    }
  }
,nodeConstraints.iterator());
}
