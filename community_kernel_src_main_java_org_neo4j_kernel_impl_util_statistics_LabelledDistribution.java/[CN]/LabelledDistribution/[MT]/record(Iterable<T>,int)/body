{
synchronized (rawData) {
    for (    T label : labels) {
      Long current=rawData.get(label);
      rawData.put(label,(current == null ? 0 : current) + ticks);
    }
    total+=ticks;
    return this;
  }
}
