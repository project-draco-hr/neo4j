{
  return new Listener<GraphDatabaseService>(){
    @Override public void receive(    GraphDatabaseService db){
      try (Transaction tx=db.beginTx()){
        db.schema().constraintFor(label).assertPropertyIsUnique(propertyKey).create();
        tx.success();
      }
     }
  }
;
}
