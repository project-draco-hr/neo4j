{
  if (IS_EMBEDDED)   assertEmpty(subject,"CALL dbms.security.changePassword( '" + newPassword + "' )");
 else   assertEmpty(adminSubject,"CALL dbms.security.changeUserPassword( '" + neo.nameOf(subject) + "', '"+ newPassword+ "' )");
}
