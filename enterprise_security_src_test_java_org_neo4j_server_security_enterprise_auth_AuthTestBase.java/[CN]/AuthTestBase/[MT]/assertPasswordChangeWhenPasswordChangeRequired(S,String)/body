{
  if (IS_EMBEDDED)   assertEmpty(subject,"CALL dbms.changePassword( '" + newPassword + "' )");
 else   assertEmpty(adminSubject,"CALL dbms.changeUserPassword( '" + neo.nameOf(subject) + "', '"+ newPassword+ "' )");
}
