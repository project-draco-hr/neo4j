{
  List<Pattern> list=new ArrayList<Pattern>();
  for (  String filterString : commaSeparatedFilters.split(",")) {
    list.add(Pattern.compile(filterString.trim()));
  }
  this.filters=list.toArray(new Pattern[list.size()]);
}
