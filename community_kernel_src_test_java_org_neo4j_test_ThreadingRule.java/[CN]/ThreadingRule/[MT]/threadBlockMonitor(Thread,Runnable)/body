{
  CancellationHandle cancellation=new CancellationHandle();
  executor.submit(new ThreadBlockMonitor(cancellation,requireNonNull(thread,"thread"),requireNonNull(action,"action")));
  return cancellation;
}
