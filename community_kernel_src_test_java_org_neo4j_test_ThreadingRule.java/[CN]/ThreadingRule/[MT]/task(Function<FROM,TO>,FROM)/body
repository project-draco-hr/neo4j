{
  return new Callable<TO>(){
    @Override public TO call() throws Exception {
      Thread thread=Thread.currentThread();
      String name=thread.getName();
      thread.setName(function.toString());
      try {
        return function.apply(parameter);
      }
  finally {
        thread.setName(name);
      }
    }
  }
;
}
