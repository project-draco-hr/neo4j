{
  if (master == Machine.NO_MACHINE || master.getServer() == null) {
    return NO_MASTER;
  }
  return masterClientFactory.instantiate(master.getServer().first(),master.getServer().other().intValue(),getStoreId());
}
