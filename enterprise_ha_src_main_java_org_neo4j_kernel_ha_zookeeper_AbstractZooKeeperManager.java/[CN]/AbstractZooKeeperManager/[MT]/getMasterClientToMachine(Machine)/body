{
  if (master == Machine.NO_MACHINE || master.getServer() == null) {
    return NO_MASTER;
  }
  return new MasterClient(master.getServer().first(),master.getServer().other(),graphDb,getConnectionLostHandler(),clientReadTimeout,clientLockReadTimeout,maxConcurrentChannelsPerSlave);
}
