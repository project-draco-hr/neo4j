{
  if (state.hasTxStateWithChanges()) {
    return state.txState().constraintIndexChanges().apply(schemaReadDelegate.uniqueIndexesGetAll(state));
  }
  return schemaReadDelegate.uniqueIndexesGetAll(state);
}
