{
  if (state.hasTxStateWithChanges()) {
    PrimitiveLongIterator wLabelChanges=state.txState().nodesWithLabelChanged(labelId).applyPrimitiveLongIterator(entityReadDelegate.nodesGetForLabel(state,labelId));
    PrimitiveLongIterator wDeletions=state.txState().nodesDeletedInTx().applyPrimitiveLongIterator(wLabelChanges);
    return wDeletions;
  }
  return entityReadDelegate.nodesGetForLabel(state,labelId);
}
