{
  if (state.hasTxStateWithChanges()) {
    return state.txState().constraintIndexDiffSetsByLabel(labelId).apply(schemaReadDelegate.uniqueIndexesGetForLabel(state,labelId));
  }
  return schemaReadDelegate.uniqueIndexesGetForLabel(state,labelId);
}
