{
  relationshipVisit(statement,relationship,new RelationshipVisitor(){
    @Override public void visit(    long relId,    long startNode,    long endNode,    int type){
      statement.txState().getRelationshipLegacyIndexChanges(indexName).addRelationship(relationship,key,value,startNode,endNode);
    }
  }
);
}
