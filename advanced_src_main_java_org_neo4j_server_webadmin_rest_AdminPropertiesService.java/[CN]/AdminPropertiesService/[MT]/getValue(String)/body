{
  String value=getConfiguration().getString(NeoServer.WEBADMIN_NAMESPACE + key);
  if (value == null) {
    return addHeaders(Response.ok("undefined",JsonRenderers.DEFAULT.getMediaType())).build();
  }
  return addHeaders(Response.ok(value,JsonRenderers.DEFAULT.getMediaType())).build();
}
