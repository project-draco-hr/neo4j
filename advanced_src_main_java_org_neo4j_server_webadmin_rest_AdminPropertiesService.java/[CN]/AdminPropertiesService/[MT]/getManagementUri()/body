{
  String managementUri=slashTerminatedUri(config.getString(configurationNamespace + MANAGEMENT_URI_KEY));
  if (managementUri != null)   return managementUri;
 else   return hostPath(Configurator.WEB_ADMIN_REST_API_PATH);
}
