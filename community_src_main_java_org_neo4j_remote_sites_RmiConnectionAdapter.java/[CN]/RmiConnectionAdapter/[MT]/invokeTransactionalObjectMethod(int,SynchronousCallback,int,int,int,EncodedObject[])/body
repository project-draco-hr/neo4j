{
  try {
    return rmi().invokeTransactionalObjectMethod(transactionId,new Callback(callback),serviceId,objectId,functionIndex,arguments);
  }
 catch (  RemoteException ex) {
    throw remoteException(ex);
  }
}
