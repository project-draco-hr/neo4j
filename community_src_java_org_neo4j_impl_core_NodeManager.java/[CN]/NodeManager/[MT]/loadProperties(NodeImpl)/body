{
  try {
    RawPropertyData properties[]=persistenceManager.loadProperties(node);
    return properties;
  }
 catch (  Exception e) {
    log.severe("Failed loading properties for node[" + node.getId() + "]");
    throw new RuntimeException(e);
  }
}
