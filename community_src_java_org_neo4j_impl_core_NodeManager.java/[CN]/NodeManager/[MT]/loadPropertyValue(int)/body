{
  try {
    return PersistenceManager.getManager().loadPropertyValue(id);
  }
 catch (  Exception e) {
    e.printStackTrace();
    log.severe("Failed loading property value[" + id + "]");
  }
  return null;
}
