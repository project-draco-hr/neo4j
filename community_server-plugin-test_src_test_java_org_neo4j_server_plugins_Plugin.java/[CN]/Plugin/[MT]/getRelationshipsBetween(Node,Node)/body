{
  return new FilteringIterable<>(start.getRelationships(),new Predicate<Relationship>(){
    @Override public boolean test(    Relationship item){
      return item.getOtherNode(start).equals(end);
    }
  }
);
}
