{
  if (!messageQueues.containsKey(to.getId()) || disconnectedMembers.contains(to.getId()) || disconnectedMembers.contains(me)) {
    return;
  }
  for (  Serializable message : messages) {
    messageQueues.get(to.getId()).add(message);
  }
}
