{
  new RunCarefully(new Runnable(){
    @Override public void run(){
      stopWebServer();
    }
  }
,new Runnable(){
    @Override public void run(){
      stopModules();
    }
  }
).run();
  log.log("Successfully shutdown Neo4j Server.");
}
