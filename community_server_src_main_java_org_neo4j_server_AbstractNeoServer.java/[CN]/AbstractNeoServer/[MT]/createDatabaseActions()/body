{
  return new DatabaseActions(database,new LeaseManager(Clock.REAL_CLOCK),ForceMode.forced,configurator.configuration().getBoolean(Configurator.SCRIPT_SANDBOXING_ENABLED_KEY,Configurator.DEFAULT_SCRIPT_SANDBOXING_ENABLED));
}
