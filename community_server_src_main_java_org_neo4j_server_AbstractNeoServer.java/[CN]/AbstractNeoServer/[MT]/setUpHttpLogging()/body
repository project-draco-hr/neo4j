{
  if (!getConfig().get(http_logging_enabled)) {
    return;
  }
  System.out.println("HELLO THERE");
  System.out.println(config.get(GraphDatabaseSettings.logs_directory).toString());
  webServer.setHttpLoggingConfiguration(config.get(GraphDatabaseSettings.logs_directory),config.get(http_logging_enabled));
}
