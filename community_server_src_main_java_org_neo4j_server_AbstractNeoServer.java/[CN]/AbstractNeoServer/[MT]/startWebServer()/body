{
  try {
    setUpHttpLogging();
    setUpTimeoutFilter();
    webServer.start();
    log.info("Remote interface available at %s",baseUri());
  }
 catch (  Exception e) {
    log.error("Failed to start Neo Server on port %d: %s",getWebServerPort(),e.getMessage());
    throw e;
  }
}
