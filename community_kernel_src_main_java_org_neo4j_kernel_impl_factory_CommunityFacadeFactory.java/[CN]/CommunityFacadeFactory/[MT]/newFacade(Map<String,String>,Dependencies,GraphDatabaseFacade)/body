{
  params.put(Configuration.editionName.name(),"Community");
  return super.newFacade(params,newDependencies(dependencies).settingsClasses(toList(append(GraphDatabaseSettings.class,dependencies.settingsClasses()))),graphDatabaseFacade);
}
