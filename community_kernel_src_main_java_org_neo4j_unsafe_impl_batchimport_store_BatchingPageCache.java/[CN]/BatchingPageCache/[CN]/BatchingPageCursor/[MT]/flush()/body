{
  if (currentPageId == -1) {
    return;
  }
  writer.write(prepared(currentBuffer),currentPageId * pageSize,bufferRing);
  currentBuffer=bufferRing.next();
  currentBuffer.clear();
  currentPageId=-1;
}
