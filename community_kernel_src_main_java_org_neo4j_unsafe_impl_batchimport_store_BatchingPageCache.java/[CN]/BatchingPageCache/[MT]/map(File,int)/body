{
  StoreChannel channel=fs.open(file,"rw");
  BatchingPagedFile pageFile=new BatchingPagedFile(channel,writerFactory.create(file,channel,monitor),pageSize,mode);
  pagedFiles.put(file,pageFile);
  return pageFile;
}
