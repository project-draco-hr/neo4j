{
  StoreChannel channel=fs.open(file,"rw");
  BatchingPagedFile pageFile=new BatchingPagedFile(channel,writerFactory.create(channel,monitor),pageSize);
  pagedFiles.put(file,pageFile);
  return pageFile;
}
