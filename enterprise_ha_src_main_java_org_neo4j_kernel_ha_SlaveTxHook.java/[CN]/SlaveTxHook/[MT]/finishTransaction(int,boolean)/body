{
  try {
    databaseOperations.receive(broker.getMaster().first().finishTransaction(databaseOperations.getSlaveContext(eventIdentifier),success));
  }
 catch (  RuntimeException e) {
    databaseOperations.exceptionHappened(e);
    throw e;
  }
}
