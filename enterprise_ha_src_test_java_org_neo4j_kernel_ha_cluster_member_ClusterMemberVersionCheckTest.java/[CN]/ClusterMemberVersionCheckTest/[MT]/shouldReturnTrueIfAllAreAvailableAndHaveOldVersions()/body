{
  ClusterMembers members=clusterMembersWith(member(ID_1,MASTER,true,StoreId.DEFAULT,true),member(ID_2,SLAVE,true,StoreId.DEFAULT,true),member(ID_3,SLAVE,true,StoreId.DEFAULT,true),member(ID_4,SLAVE,true,StoreId.DEFAULT,true));
  Outcome outcome=new ClusterMemberVersionCheck(members,ID_5,new FakeClock()).doVersionCheck(EXPECTED_STORE_ID,1,SECONDS);
  assertFalse(outcome.hasMismatched());
  assertFalse(outcome.hasUnavailable());
  verify(members,times(0)).waitForEvent(anyLong());
}
