{
  testCluster(3,DEFAULT_NETWORK(),new ClusterTestScriptDSL().rounds(250).join(100,1).join(100,2).join(100,3).message(100,"*** All nodes up and ok").down(500,3).message(1000,"*** Should have seen failure by now").up(0,3).message(200,"*** Should have recovered by now").verifyConfigurations(0).leave(200,1).leave(200,2).leave(200,3));
}
