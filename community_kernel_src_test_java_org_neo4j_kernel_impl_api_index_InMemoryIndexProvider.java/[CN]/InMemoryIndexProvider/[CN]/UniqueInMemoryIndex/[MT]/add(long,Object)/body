{
  Long previous=indexData.get(propertyValue);
  if (previous != null) {
    throw new IllegalStateException(String.format("Index already contains [%s] for node: %s.",propertyValue,nodeId));
  }
  indexData.put(propertyValue,nodeId);
}
