{
  StoreFactory storeFactory=new StoreFactory(testDirectory.directory(),pageCacheRule.getPageCache(fileSystemRule.get()),fileSystemRule.get(),NullLogProvider.getInstance());
  neoStores=storeFactory.openAllNeoStores(true);
}
