{
  if (!this.transactionEventHandlers.isEmpty()) {
    txManager.getTransaction().registerSynchronization(new TransactionEventsSyncHook(this.nodeManager,transaction,this.transactionEventHandlers));
  }
}
