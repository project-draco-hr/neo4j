{
  return new SegmentedRaftLog(fileSystem,testDirectory.directory(),rotateAtSize,new DummyRaftableContentSerializer(),NullLogProvider.getInstance(),raft_log_pruning_strategy.getDefaultValue(),0,new FakeClock(),new OnDemandJobScheduler());
}
