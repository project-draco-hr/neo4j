{
  fs=new FileSystem();
  InternalAbstractGraphDatabase graphdb=new Database(storeDir);
  createInitialData(graphdb);
  graphdb.shutdown();
  Map<String,String> config=new HashMap<>();
  config.put(GraphDatabaseSettings.rebuild_idgenerators_fast.name(),Settings.FALSE);
  factory=new StoreFactory(configForStoreDir(new Config(config,GraphDatabaseSettings.class),new File(storeDir)),new DefaultIdGeneratorFactory(),new DefaultWindowPoolFactory(),fs,StringLogger.DEV_NULL,null);
}
