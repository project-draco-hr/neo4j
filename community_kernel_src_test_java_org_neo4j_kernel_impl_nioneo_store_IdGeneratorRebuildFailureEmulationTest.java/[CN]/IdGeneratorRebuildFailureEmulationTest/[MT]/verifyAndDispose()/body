{
  try {
    AbstractGraphDatabase graphdb=new Database();
    verifyData(graphdb);
    graphdb.shutdown();
  }
  finally {
    if (fs != null)     fs.disposeAndAssertNoOpenFiles();
    fs=null;
  }
}
