{
  Object value=getConfiguration().getProperty(NeoServer.WEBADMIN_NAMESPACE + key);
  if (value == null) {
    return addHeaders(Response.ok("undefined",JsonRenderers.DEFAULT.getMediaType())).build();
  }
  return addHeaders(Response.ok((String)value,JsonRenderers.DEFAULT.getMediaType())).build();
}
