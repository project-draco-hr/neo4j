{
  return new RequestContext(EMPTY.getSessionId(),EMPTY.machineId(),EMPTY.getEventIdentifier(),new RequestContext.Tx[]{lastAppliedTx(NeoStoreXaDataSource.DEFAULT_DATA_SOURCE_NAME,1)},EMPTY.getMasterId(),EMPTY.getChecksum());
}
