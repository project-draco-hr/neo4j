{
  if (!failures.isEmpty()) {
    IndexDescriptor descriptor=getDescriptor();
    throw new UniquenessConstraintVerificationFailedKernelException(new UniquenessConstraint(descriptor.getLabelId(),descriptor.getPropertyKeyId()),new HashSet<>(failures));
  }
}
