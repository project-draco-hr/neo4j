{
  super(fileName,config,IdType.RELATIONSHIP_GROUP,idGeneratorFactory,pageCache,logProvider,TYPE_DESCRIPTOR,recordFormat,new IntStoreHeaderFormat(config.get(GraphDatabaseSettings.dense_node_threshold)),storeVersion);
}
