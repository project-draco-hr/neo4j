{
  while (iterator != null && iterator.hasNext()) {
    if (relationshipStore.fillRecord(iterator.next(),relationshipRecord,CHECK) && relationshipRecord.inUse()) {
      return true;
    }
  }
  return false;
}
