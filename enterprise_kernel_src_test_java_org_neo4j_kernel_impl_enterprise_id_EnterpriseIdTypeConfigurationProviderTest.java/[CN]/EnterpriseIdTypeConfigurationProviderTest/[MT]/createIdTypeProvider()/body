{
  Map<String,String> params=MapUtil.stringMap(EnterpriseEditionSettings.idTypesToReuse.name(),IdType.RELATIONSHIP.name());
  Config config=new Config(params);
  return new EnterpriseIdTypeConfigurationProvider(config);
}
