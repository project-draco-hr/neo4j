{
  Map<String,String> params=MapUtil.stringMap(EnterpriseEditionSettings.idTypesToReuse.name(),Strings.join(",",IdType.NODE,IdType.RELATIONSHIP));
  Config config=new Config(params);
  return new EnterpriseIdTypeConfigurationProvider(config);
}
