{
  Map<Long,SoftValue<Long,E>> softMap=new HashMap<Long,SoftValue<Long,E>>(list.size() * 2);
  for (  E entry : list) {
    Long key=entry.getId();
    SoftValue<Long,E> ref=new SoftValue<Long,E>(key,entry,(ReferenceQueue)refQueue);
    softMap.put(key,ref);
  }
  cache.putAll(softMap);
  pollClearedValues();
}
