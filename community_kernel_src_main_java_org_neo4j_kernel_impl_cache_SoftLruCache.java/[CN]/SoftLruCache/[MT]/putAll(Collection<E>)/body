{
  Map<Long,SoftValue<Long,E>> softMap=new HashMap<>(list.size() * 2);
  for (  E entry : list) {
    Long key=entry.getId();
    SoftValue<Long,E> ref=new SoftValue<>(key,entry,(ReferenceQueue)refQueue);
    softMap.put(key,ref);
  }
  cache.putAll(softMap);
  pollClearedValues();
}
