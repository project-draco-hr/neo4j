{
  if (index >= 0 && index < value.length) {
    return new IntegerValue(value[index]);
  }
 else {
    throw new ArrayIndexOutOfBoundsException("Index must be between 0 and " + value.length);
  }
}
