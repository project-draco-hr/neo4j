{
  return new PrefetchingIterator<T>(){
    @Override protected T fetchNextOrNull(){
      if (!relationshipCursor.next()) {
        return null;
      }
      return nextFrom(relationshipCursor.getId(),relationshipCursor.getType(),relationshipCursor.getStartNode(),relationshipCursor.getEndNode());
    }
  }
;
}
