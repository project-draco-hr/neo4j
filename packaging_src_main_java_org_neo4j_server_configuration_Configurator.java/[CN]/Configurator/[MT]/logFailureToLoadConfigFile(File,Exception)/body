{
  Logger.getLogger(this.getClass().toString()).log(Level.INFO,String.format("The configuration file [%s] could not be loaded as a property file.",configFile.getAbsolutePath()));
  Logger.getLogger(this.getClass().toString()).log(Level.INFO,e.getMessage());
}
