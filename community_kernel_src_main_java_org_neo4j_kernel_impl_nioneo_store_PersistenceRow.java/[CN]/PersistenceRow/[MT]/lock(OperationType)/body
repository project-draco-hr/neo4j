{
  super.lock(operationType);
  boolean success=false;
  try {
    bufferState=bufferState.transition(operationType,this);
    success=true;
  }
  finally {
    if (!success) {
      unLock();
    }
  }
}
