{
  db=(GraphDatabaseAPI)new TestGraphDatabaseFactory().newEmbeddedDatabaseBuilder(directory.absolutePath()).setConfig(GraphDatabaseSettings.cache_type.name(),SoftCacheProvider.NAME).newGraphDatabase();
  engine=new ExecutionEngine(db);
}
