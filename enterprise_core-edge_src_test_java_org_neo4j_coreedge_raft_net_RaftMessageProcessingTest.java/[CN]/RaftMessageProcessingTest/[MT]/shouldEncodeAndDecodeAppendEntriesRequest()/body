{
  CoreMember member=new CoreMember(new AdvertisedSocketAddress("host1:9000"),new AdvertisedSocketAddress("host1:9001"),new AdvertisedSocketAddress("host1:9002"));
  RaftLogEntry logEntry=new RaftLogEntry(1,ReplicatedInteger.valueOf(1));
  RaftMessages.AppendEntries.Request request=new RaftMessages.AppendEntries.Request<>(member,1,1,99,new RaftLogEntry[]{logEntry},1);
  channel.writeOutbound(request);
  channel.writeInbound(channel.readOutbound());
  assertEquals(request,channel.readInbound());
}
