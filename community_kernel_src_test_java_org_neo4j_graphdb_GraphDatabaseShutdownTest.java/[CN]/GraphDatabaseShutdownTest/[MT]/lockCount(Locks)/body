{
  final int[] counter=new int[1];
  locks.accept(new Locks.Visitor(){
    @Override public void visit(    Locks.ResourceType resourceType,    long resourceId,    String description,    long waitTime){
      counter[0]++;
    }
  }
);
  return counter[0];
}
