{
  IndexService index=db instanceof HighlyAvailableGraphDatabase ? ((HighlyAvailableGraphDatabase)db).getIndexService() : new LuceneIndexService(db);
  Node node=db.createNode();
  node.setProperty(key,value);
  index.index(node,key,value);
  tx.success();
  return node.getId();
}
