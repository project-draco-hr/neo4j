{
  try {
    if (bytesRead == length) {
      return -1;
    }
    int count=0;
    while (count < len && bytesRead < length) {
      buffer[offset + count++]=(char)bytesRead++;
    }
    return count;
  }
  finally {
    readsCompleted++;
  }
}
