{
  if (bufferedWriter != null) {
    try {
      flush();
      bufferedWriter.close();
      bufferedWriter=null;
      checkFullDisposal();
    }
 catch (    IOException e) {
      log.error("Failed to close writer: " + bufferedWriter,e);
    }
  }
}
