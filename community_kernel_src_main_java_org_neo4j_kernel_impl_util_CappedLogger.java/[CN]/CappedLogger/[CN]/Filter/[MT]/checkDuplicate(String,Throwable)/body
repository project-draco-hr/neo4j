{
  String last=lastMessage;
  Throwable exc=lastException;
  if (stringEqual(last,msg) && (exc == null ? cause == null : sameClass(cause,exc) && sameMsg(cause,exc))) {
    return false;
  }
 else {
    lastMessage=msg;
    lastException=cause;
    return true;
  }
}
