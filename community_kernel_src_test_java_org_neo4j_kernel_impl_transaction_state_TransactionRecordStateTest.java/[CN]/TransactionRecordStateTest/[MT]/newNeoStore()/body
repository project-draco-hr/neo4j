{
  File storeDir=new File("dir");
  fsr.get().mkdirs(storeDir);
  StoreFactory storeFactory=new StoreFactory(fsr.get(),storeDir,pageCacheRule.getPageCache(fsr.get(),new Config()),StringLogger.DEV_NULL,new Monitors());
  return cleanup.add(storeFactory.newNeoStore(true));
}
