{
  CommandHandler applier=new NeoStoreTransactionApplier(neoStores,mock(CacheAccessBackDoor.class),LockService.NO_LOCK_SERVICE,new LockGroup());
  transactionRepresentationOf(state).accept(new CommandApplierFacade(applier));
}
