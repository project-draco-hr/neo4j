{
  File storeDir=new File("dir");
  fsr.get().mkdirs(storeDir);
  Config configuration=new Config(stringMap(config));
  StoreFactory storeFactory=new StoreFactory(storeDir,configuration,new DefaultIdGeneratorFactory(),pageCacheRule.getPageCache(fsr.get()),fsr.get(),NullLogProvider.getInstance(),new Monitors());
  return cleanup.add(storeFactory.newNeoStore(true));
}
