{
  File storeDir=new File("dir");
  fsr.get().mkdirs(storeDir);
  Config configuration=StoreFactory.configForStoreDir(new Config(stringMap(config)),storeDir);
  StoreFactory storeFactory=new StoreFactory(configuration,new DefaultIdGeneratorFactory(),pageCacheRule.getPageCache(fsr.get()),fsr.get(),StringLogger.DEV_NULL,new Monitors());
  return cleanup.add(storeFactory.newNeoStore(true));
}
