{
  RemoteTransaction tx=current.get();
  if (tx == null) {
    return receive(connection.invokeServiceMethod(callback(objectCallback),serviceId,functionIndex,arguments));
  }
 else {
    return tx.invokeServiceMethod(callback(objectCallback),serviceId,functionIndex,arguments);
  }
}
