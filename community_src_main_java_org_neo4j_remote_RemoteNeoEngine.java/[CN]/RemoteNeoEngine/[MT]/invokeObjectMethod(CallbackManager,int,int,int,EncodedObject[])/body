{
  RemoteTransaction tx=current.get();
  if (tx == null) {
    return receive(connection.invokeObjectMethod(callback(objectCallback),serviceId,objectId,functionIndex,arguments));
  }
 else {
    return tx.invokeObjectMethod(callback(objectCallback),serviceId,objectId,functionIndex,arguments);
  }
}
