{
  return new PersistenceWindowPool(storageFileName,recordSize,fileChannel,calculateMappedMemory(configuration,storageFileName),shouldMemoryMap(configuration.get(use_memory_mapped_buffers)),isReadOnly(configuration) && !isBackupSlave(configuration),new ConcurrentHashMap<Long,PersistenceRow>(),BrickElementFactory.DEFAULT,log);
}
