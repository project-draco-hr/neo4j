{
  QuerySession session=new BoltQuerySession(new FakeTransactionalContext(AccessMode.Static.READ),"fakeSource");
  assertThat(session.toString(),equalTo(String.format("bolt-session\t%s\t%s","fakeSource","READ")));
}
