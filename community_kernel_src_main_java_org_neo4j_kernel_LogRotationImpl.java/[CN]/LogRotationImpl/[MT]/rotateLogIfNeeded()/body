{
  if (logFile.rotationNeeded()) {
    boolean rotated;
synchronized (logFile) {
      if (rotated=logFile.rotationNeeded()) {
        doRotate();
      }
    }
    if (rotated) {
      monitor.rotatedLog();
    }
    return rotated;
  }
  return false;
}
