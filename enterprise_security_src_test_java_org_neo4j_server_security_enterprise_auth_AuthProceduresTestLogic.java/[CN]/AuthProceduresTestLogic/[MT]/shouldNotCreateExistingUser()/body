{
  assertFail(adminSubject,"CALL dbms.createUser('readSubject', '1234', true)","The specified user 'readSubject' already exists");
  assertFail(adminSubject,"CALL dbms.createUser('readSubject', '', true)","A password cannot be empty.");
}
