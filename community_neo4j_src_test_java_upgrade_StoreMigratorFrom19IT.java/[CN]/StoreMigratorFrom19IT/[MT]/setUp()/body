{
  Config config=MigrationTestUtils.defaultConfig();
  storeFactory=new StoreFactory(StoreFactory.configForStoreDir(config,storeDir),idGeneratorFactory,cleanup.add(new StandardPageCache(fs,1000,1000)),fs,StringLogger.DEV_NULL,new Monitors());
}
