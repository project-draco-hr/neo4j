{
  Config config=MigrationTestUtils.defaultConfig();
  storeFileName=new File(storeDir.absolutePath(),NeoStore.DEFAULT_NAME);
  storeFactory=new StoreFactory(config,idGeneratorFactory,new DefaultWindowPoolFactory(),fs,StringLogger.DEV_NULL,new DefaultTxHook());
}
