{
  return readAllLogEntries(file,new XaCommandReaderFactory(){
    @Override public XaCommandReader newInstance(    byte logEntryVersion,    ByteBuffer scratch){
      return new PhysicalLogNeoXaCommandReaderV1(scratch);
    }
  }
);
}
