{
  return new Dump(){
    @Override public void dump() throws Exception {
      for (      Pair<Long,String> pair : DumpProcessInformation.getJPids(processFilter)) {
        DumpProcessInformation.doThreadDump(pair,baseDir);
      }
    }
  }
;
}
