{
  try {
    BusinessLayerMonitor.getMonitor().registerEvents();
  }
 catch (  EventListenerNotRegisteredException elnre) {
    throw new RuntimeException("The business layer monitor was " + "unable to register on one or more " + "event types. This can seriously "+ "impact persistence operations.",elnre);
  }
}
