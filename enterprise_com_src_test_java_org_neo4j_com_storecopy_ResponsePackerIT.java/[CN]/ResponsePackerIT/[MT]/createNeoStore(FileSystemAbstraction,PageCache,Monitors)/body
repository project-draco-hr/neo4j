{
  File storeDir=new File("/store/");
  fs.mkdirs(storeDir);
  StoreFactory storeFactory=new StoreFactory(fs,storeDir,pageCache,HighLimit.RECORD_FORMATS,NullLogProvider.getInstance());
  return storeFactory.openAllNeoStores(true);
}
