{
  File storeDir=new File("/store/");
  fs.mkdirs(storeDir);
  StoreFactory storeFactory=new StoreFactory(fs,storeDir,pageCache,RecordFormatSelector.autoSelectFormat(),NullLogProvider.getInstance());
  return storeFactory.openAllNeoStores(true);
}
