{
  return new Iterator<T>(){
    Iterator<F> iter=source.iterator();
    public boolean hasNext(){
      return iter.hasNext();
    }
    public T next(){
      return convert(iter.next());
    }
    public void remove(){
      iter.remove();
    }
  }
;
}
