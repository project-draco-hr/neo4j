{
  RemoteRelationship relationship=new RemoteRelationship(engine,spec.relationshipId,engine.type(spec.name),getNode(spec.startNodeId,true),getNode(spec.endNodeId,true));
  if (relationshipCache != null) {
    relationshipCache.put(spec.relationshipId,relationship);
  }
  return relationship;
}
