{
  return new ConversionIterable<NodeSpecification,Node>(engine.getIndexNodes(id,connection,serviceId,key,value)){
    @Override Node convert(    NodeSpecification source){
      return getNode(source.id,true);
    }
  }
;
}
