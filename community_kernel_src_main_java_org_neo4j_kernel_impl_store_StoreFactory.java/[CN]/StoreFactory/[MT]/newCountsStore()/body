{
  File storeFileName=storeFileName(StoreFactory.COUNTS_STORE);
  return config.get(GraphDatabaseSettings.read_only) ? new StillCountsTracker(logProvider,fileSystemAbstraction,pageCache,config,storeFileName) : new CountsTracker(logProvider,fileSystemAbstraction,pageCache,config,storeFileName);
}
