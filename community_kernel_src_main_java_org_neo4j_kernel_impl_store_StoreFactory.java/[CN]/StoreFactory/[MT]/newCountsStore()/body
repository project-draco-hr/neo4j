{
  File storeFileName=storeFileName(StoreFactory.COUNTS_STORE);
  return config.get(GraphDatabaseSettings.read_only) ? new StillCountsTracker(stringLogger,fileSystemAbstraction,pageCache,config,storeFileName) : new CountsTracker(stringLogger,fileSystemAbstraction,pageCache,config,storeFileName);
}
