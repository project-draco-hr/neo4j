{
  Node n=(Node)node;
  long ourId=(long)this.getId(), theirId=(long)n.getId();
  if (ourId < theirId) {
    return -1;
  }
 else   if (ourId > theirId) {
    return 1;
  }
 else {
    return 0;
  }
}
