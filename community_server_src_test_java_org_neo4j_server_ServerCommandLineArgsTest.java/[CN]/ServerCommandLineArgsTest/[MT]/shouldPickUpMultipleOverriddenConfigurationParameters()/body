{
  String[] args=array("-c","my_first_option=first","-c","myoptionenabled","-c","my_second_option=second");
  ServerCommandLineArgs parsed=ServerCommandLineArgs.parse(args);
  assertEquals(asSet(Pair.of("my_first_option","first"),Pair.of("myoptionenabled",Boolean.TRUE.toString()),Pair.of("my_second_option","second")),asSet(parsed.configOverrides()));
}
