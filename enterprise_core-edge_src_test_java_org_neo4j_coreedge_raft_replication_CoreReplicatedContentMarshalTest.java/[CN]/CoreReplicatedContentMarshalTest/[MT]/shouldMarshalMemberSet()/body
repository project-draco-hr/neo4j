{
  ByteBuf buffer=Unpooled.buffer();
  ReplicatedContent message=new CoreMemberSet(asSet(new CoreMember(UUID.randomUUID()),new CoreMember(UUID.randomUUID())));
  assertMarshalingEquality(buffer,message);
}
