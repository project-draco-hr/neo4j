{
  File schemaIndexProviderStoreDirectory=schemaIndexProvider.getStoreDirectory(storeDir);
  return schemaIndexProviderStoreDirectory.exists() && Files.newDirectoryStream(schemaIndexProviderStoreDirectory.toPath()).iterator().hasNext();
}
