{
  Node state=stack;
  do {
    if (state == released) {
      return true;
    }
    Node next;
    do {
      next=state.next;
    }
 while (next == null);
    state=next;
  }
 while (state != end);
  return false;
}
