{
  int opsLeft=numberOfOps;
  long start=System.nanoTime();
  while (opsLeft-- > 0) {
    runOperation();
  }
  double delta=(System.nanoTime() - start);
  delta=delta / (1000.0 * 1000.0);
  double opsPerUnit=numberOfOps / delta;
  return new WorkerMetric[]{new WorkerMetric(WorkerMetric.TOTAL,"Ops per sec",opsPerUnit,OPERATION.per(MILLISECOND))};
}
