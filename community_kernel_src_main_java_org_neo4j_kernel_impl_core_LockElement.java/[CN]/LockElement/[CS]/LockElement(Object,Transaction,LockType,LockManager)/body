{
  if (resource == null)   throw new IllegalArgumentException("Null resource");
  if (tx == null)   throw new IllegalArgumentException("Null tx");
  this.tx=tx;
  this.resource=resource;
  this.lockType=type;
  this.lockManager=lockManager;
}
