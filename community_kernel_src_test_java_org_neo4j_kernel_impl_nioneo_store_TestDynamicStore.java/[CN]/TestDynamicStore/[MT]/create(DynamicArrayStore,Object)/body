{
  long blockId=store.nextId();
  Collection<DynamicRecord> records=store.allocateRecords(blockId,arrayToStore);
  for (  DynamicRecord record : records) {
    store.updateRecord(record);
  }
  return blockId;
}
