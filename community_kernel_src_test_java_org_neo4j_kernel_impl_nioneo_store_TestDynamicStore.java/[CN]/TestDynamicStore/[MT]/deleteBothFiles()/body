{
  File file=new File(dynamicStoreFile());
  if (file.exists()) {
    assertTrue(file.delete());
  }
  file=new File(dynamicStoreIdFile());
  if (file.exists()) {
    assertTrue(file.delete());
  }
}
