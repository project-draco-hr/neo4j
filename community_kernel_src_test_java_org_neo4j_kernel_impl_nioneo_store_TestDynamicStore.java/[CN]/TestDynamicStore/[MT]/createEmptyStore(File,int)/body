{
  Config config=config();
  new StoreFactory(config,ID_GENERATOR_FACTORY,new DefaultWindowPoolFactory(monitors,config),fs.get(),StringLogger.DEV_NULL,null,monitors).createDynamicArrayStore(fileName,blockSize);
}
