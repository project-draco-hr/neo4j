{
  Config config=config();
  return new DynamicArrayStore(dynamicStoreFile(),config,IdType.ARRAY_BLOCK,ID_GENERATOR_FACTORY,new DefaultWindowPoolFactory(monitors,config),fs.get(),StringLogger.DEV_NULL,StoreVersionMismatchHandler.THROW_EXCEPTION,new Monitors());
}
