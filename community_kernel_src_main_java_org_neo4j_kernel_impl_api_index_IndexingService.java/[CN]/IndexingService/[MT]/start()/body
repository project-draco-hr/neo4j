{
  for (  IndexContext indexContext : indexes.values()) {
switch (indexContext.getState()) {
case ONLINE:
      break;
case POPULATING:
case NON_EXISTENT:
    indexContext.create();
  break;
case FAILED:
break;
}
}
serviceRunning=true;
}
