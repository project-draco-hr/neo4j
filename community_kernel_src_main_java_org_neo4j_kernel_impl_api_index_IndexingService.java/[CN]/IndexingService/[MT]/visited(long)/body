{
  if (state != State.NOT_STARTED) {
    throw new IllegalStateException("Can't queue recovered node ids " + recoveredNodeId + " while indexing service is "+ state);
  }
  recoveredNodeIds.add(recoveredNodeId);
  return false;
}
