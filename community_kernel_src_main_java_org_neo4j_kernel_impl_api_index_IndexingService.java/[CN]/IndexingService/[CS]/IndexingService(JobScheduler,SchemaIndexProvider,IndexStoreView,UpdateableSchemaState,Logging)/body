{
  this.scheduler=scheduler;
  this.provider=provider;
  this.storeView=storeView;
  this.logging=logging;
  this.logger=logging.getLogger(getClass());
  this.updateableSchemaState=updateableSchemaState;
  if (provider == null) {
    throw new IllegalStateException("You cannot run the database without providing a schema index provider, " + "please make sure that a valid provider is on your classpath.");
  }
}
