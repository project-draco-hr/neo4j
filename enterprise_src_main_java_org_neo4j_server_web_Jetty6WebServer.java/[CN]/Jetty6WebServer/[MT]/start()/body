{
  jetty=new Server(jettyPort);
  jetty.setStopAtShutdown(true);
  loadStaticContent();
  loadJAXRSPackages();
  try {
    jetty.start();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
