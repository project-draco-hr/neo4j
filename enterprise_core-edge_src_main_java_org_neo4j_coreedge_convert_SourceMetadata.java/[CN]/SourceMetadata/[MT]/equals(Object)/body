{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  SourceMetadata that=(SourceMetadata)o;
  return lastTxId == that.lastTxId && storeIdEquals(before,that.before) && storeIdEquals(after,that.after);
}
