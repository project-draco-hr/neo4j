{
  if (id == -1)   throw new IllegalStateException();
  return state.augmentNodeRelationshipCursor(nodeIsAddedInThisTx ? RelationshipCursor.EMPTY : cursor.relationships(direction,relTypes),nodeState,direction,relTypes);
}
