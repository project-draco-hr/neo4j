{
  if (id == -1)   throw new IllegalStateException();
  return state.augmentNodeRelationshipCursor(nodeIsAddedInThisTx ? RelationshipCursor.EMPTY : cursor.relationships(direction),nodeState,direction,null);
}
