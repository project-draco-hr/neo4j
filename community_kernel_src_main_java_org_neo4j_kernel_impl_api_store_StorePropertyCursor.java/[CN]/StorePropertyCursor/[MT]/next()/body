{
  do {
    if (cursor == null) {
      if (nextPropertyRecordId == Record.NO_NEXT_PROPERTY.intValue()) {
        return false;
      }
      nextRecord();
    }
  }
 while (!nextBlock());
  return true;
}
