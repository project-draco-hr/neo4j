{
  return "[source,cypher]\n----\n" + query.replace("start","START").replace("where","WHERE").replace("match","MATCH").replace("return","RETURN").replace(" MATCH","\nMATCH").replace(" RETURN","\nRETURN").replace(" WHERE","\nWHERE").replace("where","WHERE") + "\n----\n";
}
