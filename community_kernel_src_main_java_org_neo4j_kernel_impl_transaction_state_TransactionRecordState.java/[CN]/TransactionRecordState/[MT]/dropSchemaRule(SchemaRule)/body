{
  RecordProxy<Long,Collection<DynamicRecord>,SchemaRule> change=recordChangeSet.getSchemaRuleChanges().getOrLoad(rule.getId(),rule);
  Collection<DynamicRecord> records=change.forChangingData();
  for (  DynamicRecord record : records) {
    record.setInUse(false);
  }
}
