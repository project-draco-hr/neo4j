{
  return new IterableWrapper<Relationship,Position>(this){
    @Override public Iterator<Relationship> iterator(){
      Iterator<Relationship> iter=super.iterator();
      iter.next();
      return iter;
    }
    @Override protected Relationship underlyingObjectToObject(    Position position){
      return position.lastRelationship();
    }
  }
;
}
