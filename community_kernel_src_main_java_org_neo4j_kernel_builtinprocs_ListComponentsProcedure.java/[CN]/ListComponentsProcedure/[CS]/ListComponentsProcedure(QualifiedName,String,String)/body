{
  super(procedureSignature(name).out("name",NTString).out("versions",NTList(NTString)).out("edition",NTString).description("List DBMS components and their versions.").build());
  this.neo4jVersion=neo4jVersion;
  this.neo4jEdition=neo4jEdition;
}
