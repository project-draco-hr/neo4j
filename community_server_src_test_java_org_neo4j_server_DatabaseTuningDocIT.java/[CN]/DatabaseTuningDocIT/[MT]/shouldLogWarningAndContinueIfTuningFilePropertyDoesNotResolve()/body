{
  AssertableLogProvider logProvider=new AssertableLogProvider();
  server=CommunityServerBuilder.server(logProvider).usingDatabaseDir(folder.cleanDirectory(name.getMethodName()).getAbsolutePath()).withNonResolvableTuningFile().build();
  server.start();
  logProvider.assertContainsMessageContaining("Config file [%s] does not exist.");
}
