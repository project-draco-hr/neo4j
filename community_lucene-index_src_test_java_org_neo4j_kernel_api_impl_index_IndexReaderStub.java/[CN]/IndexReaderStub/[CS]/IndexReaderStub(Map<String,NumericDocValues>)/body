{
  this.ndvs=new Function<String,NumericDocValues>(){
    @Override public NumericDocValues apply(    String s){
      NumericDocValues dv=ndvs.get(s);
      if (dv == null) {
        return DocValues.emptyNumeric();
      }
      return dv;
    }
  }
;
}
