{
  validate();
  if (xaCon.getNeoTransaction().nodeCreated(nodeId) && !xaCon.getNeoTransaction().nodeDeleted(nodeId)) {
    return true;
  }
  if (xaCon.getNeoTransaction().nodeDeleted(nodeId)) {
    return false;
  }
  return nodeStore.loadLightNode(nodeId);
}
