{
  super(type,node);
  if (src == null) {
    src=IntArray.EMPTY;
    srcTraversed=true;
  }
  this.src=src;
  if (add == null) {
    addTraversed=true;
  }
  this.add=add;
  if (remove != null) {
    for (int i=0; i < remove.length(); i++) {
      this.remove.add(remove.get(i));
    }
  }
}
