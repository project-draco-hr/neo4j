{
  super(management);
  this.txManager=(TxManager)management.getKernelData().graphDatabase().getTxManager();
  this.xadsm=management.getKernelData().graphDatabase().getDependencyResolver().resolveDependency(XaDataSourceManager.class);
}
