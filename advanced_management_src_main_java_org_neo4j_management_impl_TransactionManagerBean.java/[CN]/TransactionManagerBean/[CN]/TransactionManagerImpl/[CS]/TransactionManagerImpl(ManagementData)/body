{
  super(management);
  this.txManager=management.getKernelData().graphDatabase().getDependencyResolver().resolveDependency(TxManager.class);
  this.xadsm=management.getKernelData().graphDatabase().getDependencyResolver().resolveDependency(XaDataSourceManager.class);
}
