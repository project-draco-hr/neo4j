{
  testRealm.newUser("andres","el password",true);
  testRealm.newUser("mats","el password",false);
  log.assertExactly(info("User created: `%s` (password change required)","andres"),info("User created: `%s`","mats"));
}
