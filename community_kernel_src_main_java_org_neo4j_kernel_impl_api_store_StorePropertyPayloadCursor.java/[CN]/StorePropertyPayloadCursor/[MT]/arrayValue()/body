{
  assertOfType(ARRAY);
  try {
    readFromStore(arrayStore);
  }
 catch (  IOException e) {
    throw new UnderlyingStorageException(e);
  }
  buffer.flip();
  return readArrayFromBuffer(buffer);
}
