{
  TransactionInterceptor first=TransactionInterceptorProvider.resolveChain(providers,NeoStoreXaDataSource.this,dependencyResolver);
  return new InterceptingWriteTransaction(identifier,getLogicalLog(),neoStore,lockReleaser,lockManager,first);
}
