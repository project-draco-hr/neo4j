{
  return new WriteTransaction(identifier,lastCommittedTxWhenTransactionStarted,getLogicalLog(),state,neoStore,cacheAccess,indexingService,labelScanStore,integrityValidator,(KernelTransactionImplementation)kernel.newTransaction(),locks);
}
