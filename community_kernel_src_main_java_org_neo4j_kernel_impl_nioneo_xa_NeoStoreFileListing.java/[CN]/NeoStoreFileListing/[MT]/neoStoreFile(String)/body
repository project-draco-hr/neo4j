{
  if (name.endsWith(".id")) {
    return false;
  }
  if (name.equals(IndexConfigStore.INDEX_DB_FILE_NAME)) {
    return true;
  }
  return name.startsWith(NeoStore.DEFAULT_NAME) && !name.startsWith(NeoStore.DEFAULT_NAME + ".transaction");
}
