{
  Map<String,String> serverConfigParams=new HashMap();
  serverConfigParams.putAll(config);
  serverConfigParams.put(ServerSettings.third_party_packages.name(),ConfiguratorWrappingConfigurationBuilder.toStringForThirdPartyPackageProperty(extensions));
  return new Config(serverConfigParams);
}
