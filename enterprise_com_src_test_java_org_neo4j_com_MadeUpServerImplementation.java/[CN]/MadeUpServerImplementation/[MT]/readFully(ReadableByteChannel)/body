{
  ByteBuffer buffer=ByteBuffer.allocate(1000);
  try {
    while (true) {
      buffer.clear();
      if (data.read(buffer) == -1) {
        break;
      }
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
