{
  NDPConn client=new NDPConn(new PackStreamMessageFormatV1());
  client.connect(server.address()).send(acceptedVersions(1337,0,0,0));
  assertThat(client,eventuallyRecieves(new byte[]{0,0,0,0}));
}
