{
  statement.assertOpen();
  return map(new Function<ProcedureDescriptor,ProcedureSignature>(){
    @Override public ProcedureSignature apply(    ProcedureDescriptor o){
      return o.signature();
    }
  }
,schemaRead().proceduresGetAll(statement));
}
