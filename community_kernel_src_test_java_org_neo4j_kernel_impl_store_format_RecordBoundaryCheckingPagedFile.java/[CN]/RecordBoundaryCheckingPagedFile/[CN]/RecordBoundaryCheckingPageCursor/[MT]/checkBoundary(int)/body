{
  shouldReport=true;
  if (size > recordSize) {
    throw new IllegalStateException("Tried to go beyond record boundaries. We seem to be on the " + (nextCalls == 1 ? "first" : "second") + " page start offset:"+ start+ " record size:"+ recordSize+ " and tried to go to "+ size);
  }
}
