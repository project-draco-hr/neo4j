{
  if (ttl <= 0) {
    throw new IllegalArgumentException("TTL must be larger than zero.");
  }
  cacheMap=Caffeine.newBuilder().maximumSize(maxCapacity).expireAfterWrite(ttl,TimeUnit.MILLISECONDS).ticker(ticker).build();
}
