{
  File failureFile=failureFile(indexId);
  BufferedWriter writer=new BufferedWriter(new OutputStreamWriter(new FileOutputStream(failureFile),"utf-8"));
  try {
    writer.write(failure);
  }
  finally {
    writer.close();
  }
}
