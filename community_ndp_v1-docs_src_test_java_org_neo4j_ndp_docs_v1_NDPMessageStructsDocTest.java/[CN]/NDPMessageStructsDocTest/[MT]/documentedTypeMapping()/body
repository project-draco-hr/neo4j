{
  Collection<Object[]> mappings=new ArrayList<>();
  for (  DocStruct struct : docs().read("dev/messaging.asciidoc","code[data-lang=\"ndp_message_struct\"]",struct_definition)) {
    mappings.add(new Object[]{struct});
  }
  return mappings;
}
