{
  config=config != null ? config : MapUtil.stringMap();
  impl=new EmbeddedGraphDbImpl(storeDir,null,config,this,CommonFactories.defaultLockManagerFactory(),idGenerators,CommonFactories.defaultRelationshipTypeCreator(),CommonFactories.defaultTxIdGeneratorFactory(),CommonFactories.defaultTxFinishHook(),CommonFactories.defaultLastCommittedTxIdSetter(),fileSystem);
}
