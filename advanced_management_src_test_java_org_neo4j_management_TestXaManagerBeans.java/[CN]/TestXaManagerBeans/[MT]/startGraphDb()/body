{
  graphDb=new EmbeddedGraphDatabase(dir.directory("test").getAbsolutePath());
  xaManager=graphDb.getDependencyResolver().resolveDependency(JmxKernelExtension.class).getSingleManagementBean(XaManager.class);
}
