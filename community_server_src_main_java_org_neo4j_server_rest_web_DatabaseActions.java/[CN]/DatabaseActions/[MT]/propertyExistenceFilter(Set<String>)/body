{
  return new Predicate<ConstraintDefinition>(){
    @Override public boolean test(    ConstraintDefinition item){
      return item.isConstraintType(ConstraintType.MANDATORY) && propertyKeysSet.equals(asSet(item.getPropertyKeys()));
    }
  }
;
}
