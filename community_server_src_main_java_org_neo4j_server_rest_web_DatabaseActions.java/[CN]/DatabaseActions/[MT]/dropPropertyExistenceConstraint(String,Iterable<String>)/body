{
  final Set<String> propertyKeysSet=asSet(propertyKeys);
  ConstraintDefinition constraint=singleOrNull(filteredConstraints(labelName,propertyExistenceFilter(propertyKeysSet)));
  if (constraint != null) {
    constraint.drop();
  }
  return constraint != null;
}
