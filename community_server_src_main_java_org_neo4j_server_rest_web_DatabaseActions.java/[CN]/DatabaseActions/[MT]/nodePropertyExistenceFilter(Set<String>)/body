{
  return new Predicate<ConstraintDefinition>(){
    @Override public boolean test(    ConstraintDefinition item){
      return item.isConstraintType(ConstraintType.MANDATORY_NODE_PROPERTY) && propertyKeysSet.equals(asSet(item.getPropertyKeys()));
    }
  }
;
}
