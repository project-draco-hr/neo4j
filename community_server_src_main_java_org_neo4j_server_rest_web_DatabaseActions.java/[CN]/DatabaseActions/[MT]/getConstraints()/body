{
  Transaction tx=database.getGraph().beginTx();
  try {
    ListRepresentation result=new ListRepresentation(CONSTRAINT_DEFINITION,map(CONSTRAINT_DEF_TO_REPRESENTATION,database.getGraph().schema().getConstraints()));
    tx.success();
    return result;
  }
  finally {
    tx.finish();
  }
}
