{
  Transaction tx=database.getGraph().beginTx();
  try {
    ListRepresentation result=new ListRepresentation(CONSTRAINT_DEFINITION,map(CONSTRAINT_DEF_TO_REPRESENTATION,filteredConstraints(labelName,Predicates.<ConstraintDefinition>TRUE())));
    tx.success();
    return result;
  }
  finally {
    tx.finish();
  }
}
