{
  Transaction tx=graphDb.beginTx();
  try {
    IndexDefinitionRepresentation result=new IndexDefinitionRepresentation(graphDb.schema().indexCreator(label(labelName)).on(propertyKey).create());
    tx.success();
    return result;
  }
  finally {
    tx.finish();
  }
}
