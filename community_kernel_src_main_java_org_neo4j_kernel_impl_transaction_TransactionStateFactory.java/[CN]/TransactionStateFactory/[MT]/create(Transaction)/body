{
  Locks.Client lockClient=locks.newClient();
  lockClient.setTx(tx);
  TransactionState transactionState=new WritableTransactionState(lockClient,nodeManager,txHook,txIdGenerator);
  return transactionState;
}
