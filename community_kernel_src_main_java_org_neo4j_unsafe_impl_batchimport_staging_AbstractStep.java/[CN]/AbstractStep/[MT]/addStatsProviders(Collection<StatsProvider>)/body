{
  providers.add(new ProcessingStats(doneBatches.get() + queuedBatches.get(),doneBatches.get(),totalProcessingTime.total(),totalProcessingTime.average() / numberOfParallelProcessors(),upstreamIdleTime.get(),downstreamIdleTime.get()));
}
