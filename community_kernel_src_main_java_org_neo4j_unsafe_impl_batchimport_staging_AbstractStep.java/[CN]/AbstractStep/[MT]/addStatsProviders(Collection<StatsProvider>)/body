{
  providers.add(new ProcessingStats(doneBatches.get() + queuedBatches.get(),doneBatches.get(),totalProcessingTime.get(),upstreamIdleTime.get(),downstreamIdleTime.get(),numberOfParallelProcessors()));
}
