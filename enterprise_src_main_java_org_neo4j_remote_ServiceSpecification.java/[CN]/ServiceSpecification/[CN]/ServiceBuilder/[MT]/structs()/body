{
  StructSpecification[] result=new StructSpecification[structs.size()];
  int i=0;
  for (  Map.Entry<String,String> struct : structs.entrySet()) {
    result[i++]=new StructSpecification(struct.getKey(),struct.getValue());
  }
  return result;
}
