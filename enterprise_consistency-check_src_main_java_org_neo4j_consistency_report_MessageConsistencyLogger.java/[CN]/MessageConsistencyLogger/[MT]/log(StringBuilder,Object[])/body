{
  if (args != null && args.length > 0) {
    log.append("\n\tInconsistent with:");
    for (    Object arg : args) {
      log.append(' ').append(arg);
    }
  }
  logger.logMessage(log.toString());
}
