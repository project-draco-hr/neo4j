from os.path import abspath, dirname, join
DATABASES = {'default': {'ENGINE': 'neo4j', 'PATH': join(dirname(dirname(dirname(dirname(dirname(abspath(__file__)))))), 'target', 'testdata', 'django-db'), }, }
