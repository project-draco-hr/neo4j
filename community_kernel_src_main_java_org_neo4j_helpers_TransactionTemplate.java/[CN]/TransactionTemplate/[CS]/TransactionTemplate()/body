{
  this(null,new Monitor.Adapter(),0,0,new Predicate<Throwable>(){
    @Override public boolean accept(    Throwable exception){
      if (exception instanceof Error) {
        return false;
      }
      return true;
    }
  }
);
}
