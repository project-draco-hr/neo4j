{
  if (lastIndex < prevIndex) {
    throw new IllegalArgumentException(format("A range cannot have the upper bound set to a value " + "(%d) less than the lower bound (%d)",lastIndex,prevIndex));
  }
  this.lastIndex=lastIndex;
}
