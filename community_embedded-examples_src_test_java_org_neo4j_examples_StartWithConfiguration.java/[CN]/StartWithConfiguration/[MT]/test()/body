{
  String pathToConfig="src/test/resources/";
  Map<String,String> config=EmbeddedGraphDatabase.loadConfigurations(pathToConfig + "neo4j.properties");
  GraphDatabaseService graphDb=new EmbeddedGraphDatabase("target/database/location",config);
  assertNotNull(graphDb);
  graphDb.shutdown();
}
