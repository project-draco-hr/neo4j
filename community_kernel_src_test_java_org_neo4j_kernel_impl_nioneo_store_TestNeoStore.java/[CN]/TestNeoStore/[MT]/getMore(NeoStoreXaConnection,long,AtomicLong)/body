{
  Triplet<Iterable<RelationshipRecord>,Iterable<RelationshipRecord>,Long> result=xaCon.getWriteTransaction().getMoreRelationships(node,pos.get());
  pos.set(result.third());
  return new CombiningIterable<RelationshipRecord>(asList(result.first(),result.second()));
}
