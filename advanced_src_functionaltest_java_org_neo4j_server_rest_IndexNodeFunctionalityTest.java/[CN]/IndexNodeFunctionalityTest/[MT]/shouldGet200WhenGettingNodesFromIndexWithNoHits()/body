{
  String indexName="empty-index";
  helper.createNodeIndex(indexName);
  ClientResponse response=Client.create().resource(functionalTestHelper.indexUri() + "/node/" + indexName+ "/non-existent-key/non-existent-value").accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  assertEquals(200,response.getStatus());
}
