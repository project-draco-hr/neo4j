{
  ClientResponse response=Client.create().resource(functionalTestHelper.indexUri() + "/node/").accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  assertEquals(204,response.getStatus());
}
