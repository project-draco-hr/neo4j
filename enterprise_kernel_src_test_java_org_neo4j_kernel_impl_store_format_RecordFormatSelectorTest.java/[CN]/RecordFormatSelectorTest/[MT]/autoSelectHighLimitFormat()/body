{
  RecordFormats recordFormats=RecordFormatSelector.autoSelectFormat();
  assertEquals("Default autoselectable format should be format from available format provider.",recordFormats,HighLimit.RECORD_FORMATS);
}
