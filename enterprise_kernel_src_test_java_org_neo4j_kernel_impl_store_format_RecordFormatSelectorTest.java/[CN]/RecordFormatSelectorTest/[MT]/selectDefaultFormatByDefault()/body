{
  Config config=Config.empty();
  RecordFormats formatSelector=RecordFormatSelector.select(config,StandardV3_0.RECORD_FORMATS,NullLogService.getInstance());
  assertEquals(StandardV3_0.RECORD_FORMATS.storeVersion(),formatSelector.storeVersion());
}
