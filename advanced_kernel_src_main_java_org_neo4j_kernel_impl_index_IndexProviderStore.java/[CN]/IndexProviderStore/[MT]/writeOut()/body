{
  buf.clear();
  buf.putLong(creationTime).putLong(randomIdentifier).putLong(version).putLong(lastCommittedTx);
  buf.flip();
  try {
    fileChannel.position(0);
    writeBuffer(fileChannel,buf);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
