{
  snapshotRecords.visitEntries(new PrimitiveLongObjectVisitor<ProviderMeta.Record,RuntimeException>(){
    @Override public boolean visited(    long key,    Record value){
      updateRecord(value);
      return false;
    }
  }
);
}
