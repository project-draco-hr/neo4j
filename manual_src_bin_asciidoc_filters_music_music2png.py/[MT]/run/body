def run(cmd):
    global verbose
    if (not verbose):
        cmd += (' 2>%s' % os.devnull)
    print_verbose(('executing: %s' % cmd))
    if os.system(cmd):
        raise EApp, ('failed command: %s' % cmd)
