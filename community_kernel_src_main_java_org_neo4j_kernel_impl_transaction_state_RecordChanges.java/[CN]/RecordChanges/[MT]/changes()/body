{
  return Iterables.filter(new Predicate<RecordProxy<KEY,RECORD,ADDITIONAL>>(){
    @Override public boolean accept(    RecordProxy<KEY,RECORD,ADDITIONAL> item){
      return item.isChanged();
    }
  }
,recordChanges.values());
}
