{
  RecordChange<KEY,RECORD,ADDITIONAL> recordChange=new RecordChange<>(recordChanges,changeCounter,key,newRecord,loader,false,additionalData);
  recordChanges.put(key,recordChange);
  recordChange.forChangingData();
}
