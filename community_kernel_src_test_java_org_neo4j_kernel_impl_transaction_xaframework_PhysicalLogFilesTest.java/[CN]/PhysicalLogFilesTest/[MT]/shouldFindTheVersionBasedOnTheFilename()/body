{
  final File file=new File("v" + DEFAULT_VERSION_SUFFIX + DEFAULT_VERSION_SUFFIX+ DEFAULT_VERSION_SUFFIX+ "2");
  long logVersion=PhysicalLogFiles.getLogVersion(file);
  assertEquals(2,logVersion);
}
