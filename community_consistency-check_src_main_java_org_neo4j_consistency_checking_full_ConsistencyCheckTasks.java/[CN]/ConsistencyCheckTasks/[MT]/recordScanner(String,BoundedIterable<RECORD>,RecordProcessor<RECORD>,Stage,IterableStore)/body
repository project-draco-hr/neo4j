{
  return stage.isParallel() ? new ParallelRecordScanner<>(name,statistics,numberOfThreads,store,progress,processor,cacheAccess,warmupStores) : new SequentialRecordScanner<>(name,statistics,numberOfThreads,store,progress,processor,warmupStores);
}
