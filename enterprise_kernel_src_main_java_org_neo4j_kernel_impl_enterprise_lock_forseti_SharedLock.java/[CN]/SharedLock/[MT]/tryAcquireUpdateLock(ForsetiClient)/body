{
  while (true) {
    int refs=refCount.get();
    if (refs > 0) {
      if (refCount.compareAndSet(refs,refs | UPDATE_LOCK_FLAG)) {
        updateHolder=client;
        return true;
      }
    }
 else {
      return false;
    }
  }
}
