{
  return getList(name,null,new Converter<Character>(){
    @Override Character convert(    AbstractGraphDatabase graphDb,    Object value) throws BadInputException {
      return convertCharacter(value);
    }
    @Override Character[] newArray(    int size){
      return new Character[size];
    }
  }
);
}
