{
  if (!propertyKeys.isEmpty())   throw new UnsupportedOperationException("Compound indexes are not yet supported, only one property per index is allowed.");
  return new IndexCreatorImpl(context,propertyKeyManager,label,addToCollection(asList(propertyKey),new ArrayList<String>(propertyKeys)));
}
