{
  if (!propertyKeys.isEmpty())   throw new UnsupportedOperationException("Only a single property key is supported at the moment");
  return new IndexCreatorImpl(context,propertyKeyManager,label,addToCollection(asList(propertyKey),new ArrayList<String>(propertyKeys)));
}
