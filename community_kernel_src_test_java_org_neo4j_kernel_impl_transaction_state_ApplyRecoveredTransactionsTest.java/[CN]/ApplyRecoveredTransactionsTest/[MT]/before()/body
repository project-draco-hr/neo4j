{
  File storeDir=new File("dir");
  StoreFactory storeFactory=new StoreFactory(storeDir,new Config(),new DefaultIdGeneratorFactory(),pageCacheRule.getPageCache(fsr.get()),fsr.get(),NullLogProvider.getInstance(),new Monitors());
  neoStore=storeFactory.newNeoStore(true);
}
