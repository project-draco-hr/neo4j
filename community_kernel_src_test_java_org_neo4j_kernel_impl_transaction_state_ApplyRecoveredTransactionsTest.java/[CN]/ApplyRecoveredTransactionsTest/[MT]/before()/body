{
  FileSystemAbstraction fs=fsr.get();
  File storeDir=new File("dir");
  StoreFactory storeFactory=new StoreFactory(storeDir,new Config(),new DefaultIdGeneratorFactory(fs),pageCacheRule.getPageCache(fs),fs,NullLogProvider.getInstance(),new Monitors());
  neoStore=storeFactory.newNeoStore(true);
}
