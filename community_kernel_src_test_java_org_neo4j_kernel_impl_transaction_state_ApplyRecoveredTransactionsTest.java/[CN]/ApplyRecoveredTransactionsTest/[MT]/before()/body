{
  FileSystemAbstraction fs=fsr.get();
  File storeDir=new File("dir");
  StoreFactory storeFactory=new StoreFactory(storeDir,Config.empty(),new DefaultIdGeneratorFactory(fs),pageCacheRule.getPageCache(fs),fs,LowLimitV3_0.RECORD_FORMATS,NullLogProvider.getInstance());
  neoStores=storeFactory.openAllNeoStores(true);
}
