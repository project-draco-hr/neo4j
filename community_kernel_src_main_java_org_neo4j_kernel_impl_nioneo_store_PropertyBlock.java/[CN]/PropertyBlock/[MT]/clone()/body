{
  PropertyBlock result=new PropertyBlock();
  result.isCreated=isCreated;
  if (valueBlocks != null)   result.valueBlocks=valueBlocks.clone();
  for (  DynamicRecord valueRecord : valueRecords)   result.valueRecords.add(valueRecord.clone());
  return result;
}
