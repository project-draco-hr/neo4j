{
  Transaction tx=dbRule.getGraphDatabaseService().beginTx();
  try {
    for (    Label label : labels) {
      node.removeLabel(label);
    }
    tx.success();
  }
  finally {
    tx.finish();
  }
}
