{
  File luceneRootDirectory=new File(storeDir.directory(),"schema/index/lucene");
  IndexStorage indexStorage=new ShardedIndexStorage(DirectoryFactory.PERSISTENT,new DefaultFileSystemAbstraction(),luceneRootDirectory,1);
  indexStorage.storeIndexFailure(failure);
}
