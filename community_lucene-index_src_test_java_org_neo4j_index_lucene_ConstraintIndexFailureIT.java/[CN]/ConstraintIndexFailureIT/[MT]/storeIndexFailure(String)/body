{
  File luceneRootDirectory=new File(storeDir.directory(),"schema/index/lucene");
  IndexStorage indexStorage=new PartitionedIndexStorage(DirectoryFactory.PERSISTENT,new DefaultFileSystemAbstraction(),luceneRootDirectory,1);
  indexStorage.storeIndexFailure(failure);
}
