{
  File luceneRootDirectory=new File(storeDir.directory(),"schema/index/lucene");
  new FailureStorage(new DefaultFileSystemAbstraction(),new FolderLayout(luceneRootDirectory)).storeIndexFailure(singleIndexId(luceneRootDirectory),failure);
}
