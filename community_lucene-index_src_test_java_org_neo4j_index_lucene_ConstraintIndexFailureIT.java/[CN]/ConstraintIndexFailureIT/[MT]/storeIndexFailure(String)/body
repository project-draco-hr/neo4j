{
  File luceneRootDirectory=new File(storeDir.directory(),"schema/index/lucene");
  PartitionedIndexStorage indexStorage=new PartitionedIndexStorage(DirectoryFactory.PERSISTENT,new DefaultFileSystemAbstraction(),luceneRootDirectory,"1");
  indexStorage.storeIndexFailure(failure);
}
