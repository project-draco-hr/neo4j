{
  File luceneRootDirectory=new File(storeDir.directory(),"schema/index/lucene");
  new FailureStorage(new FolderLayout(luceneRootDirectory)).storeIndexFailure(singleIndexId(luceneRootDirectory),failure);
}
