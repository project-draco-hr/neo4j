{
  Node node=getGraphDb().createNode();
  txCreateLoop(node);
  txCreateRel(node);
  txCreateLoop(node);
  node.delete();
  for (  Relationship rel : node.getRelationships()) {
    rel.delete();
  }
  commit();
}
