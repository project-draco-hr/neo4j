{
  int size=0;
  for (long version=getHighestLogVersion() - 1; version >= 0; version--) {
    File file=getFileName(version);
    if (FS.fileExists(file))     size+=FS.getFileSize(file);
 else     break;
  }
  return size;
}
