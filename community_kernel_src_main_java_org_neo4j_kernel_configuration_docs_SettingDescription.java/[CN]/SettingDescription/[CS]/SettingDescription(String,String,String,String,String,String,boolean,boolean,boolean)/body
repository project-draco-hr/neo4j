{
  this("config_" + (name.replace("{","").replace("}","")),name.replace("{","\\{").replace("}","\\}"),description,mandatoryDescription,deprecationDescription,validationDescription,defaultValue,isDeprecated,isMandatory,hasDefault);
}
