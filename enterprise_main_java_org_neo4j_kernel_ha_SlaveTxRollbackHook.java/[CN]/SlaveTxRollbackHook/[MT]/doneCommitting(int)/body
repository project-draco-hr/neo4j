{
  try {
    receiver.receive(broker.getMaster().doneCommitting(receiver.getSlaveContext(),eventIdentifier));
  }
 catch (  ZooKeeperException e) {
    receiver.somethingIsWrong(e);
    throw e;
  }
catch (  HaCommunicationException e) {
    receiver.somethingIsWrong(e);
    throw e;
  }
}
