{
  if (this.propertyKey != null)   throw new UnsupportedOperationException("Constraints on compound keys are not yet supported, " + "only one property per constraint is allowed.");
  return doOn(propertyKey);
}
