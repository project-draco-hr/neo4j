{
  GraphDatabaseAPI db=database();
  Transaction tx=db.beginTx();
  try {
    Node node=db.getReferenceNode();
    for (int i=0; i < propertyKeyCount; i++)     node.setProperty(key(i),true);
    tx.success();
    return db;
  }
  finally {
    tx.finish();
  }
}
