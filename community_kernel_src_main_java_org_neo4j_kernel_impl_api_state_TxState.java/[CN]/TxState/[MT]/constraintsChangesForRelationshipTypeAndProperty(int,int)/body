{
  return constraintsChangesForRelationshipType(relTypeId).filterAdded(new Predicate<RelationshipPropertyConstraint>(){
    @Override public boolean test(    RelationshipPropertyConstraint constraint){
      return constraint.propertyKey() == propertyKey;
    }
  }
);
}
