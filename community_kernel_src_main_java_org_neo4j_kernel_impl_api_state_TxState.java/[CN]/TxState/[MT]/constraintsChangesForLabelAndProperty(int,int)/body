{
  return LABEL_STATE.get(this,labelId).constraintsChanges().filterAdded(new Predicate<UniquenessConstraint>(){
    @Override public boolean test(    UniquenessConstraint item){
      return item.propertyKeyId() == propertyKey;
    }
  }
);
}
