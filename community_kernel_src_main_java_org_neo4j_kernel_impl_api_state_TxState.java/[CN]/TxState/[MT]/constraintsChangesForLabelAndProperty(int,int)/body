{
  return LABEL_STATE.get(this,labelId).constraintsChanges().filterAdded(new Predicate<PropertyConstraint>(){
    @Override public boolean test(    PropertyConstraint item){
      return item.propertyKeyId() == propertyKey;
    }
  }
);
}
