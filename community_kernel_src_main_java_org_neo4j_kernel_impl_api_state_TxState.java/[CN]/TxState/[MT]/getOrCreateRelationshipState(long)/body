{
  return getState(relationshipStates,relationshipId,new StateCreator<RelationshipState>(){
    @Override public RelationshipState newState(    long id){
      return new RelationshipState(id);
    }
  }
);
}
