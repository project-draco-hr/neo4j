{
  return asSet(map(new Function<IndexDefinition,Set<String>>(){
    @Override public Set<String> apply(    IndexDefinition from){
      return asSet(from.getPropertyKeys());
    }
  }
,indexes));
}
