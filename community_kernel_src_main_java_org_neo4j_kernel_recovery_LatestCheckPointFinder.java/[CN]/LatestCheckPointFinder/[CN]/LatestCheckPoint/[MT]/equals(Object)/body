{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  LatestCheckPoint that=(LatestCheckPoint)o;
  return commitsAfterCheckPoint == that.commitsAfterCheckPoint && oldestLogVersionFound == that.oldestLogVersionFound && checkPoint == null ? that.checkPoint == null : checkPoint.equals(that.checkPoint);
}
