{
  raft.handle(appendEntriesRequest().leaderTerm(0).prevLogIndex(-1).prevLogTerm(-1).logEntry(new RaftLogEntry(0,content)).build());
  raft.handle(appendEntriesRequest().leaderTerm(0).prevLogIndex(-1).prevLogTerm(-1).logEntry(new RaftLogEntry(0,content)).build());
  assertEquals(0,testEntryLog.appendIndex());
  assertEquals(content,testEntryLog.readEntryContent(0));
}
