{
  if (expander.equals(this.expander)) {
    return this;
  }
  return new TraversalDescriptionImpl(TraversalFactory.expander(expander),uniqueness,uniquenessParameter,pruning,filter,sourceSelector);
}
