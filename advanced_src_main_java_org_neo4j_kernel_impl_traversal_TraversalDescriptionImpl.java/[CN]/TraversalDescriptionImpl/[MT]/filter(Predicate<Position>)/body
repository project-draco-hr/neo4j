{
  if (this.filter == filter) {
    return this;
  }
  if (filter == null) {
    throw new IllegalArgumentException("Return filter may not be null, " + "use " + TraversalFactory.class.getSimpleName() + ".returnAll() instead.");
  }
  return new TraversalDescriptionImpl(expander,uniqueness,uniquenessParameter,pruning,filter,sourceSelector);
}
