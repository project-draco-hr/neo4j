{
  if (this.filter instanceof MultiFilter) {
    return ((MultiFilter)this.pruning).add(filter);
  }
 else {
    if (this.filter == Traversal.returnAll()) {
      return filter;
    }
 else {
      return new MultiFilter(new Predicate[]{this.filter,filter});
    }
  }
}
