{
  IdType idType=IdType.ACTUAL;
  Input input=new CsvInput(data("10"),header(entry("id",Type.ID,idType.extractor()),entry("name",Type.PROPERTY,idType.extractor())),null,null,idType,COMMAS);
  Iterator<InputNode> nodes=input.nodes().iterator();
  try {
    nodes.next();
    fail("Should fail");
  }
 catch (  InputException e) {
  }
}
