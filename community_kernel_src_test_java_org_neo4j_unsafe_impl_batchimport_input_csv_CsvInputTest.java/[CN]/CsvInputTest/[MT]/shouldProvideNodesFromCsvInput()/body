{
  IdType idType=IdType.ACTUAL;
  Input input=new CsvInput(data("123,Mattias Persson,HACKER"),header(entry("id",Type.ID,idType.extractor()),entry("name",Type.PROPERTY,Extractors.STRING),entry("labels",Type.LABEL,Extractors.STRING)),null,null,idType,COMMAS);
  Iterator<InputNode> nodes=input.nodes().iterator();
  assertNode(nodes.next(),123L,properties("name","Mattias Persson"),labels("HACKER"));
  assertFalse(nodes.hasNext());
}
