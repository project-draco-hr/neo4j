{
  Map<String,String> config=new HashMap<>();
  config.put("dbms.pagecache.memory","3G");
  BatchInserter inserter=BatchInserters.inserter(new File("target/batchinserter-example-config").getAbsolutePath(),fileSystem,config);
  inserter.shutdown();
}
