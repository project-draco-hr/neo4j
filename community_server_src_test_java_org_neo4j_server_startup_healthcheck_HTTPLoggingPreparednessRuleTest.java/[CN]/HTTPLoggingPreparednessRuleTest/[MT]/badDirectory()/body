{
  File f=new File(File.createTempFile("anywhere","") + "/does/not/exist");
  if (f.exists() || f.canWrite()) {
    throw new RuntimeException(String.format("File [%s] should not exist or be writable for this test",f.getAbsolutePath()));
  }
  return f;
}
