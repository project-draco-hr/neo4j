{
  PathFinder<WeightedPath> finder=GraphAlgoFactory.dijkstra(TraversalFactory.expanderForTypes(ExampleTypes.MY_TYPE,Direction.BOTH),new DoubleEvaluator("cost"));
  WeightedPath path=finder.findSinglePath(start,end);
  path.weight();
  return path;
}
