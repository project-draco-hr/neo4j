{
  return new GraphDatabaseBuilder.Delegator(actual){
    @Override public GraphDatabaseService newGraphDatabase(){
      GraphDatabaseService db=super.newGraphDatabase();
      perDbIndexProvider.put(db,provider);
      return db;
    }
  }
;
}
