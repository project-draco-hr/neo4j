{
  String url=server.baseUri() + "a/different/relative/webadmin/data/uri/";
  ClientResponse response=Client.create().resource(url).type(MediaType.APPLICATION_JSON).accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  assertThat(response.getStatus(),is(404));
}
