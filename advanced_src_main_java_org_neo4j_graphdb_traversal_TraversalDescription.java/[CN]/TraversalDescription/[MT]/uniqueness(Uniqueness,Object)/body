{
  if (this.uniqueness == uniqueness) {
    return this;
  }
switch (uniqueness) {
case NODE_RECENT:
case RELATIONSHIP_RECENT:
    acceptIntegerNumber(uniqueness,parameter);
  break;
default :
throw new IllegalArgumentException(uniqueness.name() + " doesn't accept any parameters");
}
return new TraversalDescription(expander,uniqueness,parameter,pruning,filter,order);
}
