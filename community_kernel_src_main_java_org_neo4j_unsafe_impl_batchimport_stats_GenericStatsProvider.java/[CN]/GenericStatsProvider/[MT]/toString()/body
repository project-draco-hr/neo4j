{
  StringBuilder builder=new StringBuilder();
  for (  Pair<Key,Stat> stat : stats) {
    builder.append(builder.length() > 0 ? ", " : "").append(format("%s: %s",stat.first().shortName(),stat.other().asString()));
  }
  return builder.toString();
}
