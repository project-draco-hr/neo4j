{
  return !authToken.containsKey(AuthToken.REALM_KEY) || authToken.get(AuthToken.REALM_KEY).toString().length() == 0 || authToken.get(AuthToken.REALM_KEY).equals("*") || authToken.get(AuthToken.REALM_KEY).equals(realm);
}
