{
  return new SearcherManagerFactory(){
    @Override public ReferenceManager<IndexSearcher> create(    LuceneIndexWriter indexWriter) throws IOException {
      return indexWriter.createSearcherManager();
    }
  }
;
}
