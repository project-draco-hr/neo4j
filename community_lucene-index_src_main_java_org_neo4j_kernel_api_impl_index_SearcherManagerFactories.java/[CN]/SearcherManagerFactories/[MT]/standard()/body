{
  return new SearcherManagerFactory(){
    @Override public ReferenceManager<IndexSearcher> create(    IndexWriter indexWriter) throws IOException {
      return new SearcherManager(indexWriter,true,new SearcherFactory());
    }
  }
;
}
