{
  Iterable<IndexRule> indexRules=indexRulesForLabel(labelId);
  return map(new Function<IndexRule,Long>(){
    @Override public Long apply(    IndexRule from){
      return from.getPropertyKeys()[0];
    }
  }
,indexRules);
}
