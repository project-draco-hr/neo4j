{
  try {
    for (    Long existingLabel : persistenceManager.getLabelsForNode(nodeId))     if (existingLabel.longValue() == labelId)     return true;
    return false;
  }
 catch (  InvalidRecordException e) {
    return false;
  }
}
