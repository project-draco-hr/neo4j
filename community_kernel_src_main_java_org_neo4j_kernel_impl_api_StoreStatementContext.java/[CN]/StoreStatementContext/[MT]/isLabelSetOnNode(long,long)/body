{
  try {
    for (    long existingLabel : getLabelsForNode(nodeId))     if (existingLabel == labelId)     return true;
    return false;
  }
 catch (  InvalidRecordException e) {
    return false;
  }
}
