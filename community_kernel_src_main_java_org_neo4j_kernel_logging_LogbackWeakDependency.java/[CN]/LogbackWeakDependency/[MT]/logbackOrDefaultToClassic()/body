{
  return new Function<Config,Logging>(){
    @Override public Logging apply(    Config config){
      return new LogbackWeakDependency().tryLoadLogbackService(config,DEFAULT_TO_CLASSIC);
    }
  }
;
}
