{
  if (panicCause != null) {
    throw new RuntimeException("Panic",panicCause);
  }
  for (  Step<?> step : pipeline) {
    if (!step.isCompleted()) {
      return true;
    }
  }
  return false;
}
