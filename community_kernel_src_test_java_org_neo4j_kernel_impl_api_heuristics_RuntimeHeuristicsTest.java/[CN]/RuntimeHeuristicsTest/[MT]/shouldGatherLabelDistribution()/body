{
  RuntimeHeuristicsService heuristics=new RuntimeHeuristicsService(generateStore(),null);
  heuristics.run();
  heuristics.run();
  assertThat(heuristics.labelDistribution(),closeToDistribution(new LabelledDistribution<Integer>().record(asList(0),20).record(asList(1),80).recalculate()));
}
