{
  assertEquals(path.length() / 2,state.getState().intValue());
  if (path.length() % 2 == 1)   state.setState(((Integer)state.getState()) + 1);
  return path.endNode().getRelationships(Direction.OUTGOING);
}
