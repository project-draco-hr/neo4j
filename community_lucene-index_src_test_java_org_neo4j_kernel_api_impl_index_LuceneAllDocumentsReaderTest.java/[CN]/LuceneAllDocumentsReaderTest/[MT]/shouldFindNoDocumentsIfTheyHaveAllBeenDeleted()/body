{
  final String[] elements={"A","B","C"};
  LuceneAllDocumentsReader reader=new LuceneAllDocumentsReader(new SearcherManagerStub(new SearcherStub(new IndexReaderStub(true,elements),elements)));
  Iterator<Document> iterator=reader.iterator();
  assertFalse(iterator.hasNext());
}
