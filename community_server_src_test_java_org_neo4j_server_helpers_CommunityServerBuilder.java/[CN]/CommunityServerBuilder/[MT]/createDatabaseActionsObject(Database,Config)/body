{
  Clock clockToUse=(clock != null) ? clock : SYSTEM_CLOCK;
  return new DatabaseActions(new LeaseManager(clockToUse),config.get(ServerInternalSettings.script_sandboxing_enabled),database.getGraph());
}
