{
  PhysicalTransactionRepresentation transaction=new PhysicalTransactionRepresentation(Arrays.asList(createNodeCommand()),false);
  transaction.setHeader(new byte[0],0,0,0,0);
  return transaction;
}
