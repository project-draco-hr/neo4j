{
  File tmpFile=File.createTempFile("neo4j-test","");
  tmpFile.delete();
  GraphDatabaseService db=new GraphDatabaseFactory().newEmbeddedDatabase(tmpFile.getCanonicalPath());
  db.shutdown();
  return tmpFile;
}
