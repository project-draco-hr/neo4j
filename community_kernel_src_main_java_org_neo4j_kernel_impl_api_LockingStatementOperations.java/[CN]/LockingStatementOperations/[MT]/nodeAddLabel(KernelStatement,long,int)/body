{
  state.locks().pessimistic().acquireShared(ResourceTypes.SCHEMA,schemaResource());
  state.locks().optimistic().acquireExclusive(ResourceTypes.NODE,nodeId);
  state.assertOpen();
  return entityWriteDelegate.nodeAddLabel(state,nodeId,labelId);
}
