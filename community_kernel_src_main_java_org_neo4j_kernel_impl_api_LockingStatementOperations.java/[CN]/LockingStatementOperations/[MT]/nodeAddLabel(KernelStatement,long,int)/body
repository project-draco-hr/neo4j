{
  state.locks().implicit().acquireShared(ResourceTypes.SCHEMA,schemaResource());
  state.locks().implicit().acquireExclusive(ResourceTypes.NODE,nodeId);
  state.assertOpen();
  return entityWriteDelegate.nodeAddLabel(state,nodeId,labelId);
}
