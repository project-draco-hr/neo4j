{
  state.locks().implicit().acquireExclusive(ResourceTypes.NODE,min(startNodeId,endNodeId));
  if (startNodeId != endNodeId) {
    state.locks().implicit().acquireExclusive(ResourceTypes.NODE,max(startNodeId,endNodeId));
  }
}
