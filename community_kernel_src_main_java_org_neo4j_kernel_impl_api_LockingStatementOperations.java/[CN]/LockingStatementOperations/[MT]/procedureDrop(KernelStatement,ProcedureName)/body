{
  state.locks().pessimistic().acquireExclusive(SCHEMA,schemaResource());
  state.locks().optimistic().acquireExclusive(PROCEDURE,procedureResourceId(name));
  schemaWriteDelegate.procedureDrop(state,name);
}
