{
  state.locks().implicit().acquireExclusive(SCHEMA,schemaResource());
  state.locks().implicit().acquireExclusive(PROCEDURE,procedureResourceId(name));
  schemaWriteDelegate.procedureDrop(state,name);
}
