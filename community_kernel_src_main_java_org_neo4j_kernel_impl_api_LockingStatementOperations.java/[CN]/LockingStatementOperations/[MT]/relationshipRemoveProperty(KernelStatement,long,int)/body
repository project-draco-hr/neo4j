{
  state.locks().implicit().acquireExclusive(ResourceTypes.RELATIONSHIP,relationshipId);
  state.assertOpen();
  return entityWriteDelegate.relationshipRemoveProperty(state,relationshipId,propertyKeyId);
}
