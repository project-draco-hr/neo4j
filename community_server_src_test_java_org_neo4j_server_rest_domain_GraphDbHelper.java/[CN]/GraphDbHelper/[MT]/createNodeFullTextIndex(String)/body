{
  Transaction transaction=database.getGraph().beginTx();
  try {
    Index<Node> index=database.getIndexManager().forNodes(named,MapUtil.stringMap(IndexManager.PROVIDER,"lucene","type","fulltext"));
    transaction.success();
    return index;
  }
  finally {
    transaction.finish();
  }
}
