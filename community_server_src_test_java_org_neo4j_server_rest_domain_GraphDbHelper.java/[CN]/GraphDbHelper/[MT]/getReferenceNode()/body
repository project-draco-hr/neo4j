{
  Transaction tx=database.getGraph().beginTx();
  try {
    @SuppressWarnings("deprecation") Node referenceNode=database.getGraph().getReferenceNode();
    tx.success();
    return referenceNode.getId();
  }
  finally {
    tx.finish();
  }
}
