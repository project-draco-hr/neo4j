{
  Transaction transaction=database.getGraph().beginTx();
  try {
    Index<Node> nodeIndex=database.getIndexManager().forNodes(named);
    transaction.success();
    return nodeIndex;
  }
  finally {
    transaction.finish();
  }
}
