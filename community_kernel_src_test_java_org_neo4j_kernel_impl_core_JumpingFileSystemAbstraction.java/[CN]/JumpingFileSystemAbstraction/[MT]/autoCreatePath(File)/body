{
  if (path.isFile())   path=path.getParentFile();
  if (!path.exists()) {
    if (!path.mkdirs()) {
      throw new IOException("Unable to create directory path[" + path.getPath() + "] for Neo4j store.");
    }
  }
}
