{
  resume();
  boolean value;
  try {
    value=server.hasRelationshipProperty(connection.neo,relationshiId,key);
  }
 catch (  RuntimeException ex) {
    return response().buildErrorResponse(ex);
  }
  return response().buildBooleanResponse(value);
}
