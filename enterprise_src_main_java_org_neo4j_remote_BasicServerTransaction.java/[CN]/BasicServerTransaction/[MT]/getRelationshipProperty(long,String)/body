{
  resume();
  Object value;
  try {
    value=server.getRelationshipProperty(connection.neo,relationshipId,key);
  }
 catch (  RuntimeException ex) {
    return response().buildErrorResponse(ex);
  }
  return response().buildPropertyResponse(value);
}
