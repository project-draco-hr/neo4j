{
  assertThat("Serialized version of value should match documented data: " + example,DocSerialization.normalizedHex(example.serializedData()),equalTo(DocSerialization.normalizedHex(DocSerialization.pack(example.attribute("Value")))));
}
