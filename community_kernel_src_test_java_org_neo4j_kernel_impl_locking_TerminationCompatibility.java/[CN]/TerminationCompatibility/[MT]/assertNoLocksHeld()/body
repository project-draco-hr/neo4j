{
  locks.accept(new Visitor(){
    @Override public void visit(    ResourceType resourceType,    long resourceId,    String description,    long estimatedWaitTime){
      fail("Unexpected lock on " + resourceType + " "+ resourceId);
    }
  }
);
}
