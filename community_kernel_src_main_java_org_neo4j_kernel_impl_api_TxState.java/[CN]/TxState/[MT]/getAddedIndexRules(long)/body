{
  Collection<Pair<Long,Long>> rules=indexRules.get(labelId);
  if (rules == null)   return Collections.emptyList();
  Collection<Long> result=new ArrayList<Long>();
  for (  Pair<Long,Long> rule : rules)   result.add(rule.other());
  return result;
}
