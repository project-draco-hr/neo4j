{
  Collection<Pair<Long,String>> rules=indexRules.get(labelId);
  if (rules == null)   return Collections.emptyList();
  Collection<String> result=new ArrayList<String>();
  for (  Pair<Long,String> rule : rules)   result.add(rule.other());
  return result;
}
