{
  NodeState nodeState=getState(nodeStates,nodeId,null);
  if (nodeState != null) {
    if (nodeState.getAddedLabels().contains(labelId))     return Boolean.TRUE;
    if (nodeState.getRemovedLabels().contains(labelId))     return Boolean.FALSE;
  }
  return null;
}
