{
  long startNode=createBasicTraversableGraph();
  List<Object> hits=serialize(actions.traverse(startNode,new HashMap<String,Object>(),TraverserReturnType.path));
  for (  Object hit : hits) {
    RelationshipRepresentationTest.verifySerialisation((Map<String,Object>)hit);
  }
}
