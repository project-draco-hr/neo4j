{
  ArrayMap<Integer,CowNodeElement> cowElements=cowMap.get(node.cowTxId).nodes;
  CowNodeElement element=cowElements.get(node.id);
  if (element != null) {
    return element.relationshipAddMap;
  }
  return null;
}
