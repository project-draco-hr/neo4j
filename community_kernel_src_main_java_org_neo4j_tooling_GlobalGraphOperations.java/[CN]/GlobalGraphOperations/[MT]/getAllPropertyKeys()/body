{
  assertInTransaction();
  return new ResourceIterable<String>(){
    @Override public ResourceIterator<String> iterator(){
      ReadStatement statement=statementCtxProvider.readStatement();
      return cleanupService.resourceIterator(map(new Function<Token,String>(){
        @Override public String apply(        Token propertyToken){
          return propertyToken.name();
        }
      }
,statement.propertyKeyGetAllTokens()),statement);
    }
  }
;
}
