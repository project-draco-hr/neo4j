{
  txSize+=count;
  total++;
  if (txSize >= MAX_SIZE) {
    if (printProgress) {
      System.out.println(total);
    }
    txSize=0;
    finish();
    beginTx();
    return true;
  }
  return false;
}
