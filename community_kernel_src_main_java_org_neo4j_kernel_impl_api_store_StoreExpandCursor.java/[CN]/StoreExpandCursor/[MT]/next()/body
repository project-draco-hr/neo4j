{
  if (relIterator == null) {
    if (!nextInputNode()) {
      return false;
    }
  }
  if (relIterator.hasNext()) {
    return nextRelationship();
  }
 else {
    relIterator=null;
    return next();
  }
}
