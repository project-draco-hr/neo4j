{
  canHandleProperties(new Callable<PropertyContainer>(){
    public PropertyContainer call(){
      return graphDb().createNode().createRelationshipTo(graphDb().createNode(),withName("TEST"));
    }
  }
);
}
