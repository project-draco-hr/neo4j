{
  File storeDir=dir.directory("small_store");
  initializeStores(storeDir,stringMap("string_block_size","62","array_block_size","302"));
  assertEquals(62 + AbstractDynamicStore.RECORD_HEADER_SIZE,pStore.getStringStore().getRecordSize());
  assertEquals(302 + AbstractDynamicStore.RECORD_HEADER_SIZE,pStore.getArrayStore().getRecordSize());
  ds.stop();
}
