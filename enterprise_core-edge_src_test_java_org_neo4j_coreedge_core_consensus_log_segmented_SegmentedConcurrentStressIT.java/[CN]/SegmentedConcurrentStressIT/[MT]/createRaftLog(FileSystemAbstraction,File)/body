{
  SegmentedRaftLog raftLog=new SegmentedRaftLog(fsa,dir,8 * 1024 * 1024,new DummyRaftableContentSerializer(),getInstance(),raft_log_pruning_strategy.getDefaultValue(),8,Clocks.fakeClock(),new OnDemandJobScheduler());
  raftLog.start();
  return raftLog;
}
