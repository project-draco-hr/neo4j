{
  clean("target/batchinserter-example-config");
  Map<String,String> config=new HashMap<>();
  config.put("dbms.memory.pagecache.size","512m");
  BatchInserter inserter=BatchInserters.inserter(new File("target/batchinserter-example-config").getAbsoluteFile(),config);
  inserter.shutdown();
}
