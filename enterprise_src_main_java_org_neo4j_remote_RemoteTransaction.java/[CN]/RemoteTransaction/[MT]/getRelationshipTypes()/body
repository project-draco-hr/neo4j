{
  return new ConversionIterable<String,RelationshipType>(engine.getRelationshipTypes(id)){
    @Override RelationshipType convert(    String source){
      return engine.type(source);
    }
  }
;
}
