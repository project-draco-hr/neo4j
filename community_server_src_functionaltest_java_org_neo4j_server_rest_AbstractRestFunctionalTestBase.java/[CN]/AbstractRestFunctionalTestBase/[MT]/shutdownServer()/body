{
  try {
    if (server != null)     server.stop();
  }
  finally {
    try {
      if (graphdb != null)       graphdb.shutdown();
    }
  finally {
      graphdb=null;
      server=null;
    }
  }
}
