{
  if (closed) {
    return;
  }
  if (mappedFiles != null) {
    throw new IllegalStateException("Cannot close the PageCache while files are still mapped.");
  }
  closed=true;
  for (int i=0; i < pages.length; i++) {
    pages[i]=null;
  }
  System.gc();
}
