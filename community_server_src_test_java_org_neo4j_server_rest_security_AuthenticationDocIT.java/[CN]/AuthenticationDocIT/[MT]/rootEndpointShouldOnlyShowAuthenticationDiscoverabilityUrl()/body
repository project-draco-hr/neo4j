{
  startServerWithConfiguredUser();
  HTTP.Response res=HTTP.GET(server.baseUri().resolve("").toString());
  assertThat(res.rawContent(),equalTo(format("{%n" + "  \"authentication\" : \"" + server.baseUri().resolve("authentication") + "\"%n"+ "}")));
}
