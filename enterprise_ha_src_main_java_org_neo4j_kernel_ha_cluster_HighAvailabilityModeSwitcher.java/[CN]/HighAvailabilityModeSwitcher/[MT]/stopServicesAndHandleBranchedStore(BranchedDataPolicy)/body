{
  for (  Class<Lifecycle> serviceClass : SERVICES_TO_RESTART_FOR_STORE_COPY)   graphDb.getDependencyResolver().resolveDependency(serviceClass).stop();
  branchPolicy.handle(config.get(InternalAbstractGraphDatabase.Configuration.store_dir));
}
