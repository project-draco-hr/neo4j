{
  return printingWrapper(source,new BiFunction<Path,BranchState,Boolean>(){
    @Override public Boolean apply(    Path propertyContainers,    BranchState stateBranchState){
      return true;
    }
  }
,descriptor);
}
