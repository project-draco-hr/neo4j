{
  Process process=Runtime.getRuntime().exec(new String[]{"java","-cp",getProperty("java.class.path"),ProcessThatJustWaits.class.getName(),"" + 20});
  ProcessStreamHandler handler=new ProcessStreamHandler(process,true);
  try {
    handler.waitForResult(1);
    fail("Should fail awaiting the process");
  }
 catch (  Exception e) {
  }
  assertTrue("Process should have been destroyed by now",process.exitValue() != 0);
}
