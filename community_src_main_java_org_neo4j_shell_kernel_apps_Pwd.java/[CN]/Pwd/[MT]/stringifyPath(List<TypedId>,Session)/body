{
  if (pathIds.isEmpty()) {
    return "";
  }
  StringBuilder path=new StringBuilder();
  for (  TypedId id : pathIds) {
    path.append(getDisplayName(getServer(),session,id)).append("-->");
  }
  return path.append(getDisplayNameForCurrent(session)).toString();
}
