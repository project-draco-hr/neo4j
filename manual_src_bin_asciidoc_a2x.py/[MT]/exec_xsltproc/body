def exec_xsltproc(xsl_file, xml_file, dst_dir, opts=''):
    cwd = os.getcwd()
    shell_cd(dst_dir)
    try:
        shell(('"%s" %s "%s" "%s"' % (XSLTPROC, opts, xsl_file, xml_file)))
    finally:
        shell_cd(cwd)
