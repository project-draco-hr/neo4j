{
  when(storeStatement.acquireSingleNodeCursor(1337)).thenReturn(asNodeCursor(1337,PropertyCursor.EMPTY,LabelCursor.EMPTY));
  assertFalse("Label should have been removed",txContext.nodeRemoveLabel(state,1337,12));
}
