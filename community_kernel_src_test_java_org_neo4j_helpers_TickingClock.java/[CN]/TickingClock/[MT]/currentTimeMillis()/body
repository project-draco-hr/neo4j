{
  List<Runnable> actionList=actions.get(current);
  if (actionList != null) {
    for (    Runnable runnable : actionList) {
      runnable.run();
    }
  }
  try {
    return current;
  }
  finally {
    current+=tick;
  }
}
