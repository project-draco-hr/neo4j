{
  return new Visitor<XaCommand>(){
    @Override public boolean visit(    XaCommand element){
      for (      DynamicRecord record : ((SchemaRuleCommand)element).getRecords())       assertFalse(record + " should have been heavy",record.isLight());
      return true;
    }
  }
;
}
