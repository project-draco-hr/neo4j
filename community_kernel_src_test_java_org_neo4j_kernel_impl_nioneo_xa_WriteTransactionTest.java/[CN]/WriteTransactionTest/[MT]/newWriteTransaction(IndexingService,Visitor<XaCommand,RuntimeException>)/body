{
  log=new VerifyingXaLogicalLog(fs.get(),verifier);
  WriteTransaction result=new WriteTransaction(0,log,transactionState,neoStore,cacheAccessBackDoor,indexing,NO_LABEL_SCAN_STORE);
  result.setCommitTxId(neoStore.getLastCommittedTx() + 1);
  return result;
}
