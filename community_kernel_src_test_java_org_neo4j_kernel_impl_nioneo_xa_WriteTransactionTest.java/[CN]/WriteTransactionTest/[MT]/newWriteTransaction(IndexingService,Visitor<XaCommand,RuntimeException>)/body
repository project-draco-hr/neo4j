{
  log=new VerifyingXaLogicalLog(fs.get(),verifier);
  WriteTransaction result=new WriteTransaction(0,log,transactionState,neoStore,cacheAccessBackDoor,indexing);
  result.setCommitTxId(neoStore.getLastCommittedTx() + 1);
  return result;
}
