{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  StoreId storeId=(StoreId)o;
  return (creationTime == storeId.creationTime) && (randomId == storeId.randomId) && (upgradeId == storeId.upgradeId)&& (upgradeTime == storeId.upgradeTime);
}
