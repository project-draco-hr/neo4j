{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  TestStoreId storeId=(TestStoreId)o;
  return creationTime == storeId.creationTime && randomNumber == storeId.randomNumber && upgradeTime == storeId.upgradeTime && upgradeId == storeId.upgradeId;
}
