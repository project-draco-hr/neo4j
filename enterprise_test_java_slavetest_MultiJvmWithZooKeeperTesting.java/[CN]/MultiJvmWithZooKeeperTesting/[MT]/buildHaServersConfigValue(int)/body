{
  StringBuilder builder=new StringBuilder();
  for (int i=0; i < numServers; i++) {
    builder.append((i > 0 ? "," : "") + (i + 1) + "=localhost:"+ (BASE_HA_SERVER_PORT + i));
  }
  return builder.toString();
}
