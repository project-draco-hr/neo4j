{
  pushInteger(exprs.length);
  createArray(type);
  for (int i=0; i < exprs.length; i++) {
    methodVisitor.visitInsn(Opcodes.DUP);
    pushInteger(i);
    exprs[i].accept(this);
    arrayStore(type);
  }
}
