{
  return getList(name,graphDb,new Converter<Node>(){
    @Override Node convert(    AbstractGraphDatabase graphDb,    Object value) throws BadInputException {
      return convertNode(graphDb,value);
    }
    @Override Node[] newArray(    int size){
      return new Node[size];
    }
  }
);
}
