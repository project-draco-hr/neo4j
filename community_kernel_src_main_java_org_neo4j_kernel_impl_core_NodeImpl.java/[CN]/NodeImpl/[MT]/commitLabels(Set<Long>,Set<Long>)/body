{
  if (labels != null) {
    HashSet<Long> newLabels=new HashSet<Long>(labels);
    if (added != null) {
      newLabels.addAll(added);
    }
    if (removed != null) {
      newLabels.removeAll(removed);
    }
    labels=newLabels;
  }
}
