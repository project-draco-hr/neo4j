{
  int size=16 + 8 + 8+ 8;
  if (relationships != null) {
    size+=16;
    for (    RelIdArray array : relationships) {
      size+=array.size();
      size+=8;
    }
  }
  return size + super.size();
}
