{
  int size=super.size() + 8 + 8+ 8;
  if (relationships != null) {
    size=withArrayOverhead(size,relationships.length);
    for (    RelIdArray array : relationships)     size+=array.size();
  }
  return size;
}
