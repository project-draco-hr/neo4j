{
  if (labels != null) {
    Set<Integer> newLabels=new HashSet<>(labels);
    if (added != null) {
      newLabels.addAll(added);
    }
    if (removed != null) {
      newLabels.removeAll(removed);
    }
    labels=newLabels;
  }
}
