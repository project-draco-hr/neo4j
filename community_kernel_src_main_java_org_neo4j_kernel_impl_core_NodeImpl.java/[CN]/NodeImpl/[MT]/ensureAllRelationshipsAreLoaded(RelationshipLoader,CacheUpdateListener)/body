{
  ensureRelationshipMapNotNull(relationshipLoader,cacheUpdateListener);
  while (hasMoreRelationshipsToLoad()) {
    getMoreRelationships(relationshipLoader,DirectionWrapper.BOTH,NO_RELATIONSHIP_TYPES,cacheUpdateListener);
  }
}
