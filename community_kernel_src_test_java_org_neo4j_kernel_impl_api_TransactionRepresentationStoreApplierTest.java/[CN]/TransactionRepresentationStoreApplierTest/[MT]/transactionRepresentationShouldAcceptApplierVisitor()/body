{
  TransactionRepresentationStoreApplier applier=new TransactionRepresentationStoreApplier(indexService,labelScanStore,neoStore,cacheAccess,lockService,legacyIndexProviderLookup,indexConfigStore);
  TransactionRepresentation transaction=mock(TransactionRepresentation.class);
  applier.apply(transaction,transactionId,false);
  verify(transaction,times(1)).accept(Matchers.<Visitor<Command,IOException>>any());
}
