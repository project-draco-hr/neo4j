{
  TransactionRepresentationStoreApplier applier=new TransactionRepresentationStoreApplier(indexService,labelScanStore,neoStore,cacheAccess,lockService,legacyIndexProviderLookup,indexConfigStore,DEFAULT_HIGH_ID_TRACKING);
  TransactionRepresentation transaction=mock(TransactionRepresentation.class);
  applier.apply(transaction,transactionId,false);
  verify(transaction,times(1)).accept(Matchers.<Visitor<Command,IOException>>any());
}
