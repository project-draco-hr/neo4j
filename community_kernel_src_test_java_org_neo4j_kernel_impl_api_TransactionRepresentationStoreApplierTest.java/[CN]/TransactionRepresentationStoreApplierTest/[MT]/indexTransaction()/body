{
  IndexDefineCommand definitions=new IndexDefineCommand();
  definitions.init(MapUtil.<String,Byte>genericMap("one",(byte)1),MapUtil.<String,Byte>genericMap("two",(byte)2));
  return Arrays.<Command>asList(definitions);
}
