{
  IndexDefineCommand definitions=new IndexDefineCommand();
  definitions.init(MapUtil.<String,Integer>genericMap("one",1),MapUtil.<String,Integer>genericMap("two",2));
  return Arrays.<Command>asList(definitions);
}
