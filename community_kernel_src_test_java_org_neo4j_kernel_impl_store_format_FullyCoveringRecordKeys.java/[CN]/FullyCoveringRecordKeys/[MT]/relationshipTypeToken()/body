{
  return new RecordKey<RelationshipTypeTokenRecord>(){
    @Override public void assertRecordsEquals(    RelationshipTypeTokenRecord written,    RelationshipTypeTokenRecord read){
      assertEquals(written.getNameId(),read.getNameId());
    }
  }
;
}
