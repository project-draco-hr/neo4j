{
  return new RecordKey<LabelTokenRecord>(){
    @Override public void assertRecordsEquals(    LabelTokenRecord written,    LabelTokenRecord read){
      assertEquals(written.getNameId(),read.getNameId());
    }
  }
;
}
