{
  this.addValueType("a",new OptionContext(OptionValueType.NONE,"Absolute id, new primitive doesn't need to be connected to " + "the\ncurrent one"));
  this.addValueType("r",new OptionContext(OptionValueType.NONE,"Makes the supplied id represent a relationship instead of " + "a node"));
}
