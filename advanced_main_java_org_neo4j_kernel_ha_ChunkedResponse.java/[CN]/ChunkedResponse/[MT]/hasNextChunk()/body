{
  if (chunk != null)   return true;
  if (response == null)   return false;
  chunk=state.write();
  if (chunk == null || !chunk.readable()) {
    chunk=null;
    close();
    return false;
  }
 else {
    return true;
  }
}
