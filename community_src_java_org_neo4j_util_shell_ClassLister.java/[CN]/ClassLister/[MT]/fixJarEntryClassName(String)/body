{
  entryName=entryName.replace(File.separatorChar,'.');
  String ending=".class";
  if (entryName.endsWith(ending)) {
    entryName=entryName.substring(0,entryName.length() - ending.length());
  }
  return entryName;
}
