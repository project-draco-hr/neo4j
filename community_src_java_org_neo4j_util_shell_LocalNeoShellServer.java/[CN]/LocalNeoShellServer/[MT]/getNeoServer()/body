{
  if (neoServer == null) {
    try {
      neoServer=instantiateNewNeoServer();
    }
 catch (    ShellException e) {
      throw new RuntimeException(e.getMessage(),e);
    }
  }
  return neoServer;
}
