{
  logWriter.printf("%s: In %s: %d pages acquired, %d pages mapped (%.2f%%) in %d ms%n",Format.date(),storeFileName.getName(),acquiredPages,mappedPages,(100.0 * mappedPages) / acquiredPages,samplePeriod);
  logWriter.flush();
}
