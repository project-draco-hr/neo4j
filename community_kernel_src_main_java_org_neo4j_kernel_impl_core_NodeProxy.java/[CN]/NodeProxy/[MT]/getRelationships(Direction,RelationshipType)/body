{
  Statement statement=statementContextProvider.instance();
  try {
    return map2rels(statement,statement.readOperations().nodeGetRelationships(nodeId,direction,relTypeIds(types,statement)));
  }
 catch (  EntityNotFoundException e) {
    statement.close();
    throw new NotFoundException(format("Node %d not found",nodeId),e);
  }
}
