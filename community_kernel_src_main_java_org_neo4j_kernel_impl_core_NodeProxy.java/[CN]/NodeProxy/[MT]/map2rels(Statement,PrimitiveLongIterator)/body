{
  return asResourceIterable(ResourceClosingIterator.newResourceIterator(statement,map(new FunctionFromPrimitiveLong<Relationship>(){
    @Override public Relationship apply(    long id){
      return actions.newRelationshipProxy(id);
    }
  }
,input)));
}
