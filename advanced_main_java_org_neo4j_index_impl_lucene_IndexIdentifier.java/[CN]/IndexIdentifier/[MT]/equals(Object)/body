{
  if (o == null || !getClass().equals(o.getClass())) {
    return false;
  }
  IndexIdentifier i=(IndexIdentifier)o;
  return entityType.getType().equals(i.entityType.getType()) && indexName.equals(i.indexName);
}
