{
  StoreChannel newLog=fs.get().open(newLogFile,"rw");
  LogBuffer newLogBuffer=new DirectLogBuffer(newLog,allocate(10000));
  ByteBuffer buf=allocate(100);
  writeLogHeader(buf,1,4);
  newLogBuffer.getFileChannel().write(buf);
  return newLogBuffer;
}
