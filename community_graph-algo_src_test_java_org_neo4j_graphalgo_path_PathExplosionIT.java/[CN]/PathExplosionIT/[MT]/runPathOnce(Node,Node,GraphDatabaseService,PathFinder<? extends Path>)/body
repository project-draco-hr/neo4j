{
  long startTime=currentTimeMillis();
  Path path=FIND_MULTIPLE_PATHS ? asList(pathFinder.findAllPaths(startNode,endNode)).get(0) : pathFinder.findSinglePath(startNode,endNode);
  count(path);
  return currentTimeMillis() - startTime;
}
