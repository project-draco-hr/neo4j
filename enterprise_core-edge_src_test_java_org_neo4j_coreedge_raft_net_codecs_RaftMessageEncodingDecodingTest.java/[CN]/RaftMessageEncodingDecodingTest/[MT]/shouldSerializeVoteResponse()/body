{
  CoreMember sender=new CoreMember(UUID.randomUUID());
  RaftMessages.Vote.Response request=new VoteResponseBuilder().from(sender).grant().term(3).build();
  serializeReadBackAndVerifyMessage(request);
}
