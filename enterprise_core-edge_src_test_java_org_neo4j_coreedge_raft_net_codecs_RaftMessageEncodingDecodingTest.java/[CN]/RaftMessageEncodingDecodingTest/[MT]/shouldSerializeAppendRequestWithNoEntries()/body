{
  CoreMember sender=new CoreMember(address("127.0.0.1:5001"),address("127.0.0.2:5001"));
  RaftMessages.AppendEntries.Request<CoreMember> request=new AppendEntriesRequestBuilder<CoreMember>().from(sender).leader(sender).leaderCommit(2).leaderTerm(4).build();
  serializeReadBackAndVerifyMessage(request);
}
