{
  if (propertyKeyTokenStore == null) {
    File fileName=getStoreFileName(StoreFactory.PROPERTY_KEY_TOKEN_STORE_NAME);
    propertyKeyTokenStore=initialise(new PropertyKeyTokenStore(fileName,config,idGeneratorFactory,pageCache,logProvider,getPropertyKeyTokenNamesStore()));
  }
  return propertyKeyTokenStore;
}
