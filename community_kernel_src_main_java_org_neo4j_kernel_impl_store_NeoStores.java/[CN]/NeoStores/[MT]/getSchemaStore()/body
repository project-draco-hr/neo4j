{
  if (schemaStore == null) {
    File fileName=getStoreFileName(StoreFactory.SCHEMA_STORE_NAME);
    schemaStore=initialise(new SchemaStore(fileName,config,IdType.SCHEMA,idGeneratorFactory,pageCache,logProvider));
  }
  return schemaStore;
}
