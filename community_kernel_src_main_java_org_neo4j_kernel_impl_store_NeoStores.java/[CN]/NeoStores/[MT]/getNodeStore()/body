{
  if (nodeStore == null) {
    File fileName=getStoreFileName(StoreFactory.NODE_STORE_NAME);
    nodeStore=initialise(new NodeStore(fileName,config,idGeneratorFactory,pageCache,logProvider,getNodeLabelStore(),versionMismatchHandler));
  }
  return nodeStore;
}
