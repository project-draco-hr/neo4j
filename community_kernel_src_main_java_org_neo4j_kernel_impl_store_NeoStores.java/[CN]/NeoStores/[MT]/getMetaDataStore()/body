{
  if (metaDataStore == null) {
    metaDataStore=initialise(new MetaDataStore(neoStoreFileName,config,idGeneratorFactory,pageCache,logProvider,versionMismatchHandler));
  }
  return metaDataStore;
}
