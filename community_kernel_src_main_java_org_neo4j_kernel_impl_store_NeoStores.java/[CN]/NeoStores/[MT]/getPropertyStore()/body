{
  if (propStore == null) {
    File fileName=getStoreFileName(StoreFactory.PROPERTY_STORE_NAME);
    propStore=initialise(new PropertyStore(fileName,config,idGeneratorFactory,pageCache,logProvider,getStringPropertyStore(),getPropertyKeyTokenStore(),getArrayPropertyStore()));
  }
  return propStore;
}
