{
  if (labelTokenStore == null) {
    File fileName=getStoreFileName(StoreFactory.LABEL_TOKEN_STORE_NAME);
    labelTokenStore=initialise(new LabelTokenStore(fileName,config,idGeneratorFactory,pageCache,logProvider,getLabelTokenNamesStore()));
  }
  return labelTokenStore;
}
