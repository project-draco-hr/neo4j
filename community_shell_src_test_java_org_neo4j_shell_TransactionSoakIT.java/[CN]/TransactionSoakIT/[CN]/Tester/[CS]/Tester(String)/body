{
  this.name=name;
  try {
    client=new SameJvmClient(new HashMap<String,Serializable>(),server,new SilentLocalOutput(),InterruptSignalHandler.getHandler());
  }
 catch (  ShellException e) {
    throw new RuntimeException("Error starting client",e);
  }
}
