{
  if (this.payload != null && !this.payload.trim().isEmpty() && MediaType.APPLICATION_JSON_TYPE.equals(payloadType)) {
    System.out.println("prettifying: ");
    System.out.println(this.payload);
    return JSONPrettifier.parse(this.payload);
  }
 else {
    return this.payload;
  }
}
