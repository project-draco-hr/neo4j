{
  if (this == obj) {
    return true;
  }
 else   if (obj instanceof Path) {
    Path other=(Path)obj;
    return start.equals(other.start) && Arrays.equals(path,other.path);
  }
 else {
    return false;
  }
}
