{
  System.out.println("WE got " + indexProviders.size());
  if (indexProviders != null)   return indexProviders.iterator();
 else   throw new IllegalArgumentException("indexProviders list must be set");
}
