{
  return new Data<InputNode>(){
    @Override public CharReadable stream(){
      return wrap(new StringReader(string));
    }
    @Override public Decorator<InputNode> decorator(){
      return InputEntityDecorators.NO_NODE_DECORATOR;
    }
  }
;
}
