{
  if (add == null && remove == null) {
    return src;
  }
  return new DirectionedRelIdArray(RelIdArray.from(src != null ? src.out : null,add != null ? add.out : null,remove),RelIdArray.from(src != null ? src.in : null,add != null ? add.in : null,remove));
}
