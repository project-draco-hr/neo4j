{
  clock=new FakeClock();
  server=ServerBuilder.server().usingDatabaseDir(staticFolder.getRoot().getAbsolutePath()).withClock(clock).build();
  server.start();
  functionalTestHelper=new FunctionalTestHelper(server);
}
