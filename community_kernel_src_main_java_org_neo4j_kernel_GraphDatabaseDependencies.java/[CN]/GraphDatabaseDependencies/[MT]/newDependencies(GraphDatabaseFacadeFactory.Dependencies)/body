{
  return new GraphDatabaseDependencies(deps.monitors(),deps.userLogProvider(),toList(deps.settingsClasses()),toList(deps.kernelExtensions()),deps.urlAccessRules(),toList(deps.executionEngines()));
}
