{
  return new GraphDatabaseDependencies(deps.monitors(),deps.userLogProvider(),asList(deps.settingsClasses()),asList(deps.kernelExtensions()),deps.urlAccessRules(),asList(deps.executionEngines()));
}
