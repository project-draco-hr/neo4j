{
  final HashMap<String,URLAccessRule> newUrlAccessRules=new HashMap<>(this.urlAccessRules);
  newUrlAccessRules.putAll(urlAccessRules);
  return new GraphDatabaseDependencies(monitors,userLogProvider,settingsClasses,kernelExtensions,newUrlAccessRules,queryEngineProviders);
}
