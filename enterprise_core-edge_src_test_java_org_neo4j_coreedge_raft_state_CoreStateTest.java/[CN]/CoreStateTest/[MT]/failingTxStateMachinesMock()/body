{
  CoreStateMachines stateMachines=mock(CoreStateMachines.class);
  doThrow(new IllegalStateException("This is a failing tx state machine and it's supposed to fail")).when(stateMachines).dispatch(any(ReplicatedTransaction.class),anyLong(),anyCallback());
  return stateMachines;
}
