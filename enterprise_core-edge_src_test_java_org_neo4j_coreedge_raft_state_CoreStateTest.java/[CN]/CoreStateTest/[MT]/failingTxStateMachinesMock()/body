{
  CoreStateMachines stateMachines=mock(CoreStateMachines.class);
  when(stateMachines.dispatch(any(ReplicatedTransaction.class),anyLong())).thenThrow(new IllegalStateException("This is a failing tx state machine and it's supposed to fail"));
  return stateMachines;
}
