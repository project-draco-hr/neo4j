{
  assertEquals(expectedTypes,asSet(asStrings(node.getRelationshipTypes())));
  if (commit) {
    newTransaction();
  }
  clearCache();
  assertEquals(expectedTypes,asSet(asStrings(node.getRelationshipTypes())));
}
