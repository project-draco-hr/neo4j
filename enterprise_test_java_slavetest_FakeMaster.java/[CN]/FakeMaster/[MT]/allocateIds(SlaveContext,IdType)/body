{
  IdGeneratorFactory factory=getConfig().getIdGeneratorFactory();
  IdGenerator generator=factory.get(idType);
  int size=10;
  long[] ids=new long[size];
  for (int i=0; i < size; i++) {
    ids[i]=generator.nextId();
  }
  return new Response<IdAllocation>(new IdAllocation(ids,generator.getHighId(),generator.getDefragCount()),new TransactionStreams());
}
