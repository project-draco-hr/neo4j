{
  return new BrokerFactory(){
    @Override public Broker create(    GraphDatabaseService graphDb,    Map<String,String> config){
      return new ZooKeeperBroker(graphDb,getClusterNameFromConfig(config),machineId,getCoordinatorsFromConfig(config),getHaServerFromConfig(config),getBackupPortFromConfig(config),getClientReadTimeoutFromConfig(config),getMaxConcurrentChannelsPerSlaveFromConfig(config),HighlyAvailableGraphDatabase.this);
    }
  }
;
}
