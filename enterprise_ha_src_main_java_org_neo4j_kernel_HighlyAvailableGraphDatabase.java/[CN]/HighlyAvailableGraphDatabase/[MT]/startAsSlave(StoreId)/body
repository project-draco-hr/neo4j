{
  messageLog.logMessage("Starting[" + machineId + "] as slave",true);
  this.storeId=storeId;
  SlaveGraphDatabase slaveGraphDatabase=new SlaveGraphDatabase(storeDir,configuration.getParams(),this,broker,messageLog,slaveOperations,slaveUpdateMode.createUpdater(broker),nodeLookup,relationshipLookups,fileSystemAbstraction,indexProviders,kernelExtensions,caches);
  logHaInfo("Started as slave");
  this.startupTime=System.currentTimeMillis();
  return slaveGraphDatabase;
}
