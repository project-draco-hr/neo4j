{
  return new ZooKeeperBroker(configuration,new ZooClientFactory(){
    @Override public ZooClient newZooClient(){
      return new ZooClient(storeDir,messageLog,storeIdGetter,configuration,slaveOperations,slaveOperations);
    }
  }
);
}
