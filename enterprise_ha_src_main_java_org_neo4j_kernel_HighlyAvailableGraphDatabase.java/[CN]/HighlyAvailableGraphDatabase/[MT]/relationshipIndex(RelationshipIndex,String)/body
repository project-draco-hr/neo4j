{
  return new WrappedIndex.WrappedRelationshipIndex(this){
    @Override protected ReadableIndex actual(){
      return HighlyAvailableGraphDatabase.this.index().forRelationships(indexName);
    }
  }
;
}
