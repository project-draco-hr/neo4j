{
  ZooClient.Configuration clientConfig=ConfigProxy.config(config,ZooClient.Configuration.class);
  return new ZooKeeperClusterClient(clientConfig.coordinators(),getMessageLog(),clientConfig.cluster_name(HaConfig.CONFIG_DEFAULT_HA_CLUSTER_NAME),clientConfig.zk_session_timeout(HaConfig.CONFIG_DEFAULT_ZK_SESSION_TIMEOUT));
}
