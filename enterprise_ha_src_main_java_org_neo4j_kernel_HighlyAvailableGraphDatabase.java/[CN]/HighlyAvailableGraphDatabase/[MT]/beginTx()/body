{
  try {
    return localGraph().beginTx();
  }
 catch (  TransactionFailureException e) {
    if (doingNewMaster) {
      throw e;
    }
    internalShutdown(true);
    newMaster(null,e);
    return localGraph().beginTx();
  }
}
