{
  return new WrappedIndex.WrappedNodeIndex(this){
    @Override protected ReadableIndex<Node> actual(){
      return HighlyAvailableGraphDatabase.this.graphdb.index().getNodeAutoIndexer().getAutoIndex();
    }
  }
;
}
