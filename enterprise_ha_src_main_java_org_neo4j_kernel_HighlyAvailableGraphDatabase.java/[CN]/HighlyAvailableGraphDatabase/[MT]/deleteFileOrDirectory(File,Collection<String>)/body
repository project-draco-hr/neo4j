{
  if (!file.exists()) {
    return;
  }
  if (file.isDirectory()) {
    for (    File child : file.listFiles()) {
      deleteFileOrDirectory(child,except);
    }
  }
  if (!except.contains(file.getName())) {
    file.delete();
  }
}
