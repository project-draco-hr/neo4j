{
  if (value instanceof Relationship) {
    Relationship relationship=(Relationship)value;
    return relationship.getType().name().equals(relationshipTypeName) && propertyMatcher.matches(relationship.getAllProperties());
  }
  return false;
}
