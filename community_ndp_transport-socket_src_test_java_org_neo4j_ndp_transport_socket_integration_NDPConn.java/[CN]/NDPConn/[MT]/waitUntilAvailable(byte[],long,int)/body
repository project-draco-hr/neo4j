{
  while (in.available() == 0) {
    if (System.currentTimeMillis() > timeout) {
      throw new IOException("Waited 30 seconds for " + left + " bytes, "+ "recieved "+ (recieved.length - left)+ ":\n"+ HexPrinter.hex(ByteBuffer.wrap(recieved),0,recieved.length - left));
    }
  }
}
