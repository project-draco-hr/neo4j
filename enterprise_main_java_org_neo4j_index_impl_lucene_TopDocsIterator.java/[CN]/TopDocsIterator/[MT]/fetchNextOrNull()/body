{
  if (!iterator.hasNext()) {
    return null;
  }
  ScoreDoc doc=iterator.next();
  try {
    return searcher.getSearcher().doc(doc.doc);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
