{
  return new NeoRegister.RelTypeRegister(){
    private int id=initialValue;
    @Override public int read(){
      return id;
    }
    @Override public void write(    int type){
      this.id=type;
    }
  }
;
}
