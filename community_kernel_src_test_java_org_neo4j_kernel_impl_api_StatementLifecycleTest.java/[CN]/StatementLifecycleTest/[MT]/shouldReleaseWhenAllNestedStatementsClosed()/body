{
  KernelTransactionImplementation transaction=mock(KernelTransactionImplementation.class);
  KernelStatement statement=new KernelStatement(transaction,mock(IndexReaderFactory.class),null,null,null,null,null,mock(SchemaRuleVerifier.class));
  statement.acquire();
  statement.acquire();
  statement.close();
  statement.close();
  verify(transaction).releaseStatement(statement);
}
