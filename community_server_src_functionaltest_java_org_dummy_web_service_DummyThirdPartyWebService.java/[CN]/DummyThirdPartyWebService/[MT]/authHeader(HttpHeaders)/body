{
  final List<String> authentication=headers.getRequestHeader("Authorization");
  if (authentication == null || authentication.size() != 1)   throw new IllegalArgumentException("missing header");
  return Response.ok().entity("{\"Authorization\":\"" + authentication.get(0) + "\"}").build();
}
