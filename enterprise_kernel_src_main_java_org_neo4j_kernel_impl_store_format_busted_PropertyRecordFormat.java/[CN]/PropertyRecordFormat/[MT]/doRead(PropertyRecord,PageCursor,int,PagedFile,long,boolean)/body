{
  int blockCount=(int)(headerByte >>> 4);
  record.initialize(inUse,Reference.decode(cursor,PAGE_CURSOR_ADAPTER),Reference.decode(cursor,PAGE_CURSOR_ADAPTER));
  while (blockCount-- > 0) {
    record.addLoadedBlock(cursor.getLong());
  }
}
