{
  super();
  this.neo=neo;
  this.relationshipType=relationshipType;
  this.numberOfNodes=numberOfNodes;
  this.numberOfRelationships=numberOfRelationships;
  this.random=new Random(System.currentTimeMillis());
  String ident="RandomGraph," + relationshipType + ","+ numberOfNodes+ ","+ numberOfRelationships;
  if (graphStore != null) {
    Graph graph=graphStore.loadGraph(ident);
    if (graph != null) {
      System.out.println("Loading graph");
      this.nodes=graph.loadNodeIds();
    }
  }
  if (this.nodes == null) {
    Generate();
    if (graphStore != null) {
      graphStore.createGraph(ident).saveNodeIds(nodes);
    }
  }
}
