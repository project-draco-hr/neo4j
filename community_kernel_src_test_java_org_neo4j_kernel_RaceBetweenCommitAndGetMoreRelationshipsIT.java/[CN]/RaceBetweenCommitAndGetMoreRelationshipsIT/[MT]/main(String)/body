{
  String path="target/commit-getMore-race";
  if (args != null && args.length >= 1) {
    path=args[0];
  }
  delete(new File(path));
  AbstractGraphDatabase graphdb=new EmbeddedGraphDatabase(path);
  RaceBetweenCommitAndGetMoreRelationshipsIT race=instance=new RaceBetweenCommitAndGetMoreRelationshipsIT(graphdb,graphdb.getNodeManager());
  try {
    race.execute();
  }
 catch (  Throwable e) {
    e.printStackTrace();
  }
 finally {
    graphdb.shutdown();
  }
}
