{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  SortedKeyValueStoreHeader that=(SortedKeyValueStoreHeader)o;
  return dataRecords == that.dataRecords && lastTxId == that.lastTxId && minorVersion == that.minorVersion && Arrays.equals(storeFormatVersion,that.storeFormatVersion);
}
