{
  if (versionLength < 0 && versionLength > versionSpace || versionLength < (versionSpace - RECORD_SIZE)) {
    throw new IOException(format("Invalid header data, versionLength=%d, versionSpace=%d.",versionLength,versionSpace));
  }
}
