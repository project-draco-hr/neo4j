{
  if (tail == null) {
    throw new IOException("Unexpected header data.");
  }
  for (int i=0; i < tail.length; i++) {
    if (tail[i] != 0) {
      throw new IOException("Unexpected header data.");
    }
  }
}
