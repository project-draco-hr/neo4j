{
  super(hostNameOrIp,port,logging,monitors,storeId,MasterServer.FRAME_LENGTH,PROTOCOL_VERSION,readTimeoutSeconds,maxConcurrentChannels,chunkSize);
  this.bufferMonitor=monitors.newMonitor(ByteCounterMonitor.class,getClass());
  this.lockReadTimeout=lockReadTimeout;
}
