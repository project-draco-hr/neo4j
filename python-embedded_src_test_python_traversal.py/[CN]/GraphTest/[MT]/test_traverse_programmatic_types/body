def test_traverse_programmatic_types(self):
    self.create_data()
    t = neo4j.Traversal.description().depthFirst().relationships(ANY.related_to).traverse(self.source)
    res = list(t.nodes())
    self.assertEqual(len(res), 2)
