def test_dynamic_evaluator(self):
    self.create_data()

    def myeval(path):
        return Evaluation.INCLUDE_AND_CONTINUE
    t = neo4j.Traversal.description().depthFirst().evaluator(myeval).traverse(self.source)
    res = list(t.nodes())
    self.assertEqual(len(res), 2)
