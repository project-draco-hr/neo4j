def test_ordering(self):
    self.create_data()
    db = self.graphdb
    start_node = self.source
    traverser = db.traversal().depthFirst().traverse(self.source)
    traverser = db.traversal().breadthFirst().traverse(start_node)
    res = list(traverser.nodes())
    self.assertEqual(len(res), 2)
