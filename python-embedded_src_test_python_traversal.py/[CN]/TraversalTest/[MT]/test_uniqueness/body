def test_uniqueness(self):
    self.create_data()
    t = self.graphdb.traversal().depthFirst().uniqueness(Uniqueness.NODE_PATH).traverse(self.source)
    res = list(t.nodes())
    self.assertEqual(len(res), 3)
