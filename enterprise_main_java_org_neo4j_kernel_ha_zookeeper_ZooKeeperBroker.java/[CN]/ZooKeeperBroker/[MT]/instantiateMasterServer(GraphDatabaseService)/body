{
  MasterServer server=new MasterServer(new MasterImpl(graphDb),Machine.splitIpAndPort(haServer).other());
  zooClient.setDataChangeWatcher(ZooClient.MASTER_REBOUND_CHILD,machineId);
  return server;
}
