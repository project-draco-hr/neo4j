{
  admin.send(TransportTestUtil.chunk(run("CALL dbms.terminateSessionsForUser( 'NonExistentUser' ) YIELD username, sessionCount"),pullAll()));
  assertThat(admin,eventuallyRecieves(msgFailure(Status.Security.InvalidArguments,"User 'NonExistentUser' does not exist.")));
}
