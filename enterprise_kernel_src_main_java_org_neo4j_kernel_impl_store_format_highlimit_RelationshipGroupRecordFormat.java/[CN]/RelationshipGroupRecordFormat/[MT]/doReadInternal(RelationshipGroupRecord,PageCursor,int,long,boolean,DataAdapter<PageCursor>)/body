{
  record.initialize(inUse,cursor.getShort() & 0xFFFF,decode(cursor,adapter,headerByte,HAS_OUTGOING_BIT,NULL),decode(cursor,adapter,headerByte,HAS_INCOMING_BIT,NULL),decode(cursor,adapter,headerByte,HAS_LOOP_BIT,NULL),decode(cursor,adapter),decode(cursor,adapter,headerByte,HAS_NEXT_BIT,NULL));
}
