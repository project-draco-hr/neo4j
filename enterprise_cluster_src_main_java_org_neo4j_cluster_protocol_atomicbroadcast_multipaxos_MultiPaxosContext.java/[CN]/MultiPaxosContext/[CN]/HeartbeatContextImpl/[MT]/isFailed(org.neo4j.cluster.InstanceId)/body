{
  List<org.neo4j.cluster.InstanceId> suspicions=getSuspicionsOf(node);
  int adjust=failed.contains(node) ? 0 : 1;
  return suspicions.size() > (configuration.getMembers().size() - failed.size() - adjust) / 2;
}
