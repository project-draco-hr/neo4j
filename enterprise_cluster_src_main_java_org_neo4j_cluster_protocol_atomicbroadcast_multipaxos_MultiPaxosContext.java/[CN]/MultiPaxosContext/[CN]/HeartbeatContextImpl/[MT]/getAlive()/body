{
  return filter(new Predicate<org.neo4j.cluster.InstanceId>(){
    @Override public boolean accept(    org.neo4j.cluster.InstanceId item){
      return !isFailed(item);
    }
  }
,configuration.getMemberIds());
}
