{
  long index=Math.max(0,commitIndex);
  for (; index <= appendIndex; index++) {
    ReplicatedContent content=readEntryContent(index);
    for (    Listener listener : listeners) {
      listener.onAppended(content,index);
    }
  }
}
