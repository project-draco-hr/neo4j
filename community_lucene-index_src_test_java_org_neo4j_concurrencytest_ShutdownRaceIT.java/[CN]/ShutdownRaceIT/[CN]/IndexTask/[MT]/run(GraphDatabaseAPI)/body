{
  try {
    Transaction tx=graphdb.beginTx();
    try {
      index(graphdb.index().forNodes("name"),graphdb.createNode());
      tx.success();
    }
  finally {
      tx.finish();
    }
  }
  finally {
    done();
  }
}
