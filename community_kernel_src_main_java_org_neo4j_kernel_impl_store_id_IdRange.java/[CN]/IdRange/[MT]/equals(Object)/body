{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  IdRange idRange=(IdRange)o;
  return rangeStart == idRange.rangeStart && rangeLength == idRange.rangeLength && Arrays.equals(defragIds,idRange.defragIds);
}
