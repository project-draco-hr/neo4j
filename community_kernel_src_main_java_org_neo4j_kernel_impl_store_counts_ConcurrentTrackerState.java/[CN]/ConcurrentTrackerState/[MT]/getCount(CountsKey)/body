{
  final AtomicLong count=state.get(key);
  if (count != null) {
    return count.get();
  }
  final Register.LongRegister value=Registers.newLongRegister();
  store.get(key,value);
  return value.read();
}
