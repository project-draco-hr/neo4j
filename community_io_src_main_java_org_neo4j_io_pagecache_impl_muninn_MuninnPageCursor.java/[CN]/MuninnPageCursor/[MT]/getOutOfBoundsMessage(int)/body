{
  if (size > 0) {
    return "Position " + position + " is greater than the upper "+ "page size bound of "+ size;
  }
 else {
    return "The PageCursor is not bound to a page. " + "Maybe next() returned false or was not called, or the cursor has been closed.";
  }
}
