{
  sortBuckets=new ParallelSort(radixIndexCount,dataCache,trackerCache,Runtime.getRuntime().availableProcessors() - 1).run();
  if (detectAndMarkCollisions() > 0) {
    buildCollisionInfo(ids.iterator());
  }
  readyForUse=true;
}
