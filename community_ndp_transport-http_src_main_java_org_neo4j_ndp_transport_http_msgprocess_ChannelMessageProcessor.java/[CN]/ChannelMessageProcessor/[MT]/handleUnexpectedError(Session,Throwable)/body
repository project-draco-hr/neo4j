{
  log.error("Unexpected error while processing messages for '" + session + "', "+ "session will be terminated: "+ e.getMessage()+ ".",e);
  writer.handleFailureMessage(new Neo4jError(Status.General.UnknownFailure,e.getMessage()));
  writer.flush();
}
