{
  GraphDatabaseService db=null;
  try {
    db=new GraphDatabaseFactory().newEmbeddedDatabase(storeDir);
  }
  finally {
    db.shutdown();
  }
  try {
    db=new GraphDatabaseFactory().newEmbeddedDatabase(storeDir);
  }
  finally {
    db.shutdown();
  }
}
