{
  GraphDatabaseService db=null;
  try {
    db=new EmbeddedGraphDatabase(storeDir);
  }
  finally {
    db.shutdown();
  }
  try {
    db=new EmbeddedGraphDatabase(storeDir);
  }
  finally {
    db.shutdown();
  }
}
