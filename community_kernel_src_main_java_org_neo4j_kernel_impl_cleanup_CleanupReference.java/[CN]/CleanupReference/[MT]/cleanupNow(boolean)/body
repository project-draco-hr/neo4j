{
synchronized (this) {
    if (handler != null) {
      if (!explicit) {
        cleanupService.logLeakedReference(this);
      }
      try {
        handler.close();
      }
  finally {
        handler=null;
      }
    }
  }
  cleanupService.unlink(this);
}
