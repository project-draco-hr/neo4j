{
  readLock.lock();
  readBuffer.clear();
  writeLock.lock();
  writeBuffer.clear();
  try {
    channel.close();
  }
 catch (  IOException e) {
    throw new SocketException("Error closing channel to [" + ip + ":"+ port+ "]",e);
  }
  readLock.unlock();
  writeLock.unlock();
}
