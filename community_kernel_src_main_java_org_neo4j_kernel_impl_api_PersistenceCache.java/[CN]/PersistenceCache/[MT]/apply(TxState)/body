{
  for (  NodeState stateEntity : state.getNodeStates()) {
    NodeImpl node=nodeCache.getIfCached(stateEntity.getId());
    if (node == null) {
      continue;
    }
    node.commitLabels(stateEntity.getLabelDiffSets().getAdded(),stateEntity.getLabelDiffSets().getRemoved());
  }
}
