{
  if (retries > 20) {
    close();
  }
  log("Send ok");
  connection.write();
  if (!buffer.hasRemaining()) {
    buffer.clear();
    releaseWriteBuffer();
    setStatus(Status.GET_LOG);
    return true;
  }
  retries++;
  return false;
}
