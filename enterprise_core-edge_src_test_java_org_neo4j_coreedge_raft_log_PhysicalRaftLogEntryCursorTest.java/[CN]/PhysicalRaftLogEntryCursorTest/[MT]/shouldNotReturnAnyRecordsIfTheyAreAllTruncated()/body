{
  ReplicatedInteger content=ReplicatedInteger.valueOf(1);
  RaftLogEntry payload=new RaftLogEntry(0,content);
  RaftRecordCursor recordCursor=mock(RaftRecordCursor.class);
  when(recordCursor.next()).thenReturn(true).thenReturn(true).thenReturn(true).thenReturn(true).thenReturn(false);
  when(recordCursor.get()).thenReturn(new RaftLogAppendRecord(1,payload)).thenReturn(new RaftLogAppendRecord(2,payload)).thenReturn(new RaftLogTruncateRecord(2)).thenReturn(new RaftLogTruncateRecord(1)).thenReturn(null);
  PhysicalRaftLogEntryCursor entryCursor=new PhysicalRaftLogEntryCursor(recordCursor);
  assertFalse(entryCursor.next());
}
