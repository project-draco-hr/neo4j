{
  if (!isEncrypted) {
    return new ErrorReportingSession(connectionDescriptor,new Neo4jError(Status.Security.EncryptionRequired,"This server requires a TLS encrypted connection."));
  }
  return delegate.newSession(connectionDescriptor,isEncrypted);
}
