{
  @SuppressWarnings("unchecked") HeaderField<Map<String,byte[]>,byte[]>[] fields=new HeaderField[keys.length];
  for (int i=0; i < keys.length; i++) {
    fields[i]=headerField(keys[i]);
  }
  return fields;
}
