{
  return getList(name,null,new Converter<Short>(){
    @Override Short convert(    AbstractGraphDatabase graphDb,    Object value) throws BadInputException {
      return convertShort(value);
    }
    @Override Short[] newArray(    int size){
      return new Short[size];
    }
  }
);
}
