{
  EnterpriseDatabase db=new EnterpriseDatabase(Configurator.EMPTY,new SingleLoggingService(StringLogger.DEV_NULL));
  try {
    db.start();
    assertThat(db.getGraph(),is(EmbeddedGraphDatabase.class));
  }
  finally {
    db.stop();
  }
}
