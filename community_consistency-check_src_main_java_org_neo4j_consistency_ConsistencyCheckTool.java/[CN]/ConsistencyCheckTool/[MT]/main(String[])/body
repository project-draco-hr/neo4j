{
  ConsistencyCheckTool tool=new ConsistencyCheckTool(new ConsistencyCheckService(),new DefaultFileSystemAbstraction(),System.err);
  try {
    tool.run(args);
  }
 catch (  ToolFailureException e) {
    e.exitTool();
  }
}
