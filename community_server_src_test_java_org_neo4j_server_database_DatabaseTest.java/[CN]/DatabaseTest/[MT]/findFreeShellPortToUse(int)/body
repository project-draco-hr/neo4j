{
  while (true) {
    try {
      ShellLobby.newClient(startingPort,AbstractServer.DEFAULT_NAME).shutdown();
      startingPort++;
    }
 catch (    ShellException e) {
      return startingPort;
    }
  }
}
