{
  return new Function<TransactionId,Boolean>(){
    @Override public Boolean apply(    TransactionId txInfo){
      return txInfo.transactionId() == id && txInfo.commitTimestamp() == timestamp;
    }
  }
;
}
