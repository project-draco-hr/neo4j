{
  BooleanQuery queries=new BooleanQuery();
  queries.add(idTermQuery(entityId),Occur.MUST);
  if (queryOrQueryObjectOrNull != null) {
    queries.add(query(null,queryOrQueryObjectOrNull),Occur.MUST);
  }
  return queries;
}
