{
  StringBuilder sb=new StringBuilder();
  sb.append(getDataUri()).append("autoIndexer/").append(uriPartForType).append("/status/").append(enabled);
  gen.get().expectedStatus(204).put(sb.toString());
}
