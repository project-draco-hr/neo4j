{
  LuceneSchemaIndex luceneIndex=new LuceneSchemaIndex(indexStorage,IndexConfiguration.UNIQUE,new IndexSamplingConfig(new Config()));
  luceneIndex.open();
  return new LuceneIndexAccessor(luceneIndex);
}
