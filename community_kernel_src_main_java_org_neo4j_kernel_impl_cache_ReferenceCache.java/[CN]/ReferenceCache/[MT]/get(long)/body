{
  ReferenceWithKey<Long,E> ref=cache.get(key);
  if (ref != null) {
    E value=ref.get();
    if (value == null) {
      cache.remove(key);
    }
    return counter.count(value);
  }
  return counter.count(null);
}
