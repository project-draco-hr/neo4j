{
  ReferenceWithKey<Long,E> ref=cache.get(key);
  if (ref != null) {
    E value=ref.get();
    if (value != null) {
      return hitCounter.count(value);
    }
    pollClearedValues();
  }
  return hitCounter.count(null);
}
