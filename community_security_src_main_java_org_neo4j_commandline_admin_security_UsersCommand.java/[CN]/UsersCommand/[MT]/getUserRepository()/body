{
  Config config=loadNeo4jConfig(homeDir,configDir);
  FileUserRepository userRepository=BasicAuthManagerFactory.getUserRepository(config,NullLogProvider.getInstance(),outsideWorld.fileSystem());
  userRepository.start();
  return userRepository;
}
