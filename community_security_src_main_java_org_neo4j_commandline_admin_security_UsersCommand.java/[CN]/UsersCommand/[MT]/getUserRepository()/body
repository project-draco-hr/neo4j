{
  Config config=loadNeo4jConfig(homeDir,configDir);
  FileUserRepository userRepository=BasicAuthManagerFactory.getUserRepository(config,NullLogProvider.getInstance());
  userRepository.start();
  return userRepository;
}
