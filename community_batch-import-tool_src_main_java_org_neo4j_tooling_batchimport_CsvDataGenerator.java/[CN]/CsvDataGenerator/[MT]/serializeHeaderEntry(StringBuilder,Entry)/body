{
  String value;
switch (entry.type()) {
case PROPERTY:
    value=entry.name() + ":" + entry.extractor().toString();
  break;
case IGNORE:
return;
default :
value=entry.name() + ":" + entry.type().name();
break;
}
builder.append(value);
}
