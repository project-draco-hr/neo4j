{
  long[] labelsForNode=nodeStore.getLabelsForNode(node);
  labelsReference.set(labelsForNode);
  for (  long nodeLabelId : labelsForNode)   if (labelPredicate.accept(nodeLabelId))   return true;
  return false;
}
