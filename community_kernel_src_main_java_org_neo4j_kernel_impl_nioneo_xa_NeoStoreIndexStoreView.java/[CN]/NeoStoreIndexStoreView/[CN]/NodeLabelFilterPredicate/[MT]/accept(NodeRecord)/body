{
  if (node.inUse()) {
    long[] labelsForNode=parseLabelsField(node).get(nodeStore);
    labelsReference.set(labelsForNode);
    for (    long nodeLabelId : labelsForNode) {
      if (labelPredicate.accept(safeCastLongToInt(nodeLabelId))) {
        return true;
      }
    }
  }
  return false;
}
