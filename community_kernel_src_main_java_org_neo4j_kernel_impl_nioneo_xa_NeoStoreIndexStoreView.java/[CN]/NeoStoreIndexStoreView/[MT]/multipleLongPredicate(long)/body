{
  return new PrimitiveLongPredicate(){
    @Override public boolean accept(    long value){
      for (int i=0; i < acceptedValues.length; i++)       if (value == acceptedValues[i])       return true;
      return false;
    }
  }
;
}
