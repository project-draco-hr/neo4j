{
  return new PrimitiveIntPredicate(){
    @Override public boolean accept(    int value){
      for (      int acceptedValue : acceptedValues) {
        if (value == acceptedValue) {
          return true;
        }
      }
      return false;
    }
  }
;
}
