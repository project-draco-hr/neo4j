{
  LabelsReference labelsReference=new LabelsReference();
  RecordStore.Processor<FAILURE> processor=new NodePropertyUpdateProcessor<>(propertyStore,singleLongPredicate(descriptor.getPropertyKeyId()),labelsReference,visitor);
  Predicate<NodeRecord> predicate=new NodeLabelFilterPredicate(nodeStore,singleLongPredicate(descriptor.getLabelId()),labelsReference);
  return new ProcessStoreScan<>(processor,predicate);
}
