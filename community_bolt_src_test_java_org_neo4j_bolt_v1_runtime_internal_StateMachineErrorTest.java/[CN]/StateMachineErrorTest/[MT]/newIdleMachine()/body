{
  SessionStateMachine machine=new SessionStateMachine("<idle>",new UsageData(scheduler),db,txBridge,runner,NullLogService.getInstance(),Authentication.NONE,sessionTracker);
  machine.init("FunClient",map(),null,Session.Callback.NO_OP);
  return machine;
}
