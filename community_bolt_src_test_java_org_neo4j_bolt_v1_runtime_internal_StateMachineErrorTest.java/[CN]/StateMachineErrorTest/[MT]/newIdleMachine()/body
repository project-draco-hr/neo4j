{
  SessionStateMachine machine=new SessionStateMachine(new UsageData(scheduler),db,txBridge,runner,NullLogService.getInstance(),Authentication.NONE);
  machine.init("FunClient",map(),null,Session.Callback.NO_OP);
  return machine;
}
