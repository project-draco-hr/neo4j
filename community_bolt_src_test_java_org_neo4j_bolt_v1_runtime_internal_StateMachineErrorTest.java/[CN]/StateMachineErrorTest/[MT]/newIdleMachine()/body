{
  SessionStateMachine machine=new SessionStateMachine(new UsageData(),db,txBridge,runner,NullLogService.getInstance(),Authentication.NONE);
  machine.init("FunClient",Collections.<String,Object>emptyMap(),null,Session.Callback.NO_OP);
  return machine;
}
