{
  SessionStateMachine machine=new SessionStateMachine("<idle>",new UsageData(scheduler),db,txBridge,runner,NullLogService.getInstance(),Authentication.NONE,sessionManager);
  machine.init("FunClient",map(),null,Session.Callback.NO_OP);
  return machine;
}
