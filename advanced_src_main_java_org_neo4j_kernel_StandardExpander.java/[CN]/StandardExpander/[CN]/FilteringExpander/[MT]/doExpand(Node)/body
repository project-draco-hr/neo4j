{
  return new FilteringIterator<Relationship>(expander.doExpand(start)){
    @Override protected boolean passes(    Relationship item){
      for (      Filter filter : filters) {
        if (filter.exclude(start,item))         return false;
      }
      return true;
    }
  }
;
}
