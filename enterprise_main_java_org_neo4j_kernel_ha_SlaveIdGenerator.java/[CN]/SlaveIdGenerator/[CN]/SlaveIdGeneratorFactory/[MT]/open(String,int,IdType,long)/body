{
  IdGenerator localIdGenerator=IdGeneratorFactory.DEFAULT.open(fileName,grabSize,idType,highestIdInUse);
  IdGenerator generator=new SlaveIdGenerator(idType,highestIdInUse,broker,receiver,localIdGenerator);
  generators.put(idType,generator);
  return generator;
}
