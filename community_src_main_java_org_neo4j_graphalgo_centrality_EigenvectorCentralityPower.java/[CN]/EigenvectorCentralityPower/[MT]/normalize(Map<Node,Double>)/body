{
  double sum=0;
  for (  Node node : vector.keySet()) {
    double d=vector.get(node);
    sum+=d * d;
  }
  sum=Math.sqrt(sum);
  if (sum > 0.0) {
    for (    Node node : vector.keySet()) {
      vector.put(node,vector.get(node) / sum);
    }
  }
}
