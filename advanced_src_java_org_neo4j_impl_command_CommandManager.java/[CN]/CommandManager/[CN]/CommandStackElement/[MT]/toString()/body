{
  int commandCount=0;
  if (commands != null) {
    commandCount=commands.size();
  }
  int lockCount=0;
  if (locks != null) {
    lockCount=locks.size();
  }
  return "CommandCount[" + commandCount + "], LockCount["+ lockCount+ "]";
}
