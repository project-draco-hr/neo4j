{
  ConcurrentMap<Long,DynamicOwner> dynamicOwners=dynamicOwners(RELATIONSHIP_LABEL_NAME);
  if (dynamicOwners == null) {
    return checker;
  }
  return new NameCheckerDecorator<RelationshipTypeRecord,ConsistencyReport.LabelConsistencyReport>(checker,dynamicOwners){
    @Override DynamicOwner.NameOwner owner(    RelationshipTypeRecord record){
      return new DynamicOwner.RelationshipLabel(record);
    }
  }
;
}
