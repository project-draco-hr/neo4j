{
  return new Function<String,Character>(){
    @Override public Character apply(    String value){
      if (value.length() > 1) {
        throw new IllegalArgumentException("Invalid delimiter '" + value + "', expected one character");
      }
      return value.charAt(0);
    }
  }
;
}
