{
  try {
    CommandManager.getManager().dumpStack();
    LockManager.getManager().dumpRagStack();
    LockManager.getManager().dumpAllLocks();
  }
 catch (  Throwable t) {
    System.err.println("Unable to dump Neo command stack and " + "lock information: " + t);
  }
  neo.shutdown();
}
