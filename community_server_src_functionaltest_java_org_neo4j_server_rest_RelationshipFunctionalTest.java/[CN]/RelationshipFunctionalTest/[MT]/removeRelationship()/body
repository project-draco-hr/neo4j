{
  Relationship loves=data.get().get("Romeo").getRelationships().iterator().next();
  String response=gen.get().description("_Starting Graph:_\n\n" + gen.get().createGraphViz(graphdb(),"Delete relationship1")).expectedStatus(Status.NO_CONTENT.getStatusCode()).delete(getRelationshipUri(loves)).entity();
}
