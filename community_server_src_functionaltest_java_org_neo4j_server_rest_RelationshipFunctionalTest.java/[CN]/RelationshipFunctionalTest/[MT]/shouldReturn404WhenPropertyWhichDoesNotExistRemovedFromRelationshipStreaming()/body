{
  data.get();
  Relationship loves=IteratorUtil.first(getNode("Romeo").getRelationships());
  gen().withHeader(StreamingJsonFormat.STREAM_HEADER,"true").expectedStatus(Status.NOT_FOUND.getStatusCode()).delete(getPropertiesUri(loves) + "/non-existent").entity();
}
