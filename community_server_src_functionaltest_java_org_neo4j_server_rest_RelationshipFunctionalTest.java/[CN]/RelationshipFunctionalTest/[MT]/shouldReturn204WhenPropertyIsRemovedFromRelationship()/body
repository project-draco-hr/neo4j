{
  Relationship loves=getNode("Romeo").getRelationships().iterator().next();
  gen.get().description(startGraph("Remove property from a relationship1")).expectedStatus(Status.NO_CONTENT.getStatusCode()).delete(getPropertiesUri(loves) + "/cost").entity();
}
