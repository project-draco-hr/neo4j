{
  Relationship loves=getNode("Romeo").getRelationships().iterator().next();
  gen.get().expectedStatus(Status.NOT_FOUND.getStatusCode()).delete(getPropertiesUri(loves) + "/non-existent").entity();
}
