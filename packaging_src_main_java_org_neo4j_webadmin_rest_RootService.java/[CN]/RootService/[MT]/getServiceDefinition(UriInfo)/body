{
  Mode serverMode=Mode.EMBEDDED;
  String entity=JsonRenderers.DEFAULT.render(new ServerRootRepresentation(uriInfo.getBaseUri(),serverMode));
  return addHeaders(Response.ok(entity,JsonRenderers.DEFAULT.getMediaType())).build();
}
