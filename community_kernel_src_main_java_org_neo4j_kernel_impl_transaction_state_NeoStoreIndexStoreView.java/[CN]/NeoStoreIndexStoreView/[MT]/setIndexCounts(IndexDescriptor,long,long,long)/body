{
  counts.replaceIndexSize(descriptor.getLabelId(),descriptor.getPropertyKeyId(),indexSize);
  counts.replaceIndexSample(descriptor.getLabelId(),descriptor.getPropertyKeyId(),uniqueElements,maxUniqueElements);
}
