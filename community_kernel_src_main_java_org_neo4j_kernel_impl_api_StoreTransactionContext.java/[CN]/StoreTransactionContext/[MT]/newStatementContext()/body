{
  return new StoreStatementContext(propertyKeyTokenHolder,labelTokenHolder,nodeManager,new SchemaStorage(neoStore.getSchemaStore()),neoStore,indexingService,new IndexReaderFactory.Caching(indexingService));
}
