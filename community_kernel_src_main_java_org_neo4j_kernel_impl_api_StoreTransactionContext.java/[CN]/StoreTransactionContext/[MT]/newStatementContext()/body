{
  return new StoreStatementContext(propertyKeyTokenHolder,labelTokenHolder,nodeManager,new SchemaStorage(neoStore.getSchemaStore()),neoStore,persistenceManager,indexingService,new IndexReaderFactory.Caching(indexingService));
}
