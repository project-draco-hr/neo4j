{
  NeoStore neoStore=((GraphDatabaseAPI)db).getDependencyResolver().resolveDependency(NeoStoreSupplier.class).get();
  NodeRecord record=neoStore.getNodeStore().getRecord(node.getId());
  assertEquals(dense,record.isDense());
}
