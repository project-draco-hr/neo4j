{
  NeoStores neoStores=((GraphDatabaseAPI)db).getDependencyResolver().resolveDependency(NeoStoresSupplier.class).get();
  NodeRecord record=neoStores.getNodeStore().getRecord(node.getId());
  assertEquals(dense,record.isDense());
}
