{
  NeoStore neoStore=((GraphDatabaseAPI)db).getDependencyResolver().resolveDependency(NeoStoreProvider.class).evaluate();
  NodeRecord record=neoStore.getNodeStore().getRecord(node.getId());
  assertEquals(dense,record.isDense());
}
