{
  Integer count=inner.get(value);
  if (count == null) {
    return 0;
  }
  size--;
  int newCount=count - 1;
  if (newCount <= 0) {
    inner.remove(value);
  }
 else {
    inner.put(value,newCount);
  }
  return newCount;
}
