{
  final RecoveryVisitor visitor=new RecoveryVisitor(store,storeApplier,recoveredCount,mock(RecoveryVisitor.Monitor.class));
  visitor.close();
  verify(store,never()).setLastCommittedAndClosedTransactionId(anyLong());
}
