{
  Serializable result=config.get(key);
  if (result == null) {
    try {
      result=(Serializable)AbstractServer.class.getDeclaredField(defaultVariableName).get(null);
    }
 catch (    Exception e) {
      throw new RemoteException("Default variable not found",e);
    }
  }
  return result;
}
