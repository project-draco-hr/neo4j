{
  DefaultFileSystemAbstraction fileSystem=new DefaultFileSystemAbstraction();
  compareLogStreams(LogExtractor.from(fileSystem,new File(args[0])),LogExtractor.from(fileSystem,new File(args[1])));
}
