{
  try {
    RawIterator<Object[],ProcedureException> stream=dbmsOperationsInNewTransaction().procedureCallDbms(procedureName("sys","procedures"),new Object[0]);
    assertThat("This should never get here",1 == 2);
  }
 catch (  Exception e) {
    assertThat(e.getClass(),equalTo(AuthorizationViolationException.class));
  }
}
