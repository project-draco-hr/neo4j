{
  try {
    RawIterator<Object[],ProcedureException> stream=dbmsOperations().procedureCallDbms(procedureName("sys","procedures"),new Object[0],AccessMode.Static.NONE);
    assertThat("This should never get here",1 == 2);
  }
 catch (  Exception e) {
    assertThat(e.getClass(),equalTo(ProcedureException.class));
  }
}
