{
  try {
    Object[] inputArray=new Object[1];
    inputArray[0]="newPassword";
    RawIterator<Object[],ProcedureException> stream=dbmsOperations().procedureCallDbms(procedureName("dbms","changePassword"),inputArray,AccessMode.Static.NONE);
    fail("Should have failed.");
  }
 catch (  Exception e) {
    assertThat(e.getClass(),equalTo(AuthorizationViolationException.class));
  }
}
