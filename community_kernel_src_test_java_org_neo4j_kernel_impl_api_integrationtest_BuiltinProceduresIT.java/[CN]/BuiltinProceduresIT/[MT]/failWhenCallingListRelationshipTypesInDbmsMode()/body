{
  try {
    dbmsOperations().procedureCallDbms(procedureName("db","relationshipTypes"),new Object[0],Static.NONE);
    fail("Should have failed.");
  }
 catch (  Exception e) {
    assertThat(e.getClass(),equalTo(ProcedureException.class));
  }
}
