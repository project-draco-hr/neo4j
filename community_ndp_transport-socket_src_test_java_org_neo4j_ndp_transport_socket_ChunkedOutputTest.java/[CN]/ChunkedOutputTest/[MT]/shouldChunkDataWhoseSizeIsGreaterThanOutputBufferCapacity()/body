{
  out.writeBytes(new byte[16],0,16);
  out.messageBoundaryHook().run();
  out.flush();
  assertThat(writtenData.limit(),equalTo(22));
  assertThat(HexPrinter.hex(writtenData,0,22),equalTo("00 0E 00 00 00 00 00 00    00 00 00 00 00 00 00 00    00 02 00 00 00 00"));
}
