{
  super.phase(phase);
  if (current != null) {
    current.stop();
  }
  Timer timer=timers.get(phase);
  if (timer == null) {
    timers.put(phase,timer=new Timer(phase));
  }
  timer.start();
  current=timer;
}
