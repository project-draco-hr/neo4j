{
  ClientResponse response=Client.create().resource(indexUri() + "/node/non-existent-key/non-existent-value").accept(MediaType.APPLICATION_JSON).get(ClientResponse.class);
  assertEquals(200,response.getStatus());
}
