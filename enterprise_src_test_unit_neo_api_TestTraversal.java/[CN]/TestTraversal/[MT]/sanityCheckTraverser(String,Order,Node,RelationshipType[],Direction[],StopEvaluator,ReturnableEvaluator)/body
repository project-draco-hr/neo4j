{
  try {
    TraverserFactory.getFactory().createTraverser(type,startNode,traversableRels,traversableDirs,stopEval,retEval);
    fail(failMessage);
  }
 catch (  IllegalArgumentException iae) {
  }
}
