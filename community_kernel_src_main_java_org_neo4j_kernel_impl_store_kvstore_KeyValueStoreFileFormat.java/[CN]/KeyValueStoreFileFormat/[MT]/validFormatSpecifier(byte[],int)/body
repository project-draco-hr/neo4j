{
  for (int i=0, key=0; i < buffer.length; i++) {
    if (buffer[i] == 0) {
      if (i == 0 || ++key == keySize || i == buffer.length - 1) {
        return false;
      }
    }
 else {
      key=0;
    }
  }
  return true;
}
