{
  if (version == null) {
    return "<unknown>";
  }
 else   if (version.contains("SNAPSHOT")) {
    return version + " (revision: " + defaultValue(getRevision(),"<unknown>")+ ")";
  }
 else {
    return version;
  }
}
