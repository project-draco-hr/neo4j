{
  if (outcome.needsFreshSnapshot()) {
    CoreServerSelectionStrategy strategy=outcome.isProcessable() ? defaultStrategy : new LeaderOnlySelectionStrategy(outcome);
    raftStateMachine.notifyNeedFreshSnapshot(strategy);
  }
}
