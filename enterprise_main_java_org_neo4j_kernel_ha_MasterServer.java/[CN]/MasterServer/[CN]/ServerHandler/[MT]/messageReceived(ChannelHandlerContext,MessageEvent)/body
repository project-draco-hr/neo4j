{
  try {
    ChannelBuffer message=(ChannelBuffer)event.getMessage();
    ChannelBuffer result=handleRequest(realMaster,message,event.getChannel(),MasterServer.this);
    if (result != null) {
      if (result.writerIndex() > 0) {
        event.getChannel().write(result);
      }
    }
 else {
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
    throw e;
  }
}
