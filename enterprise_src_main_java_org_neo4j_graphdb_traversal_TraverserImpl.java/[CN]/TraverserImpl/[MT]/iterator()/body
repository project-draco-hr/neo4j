{
switch (description.order) {
case DEPTH_FIRST:
    return new DepthFirstTraversal(description,startNode);
case BREADTH_FIRST:
  return new BreadthFirstTraversal(description,startNode);
default :
throw new IllegalStateException("Unknown order " + description.order);
}
}
