{
  if (propertyKey == null)   throw new IllegalStateException("Need a property key to define an index");
  createIndexRule(label,propertyKey);
  return new BatchIndexDefinition(label,propertyKey);
}
