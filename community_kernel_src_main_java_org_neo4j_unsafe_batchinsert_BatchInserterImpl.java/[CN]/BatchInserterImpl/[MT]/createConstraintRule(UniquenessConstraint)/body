{
  SchemaStore schemaStore=getSchemaStore();
  UniquenessConstraintRule rule=new UniquenessConstraintRule(schemaStore.nextId(),constraint.label(),constraint.property());
  for (  DynamicRecord record : schemaStore.allocateFrom(rule))   schemaStore.updateRecord(record);
  schemaCache.addSchemaRule(rule);
}
