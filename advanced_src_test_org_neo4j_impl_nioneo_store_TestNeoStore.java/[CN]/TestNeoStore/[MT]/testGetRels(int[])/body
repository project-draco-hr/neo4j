{
  for (int i=0; i < relIds.length; i++) {
    try {
      rStore.getRelationship(relIds[i]);
      fail("Got deleted relationship[" + relIds[i] + "]");
    }
 catch (    IOException e) {
    }
  }
}
