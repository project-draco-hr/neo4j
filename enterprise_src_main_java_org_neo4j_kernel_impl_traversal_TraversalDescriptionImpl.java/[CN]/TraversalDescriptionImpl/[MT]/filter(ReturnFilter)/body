{
  if (this.filter == filter) {
    return this;
  }
  nullCheck(filter,ReturnFilter.class,"ALL");
  return new TraversalDescriptionImpl(expander,uniqueness,uniquenessParameter,pruning,filter,sourceSelector);
}
