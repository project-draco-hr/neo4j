{
  StorePropertyPayloadCursor cursor=new StorePropertyPayloadCursor(dynamicStringStore.newRecordCursor(null),dynamicArrayStore.newRecordCursor(null));
  long[] blocks=asBlocks(values);
  cursor.init(blocks,blocks.length);
  return cursor;
}
