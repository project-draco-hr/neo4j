{
  if (channel.isOpen()) {
    return;
  }
  if (closed) {
    throw closedException;
  }
  try {
    channel=fs.open(file,"rw");
  }
 catch (  IOException e) {
    closedException.addSuppressed(e);
    throw closedException;
  }
}
