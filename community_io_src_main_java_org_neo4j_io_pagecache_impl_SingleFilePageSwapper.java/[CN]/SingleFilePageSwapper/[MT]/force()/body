{
  try {
    channel.force(false);
  }
 catch (  ClosedChannelException e) {
    tryReopen(e);
    boolean interrupted=Thread.interrupted();
    force();
    if (interrupted) {
      Thread.currentThread().interrupt();
    }
  }
}
