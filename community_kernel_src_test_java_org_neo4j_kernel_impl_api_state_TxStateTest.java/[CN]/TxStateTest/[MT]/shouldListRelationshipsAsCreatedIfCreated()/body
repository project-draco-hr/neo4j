{
  long relId=state.relationshipDoCreate(0,1,2);
  when(legacyState.relationshipIsAddedInThisTx(relId)).thenReturn(true);
  assertTrue(state.hasChanges());
  assertTrue(state.relationshipIsAddedInThisTx(relId));
}
