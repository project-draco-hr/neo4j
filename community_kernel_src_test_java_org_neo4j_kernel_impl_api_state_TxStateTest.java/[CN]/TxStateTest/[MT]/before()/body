{
  legacyState=mock(OldTxStateBridge.class);
  state=new TxState(legacyState,mock(PersistenceManager.class),mock(TxState.IdGeneration.class),new DefaultSchemaIndexProviderMap(SchemaIndexProvider.NO_INDEX_PROVIDER));
}
