{
  legacyState=mock(OldTxStateBridge.class);
  persistenceManager=mock(PersistenceManager.class);
  state=new TxState(legacyState,persistenceManager,mock(TxState.IdGeneration.class),new DefaultSchemaIndexProviderMap(SchemaIndexProvider.NO_INDEX_PROVIDER));
}
