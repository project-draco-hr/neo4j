{
  if (indexes.contains(intendedIndex) || indexes.contains(actualIndex) || values.contains(key)) {
    System.out.println(format("[%d] --> |%d| <-- %d  %s",intendedIndex,actualIndex,key,hopBitsAsString(newHopBits)));
  }
  return true;
}
