{
  if (indexes.contains(intendedIndex) || indexes.contains(fromIndex) || indexes.contains(toIndex)|| values.contains(key)) {
    System.out.println(format("[%d] --- [%d] <<-- %d <-- [%d]  %s",intendedIndex,toIndex,key,fromIndex,hopBitsAsString(newHopBits)));
  }
  return true;
}
