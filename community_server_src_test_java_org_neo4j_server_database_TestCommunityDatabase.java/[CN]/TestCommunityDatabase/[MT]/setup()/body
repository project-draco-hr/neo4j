{
  databaseDirectory=createTempDir();
  theDatabase=new CommunityDatabase(configuratorWithServerProperties(stringMap(Configurator.DATABASE_LOCATION_PROPERTY_KEY,databaseDirectory.getAbsolutePath())));
}
