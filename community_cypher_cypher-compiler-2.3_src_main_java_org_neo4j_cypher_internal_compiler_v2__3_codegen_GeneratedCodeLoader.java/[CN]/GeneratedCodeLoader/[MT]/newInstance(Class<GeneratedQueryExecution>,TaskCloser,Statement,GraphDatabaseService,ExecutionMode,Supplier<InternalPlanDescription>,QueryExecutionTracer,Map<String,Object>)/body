{
  Constructor<GeneratedQueryExecution> constructor=clazz.getDeclaredConstructor(TaskCloser.class,Statement.class,GraphDatabaseService.class,ExecutionMode.class,Supplier.class,QueryExecutionTracer.class,Map.class);
  GeneratedQueryExecution compiledCode=constructor.newInstance(closer,statement,db,executionMode,description,tracer,params);
  return new CompiledExecutionResult(closer,statement,compiledCode,description);
}
