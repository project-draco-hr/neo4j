{
  try {
    return new Bootstrapper(test,0,config){
      @Override protected void shutdown(      AbstractGraphDatabase graphdb,      boolean normal){
        if (normal)         super.shutdown(graphdb,normal);
      }
    }
;
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
