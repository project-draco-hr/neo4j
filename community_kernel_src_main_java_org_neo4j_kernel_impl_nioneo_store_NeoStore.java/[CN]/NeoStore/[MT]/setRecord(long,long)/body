{
  try (PageCursor cursor=storeFile.io(pageIdForRecord(id),PagedFile.PF_EXCLUSIVE_LOCK,neoStoreWriteRecord,offsetForId(id),value)){
    cursor.next();
  }
 catch (  IOException e) {
    throw new UnderlyingStorageException(e);
  }
}
