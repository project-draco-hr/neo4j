{
  try (PageCursor cursor=storeFile.io(pageIdForRecord(VERSION_POSITION),PF_EXCLUSIVE_LOCK,this,offsetForId(VERSION_POSITION),IO_INCREMENT_VERSION_FIELD)){
    cursor.next();
    return versionField - 1;
  }
 catch (  IOException e) {
    throw new UnderlyingStorageException(e);
  }
}
