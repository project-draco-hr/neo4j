{
  long txId=lastCommittingTx.get();
  if (txId == -1) {
synchronized (this) {
      txId=getRecord(LATEST_TX_POSITION);
      lastCommittingTx.compareAndSet(-1,txId);
    }
  }
  return lastCommittingTx.incrementAndGet();
}
