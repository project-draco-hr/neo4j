{
  if (relTypeStore == null || propStore == null || relStore == null || nodeStore == null) {
    return;
  }
  super.flushAll();
  relTypeStore.flushAll();
  propStore.flushAll();
  relStore.flushAll();
  nodeStore.flushAll();
}
