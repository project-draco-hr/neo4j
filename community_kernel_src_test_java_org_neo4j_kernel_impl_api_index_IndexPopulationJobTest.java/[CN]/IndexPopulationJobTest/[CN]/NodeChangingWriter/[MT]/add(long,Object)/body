{
  if (nodeId == 2) {
    long[] labels=new long[]{label};
    job.update(asList(NodePropertyUpdate.change(nodeToChange,propertyKeyId,previousValue,labels,newValue,labels)));
  }
  added.add(Pair.of(nodeId,propertyValue));
}
