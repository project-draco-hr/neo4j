{
  Long result;
  Transaction tx=db.beginTx();
  try {
    result=ctxProvider.statement().readOperations().propertyKeyGetForName(name);
    tx.success();
  }
  finally {
    tx.finish();
  }
  return result;
}
