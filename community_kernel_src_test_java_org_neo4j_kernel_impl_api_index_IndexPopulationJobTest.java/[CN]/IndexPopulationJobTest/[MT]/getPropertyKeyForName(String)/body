{
  Long result;
  Transaction tx=db.beginTx();
  try {
    result=ctxProvider.getCtxForWriting().propertyKeyGetForName(name);
    tx.success();
  }
  finally {
    tx.finish();
  }
  return result;
}
