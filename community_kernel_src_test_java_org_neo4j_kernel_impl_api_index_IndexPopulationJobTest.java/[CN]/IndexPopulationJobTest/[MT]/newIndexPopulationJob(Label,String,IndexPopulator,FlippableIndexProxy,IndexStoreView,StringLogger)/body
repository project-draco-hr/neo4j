{
  IndexRule indexRule=new IndexRule(0,context.getLabelId(label.name()),PROVIDER_DESCRIPTOR,context.getPropertyKeyId(propertyKey));
  IndexDescriptor descriptor=new IndexDescriptor(indexRule.getLabel(),indexRule.getPropertyKey());
  flipper.setFlipTarget(mock(IndexProxyFactory.class));
  return new IndexPopulationJob(descriptor,PROVIDER_DESCRIPTOR,populator,flipper,storeView,stateHolder,new SingleLoggingService(logger));
}
