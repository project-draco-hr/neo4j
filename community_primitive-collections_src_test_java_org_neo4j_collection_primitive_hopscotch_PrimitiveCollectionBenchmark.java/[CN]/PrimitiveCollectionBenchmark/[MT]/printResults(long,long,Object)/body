{
  for (int i=0; i < 5; i++) {
    System.gc();
    sleep(1000);
  }
  System.out.println("  add: " + addTime + ", contains: "+ containsTime+ ", mem: "+ Runtime.getRuntime().totalMemory()+ "   "+ set);
}
