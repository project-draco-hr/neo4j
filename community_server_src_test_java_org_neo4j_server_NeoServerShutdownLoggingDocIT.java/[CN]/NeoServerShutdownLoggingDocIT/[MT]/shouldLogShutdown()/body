{
  InMemoryAppender appender=new InMemoryAppender(AbstractNeoServer.log);
  server.stop();
  String actual=appender.toString();
  assertTrue(actual.contains("INFO: Successfully shutdown database.") || actual.contains("Information: Successfully shutdown database."));
}
