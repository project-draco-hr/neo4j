{
  @SuppressWarnings({"UnnecessaryLocalVariable","unchecked"}) Iterator<R> result=(Iterator)filter(new Predicate<SchemaRule>(){
    @Override public boolean accept(    SchemaRule rule){
      return ruleClass.isInstance(rule);
    }
  }
,loadAllSchemaRules());
  return result;
}
