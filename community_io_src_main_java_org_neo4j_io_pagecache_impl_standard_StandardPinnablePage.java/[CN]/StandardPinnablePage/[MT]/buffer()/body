{
  assertLocked();
  if (buffer == null) {
    try {
      buffer=ByteBuffer.allocate(pageSize);
    }
 catch (    OutOfMemoryError e) {
      buffer=ByteBuffer.allocate(pageSize);
    }
  }
  return buffer;
}
