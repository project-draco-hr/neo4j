{
  if (assertionsHold(assertIO,assertPageId)) {
    lock(lockType);
    if (assertionsHold(assertIO,assertPageId)) {
      return true;
    }
 else {
      unpin(lockType);
    }
  }
  return false;
}
