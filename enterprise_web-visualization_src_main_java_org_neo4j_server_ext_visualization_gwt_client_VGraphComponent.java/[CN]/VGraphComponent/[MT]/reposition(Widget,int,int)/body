{
  Style style=node.getElement().getStyle();
  style.setLeft(limit(0,newX,getOffsetWidth() - node.getOffsetWidth()),Unit.PX);
  style.setTop(limit(0,newY,getOffsetHeight() - node.getOffsetHeight()),Unit.PX);
  updateLinesFor(node);
}
