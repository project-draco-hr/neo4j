{
  return new FilteringIterable<Relationship>(start.getRelationships(),new Predicate<Relationship>(){
    @Override public boolean accept(    Relationship item){
      return item.getOtherNode(start).equals(end);
    }
  }
);
}
