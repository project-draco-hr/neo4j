{
  return new StoreFactory(config,idGeneratorFactory,fileSystem,lastCommittedTxIdSetter,msgLog,txHook){
    @Override public NeoStore createNeoStore(    String fileName){
      return super.createNeoStore(fileName,storeId);
    }
  }
;
}
