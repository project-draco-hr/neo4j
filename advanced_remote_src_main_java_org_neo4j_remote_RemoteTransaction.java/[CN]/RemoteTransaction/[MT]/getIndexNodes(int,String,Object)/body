{
  BatchIterable<NodeSpecification> iter=engine.getIndexNodes(id,indexId,key,value);
  return new IndexHitsImpl(iter){
    @Override Node convert(    NodeSpecification source){
      return getNode(source.id,true);
    }
  }
;
}
