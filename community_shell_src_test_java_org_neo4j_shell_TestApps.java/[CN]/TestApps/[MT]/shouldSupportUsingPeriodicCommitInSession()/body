{
  long fileSize=120;
  long batch=40;
  String csvFileUrl=createCsvFile(fileSize);
  long expectedCommitCount=fileSize / batch;
  verifyNumberOfCommits("USING PERIODIC COMMIT " + batch + " LOAD CSV FROM '"+ csvFileUrl+ "' AS l CREATE ();",expectedCommitCount);
}
