{
  executeCommandExpectingException("mknode -r","exists");
  executeCommand("rmnode");
  executeCommand("mknode -r --cd --np \"{'name':'test'}\"");
  executeCommand("cd");
  executeCommand("ls -p","name","test");
}
