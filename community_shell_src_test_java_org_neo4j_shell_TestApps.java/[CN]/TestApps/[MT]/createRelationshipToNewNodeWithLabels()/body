{
  String type="TEST";
  executeCommand("mkrel -ctl " + type + " Person");
  assertEquals(asSet("Person"),asSet(names(getCurrentNode().getSingleRelationship(withName(type),OUTGOING).getEndNode().getLabels())));
}
