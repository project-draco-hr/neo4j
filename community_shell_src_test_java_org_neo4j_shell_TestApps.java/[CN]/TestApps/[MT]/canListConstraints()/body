{
  Label label=label("PERSON");
  beginTx();
  ConstraintDefinition constraint1=db.schema().constraintCreator(label).unique().on("name").create();
  finishTx();
  executeCommand("schema ls",constraint1.getLabel().name(),constraint1.getConstraintType().name(),constraint1.toString());
}
