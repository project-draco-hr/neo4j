{
  StoreMigrator defaultMigrator=new StoreMigrator(new SilentMigrationProgressMonitor(),fileSystem,NullLogService.getInstance());
  return newUpgrader(upgradeConfig,defaultMigrator,StoreUpgrader.NO_MONITOR);
}
