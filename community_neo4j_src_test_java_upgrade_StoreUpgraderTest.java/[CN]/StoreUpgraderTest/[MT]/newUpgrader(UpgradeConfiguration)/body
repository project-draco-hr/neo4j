{
  StoreMigrator defaultMigrator=new StoreMigrator(new SilentMigrationProgressMonitor(),fileSystem,DevNullLoggingService.DEV_NULL);
  return newUpgrader(upgradeConfig,defaultMigrator,StoreUpgrader.NO_MONITOR);
}
