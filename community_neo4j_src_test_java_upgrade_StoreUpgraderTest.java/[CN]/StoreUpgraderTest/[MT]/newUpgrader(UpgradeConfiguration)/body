{
  StoreMigrator defaultMigrator=new StoreMigrator(new SilentMigrationProgressMonitor(),fileSystem,DEV_NULL,dependencyResolver);
  return newUpgrader(upgradeConfig,defaultMigrator,StoreUpgrader.NO_MONITOR);
}
