{
  check=new StoreVersionCheck(pageCache);
  SilentMigrationProgressMonitor progressMonitor=new SilentMigrationProgressMonitor();
  NullLogService logService=NullLogService.getInstance();
  return new DatabaseMigrator(progressMonitor,fileSystem,config,logService,schemaIndexProvider,labelScanStoreProvider,new HashMap<>(),pageCache);
}
