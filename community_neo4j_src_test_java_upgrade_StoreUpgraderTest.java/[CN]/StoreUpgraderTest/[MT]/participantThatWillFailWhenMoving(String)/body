{
  return new StoreMigrationParticipant(){
    @Override public void migrate(    File storeDir,    File migrationDir,    SchemaIndexProvider schemaIndexProvider,    String versionToMigrateFrom) throws IOException {
    }
    @Override public void moveMigratedFiles(    File migrationDir,    File storeDir,    String versionToUpgradeFrom) throws IOException {
      throw new IOException(failureMessage);
    }
    @Override public void rebuildCounts(    File storeDir,    String versionToMigrateFrom) throws IOException {
    }
    @Override public void cleanup(    File migrationDir) throws IOException {
    }
  }
;
}
