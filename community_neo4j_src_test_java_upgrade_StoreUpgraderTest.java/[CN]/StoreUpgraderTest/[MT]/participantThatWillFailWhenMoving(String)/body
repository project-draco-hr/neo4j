{
  return new StoreMigrationParticipant(){
    @Override public boolean needsMigration(    File storeDir) throws IOException {
      return true;
    }
    @Override public void migrate(    File storeDir,    File migrationDir,    SchemaIndexProvider schemaIndexProvider) throws IOException {
    }
    @Override public void moveMigratedFiles(    File migrationDir,    File storeDir) throws IOException {
      throw new IOException(failureMessage);
    }
    @Override public void close(){
    }
    @Override public void cleanup(    File migrationDir) throws IOException {
    }
  }
;
}
