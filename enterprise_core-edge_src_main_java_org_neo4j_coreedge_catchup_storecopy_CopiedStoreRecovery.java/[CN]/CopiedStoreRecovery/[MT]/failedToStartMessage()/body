{
  String recordFormat=config.get(GraphDatabaseSettings.record_format);
  return String.format("Failed to start database with copied store. This may be because the core and edge " + "servers have a different record format. On this machine: `%s=%s`. Check the equivalent value" + " on the core server.",GraphDatabaseSettings.record_format.name(),recordFormat);
}
