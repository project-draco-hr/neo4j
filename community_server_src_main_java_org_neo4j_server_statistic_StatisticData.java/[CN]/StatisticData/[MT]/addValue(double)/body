{
  min=count > 0 && min < value ? min : value;
  max=count > 0 && max > value ? max : value;
  count+=1;
  sum+=value;
  sumSq+=value * value;
  int v=value >= MEDIAN_MAX ? MEDIAN_MAX - 1 : value < 0 ? 0 : (int)value;
  requests[v]++;
}
