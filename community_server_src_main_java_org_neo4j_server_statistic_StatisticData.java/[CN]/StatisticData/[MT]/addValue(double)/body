{
  min=count > 0 && min < value ? min : value;
  max=count > 0 && max > value ? max : value;
  count+=1;
  sum+=value;
  sumSq+=value * value;
  int v=value > requests.length ? requests.length : value < 0 ? 0 : (int)value;
  requests[v]++;
}
