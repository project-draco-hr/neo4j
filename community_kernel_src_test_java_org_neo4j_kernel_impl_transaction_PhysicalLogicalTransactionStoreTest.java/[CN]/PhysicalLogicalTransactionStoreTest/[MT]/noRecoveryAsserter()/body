{
  return new Visitor<ReadableVersionableLogChannel,IOException>(){
    @Override public boolean visit(    ReadableVersionableLogChannel channel) throws IOException {
      fail("Should be nothing to recover");
      return false;
    }
  }
;
}
