{
  new Thread(){
    @Override public void run(){
      Transaction tx=graphdb.beginTx();
      try {
        perform();
        tx.success();
      }
  finally {
        tx.finish();
      }
    }
  }
.start();
}
