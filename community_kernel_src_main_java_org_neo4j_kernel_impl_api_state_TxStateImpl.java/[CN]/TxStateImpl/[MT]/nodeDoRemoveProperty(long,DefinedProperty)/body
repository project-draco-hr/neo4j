{
  getOrCreateNodeState(nodeId).removeProperty(removedProperty.propertyKeyId());
  nodePropertyChanges().removeProperty(nodeId,removedProperty.propertyKeyId(),removedProperty.value());
  hasChanges=true;
}
