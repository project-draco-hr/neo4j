{
  subject.response=authenticate(subject.principalCredentials);
  return subject.response.status() == 200 || (subject.response.status() == 403 && subject.response.rawContent().contains("password_change"));
}
