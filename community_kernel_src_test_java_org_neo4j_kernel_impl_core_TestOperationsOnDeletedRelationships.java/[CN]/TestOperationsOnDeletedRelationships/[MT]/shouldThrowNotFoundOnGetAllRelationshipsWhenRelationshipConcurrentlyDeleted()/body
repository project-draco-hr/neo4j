{
  NodeImpl nodeImpl=new NodeImpl(1337l,false);
  NodeManager nodeManager=mock(NodeManager.class);
  when(nodeManager.getRelationshipChainPosition(any(NodeImpl.class))).thenReturn(new SingleChainPosition(1));
  when(nodeManager.getMoreRelationships(any(NodeImpl.class),any(DirectionWrapper.class),any(int[].class))).thenThrow(new InvalidRecordException("LURING!"));
  try {
    nodeImpl.getAllRelationships(nodeManager,RelIdArray.DirectionWrapper.BOTH);
    fail("Should throw exception");
  }
 catch (  NotFoundException e) {
  }
}
