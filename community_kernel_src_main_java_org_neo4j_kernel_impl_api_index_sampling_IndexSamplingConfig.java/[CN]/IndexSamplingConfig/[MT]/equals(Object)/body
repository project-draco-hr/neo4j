{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  IndexSamplingConfig that=(IndexSamplingConfig)o;
  return backgroundSampling == that.backgroundSampling && sampleSizeLimit == that.sampleSizeLimit && Double.compare(that.updateRatio,updateRatio) == 0;
}
