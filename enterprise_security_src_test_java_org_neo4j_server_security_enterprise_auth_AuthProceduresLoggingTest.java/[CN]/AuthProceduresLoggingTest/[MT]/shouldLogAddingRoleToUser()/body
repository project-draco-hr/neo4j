{
  authProcedures.createUser("mats","neo4j",false);
  authProcedures.addRoleToUser(ARCHITECT,"mats");
  log.assertExactly(info("[admin]: created user `%s`","mats"),info("[admin]: added role `%s` to user `%s`",ARCHITECT,"mats"));
}
