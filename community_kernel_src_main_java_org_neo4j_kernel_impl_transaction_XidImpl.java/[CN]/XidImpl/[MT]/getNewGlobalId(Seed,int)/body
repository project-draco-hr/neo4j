{
  byte globalId[]=Arrays.copyOf(INSTANCE_ID,INSTANCE_ID.length + 20);
  wrap(globalId,INSTANCE_ID.length,20).putLong(seed.nextRandomLong()).putLong(seed.nextSequenceId()).putInt(localId);
  return globalId;
}
