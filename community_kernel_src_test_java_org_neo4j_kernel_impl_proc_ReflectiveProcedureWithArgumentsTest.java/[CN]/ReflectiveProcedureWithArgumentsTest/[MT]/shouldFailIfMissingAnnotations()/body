{
  exception.expect(ProcedureException.class);
  exception.expectMessage("Argument at position 0 in method `listCoolPeople` is missing an " + "`@Name` annotation. Please add the annotation, recompile the class " + "and try again.");
  compile(ClassWithProcedureWithoutAnnotatedArgs.class);
}
