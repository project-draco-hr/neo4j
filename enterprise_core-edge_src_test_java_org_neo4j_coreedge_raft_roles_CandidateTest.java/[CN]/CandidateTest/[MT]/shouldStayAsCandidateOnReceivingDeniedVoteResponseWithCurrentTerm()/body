{
  RaftState state=newState();
  Outcome outcome=CANDIDATE.handler.handle(voteResponse().term(state.term()).from(member1).deny().build(),state,log(),storeId);
  assertEquals(CANDIDATE,outcome.getRole());
}
