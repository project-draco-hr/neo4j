{
  RaftState<RaftTestMember> state=newState();
  Outcome<RaftTestMember> outcome=CANDIDATE.handler.handle(voteResponse().term(state.term()).from(member1).deny().build(),state,log(),storeId);
  assertEquals(CANDIDATE,outcome.getRole());
}
