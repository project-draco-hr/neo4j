{
  msgLog.info("@@@ acquireSharedLock: type: " + type + " resources: "+ Arrays.toString(resourceIds));
  return sendRequest(HaRequestType210.ACQUIRE_SHARED_LOCK,context,new AcquireLockSerializer(type,resourceIds),LOCK_RESULT_DESERIALIZER);
}
