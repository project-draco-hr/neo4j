{
  new Thread(){
    public void run(){
      try {
        DatagramChannel sendChannel=DatagramChannel.open();
        sendChannel.send(buf,host);
        sendChannel.close();
      }
 catch (      Exception e) {
      }
    }
  }
.start();
}
