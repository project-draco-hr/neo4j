{
  if ("-pid".equalsIgnoreCase(arg)) {
    return PID;
  }
 else   if ("-read_only".equalsIgnoreCase(arg)) {
    args.read_only=true;
    return this;
  }
 else {
    args.uri=arg;
    if (args.pid == null) {
      return PID_SHIFT;
    }
 else {
      return PATH;
    }
  }
}
