{
  ConsistencySummaryStatistics summary=new ConsistencySummaryStatistics();
  ConsistencyReporter reporter=new ConsistencyReporter(new MessageConsistencyLogger(logger),new DirectRecordAccess(diffs),summary);
  diffs.applyToAll(new StoreProcessor(reporter));
  return summary;
}
