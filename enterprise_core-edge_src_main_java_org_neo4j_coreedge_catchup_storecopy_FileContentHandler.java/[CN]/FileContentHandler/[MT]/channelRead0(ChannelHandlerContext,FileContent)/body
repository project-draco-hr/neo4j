{
  try (FileContent content=fileContent;OutputStream outputStream=location.getStoreFileStreams().createStream(destination)){
    expectedBytes-=content.writeTo(outputStream);
  }
   if (expectedBytes <= 0) {
    protocol.expect(CatchupClientProtocol.State.MESSAGE_TYPE);
  }
}
