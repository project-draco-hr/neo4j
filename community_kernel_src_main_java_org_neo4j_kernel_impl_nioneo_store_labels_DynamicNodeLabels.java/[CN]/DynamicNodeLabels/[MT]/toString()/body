{
  if (node.isLight()) {
    return format("Dynamic(id:%d)",getFirstDynamicRecordId());
  }
  return format("Dynamic(id:%d,[%s])",getFirstDynamicRecordId(),Arrays.toString(NodeStore.getDynamicLabelsArrayFromHeavyRecords(node.getDynamicLabelRecords())));
}
