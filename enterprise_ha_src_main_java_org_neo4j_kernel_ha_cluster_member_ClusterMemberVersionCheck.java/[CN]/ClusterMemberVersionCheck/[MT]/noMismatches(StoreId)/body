{
  for (  ClusterMember member : clusterMembers.getMembers()) {
    if (member.isAlive() && !member.getStoreId().equals(StoreId.DEFAULT) && !member.getStoreId().equals(expectedStoreId)) {
      return false;
    }
  }
  return true;
}
