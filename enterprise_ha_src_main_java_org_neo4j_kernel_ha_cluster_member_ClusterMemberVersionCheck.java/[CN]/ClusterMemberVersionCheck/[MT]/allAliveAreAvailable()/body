{
  for (  ClusterMember member : clusterMembers.getMembers()) {
    if (!myId.equals(member.getInstanceId()) && member.isAlive() && HighAvailabilityModeSwitcher.UNKNOWN.equals(member.getHARole())) {
      return false;
    }
  }
  return true;
}
