{
  String url=createCsvFile("foo");
  Result result=db.execute("LOAD CSV FROM '" + url + "' AS line "+ "CALL org.neo4j.procedure.createNode(line[0]) YIELD node as n "+ "SET n.p = 42 "+ "RETURN n.p");
  assertThat(result.next().get("n.p"),equalTo(42L));
}
