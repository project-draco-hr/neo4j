{
  IteratingPropertyReceiver receiver=new IteratingPropertyReceiver();
  threadToTransactionBridge.getTransactionRecordStateBoundToThisThread(true).graphLoadProperties(light,receiver);
  return receiver;
}
