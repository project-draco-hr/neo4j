{
  IteratingPropertyReceiver receiver=new IteratingPropertyReceiver();
  threadToTransactionBridge.getNeoStoreTransactionBoundToThisThread(true).graphLoadProperties(light,receiver);
  return receiver;
}
