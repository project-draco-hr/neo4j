{
  threadToTransactionBridge.assertInTransaction();
  NodeImpl node=getLightNode(nodeId);
  return node != null ? new NodeProxy(nodeId,nodeLookup,relationshipLookups,threadToTransactionBridge) : null;
}
