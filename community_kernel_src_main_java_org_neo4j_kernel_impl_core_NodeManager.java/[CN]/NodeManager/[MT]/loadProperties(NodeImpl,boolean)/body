{
  IteratingPropertyReceiver receiver=new IteratingPropertyReceiver();
  threadToTransactionBridge.getTransactionRecordStateBoundToThisThread(true).nodeLoadProperties(node.getId(),light,receiver);
  return receiver;
}
