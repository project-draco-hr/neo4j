{
  IteratingPropertyReceiver receiver=new IteratingPropertyReceiver();
  threadToTransactionBridge.getNeoStoreTransactionBoundToThisThread(true).nodeLoadProperties(node.getId(),light,receiver);
  return receiver;
}
