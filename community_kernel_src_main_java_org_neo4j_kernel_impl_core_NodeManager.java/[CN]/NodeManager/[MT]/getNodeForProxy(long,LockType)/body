{
  if (lock != null)   acquireTxBoundLock(new NodeProxy(nodeId,nodeLookup),lock);
  NodeImpl node=getLightNode(nodeId);
  if (node == null)   throw new NotFoundException("Node[" + nodeId + "] not found.");
  return node;
}
