{
  IteratingPropertyReceiver receiver=new IteratingPropertyReceiver();
  threadToTransactionBridge.getTransactionRecordStateBoundToThisThread(true).relLoadProperties(relationship.getId(),light,receiver);
  return receiver;
}
