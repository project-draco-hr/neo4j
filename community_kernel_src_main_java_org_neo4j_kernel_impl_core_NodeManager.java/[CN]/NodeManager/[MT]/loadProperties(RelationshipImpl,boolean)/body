{
  IteratingPropertyReceiver receiver=new IteratingPropertyReceiver();
  threadToTransactionBridge.getNeoStoreTransactionBoundToThisThread(true).relLoadProperties(relationship.getId(),light,receiver);
  return receiver;
}
