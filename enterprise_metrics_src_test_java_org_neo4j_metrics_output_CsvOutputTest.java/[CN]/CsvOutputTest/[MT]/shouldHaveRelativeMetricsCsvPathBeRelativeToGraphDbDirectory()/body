{
  Config config=config(MetricsSettings.csvEnabled.name(),"true",MetricsSettings.csvInterval.name(),"10ms",MetricsSettings.csvPath.name(),"test.csv");
  life.add(new CsvOutput(config,new MetricRegistry(),NullLog.getInstance(),kernelContext));
  life.start();
  waitForFileToAppear(new File(storeDir,"test.csv"));
}
