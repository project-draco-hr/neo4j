{
  if (nextElement != null) {
    return true;
  }
  if (position >= src.length()) {
    while (getNode().getMoreRelationships(nodeManager) && position >= src.length())     ;
  }
  while (position < src.length()) {
    nextElement=src.get(position++);
    return true;
  }
  return false;
}
