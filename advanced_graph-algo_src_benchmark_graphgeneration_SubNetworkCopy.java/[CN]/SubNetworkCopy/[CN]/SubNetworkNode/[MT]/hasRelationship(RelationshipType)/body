{
  for (  RelationshipType relationshipType : types) {
    List<SubNetworkRelationship> rels=relationships.get(relationshipType);
    if (rels != null && !rels.isEmpty()) {
      return true;
    }
  }
  return false;
}
