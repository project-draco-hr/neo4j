{
  StringBuilder builder=new StringBuilder();
  for (  LogMessage message : buffer) {
    builder.append(message.message).append(message.throwable != null ? message.throwable.getMessage() : "").append(format("%n"));
  }
  return builder.toString();
}
