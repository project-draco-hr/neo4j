{
  if (!searchFilter.contains("{0}")) {
    log.warn("LDAP user search filter does not contain the argument placeholder {0}, so the search result " + "will be independent of the user principal.");
  }
}
