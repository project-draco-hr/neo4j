{
  HPCMemoryConfig mem=config.get(HighPerformanceCacheSettings.cache_memory);
  return new HighPerformanceCache<>(mem.relCacheSize(),mem.relLookupTableFraction(),config.get(HighPerformanceCacheSettings.log_interval),RELATIONSHIP_CACHE_NAME,logger,monitors.newMonitor(HighPerformanceCache.Monitor.class));
}
