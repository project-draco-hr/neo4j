{
  Config config=Config.empty().with(stringMap(DatabaseManagementSystemSettings.database_path.name(),coreStoreDir.getAbsolutePath()));
  new RestoreDatabaseCommand(new DefaultFileSystemAbstraction(),backupPath,config,"graph.db",true).execute();
}
