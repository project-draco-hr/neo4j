{
  return new Threshold(){
    private int size;
    @Override public boolean reached(    String file,    long version,    LogLoader source){
      size+=fileSystem.getFileSize(file);
      return size >= maxSize;
    }
  }
;
}
