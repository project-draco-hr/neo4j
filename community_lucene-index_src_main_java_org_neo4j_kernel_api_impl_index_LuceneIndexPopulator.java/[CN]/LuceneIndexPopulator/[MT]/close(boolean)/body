{
  try {
    if (populationCompletedSuccessfully) {
      flush();
      writerStatus.commitAsOnline(writer);
    }
  }
  finally {
    if (writer != null) {
      writerStatus.close(writer);
    }
    if (directory != null) {
      directory.close();
    }
  }
}
