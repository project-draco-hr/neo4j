{
  for (  NodePropertyUpdate update : updates)   this.updates.add(update);
  if (this.updates.size() > queueThreshold) {
    try {
      applyQueuedUpdates();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
    this.updates.clear();
  }
}
