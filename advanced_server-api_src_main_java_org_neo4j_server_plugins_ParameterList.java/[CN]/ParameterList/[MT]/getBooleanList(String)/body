{
  return getList(name,null,new Converter<Boolean>(){
    @Override Boolean convert(    AbstractGraphDatabase graphDb,    Object value) throws BadInputException {
      return convertBoolean(value);
    }
    @Override Boolean[] newArray(    int size){
      return new Boolean[size];
    }
  }
);
}
