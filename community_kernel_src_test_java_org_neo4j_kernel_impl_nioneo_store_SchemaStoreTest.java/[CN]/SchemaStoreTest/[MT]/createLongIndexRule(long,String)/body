{
  StringBuilder builder=new StringBuilder(tag);
  for (int i=1; i < SchemaStore.BLOCK_SIZE; i++)   builder.append((i % 2 == 1) ? "ding" : "dong");
  return new IndexRule(1,label,builder.toString());
}
