{
  Collection<SchemaRule> rules=Arrays.<SchemaRule>asList(createLongIndexRule(0,100),createLongIndexRule(1,6),createLongIndexRule(2,50));
  for (  SchemaRule rule : rules)   storeRule(rule);
  Collection<SchemaRule> readRules=asCollection(store.loadAll());
  assertEquals(rules,readRules);
}
