{
  when(schemaIndexProvider.getProviderDescriptor()).thenReturn(new SchemaIndexProvider.Descriptor("key","version"));
  migrator.migrate(storeDir,migrationDir,schemaIndexProvider,labelScanStoreProvider,Legacy23Store.LEGACY_VERSION);
  verify(fs).deleteRecursively(schemaIndexProvider.getStoreDirectory(storeDir));
  verify(fs).deleteRecursively(labelScanStoreProvider.getStoreDirectory(storeDir));
}
