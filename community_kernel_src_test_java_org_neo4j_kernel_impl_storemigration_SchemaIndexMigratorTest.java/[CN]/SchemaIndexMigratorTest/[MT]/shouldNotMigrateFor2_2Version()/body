{
  when(upgradableDatabase.hasCurrentVersion(pageCache,storeDir)).thenReturn(true);
  when(upgradableDatabase.checkUpgradeable(storeDir)).thenThrow(new UnexpectedUpgradingStoreVersionException(MetaDataStore.DEFAULT_NAME,Legacy21Store.LEGACY_VERSION,AbstractStore.ALL_STORES_VERSION));
  boolean needsMigration=migrator.needsMigration(storeDir);
  assertFalse(needsMigration);
}
