{
  while (blocks.hasNext()) {
    PropertyBlock block=blocks.next();
    if (block.getType() == PropertyType.LABEL && block.getKeyIndexId() == matchingLabelId)     return block.getValueBlocks()[1];
  }
  return null;
}
