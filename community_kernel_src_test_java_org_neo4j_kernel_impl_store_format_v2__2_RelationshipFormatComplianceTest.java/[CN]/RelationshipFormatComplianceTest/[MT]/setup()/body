{
  EphemeralFileSystemAbstraction fs=fsRule.get();
  pageCache=pageCacheRule.getPageCache(fs);
  storeFactory=new StoreFactory(storeDir,new Config(),new DefaultIdGeneratorFactory(fs),pageCache,fs,NullLogProvider.getInstance(),new Monitors());
}
