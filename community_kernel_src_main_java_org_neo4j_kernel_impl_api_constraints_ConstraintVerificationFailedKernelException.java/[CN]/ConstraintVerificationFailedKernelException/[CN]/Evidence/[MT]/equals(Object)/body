{
  if (this == obj) {
    return true;
  }
  if (obj != null && getClass() == obj.getClass()) {
    Evidence that=(Evidence)obj;
    return this.addedNodeId == that.addedNodeId && this.existingNodeId == that.existingNodeId && this.propertyValue.equals(that.propertyValue);
  }
  return false;
}
