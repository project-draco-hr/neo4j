{
  StringBuilder message=new StringBuilder();
  for (  Evidence evidenceItem : evidence) {
    IndexEntryConflictException conflict=evidenceItem.conflict;
    message.append(conflict.evidenceMessage(keyNameLookup.getLabelName(constraint.label()),keyNameLookup.getPropertyKeyName(constraint.property())));
  }
  return message.toString();
}
