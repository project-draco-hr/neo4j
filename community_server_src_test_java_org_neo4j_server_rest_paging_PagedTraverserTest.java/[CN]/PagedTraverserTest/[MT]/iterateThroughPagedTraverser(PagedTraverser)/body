{
  Transaction transaction=database.getGraph().beginTx();
  try {
    int count=0;
    for (    List<Path> paths : traversalPager) {
      count++;
    }
    return count;
  }
  finally {
    transaction.finish();
  }
}
