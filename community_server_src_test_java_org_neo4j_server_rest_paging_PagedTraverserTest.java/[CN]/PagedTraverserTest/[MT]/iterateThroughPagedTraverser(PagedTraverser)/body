{
  try (Transaction transaction=database.getGraph().beginTx()){
    int count=0;
    for (    List<Path> paths : traversalPager) {
      count++;
    }
    transaction.success();
    return count;
  }
 }
