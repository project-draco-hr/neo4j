{
  database=new Database(ServerTestUtils.createTempDir().getAbsolutePath());
  helper=new GraphDbHelper(database);
  service=new RestfulGraphDatabase(uriInfo(),database,new JsonFormat(),new OutputFormat(new JsonFormat(),URI.create(BASE_URI),null));
}
