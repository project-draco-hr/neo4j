{
  int flags=PF_SHARED_LOCK;
  if ((storeCursorFlags & SF_SCAN) != 0) {
    flags|=PF_TRANSIENT;
    if ((storeCursorFlags & SF_REVERSE_CURSOR) == 0) {
      flags|=PF_READ_AHEAD;
    }
  }
  return flags;
}
