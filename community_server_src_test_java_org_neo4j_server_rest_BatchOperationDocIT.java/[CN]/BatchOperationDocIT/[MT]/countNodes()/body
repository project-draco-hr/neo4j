{
  Transaction transaction=graphdb().beginTx();
  try {
    int count=0;
    for (    Node node : graphdb().getAllNodes()) {
      count++;
    }
    return count;
  }
  finally {
    transaction.finish();
  }
}
