{
  if (Record.NO_NEXT_BLOCK.value(key.getKeyBlockId()))   return false;
  DynamicRecord record=propKeys.forceGetRecord(key.getKeyBlockId());
  if (!record.inUse())   return inconsistent(key,record,"reference to unused key string");
  return false;
}
