{
  super.setUpGlobal();
  RandomGraph graph1;
  SmallWorldGraph graph2;
  PreferentialAttachment graph3;
  graph=new PreferentialAttachment(neo,new GraphStore(neo),MyRelTypes.R1,100000,10);
  numberOfRuns=10;
}
