{
  List<String> nodeIds=nodeIds();
  Configuration config=Configuration.COMMAS;
  BatchImporterTool.main(arguments("--into",directory.absolutePath(),"--nodes",nodeData(true,config,nodeIds).getAbsolutePath(),"--relationships",relationshipData(true,config,nodeIds).getAbsolutePath()));
  verifyData();
}
