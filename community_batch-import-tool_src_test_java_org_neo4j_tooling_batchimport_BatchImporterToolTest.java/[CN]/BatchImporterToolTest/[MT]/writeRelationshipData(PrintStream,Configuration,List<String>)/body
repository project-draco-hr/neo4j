{
  char delimiter=config.delimiter();
  for (int i=0; i < RELATIONSHIP_COUNT; i++) {
    writer.println(nodeIds.get(random.nextInt(nodeIds.size())) + delimiter + nodeIds.get(random.nextInt(nodeIds.size()))+ delimiter+ randomType()+ delimiter+ currentTimeMillis());
  }
}
