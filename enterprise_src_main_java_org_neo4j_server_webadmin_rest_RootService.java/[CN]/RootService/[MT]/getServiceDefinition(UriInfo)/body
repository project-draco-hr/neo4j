{
  ServerRootRepresentation rootRepresentation=new ServerRootRepresentation(uriInfo.getBaseUri(),new ConsoleService(null),new JmxService(),new MonitorService(null));
  String entity=JsonRenderers.DEFAULT.render(rootRepresentation);
  return Response.ok(entity).header("Content-Type",MediaType.APPLICATION_JSON).build();
}
