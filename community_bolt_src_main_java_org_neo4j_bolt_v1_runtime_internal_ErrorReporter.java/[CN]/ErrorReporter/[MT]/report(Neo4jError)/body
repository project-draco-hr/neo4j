{
  if (!error.status().code().classification().publishable()) {
    userLog.error(String.format("Client triggered unexpected error. Help us fix this error by emailing the " + "following report to issues@neotechnology.com: %n%s",generateReport(error)));
  }
}
