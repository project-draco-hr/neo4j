{
  if (cause == null) {
    return NO_ERROR_DATA_AVAILABLE;
  }
 else {
    return printBase64Binary(Exceptions.stringify(cause).getBytes(StandardCharsets.UTF_8)).replaceAll("(.{100})","$1" + System.getProperty("line.separator"));
  }
}
