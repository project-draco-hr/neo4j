{
  if (labelDiffSet.isAdded(labelId)) {
    label=labelId;
    return true;
  }
  if (labelDiffSet.isRemoved(labelId)) {
    label=-1;
    return false;
  }
  if (cursor.seek(labelId)) {
    label=labelId;
    return true;
  }
 else {
    label=-1;
    return false;
  }
}
