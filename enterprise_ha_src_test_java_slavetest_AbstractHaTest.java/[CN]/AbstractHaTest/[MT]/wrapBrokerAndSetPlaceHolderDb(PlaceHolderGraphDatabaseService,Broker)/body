{
  return new BrokerFactory(){
    @Override public Broker create(    AbstractGraphDatabase graphDb,    Map<String,String> graphDbConfig){
      placeHolderDb.setDb(graphDb);
      return broker;
    }
  }
;
}
