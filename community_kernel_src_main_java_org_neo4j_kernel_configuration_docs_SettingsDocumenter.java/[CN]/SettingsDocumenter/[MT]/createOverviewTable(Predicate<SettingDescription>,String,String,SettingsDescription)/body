{
  List<SettingDescription> settings=desc.settings().filter(filter).collect(toList());
  if (settings.size() > 0) {
    out.print(new AsciiDocListGenerator(tableId,tableDescription,true).generateListAndTableCombo(settings));
  }
}
