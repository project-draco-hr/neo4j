{
  ExpansionSource result=null;
  while (result == null) {
    ExpansionSource next=current.next();
    if (next != null) {
      queue.add(next);
      result=next;
    }
 else {
      current=queue.poll();
      if (current == null) {
        return null;
      }
    }
  }
  return result;
}
