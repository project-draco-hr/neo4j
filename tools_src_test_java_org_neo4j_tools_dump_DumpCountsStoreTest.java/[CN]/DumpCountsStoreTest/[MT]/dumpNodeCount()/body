{
  DumpCountsStore countsStore=getCountStore();
  countsStore.visitNodeCount(NODE_LABEL_ID,70);
  assertThat(suppressOutput.getOutputVoice().toString(),containsString("Node[(:testLabel [labelId=10])]:\t70"));
}
