{
  final GetServersProcedure proc=new GetServersProcedure(null,null,config,getInstance());
  ProcedureSignature signature=proc.signature();
  assertThat(signature.outputSignature(),containsInAnyOrder(new FieldSignature("ttl",Neo4jTypes.NTInteger),new FieldSignature("servers",NTMap)));
}
