{
  final GetServersProcedure proc=new GetServersProcedure(null,null,NullLogProvider.getInstance());
  ProcedureSignature signature=proc.signature();
  assertThat(signature.outputSignature(),containsInAnyOrder(new FieldSignature("address",NTString),new FieldSignature("role",NTString),new FieldSignature("expiry",NTInteger)));
}
