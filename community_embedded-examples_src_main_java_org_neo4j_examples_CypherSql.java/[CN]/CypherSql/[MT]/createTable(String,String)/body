{
  String sql="CREATE TABLE " + identifierQuoteString + name+ identifierQuoteString+ "("+ definition+ ")";
  Statement statement=sqldb.createStatement();
  try {
    statement.execute(sql);
  }
  finally {
    statement.close();
  }
}
