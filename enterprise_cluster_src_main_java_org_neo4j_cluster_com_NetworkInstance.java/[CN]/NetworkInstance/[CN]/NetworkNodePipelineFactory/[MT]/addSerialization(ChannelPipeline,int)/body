{
  pipeline.addLast("frameDecoder",new ObjectDecoder(frameLength,ClassResolvers.cacheDisabled(NetworkNodePipelineFactory.this.getClass().getClassLoader())));
  pipeline.addLast("frameEncoder",new ObjectEncoder(2048));
}
