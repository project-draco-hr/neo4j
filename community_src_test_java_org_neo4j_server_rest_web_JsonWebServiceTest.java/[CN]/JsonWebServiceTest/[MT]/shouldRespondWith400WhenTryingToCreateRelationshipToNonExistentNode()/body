{
  long nodeId=helper.createNode();
  Response response=service.jsonCreateRelationship(nodeId,"{\"to\" : \"" + BASE_URI + (nodeId * 1000)+ "\", \"type\" : \"LOVES\"}");
  assertEquals(400,response.getStatus());
}
