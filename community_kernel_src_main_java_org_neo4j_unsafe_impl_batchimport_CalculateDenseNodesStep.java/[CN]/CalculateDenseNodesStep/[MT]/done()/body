{
  long numberOfDenseNodes=0;
  for (long i=0; i < highestSeenNodeId; i++) {
    if (nodeRelationshipLink.isDense(i)) {
      numberOfDenseNodes++;
    }
  }
  System.out.println("# dense nodes: " + numberOfDenseNodes + ", which is "+ round(100D * numberOfDenseNodes / highestSeenNodeId)+ " %");
  super.done();
}
