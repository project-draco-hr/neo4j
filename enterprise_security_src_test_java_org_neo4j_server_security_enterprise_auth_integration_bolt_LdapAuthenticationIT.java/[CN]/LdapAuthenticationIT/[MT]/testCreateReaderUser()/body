{
  assertAuth("neo4j","abc123");
  client.send(TransportTestUtil.chunk(run("CALL dbms.changeUserPassword('neo4j', '123') CALL dbms.createUser( 'neo', 'invalid', false ) " + "CALL dbms.addRoleToUser( 'reader', 'neo' ) RETURN 0"),pullAll()));
  assertThat(client,eventuallyReceives(msgSuccess()));
}
