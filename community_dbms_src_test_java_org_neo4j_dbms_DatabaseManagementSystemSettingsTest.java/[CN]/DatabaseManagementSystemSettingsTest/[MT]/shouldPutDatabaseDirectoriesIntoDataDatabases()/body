{
  Config config=new Config(stringMap(DatabaseManagementSystemSettings.data_directory.name(),"the-data-directory"));
  assertThat(config.get(DatabaseManagementSystemSettings.database_path),equalTo(new File(System.getProperty("user.dir") + "/the-data-directory/databases/graph.db")));
}
