{
  try {
    return server.getClass().getClassLoader().loadClass(interfaceName);
  }
 catch (  ClassNotFoundException e) {
    throw new RuntimeException("TODO: Better exception type. The server does not support the service interface: " + interfaceName);
  }
}
