{
  long node=config.get(Configuration.node_cache_size);
  long rel=config.get(Configuration.relationship_cache_size);
  checkMemToUse(logger,node,rel,Runtime.getRuntime().maxMemory());
  return new GCResistantCache<RelationshipImpl>(rel,config.get(Configuration.relationship_cache_array_fraction),config.get(Configuration.log_interval),RELATIONSHIP_CACHE_NAME,logger);
}
