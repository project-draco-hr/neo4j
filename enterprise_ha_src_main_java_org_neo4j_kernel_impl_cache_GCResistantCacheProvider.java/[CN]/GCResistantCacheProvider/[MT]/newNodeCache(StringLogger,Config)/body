{
  long node=config.get(node_cache_size);
  long rel=config.get(relationship_cache_size);
  checkMemToUse(logger,node,rel,Runtime.getRuntime().maxMemory());
  return new GCResistantCache<NodeImpl>(node,config.get(node_cache_array_fraction),config.get(gcr_cache_min_log_interval),NODE_CACHE_NAME,logger);
}
