{
  return Iterables.filter(new Specification<URI>(){
    @Override public boolean satisfiedBy(    URI item){
      return !isFailed(item);
    }
  }
,clusterContext.getConfiguration().getMembers());
}
