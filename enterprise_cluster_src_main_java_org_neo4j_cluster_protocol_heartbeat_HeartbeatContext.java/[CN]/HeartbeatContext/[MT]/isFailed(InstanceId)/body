{
  List<InstanceId> suspicions=getSuspicionsOf(node);
  int adjust=failed.contains(node) ? 0 : 1;
  return suspicions.size() > (clusterContext.getConfiguration().getMembers().size() - failed.size() - adjust) / 2;
}
