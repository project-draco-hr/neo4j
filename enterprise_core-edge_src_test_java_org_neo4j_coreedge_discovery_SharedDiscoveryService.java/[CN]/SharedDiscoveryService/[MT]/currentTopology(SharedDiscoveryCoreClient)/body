{
  lock.lock();
  try {
    return new TestOnlyClusterTopology(coreClients.get(0) == client,unmodifiableSet(coreMembers),unmodifiableSet(coreBoltAddresses),unmodifiableSet(edgeBoltAddresses));
  }
  finally {
    lock.unlock();
  }
}
