{
  lock.lock();
  try {
    return new ClusterTopology(coreClients.size() > 0 && coreClients.get(0) == client,unmodifiableMap(coreMembers),unmodifiableSet(edgeAddresses));
  }
  finally {
    lock.unlock();
  }
}
