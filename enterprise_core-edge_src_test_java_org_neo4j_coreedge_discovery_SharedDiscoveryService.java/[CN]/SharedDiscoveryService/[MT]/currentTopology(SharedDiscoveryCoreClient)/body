{
  lock.lock();
  try {
    return new ClusterTopology(coreClients.get(0) == client,unmodifiableSet(coreMembers),unmodifiableSet(coreBoltAddresses),unmodifiableSet(edgeBoltAddresses));
  }
  finally {
    lock.unlock();
  }
}
