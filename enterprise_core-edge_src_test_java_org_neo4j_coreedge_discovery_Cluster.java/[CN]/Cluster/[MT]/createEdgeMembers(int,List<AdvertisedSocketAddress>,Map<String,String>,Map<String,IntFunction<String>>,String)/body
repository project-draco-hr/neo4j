{
  for (int i=0; i < noOfEdgeMembers; i++) {
    edgeMembers.put(i,new EdgeClusterMember(parentDir,i,discoveryServiceFactory,coreMemberAddresses,extraParams,instanceExtraParams,recordFormat));
  }
}
