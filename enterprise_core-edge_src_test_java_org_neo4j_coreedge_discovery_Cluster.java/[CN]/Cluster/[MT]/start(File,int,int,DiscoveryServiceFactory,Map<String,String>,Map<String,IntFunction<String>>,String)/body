{
  Cluster cluster=new Cluster(parentDir,noOfCoreServers,noOfEdgeServers,discoveryServiceFactory,coreParams,instanceCoreParams,stringMap(),emptyMap(),recordFormat);
  cluster.start();
  return cluster;
}
