{
  for (int i=0; i < noOfCoreServers; i++) {
    CoreServer coreServer=new CoreServer(i,noOfCoreServers,addresses,discoveryServiceFactory,recordFormat,parentDir,extraParams,instanceExtraParams);
    coreServers.put(i,coreServer);
  }
}
