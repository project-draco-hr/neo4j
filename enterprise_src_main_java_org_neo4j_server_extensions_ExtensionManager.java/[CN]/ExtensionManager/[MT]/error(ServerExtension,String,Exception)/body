{
  if (cause == null) {
    log.warn("Failed to load server extension: " + extension + "\n"+ message);
  }
 else {
    log.warn("Failed to load server extension: " + extension + "\n"+ message,cause);
  }
}
