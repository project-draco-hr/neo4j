{
switch ((Status)getStatus()) {
case SETUP_OFFER:
    return setupOffer();
case SEND_OFFER:
  return sendOffer();
case GET_OK:
return getOk();
case SEND_LOG:
return sendLog();
default :
throw new IllegalStateException("Unkown status: " + getStatus());
}
}
