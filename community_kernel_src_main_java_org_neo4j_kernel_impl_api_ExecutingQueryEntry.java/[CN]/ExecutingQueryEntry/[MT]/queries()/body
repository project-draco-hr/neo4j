{
  Stream.Builder<ExecutingQuery> builder=Stream.builder();
  ExecutingQueryEntry entry=this;
  while (entry != null) {
    builder.accept(entry.query);
    entry=entry.next;
  }
  return builder.build();
}
