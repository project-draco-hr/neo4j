{
  if (cluster == null) {
    cluster=new Cluster(clusterDirectory,noCoreServers,noEdgeServers,factory,coreParams,instanceCoreParams,edgeParams,instanceEdgeParams,recordFormat);
  }
  return cluster;
}
