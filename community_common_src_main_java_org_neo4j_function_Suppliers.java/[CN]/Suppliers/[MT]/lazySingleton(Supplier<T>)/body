{
  return new Supplier<T>(){
    volatile T instance=null;
    @Override public T get(){
      if (instance != null) {
        return instance;
      }
synchronized (this) {
        if (instance == null) {
          instance=supplier.get();
        }
      }
      return instance;
    }
  }
;
}
