{
  for (  Synchronization s : syncHooks) {
    try {
      s.afterCompletion(status);
    }
 catch (    Throwable t) {
      log.log(Level.WARNING,"Caught exception from tx syncronization[" + s + "] afterCompletion()",t);
    }
  }
  syncHooks=null;
}
