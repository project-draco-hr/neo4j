{
  StringBuffer txString=new StringBuffer("Transaction(" + eventIdentifier + ", owner:\""+ owner.getName()+ "\")["+ txManager.getTxStatusAsString(status)+ ",Resources="+ resourceList.size()+ "]");
  return txString.toString();
}
