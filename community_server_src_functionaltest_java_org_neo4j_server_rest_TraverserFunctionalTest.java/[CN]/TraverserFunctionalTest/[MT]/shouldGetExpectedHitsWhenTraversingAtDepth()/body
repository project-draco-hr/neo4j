{
  Node start=getNode("Root");
  String description=JsonHelper.createJsonFrom(MapUtil.map("prune_evaluator",MapUtil.map("language","builtin","name","none"),"return_filter",MapUtil.map("language","javascript","body","position.length()<3;")));
  String entity=gen.get().expectedStatus(200).payload(description).post(getTraverseUriNodes(start)).entity();
  expectNodes(entity,getNodes("Root","Mattias","Johan","Emil"));
}
