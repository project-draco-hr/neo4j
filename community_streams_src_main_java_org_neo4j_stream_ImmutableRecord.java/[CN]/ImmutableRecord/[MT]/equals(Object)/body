{
  if (this == o) {
    return true;
  }
  if (o == null || !(o instanceof Record)) {
    return false;
  }
  Record that=(Record)o;
  if (!Arrays.equals(fields,that.fields())) {
    return false;
  }
  return true;
}
