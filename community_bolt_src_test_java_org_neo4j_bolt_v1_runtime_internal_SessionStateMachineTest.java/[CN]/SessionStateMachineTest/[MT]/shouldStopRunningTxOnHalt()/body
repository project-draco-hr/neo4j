{
  machine.init("FunClient/1.2",emptyMap(),-1,Session.Callback.NO_OP);
  machine.beginTransaction();
  machine.close();
  assertThat(machine.state(),CoreMatchers.equalTo(SessionStateMachine.State.STOPPED));
  verify(spi).beginTransaction(any(KernelTransaction.Type.class),any(AccessMode.class),any(VersionTracker.class));
  verify(ktx).close();
}
