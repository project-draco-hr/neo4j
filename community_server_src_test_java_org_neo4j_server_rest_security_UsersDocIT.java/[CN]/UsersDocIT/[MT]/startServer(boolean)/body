{
  FileUtils.deleteRecursively(new File("neo4j-home/data/"));
  server=CommunityServerBuilder.server().withProperty(ServerSettings.auth_enabled.name(),Boolean.toString(authEnabled)).build();
  server.start();
}
