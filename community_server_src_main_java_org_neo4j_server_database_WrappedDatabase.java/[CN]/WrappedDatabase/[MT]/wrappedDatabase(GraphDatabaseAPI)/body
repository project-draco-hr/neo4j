{
  return new Factory(){
    @Override public Database newDatabase(    Config config,    InternalAbstractGraphDatabase.Dependencies dependencies){
      return new WrappedDatabase(db);
    }
  }
;
}
