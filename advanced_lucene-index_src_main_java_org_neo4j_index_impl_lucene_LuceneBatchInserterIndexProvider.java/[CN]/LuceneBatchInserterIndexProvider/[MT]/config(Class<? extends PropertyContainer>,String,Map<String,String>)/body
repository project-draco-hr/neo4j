{
  if (config != null) {
    config=MapUtil.stringMap(new HashMap<String,String>(config),"provider",LuceneIndexImplementation.SERVICE_NAME);
    indexStore.setIfNecessary(cls,indexName,config);
    return config;
  }
 else {
    return indexStore.get(cls,indexName);
  }
}
