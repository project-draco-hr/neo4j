{
  if (database.getIndexManager().relationshipIndexNames().length == 0) {
    return Response.status(Status.NO_CONTENT).build();
  }
  return addHeaders(Response.ok(renderer.render(new RelationshipIndexRootRepresentation(uriInfo.getBaseUri(),database.getIndexManager())),renderer.getMediaType())).build();
}
