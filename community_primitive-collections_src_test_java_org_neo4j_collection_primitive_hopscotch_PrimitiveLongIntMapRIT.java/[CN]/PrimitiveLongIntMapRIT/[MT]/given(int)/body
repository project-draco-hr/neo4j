{
  return new Printable(){
    @Override public void print(    LinePrinter out){
      out.println(PrimitiveLongIntMap.class.getSimpleName() + " map = new " + PrimitiveLongIntHashMap.class.getSimpleName()+ "( "+ h+ " );");
    }
  }
;
}
