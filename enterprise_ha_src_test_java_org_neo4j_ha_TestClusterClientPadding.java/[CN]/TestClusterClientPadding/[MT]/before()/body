{
  clusterManager=new ClusterManager(clusterWithAdditionalClients(2,1),dir.directory("dbs",true),stringMap());
  clusterManager.start();
  cluster=clusterManager.getDefaultCluster();
  cluster.await(masterSeesMembers(3));
}
