{
  if (isPortableEditFileSupported()) {
    try {
      portableEditFile(file);
      return;
    }
 catch (    IOException e) {
      e.printStackTrace(System.out);
    }
  }
  windowsEditFile(file);
}
