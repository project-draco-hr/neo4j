{
  long queryId=lastQueryId.incrementAndGet();
  ExecutingQuery executingQuery=new ExecutingQuery(queryId,statement.username(),queryText,queryParameters,clock.millis());
  registerExecutingQuery(statement,executingQuery);
  return executingQuery;
}
