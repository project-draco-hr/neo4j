{
  long queryId=lastQueryId.incrementAndGet();
  ExecutingQuery executingQuery=new ExecutingQuery(queryId,statement.authSubjectName(),queryText,queryParameters,clock.millis());
  registerExecutingQuery(statement,executingQuery);
  return executingQuery;
}
