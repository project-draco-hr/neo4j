{
  if (this.pruning == pruning) {
    return this;
  }
  nullCheck(pruning,PruneEvaluator.class,"NO_PRUNING");
  return new TraversalDescriptionImpl(expander,uniqueness,uniquenessParameter,addPruneEvaluator(pruning),filter,branchSelector);
}
