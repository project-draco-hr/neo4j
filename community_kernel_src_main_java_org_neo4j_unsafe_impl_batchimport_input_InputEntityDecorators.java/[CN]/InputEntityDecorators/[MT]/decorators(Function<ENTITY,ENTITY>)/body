{
  return new Function<ENTITY,ENTITY>(){
    @Override public ENTITY apply(    ENTITY from) throws RuntimeException {
      for (      Function<ENTITY,ENTITY> decorator : decorators) {
        from=decorator.apply(from);
      }
      return from;
    }
  }
;
}
