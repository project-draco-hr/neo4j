{
  Config config=new Config(stringMap(ServerSettings.data_directory.name(),"the-data-directory"));
  assertThat(config.get(ServerSettings.database_path),equalTo(new File("the-data-directory/databases/graph.db")));
}
