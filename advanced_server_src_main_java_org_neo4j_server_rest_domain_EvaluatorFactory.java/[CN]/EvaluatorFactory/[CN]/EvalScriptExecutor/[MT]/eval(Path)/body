{
  try {
    this.script.getContext().setAttribute("position",position,ScriptContext.ENGINE_SCOPE);
    return this.script.eval(body);
  }
 catch (  ScriptException e) {
    throw new EvaluationException(e);
  }
}
