{
  Transaction tx=neo.beginTx();
  EigenvectorCentralityPower eigenvectorCentralityPower=new EigenvectorCentralityPower(Direction.OUTGOING,new CostEvaluator<Double>(){
    public Double getCost(    Relationship relationship,    boolean backwards){
      return 1.0;
    }
  }
,graph.getNodes(),graph.getRelationships(),0.001);
  eigenvectorCentralityPower.calculate();
  tx.finish();
}
