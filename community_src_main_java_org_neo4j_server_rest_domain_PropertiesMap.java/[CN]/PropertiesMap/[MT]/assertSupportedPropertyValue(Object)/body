{
  if (value == null) {
    throw new PropertyValueException("null value not supported");
  }
  if (value instanceof String) {
  }
 else   if (value instanceof Number) {
  }
 else   if (value instanceof Boolean) {
  }
 else {
    throw new PropertyValueException("Unsupported value type " + value.getClass() + "."+ " Supported value types are all java primitives (byte, char, short, int, "+ "long, float, double) and String, as well as arrays of all those types");
  }
  return value;
}
