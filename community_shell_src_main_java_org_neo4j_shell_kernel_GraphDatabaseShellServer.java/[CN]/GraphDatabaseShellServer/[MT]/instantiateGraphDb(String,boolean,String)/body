{
  GraphDatabaseBuilder builder=new GraphDatabaseFactory().newEmbeddedDatabaseBuilder(path).setConfig(read_only,Boolean.toString(readOnly));
  if (configFileOrNull != null)   builder.loadPropertiesFromFile(configFileOrNull);
  return builder.newGraphDatabase();
}
