{
  TopLevelTransaction tx=transactions.get(clientId);
  if (tx != null) {
    try {
      getDb().getDependencyResolver().resolveDependency(ThreadToStatementContextBridge.class).bindTransactionToCurrentThread(tx);
    }
 catch (    Exception e) {
      throw wrapException(e);
    }
  }
}
