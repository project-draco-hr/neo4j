{
  if (patternOrNull == null) {
    return true;
  }
  value=fixCaseSensitivity(value,caseSensitive);
  return exactMatch ? patternOrNull.matcher(value).matches() : patternOrNull.matcher(value).find();
}
