{
  DefaultIdGeneratorFactory idGeneratorFactory=new DefaultIdGeneratorFactory(fs);
  AbstractDynamicStore store=new AbstractDynamicStore(fileName,new Config(),IdType.ARRAY_BLOCK,idGeneratorFactory,pageCache,NullLogProvider.getInstance(),"TestDynamicStore",BLOCK_SIZE){
    @Override public void accept(    Processor processor,    DynamicRecord record){
    }
  }
;
  store.initialise(true);
  return store;
}
