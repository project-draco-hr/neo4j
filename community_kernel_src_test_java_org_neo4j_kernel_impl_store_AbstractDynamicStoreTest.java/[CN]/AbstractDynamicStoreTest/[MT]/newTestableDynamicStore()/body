{
  return new AbstractDynamicStore(fileName,new Config(),IdType.ARRAY_BLOCK,new DefaultIdGeneratorFactory(),pageCache,fsr.get(),NullLogProvider.getInstance(),StoreVersionMismatchHandler.ALLOW_OLD_VERSION,new Monitors()){
    @Override public void accept(    Processor processor,    DynamicRecord record){
    }
    @Override public String getTypeDescriptor(){
      return "TestDynamicStore";
    }
  }
;
}
