{
  server=server().withProperty(ServerSettings.bolt_enabled.name(),"true").withProperty(ServerSettings.bolt_tls_enabled.name(),"true").usingDatabaseDir(tmpDir.getRoot().getAbsolutePath()).build();
  server.start();
  assertEventuallyServerResponds("localhost",7687);
}
