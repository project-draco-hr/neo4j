{
  server=server().withProperty(connector(0,ServerSettings.bolt_enabled).name(),"true").withProperty(connector(0,ServerSettings.bolt_tls_level).name(),"REQUIRED").usingDataDir(tmpDir.getRoot().getAbsolutePath()).build();
  server.start();
  assertEventuallyServerResponds("localhost",7687);
}
