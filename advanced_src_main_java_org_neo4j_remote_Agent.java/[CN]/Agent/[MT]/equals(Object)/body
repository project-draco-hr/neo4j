{
  if (obj instanceof Arguments) {
    Arguments other=(Arguments)obj;
    if (other.read_only == read_only)     if (path == other.path || path.equals(other.path))     if (uri == other.uri || uri.equals(other.uri))     return true;
  }
  return false;
}
