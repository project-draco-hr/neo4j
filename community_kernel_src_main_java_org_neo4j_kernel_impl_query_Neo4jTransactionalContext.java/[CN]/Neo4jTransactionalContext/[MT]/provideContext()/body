{
  if (isOpen) {
    return this;
  }
 else {
    InternalTransaction transaction=graph.beginTransaction(transactionType,mode);
    Statement statement=txBridge.get();
    return new Neo4jTransactionalContext(graph,transaction,statement,executingQuery.queryText(),executingQuery.queryParameters(),locker);
  }
}
