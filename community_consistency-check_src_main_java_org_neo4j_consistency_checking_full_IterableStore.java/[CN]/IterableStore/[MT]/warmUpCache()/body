{
  int recordsPerPage=store.getRecordsPerPage();
  long id=0;
  long half=store.getHighId() / 2;
  while (id < half) {
    try {
      store.getRecord(id);
    }
 catch (    Exception e) {
    }
    id+=(recordsPerPage - 1);
  }
}
