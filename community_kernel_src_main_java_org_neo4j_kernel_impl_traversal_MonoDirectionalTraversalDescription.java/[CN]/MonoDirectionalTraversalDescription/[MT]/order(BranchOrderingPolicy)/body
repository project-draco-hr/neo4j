{
  if (this.branchOrdering == order) {
    return this;
  }
  return new MonoDirectionalTraversalDescription(expander,uniqueness,uniquenessParameter,evaluator,initialState,order,sorting,endNodes,statementSupplier);
}
