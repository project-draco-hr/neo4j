{
  return new TransactionHeaderInformationFactory(){
    @Override public TransactionHeaderInformation create(){
      return new TransactionHeaderInformation(memberContext.getElectedMasterId().toIntegerIndex(),memberContext.getMyId().toIntegerIndex(),new byte[0]);
    }
  }
;
}
