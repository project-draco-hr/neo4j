{
  Logging loggingService=life.add(LogbackWeakDependency.tryLoadLogbackService(config,NEW_LOGGER_CONTEXT,DEFAULT_TO_CLASSIC));
  InternalLoggerFactory.setDefaultFactory(new NettyLoggerFactory(loggingService));
  return loggingService;
}
