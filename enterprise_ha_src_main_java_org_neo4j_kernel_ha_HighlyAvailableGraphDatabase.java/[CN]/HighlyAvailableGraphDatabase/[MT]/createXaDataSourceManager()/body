{
  XaDataSourceManager toReturn=new HaXaDataSourceManager(logging.getLogger(Loggers.DATASOURCE));
  requestContextFactory=new RequestContextFactory(config.get(HaSettings.server_id),toReturn,dependencyResolver);
  return toReturn;
}
