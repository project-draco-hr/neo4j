{
  if (++badEntries > tolerance) {
    throw new InputException(format("Too many bad entries, saw %d where last one was " + "%s refering to missing node %s",badEntries,relationship,specificValue));
  }
  out.println(specificValue + " in " + relationship);
}
