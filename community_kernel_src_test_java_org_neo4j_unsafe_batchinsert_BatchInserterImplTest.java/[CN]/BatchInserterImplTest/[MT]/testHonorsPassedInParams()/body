{
  int mappedMemoryTotalSize=createInserterAndGetMemoryMappingConfig(stringMap(GraphDatabaseSettings.pagecache_memory.name(),"16K",GraphDatabaseSettings.mapped_memory_page_size.name(),"1K"));
  assertThat("memory mapped config is active",mappedMemoryTotalSize,is(16 * 1024));
}
