{
  Long mappedMemoryTotalSize=createInserterAndGetMemoryMappingConfig(stringMap(GraphDatabaseSettings.mapped_memory_total_size.name(),"16K"));
  assertThat("memory mapped config is active",mappedMemoryTotalSize,is(16 * 1024L));
}
