{
  db=new GraphDatabaseFactory().newEmbeddedDatabaseBuilder(testDirectory.graphDbDir()).setConfig(GraphDatabaseSettings.auth_store,testDirectory.file("auth").getAbsolutePath()).newGraphDatabase();
}
