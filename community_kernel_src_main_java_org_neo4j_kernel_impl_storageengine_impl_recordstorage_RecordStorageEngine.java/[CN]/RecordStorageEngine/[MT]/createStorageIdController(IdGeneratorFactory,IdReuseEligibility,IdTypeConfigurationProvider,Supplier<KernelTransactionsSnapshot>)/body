{
  return safeIdBuffering ? new BufferedRecordStorageIdController(idGeneratorFactory,transactionsSnapshotSupplier,eligibleForReuse,idTypeConfigurationProvider,scheduler) : new DefaultRecordStorageIdController(idGeneratorFactory);
}
