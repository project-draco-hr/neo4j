{
  return safeIdBuffering ? new BufferedIdController(idGeneratorFactory,transactionsSnapshotSupplier,eligibleForReuse,idTypeConfigurationProvider,scheduler) : new DefaultIdController(idGeneratorFactory);
}
