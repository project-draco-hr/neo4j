{
  database=new Database(ServerTestUtils.createTempDir().getAbsolutePath());
  helper=new GraphDbHelper(database);
  OutputFormat outputFormat=new EntityOutputFormat();
  service=new RestfulGraphDatabase(uriInfo(),database,new JsonFormat(),outputFormat);
}
