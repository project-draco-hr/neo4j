{
  assertNotNull(globalId,"global id");
  assertNotNull(branchId,"branch id");
  logBuffer.put(BRANCH_ADD).put((byte)globalId.length).put((byte)branchId.length).put(globalId).put(branchId);
  recordCount++;
}
