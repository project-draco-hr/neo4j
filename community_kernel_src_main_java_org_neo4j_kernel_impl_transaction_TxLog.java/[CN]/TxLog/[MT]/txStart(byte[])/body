{
  assertNotNull(globalId,"global id");
  if (!activeTransactions.add(new ByteArrayKey(globalId)))   throw new IllegalStateException("Global ID " + Arrays.toString(globalId) + " already started");
  logBuffer.put(TX_START).put((byte)globalId.length).put(globalId);
  recordCount++;
}
