{
  if (inUse && !(size() > 0)) {
    throw new IllegalStateException("You cannot set a property record as in use when no property blocks are set as in use. Offensive record is:\n\t" + this);
  }
  super.setInUse(inUse);
}
