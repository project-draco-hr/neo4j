{
  fsa.mkdir(testDir.directory());
  try {
    return new OnDiskGlobalSessionTrackerState<>(fsa,testDir.directory(),new RaftTestMemberMarshal(),100,mock(Supplier.class),NullLogProvider.getInstance());
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
