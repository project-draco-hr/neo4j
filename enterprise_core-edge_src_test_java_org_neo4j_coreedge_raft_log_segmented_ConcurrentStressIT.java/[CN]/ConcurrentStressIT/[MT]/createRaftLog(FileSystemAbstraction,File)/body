{
  SegmentedRaftLog raftLog=new SegmentedRaftLog(fsa,dir,8 * 1024 * 1024,new DummyRaftableContentSerializer(),NullLogProvider.getInstance(),raft_log_pruning_strategy.getDefaultValue());
  raftLog.start();
  return raftLog;
}
