{
  createIndexes();
  createUniqueIndexes();
  long totalNodeCount=populateDatabase();
  findLastNodesByLabelAndProperties(db,totalNodeCount);
  dropAllIndexes();
  createUniqueIndexes();
  createIndexes();
  findLastNodesByLabelAndProperties(db,totalNodeCount);
}
