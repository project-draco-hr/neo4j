{
  NodeOrRelationship current=this.getCurrent(session);
  out.println("Current is " + getDisplayName(getNeoServer(),session,current));
  String path=stringifyPath(Cd.readPaths(session),session);
  if (path.length() > 0) {
    out.println(path);
  }
  return null;
}
