{
  super(procedureSignature(new QualifiedName(new String[]{"dbms","cluster","routing"},NAME)).out("address",Neo4jTypes.NTString).out("role",Neo4jTypes.NTString).out("expiry",Neo4jTypes.NTInteger).build());
  this.discoveryService=discoveryService;
  this.leaderLocator=leaderLocator;
  this.log=logProvider.getLog(getClass());
}
