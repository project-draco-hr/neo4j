{
  Map<String,String> configParams=stringMap(Configuration.read_only.name(),"false",Configuration.backup_slave.name(),"false");
  return new StoreFactory(new Config(configParams),new DefaultIdGeneratorFactory(),new DefaultWindowPoolFactory(),new EphemeralFileSystemAbstraction(),StringLogger.DEV_NULL,mock(RemoteTxHook.class));
}
