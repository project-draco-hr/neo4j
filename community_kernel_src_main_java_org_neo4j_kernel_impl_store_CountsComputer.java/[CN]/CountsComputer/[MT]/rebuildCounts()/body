{
  NodeLabelsCache cache=new NodeLabelsCache(NumberArrayFactory.AUTO,highLabelId);
  try {
    superviseDynamicExecution(new NodeStoreProcessorStage("COUNT NODES",Configuration.DEFAULT,nodes,new NodeCountsProcessor(nodes,cache,highLabelId,countsTracker)));
    superviseDynamicExecution(new RelationshipCountsStage(Configuration.DEFAULT,cache,relationships,highLabelId,highRelationshipTypeId,countsTracker));
  }
  finally {
    cache.close();
  }
}
