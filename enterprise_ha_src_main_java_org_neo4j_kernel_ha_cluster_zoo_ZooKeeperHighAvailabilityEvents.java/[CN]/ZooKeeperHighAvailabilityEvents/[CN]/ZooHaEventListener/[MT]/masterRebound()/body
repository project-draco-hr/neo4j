{
  logger.getMessagesLog(getClass()).logMessage("Got master rebound");
  Listeners.notifyListeners(haListeners,new Listeners.Notification<ClusterMemberListener>(){
    @Override public void notify(    ClusterMemberListener listener){
      listener.memberIsAvailable(HighAvailabilityModeSwitcher.MASTER,client.getCachedMaster().getMachineId(),URI.create("ha://" + client.getCachedMaster().getServerAsString()));
    }
  }
);
}
