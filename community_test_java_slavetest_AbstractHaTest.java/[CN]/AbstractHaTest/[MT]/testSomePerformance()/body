{
  initializeDbs(1);
  final Job<Void> job=new CommonJobs.PerformanceCreateNodesJob(100,1);
  time(new Callable<Void>(){
    public Void call() throws Exception {
      return executeJob(job,0);
    }
  }
);
  pullUpdates();
}
