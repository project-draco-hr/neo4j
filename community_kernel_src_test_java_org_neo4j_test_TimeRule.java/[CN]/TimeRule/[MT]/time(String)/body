{
  this.name=name;
  if (time == 0) {
    time=currentTimeMillis();
  }
 else {
    long now=currentTimeMillis();
    long diff=now - time;
    System.out.println((name != null ? name + " " : "") + diff);
    time=now;
  }
}
