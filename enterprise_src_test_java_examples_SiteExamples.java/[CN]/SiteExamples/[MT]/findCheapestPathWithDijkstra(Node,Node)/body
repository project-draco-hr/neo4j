{
  PathFinder<WeightedPath> finder=GraphAlgoFactory.dijkstra(Traversal.expanderForTypes(ExampleTypes.MY_TYPE,Direction.BOTH),CommonEvaluators.doubleCostEvaluator("cost"));
  WeightedPath path=finder.findSinglePath(start,end);
  path.weight();
  return path;
}
