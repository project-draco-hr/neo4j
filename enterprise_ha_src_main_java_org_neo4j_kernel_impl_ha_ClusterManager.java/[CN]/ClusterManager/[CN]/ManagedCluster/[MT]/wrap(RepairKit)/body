{
  pendingRepairs.add(actual);
  return new RepairKit(){
    @Override public HighlyAvailableGraphDatabase repair() throws Throwable {
      try {
        return actual.repair();
      }
  finally {
        pendingRepairs.remove(actual);
      }
    }
  }
;
}
