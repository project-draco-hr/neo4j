{
  record.setIsLight(false);
  Collection<DynamicRecord> keyRecords=keyPropertyStore.getRecords(record.getKeyBlockId(),buffer);
  for (  DynamicRecord keyRecord : keyRecords) {
    record.addKeyRecord(keyRecord);
  }
}
