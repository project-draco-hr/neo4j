{
  if (min != null && value.compareTo(min) < 0)   throw new IllegalArgumentException("Minimum allowed value is:" + min);
  if (max != null && value.compareTo(max) > 0)   throw new IllegalArgumentException("Maximum allowed value is:" + max);
}
