{
  if (value == null)   throw illegalValue(locale,value);
  long val;
  try {
    val=Long.parseLong(value);
  }
 catch (  Exception e) {
    throw illegalValue(locale,value);
  }
  rangeCheck(val);
}
