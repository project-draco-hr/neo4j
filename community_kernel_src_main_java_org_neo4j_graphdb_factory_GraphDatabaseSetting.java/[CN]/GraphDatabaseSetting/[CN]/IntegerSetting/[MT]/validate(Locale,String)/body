{
  if (value == null)   throw illegalValue(locale,value,new String[0]);
  int val;
  try {
    val=Integer.parseInt(value);
  }
 catch (  Exception e) {
    throw illegalValue(locale,value,new String[0]);
  }
  rangeCheck(locale,val);
}
