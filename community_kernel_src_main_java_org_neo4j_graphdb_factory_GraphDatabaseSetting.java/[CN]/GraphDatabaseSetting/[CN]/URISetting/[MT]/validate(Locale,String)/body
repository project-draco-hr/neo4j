{
  if (value == null)   throw illegalValue(locale,null,new String[0]);
  try {
    new URI(value).normalize();
  }
 catch (  URISyntaxException e) {
    throw illegalValue(locale,value,new String[0]);
  }
}
