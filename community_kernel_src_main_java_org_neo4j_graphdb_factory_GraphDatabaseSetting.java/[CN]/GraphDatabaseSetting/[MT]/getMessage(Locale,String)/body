{
  if (locale.getLanguage().equals(Locale.ENGLISH.getLanguage()))   return defaultMessage;
  try {
    ResourceBundle bundle=ResourceBundle.getBundle(getClass().getName());
    return bundle.getString(name());
  }
 catch (  Exception e) {
    return defaultMessage;
  }
}
