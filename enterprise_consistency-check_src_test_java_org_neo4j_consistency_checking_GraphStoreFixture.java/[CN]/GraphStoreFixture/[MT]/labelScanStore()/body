{
  if (labelScanStore == null) {
    labelScanStore=new LuceneLabelScanStoreBuilder(directory().getAbsolutePath(),storeAccess.getRawNeoStore(),new DefaultFileSystemAbstraction(),StringLogger.SYSTEM).build();
  }
  return labelScanStore;
}
