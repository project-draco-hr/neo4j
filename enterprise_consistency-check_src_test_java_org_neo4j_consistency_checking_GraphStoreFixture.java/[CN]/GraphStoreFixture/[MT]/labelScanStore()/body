{
  if (labelScanStore == null) {
    labelScanStore=new LuceneLabelScanStoreBuilder(directory().getAbsolutePath(),nativeStores().getRawNeoStore(),new DefaultFileSystemAbstraction(),StringLogger.SYSTEM).build();
  }
  return labelScanStore;
}
