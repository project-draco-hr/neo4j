{
  if (indexes == null) {
    Config config=new Config(singletonMap(GraphDatabaseSettings.store_dir.name(),directory().getAbsolutePath()));
    indexes=new LuceneSchemaIndexProvider(DirectoryFactory.PERSISTENT,config);
  }
  return indexes;
}
