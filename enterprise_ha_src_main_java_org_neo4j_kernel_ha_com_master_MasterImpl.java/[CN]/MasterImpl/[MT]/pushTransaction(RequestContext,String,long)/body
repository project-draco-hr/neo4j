{
  graphDb.getTxIdGenerator().committed(graphDb.getXaDataSourceManager().getXaDataSource(resourceName),context.getEventIdentifier(),tx,context.machineId());
  return new Response<>(null,graphDb.getStoreId(),TransactionStream.EMPTY,ResourceReleaser.NO_OP);
}
