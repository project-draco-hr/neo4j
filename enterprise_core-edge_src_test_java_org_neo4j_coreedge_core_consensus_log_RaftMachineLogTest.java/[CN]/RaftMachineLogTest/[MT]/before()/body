{
  testEntryLog=new InMemoryRaftLog();
  testEntryLog.append(new RaftLogEntry(0,new RaftTestGroup(myself)));
  raft=new RaftMachineBuilder(myself,3,RaftTestMemberSetBuilder.INSTANCE).raftLog(testEntryLog).commitListener(commitListener).build();
}
