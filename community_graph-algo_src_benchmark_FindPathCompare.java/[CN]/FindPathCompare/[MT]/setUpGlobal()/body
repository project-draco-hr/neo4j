{
  super.setUpGlobal();
  RandomGraph graph1;
  SmallWorldGraph graph2;
  PreferentialAttachment graph3;
  graph1=new RandomGraph(neo,new GraphStore(neo),MyRelTypes.R1,2500,10000);
  graph=graph1;
  numberOfRuns=1000;
}
