{
  return new Runnable(){
    @Override public void run(){
      try {
        node.acquireReadLock(transaction);
      }
 catch (      DeadlockDetectedException e) {
        latch.countDown();
      }
    }
  }
;
}
