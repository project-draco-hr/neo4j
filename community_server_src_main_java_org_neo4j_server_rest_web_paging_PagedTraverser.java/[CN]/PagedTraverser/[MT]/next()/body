{
  if (!iterator.hasNext()) {
    return null;
  }
  ArrayList<Path> result=new ArrayList<Path>();
  for (int i=0; i < pageSize; i++) {
    if (!iterator.hasNext()) {
      break;
    }
 else {
      result.add(iterator.next());
    }
  }
  return result;
}
