{
  Map<Method,Invocator> methods=new HashMap<Method,Invocator>();
  for (  Method method : iface.getMethods()) {
    IndexedMethodType type=builder.getServiceSignature(method,this);
    methods.put(method,new Invocator(this,type.index,type.signature));
  }
  objects.put(id,new ObjectConverter(iface,Collections.unmodifiableMap(methods)));
}
