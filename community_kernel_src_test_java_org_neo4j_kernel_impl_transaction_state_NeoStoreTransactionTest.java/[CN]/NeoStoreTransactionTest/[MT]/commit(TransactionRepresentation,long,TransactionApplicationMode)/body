{
  LabelScanStore labelScanStore=mock(LabelScanStore.class);
  when(labelScanStore.newWriter()).thenReturn(mock(LabelScanWriter.class));
  TransactionRepresentationStoreApplier storeApplier=new TransactionRepresentationStoreApplier(mockIndexing,labelScanStore,neoStore,cacheAccessBackDoor,locks,mock(ProviderLookup.class),mock(IndexConfigStore.class),IdOrderingQueue.BYPASS);
  try (LockGroup locks=new LockGroup()){
    storeApplier.apply(recoveredTx,locks,txId,mode);
  }
 }
