{
  NeoStoreTransactionContext context=new NeoStoreTransactionContext(neoStore);
  context.bind(mock(Locks.Client.class));
  TransactionRecordState result=new TransactionRecordState(neoStore,new IntegrityValidator(neoStore,indexing),context);
  return Pair.of(result,context);
}
