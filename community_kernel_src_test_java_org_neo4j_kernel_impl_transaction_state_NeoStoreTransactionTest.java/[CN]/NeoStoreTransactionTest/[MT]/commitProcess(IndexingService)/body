{
  KernelHealth kernelHealth=mock(KernelHealth.class);
  return commitProcess(indexing,new OnlineIndexUpdatesValidator(neoStores,kernelHealth,new PropertyLoader(neoStores),indexing,IndexUpdateMode.ONLINE));
}
