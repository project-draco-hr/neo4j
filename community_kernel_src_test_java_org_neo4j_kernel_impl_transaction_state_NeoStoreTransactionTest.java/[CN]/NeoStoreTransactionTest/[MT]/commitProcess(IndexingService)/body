{
  OnlineIndexUpdatesValidator indexUpdatesValidator=new OnlineIndexUpdatesValidator(neoStore,new PropertyLoader(neoStore),indexing,IndexUpdateMode.ONLINE);
  return commitProcess(indexing,indexUpdatesValidator);
}
