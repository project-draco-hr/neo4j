{
  Node node=nodes.get(name);
  if (node == null) {
    if (nodes.size() == 0) {
      node=graphdb.createNode();
    }
 else {
      node=graphdb.createNode();
    }
    node.setProperty("name",name);
    nodes.put(name,node);
  }
  return node;
}
