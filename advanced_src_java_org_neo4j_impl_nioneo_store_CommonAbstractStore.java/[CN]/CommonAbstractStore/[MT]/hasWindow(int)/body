{
  if (!isInRecoveryMode() && (position > idGenerator.getHighId() || !storeOk)) {
    throw new IOException("Position[" + position + "] high id["+ idGenerator.getHighId()+ "] storeOk="+ storeOk);
  }
  return windowPool.hasWindow(position);
}
