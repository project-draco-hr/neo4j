{
  return new Function<GraphDatabaseService,Pair<Node,Node>>(){
    @Override public Pair<Node,Node> apply(    GraphDatabaseService graphDb){
      return Pair.of(graphDb.createNode(),graphDb.createNode());
    }
  }
;
}
