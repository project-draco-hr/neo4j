{
  return new Function<GraphDatabaseService,Relationship>(){
    @Override public Relationship apply(    GraphDatabaseService graphDb){
      return nodes.first().createRelationshipTo(nodes.other(),withName("RELATED"));
    }
  }
;
}
