{
  return new CypherPlanRepresentation(){
    private PlanDescription plan=null;
    private boolean fetched=false;
    @Override protected PlanDescription getPlan(){
      if (!fetched) {
        plan=planProvider.apply(null);
        fetched=true;
      }
      return plan;
    }
  }
;
}
