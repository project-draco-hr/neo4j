{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  if (!super.equals(o)) {
    return false;
  }
  RelationshipGroupRecord that=(RelationshipGroupRecord)o;
  return type == that.type && next == that.next && firstOut == that.firstOut && firstIn == that.firstIn && firstLoop == that.firstLoop && owningNode == that.owningNode && prev == that.prev;
}
