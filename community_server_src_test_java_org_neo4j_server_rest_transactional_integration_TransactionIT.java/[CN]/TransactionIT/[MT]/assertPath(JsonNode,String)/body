{
  assertTrue("Expected a uri matching 'http://localhost:\\d+/db/data" + path + "', "+ "but got '"+ jsonURIString.asText()+ "'.",jsonURIString.asText().matches("http://localhost:\\d+/db/data" + path));
}
