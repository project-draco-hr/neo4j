{
  Response begin=http.POST("/db/data/transaction",quotedJson("{ 'statements': [ { 'statement': 'USING AUTOCOMMIT CREATE ()' } ] }"));
  assertThat(begin,hasErrors(Status.Statement.InvalidSemantics));
}
