{
  Response begin=http.POST("/db/data/transaction",quotedJson("{ 'statements': [ { 'statement': 'USING PERIODIC COMMIT CREATE ()' } ] }"));
  assertThat(begin,hasErrors(Status.Statement.InvalidSemantics));
}
