{
  try {
    return rmi().invokeTransactionalServiceMethod(transactionId,new Callback(callback),serviceId,functionIndex,arguments);
  }
 catch (  RemoteException ex) {
    throw remoteException(ex);
  }
}
