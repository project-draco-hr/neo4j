{
  KernelTransactionImplementation transaction=mock(KernelTransactionImplementation.class);
  when(transaction.terminationReason()).thenReturn(Status.General.UnknownFailure);
  KernelStatement statement=new KernelStatement(transaction,mock(IndexReaderFactory.class),mock(LabelScanStore.class),null,null,null,null);
  statement.readOperations().nodeExists(0);
}
