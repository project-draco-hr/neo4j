{
  StorageStatement storeStatement=mock(StorageStatement.class);
  KernelStatement statement=new KernelStatement(mock(KernelTransactionImplementation.class),null,null,null,storeStatement,new Procedures());
  statement.forceClose();
  verify(storeStatement).close();
}
