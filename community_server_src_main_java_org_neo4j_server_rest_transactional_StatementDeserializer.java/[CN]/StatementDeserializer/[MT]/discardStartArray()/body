{
  JsonToken token=input.nextToken();
  if (token == null) {
    return false;
  }
  if (token != START_ARRAY) {
    throw new InvalidRequestError("Unable to deserialize request, expected " + START_ARRAY + ", "+ "found "+ token+ ".");
  }
  return true;
}
