{
  long code=0;
  for (int i=0; i < length; i++) {
    long val=bytes[(i * order) % length];
    for (int k=1; k <= i; k++) {
      long prev=val;
      val=((val << 4) + prev);
    }
    code+=val;
  }
  return (int)code;
}
