{
  String[] cmdarray=new String[]{"jmap","-dump:file=" + new File(dir,"heapdump-" + pid.other() + "-"+ currentTimeMillis()).getAbsolutePath(),"" + pid.first()};
  getRuntime().exec(cmdarray).waitFor();
}
