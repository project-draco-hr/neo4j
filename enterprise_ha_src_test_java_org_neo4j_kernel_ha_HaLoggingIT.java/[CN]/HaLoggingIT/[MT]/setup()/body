{
  cluster=clusterRule.withCluster(clusterWithAdditionalClients(2,1)).withAvailabilityChecks(masterAvailable(),masterSeesMembers(3),allSeesAllAsJoined()).withSharedSetting(logs_directory,clusterRule.directory("logs").getAbsolutePath()).startCluster();
}
