{
  int count=0;
  for (  String line : asIterable(new File(cluster.getStoreDir(db),StoreLogService.INTERNAL_LOG_NAME),StandardCharsets.UTF_8)) {
    if (line.endsWith(toLookFor)) {
      count++;
    }
  }
  return count;
}
