{
  resume();
  SimpleIterator<String> strings;
  try {
    strings=server.getRelationshipPropertyKeys(connection.graphDb,relationshipId);
  }
 catch (  RuntimeException ex) {
    return response().buildErrorResponse(ex);
  }
  return keys(strTokenPool++,strings);
}
