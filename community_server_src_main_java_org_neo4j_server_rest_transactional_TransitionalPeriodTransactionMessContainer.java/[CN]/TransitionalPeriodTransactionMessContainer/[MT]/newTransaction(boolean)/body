{
  Transaction tx=db.beginTransaction(implicitTransaction ? KernelTransaction.Type.implicit : KernelTransaction.Type.explicit);
  return new TransitionalTxManagementKernelTransaction(new TransactionTerminator(tx),txBridge);
}
