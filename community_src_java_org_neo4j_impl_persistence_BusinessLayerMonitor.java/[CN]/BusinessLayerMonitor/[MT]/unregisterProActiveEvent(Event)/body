{
  try {
    EventManager.getManager().unregisterProActiveEventListener(this,event);
  }
 catch (  EventListenerNotRegisteredException elnre) {
    elnre.printStackTrace();
    log.warning("Failed to unregister ourselves as a listener on " + event + " events because we weren't registered.");
  }
}
