{
  Args args=Args.withFlags().parse();
  Collection<String> interpreted=args.interpretOptions("something",Converters.<String>optional(),Functions.<String>identity());
  assertTrue(interpreted.isEmpty());
}
