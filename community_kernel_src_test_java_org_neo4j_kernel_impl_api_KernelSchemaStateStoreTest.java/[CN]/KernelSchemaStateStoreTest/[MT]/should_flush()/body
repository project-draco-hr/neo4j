{
  stateStore.apply(MapUtil.stringMap("key","created_value"));
  stateStore.clear();
  String result=stateStore.get("key");
  assertEquals(null,result);
  logProvider.assertExactly(inLog(KernelSchemaStateStore.class).info("Schema state store has been cleared."));
}
