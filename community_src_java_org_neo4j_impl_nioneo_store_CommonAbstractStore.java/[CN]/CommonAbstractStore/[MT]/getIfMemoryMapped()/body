{
  if (getConfig() != null) {
    String useMemMapped=(String)getConfig().get("use_memory_mapped_buffers");
    if (useMemMapped != null && useMemMapped.toLowerCase().equals("false")) {
      return false;
    }
  }
  return true;
}
