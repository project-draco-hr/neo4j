{
  if (list.size() == length) {
    for (int i=0; i < length; ++i) {
      if (!list.get(i).matches(resultList.apply(i))) {
        return false;
      }
    }
    return true;
  }
  return false;
}
