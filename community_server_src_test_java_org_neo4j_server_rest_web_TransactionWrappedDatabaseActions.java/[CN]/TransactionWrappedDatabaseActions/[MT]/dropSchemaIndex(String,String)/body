{
  Transaction transaction=graph.beginTx();
  try {
    boolean result=super.dropSchemaIndex(labelName,propertyKey);
    transaction.success();
    return result;
  }
  finally {
    transaction.finish();
  }
}
