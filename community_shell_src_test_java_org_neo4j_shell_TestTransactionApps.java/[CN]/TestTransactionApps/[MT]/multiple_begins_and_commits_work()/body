{
  executeCommand("begin transaction");
  executeCommand("begin transaction");
  executeCommand("begin transaction");
  executeCommand("commit");
  executeCommand("commit");
  executeCommand("commit");
  assertWeAreNotInATransaction();
}
