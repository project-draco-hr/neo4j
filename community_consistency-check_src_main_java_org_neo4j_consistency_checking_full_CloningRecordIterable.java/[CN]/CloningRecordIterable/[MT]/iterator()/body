{
  return new PrefetchingIterator<R>(){
    private final Iterator<R> actualIterator=actual.iterator();
    @Override protected R fetchNextOrNull(){
      return actualIterator.hasNext() ? (R)actualIterator.next().clone() : null;
    }
  }
;
}
