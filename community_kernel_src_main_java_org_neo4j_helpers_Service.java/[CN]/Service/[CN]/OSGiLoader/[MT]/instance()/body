{
  if (instance == null) {
    try {
      @SuppressWarnings("unchecked") Class<? extends OSGiLoader> loaderClass=(Class<? extends OSGiLoader>)Class.forName("org.neo4j.helpers.OSGiServiceLoader");
      instance=loaderClass.newInstance();
    }
 catch (    LinkageError err) {
    }
catch (    Exception ex) {
    }
    if (instance == null) {
      instance=new OSGiLoader();
    }
  }
  return instance;
}
