{
  int toFill=min(dst.remaining(),bytesLeftToProduce), leftToFill=toFill;
  if (toFill <= 0)   return -1;
  while (leftToFill-- > 0)   dst.put((byte)5);
  bytesLeftToProduce-=toFill;
  return toFill;
}
