{
  if (!inUse)   return DELETE;
  if (created && inUse)   return CREATE;
  if (!created && inUse)   return UPDATE;
  throw new IllegalStateException("A record can't be both created and deleted at the same time.");
}
