{
  if (haDb == null) {
    return Response.status(Response.Status.FORBIDDEN).build();
  }
  String masterURI=getMasterUriAsString();
  return Response.status(Response.Status.SEE_OTHER).entity(Boolean.toString(false).getBytes()).header(HttpHeaders.LOCATION,masterURI).build();
}
