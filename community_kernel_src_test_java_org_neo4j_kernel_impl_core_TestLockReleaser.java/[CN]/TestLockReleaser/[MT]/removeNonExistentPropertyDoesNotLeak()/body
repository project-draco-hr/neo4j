{
  Node n=getGraphDb().createNode();
  n.removeProperty("dummy");
  NodeImpl primitive=getEmbeddedGraphDb().getConfig().getGraphDbModule().getNodeManager().getNodeForProxy(n.getId());
  assertNull(getEmbeddedGraphDb().getConfig().getLockReleaser().getCowPropertyRemoveMap(primitive));
  getTransaction().finish();
}
