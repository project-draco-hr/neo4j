{
  return Iterables.filter(new Predicate<InstanceId>(){
    @Override public boolean accept(    InstanceId item){
      return !isFailed(item);
    }
  }
,commonState.configuration().getMemberIds());
}
