{
  String json=JsonHelper.createJsonFrom(params);
  ClientResponse response=CLIENT.resource(url).accept(MediaType.APPLICATION_JSON_TYPE).entity(json,MediaType.APPLICATION_JSON_TYPE).post(ClientResponse.class);
  String body=getResponseText(response);
  response.close();
  return deserializeList(body);
}
