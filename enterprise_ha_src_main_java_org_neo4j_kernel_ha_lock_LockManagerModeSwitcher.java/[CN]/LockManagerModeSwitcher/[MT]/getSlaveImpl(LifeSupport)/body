{
  SlaveLockManager lockManager=new SlaveLockManager(locksFactory.newInstance(),requestContextFactory,master.cement(),availabilityGuard,logProvider,config);
  return life.add(maybeWrapWithDeferringLockManager(config,lockManager));
}
