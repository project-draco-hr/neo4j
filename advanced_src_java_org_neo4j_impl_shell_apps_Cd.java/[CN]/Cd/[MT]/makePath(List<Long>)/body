{
  StringBuffer buffer=new StringBuffer();
  for (  Long id : paths) {
    if (buffer.length() > 0) {
      buffer.append(",");
    }
    buffer.append(id);
  }
  return buffer.length() > 0 ? buffer.toString() : null;
}
