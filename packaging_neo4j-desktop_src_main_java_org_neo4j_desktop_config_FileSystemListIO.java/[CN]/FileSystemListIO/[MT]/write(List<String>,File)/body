{
  PrintStream writer=null;
  try {
    writer=new PrintStream(file,ENCODING);
    for (    String line : list) {
      writer.println(line);
    }
  }
  finally {
    if (writer != null) {
      writer.close();
    }
  }
}
