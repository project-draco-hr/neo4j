{
  try (SchemaIndex schemaIndex=LuceneSchemaIndexBuilder.create().withConfig(getReadOnlyConfig()).withOperationalMode(OperationalMode.single).withIndexRootFolder(testDir.graphDbDir()).withIndexIdentifier("a").build()){
    assertTrue("Builder should construct read only index.",schemaIndex.isReadOnly());
  }
 }
