{
  authenticate(user,"Igor","123",null);
  user.send(TransportTestUtil.chunk(run("CALL dbms.listSessions() YIELD username, sessionCount"),pullAll()));
  assertThat(user,eventuallyRecieves(msgFailure(Status.Security.Forbidden,AuthProcedures.PERMISSION_DENIED)));
}
