{
  Constructor<InternalExecutionResult> constructor=clazz.getDeclaredConstructor(Statement.class,GraphDatabaseService.class,InternalPlanDescription.class);
  return constructor.newInstance(statement,db,description);
}
