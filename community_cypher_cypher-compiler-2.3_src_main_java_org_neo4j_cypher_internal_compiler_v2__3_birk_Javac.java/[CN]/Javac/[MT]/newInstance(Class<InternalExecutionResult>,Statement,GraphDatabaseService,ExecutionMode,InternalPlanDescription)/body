{
  Constructor<InternalExecutionResult> constructor=clazz.getDeclaredConstructor(Statement.class,GraphDatabaseService.class,ExecutionMode.class,InternalPlanDescription.class);
  return constructor.newInstance(statement,db,executionMode,description);
}
