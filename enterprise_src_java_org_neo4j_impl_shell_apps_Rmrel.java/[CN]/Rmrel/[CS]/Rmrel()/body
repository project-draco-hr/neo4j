{
  this.addValueType("r",new OptionContext(OptionValueType.MUST,"The relationship id."));
  this.addValueType("d",new OptionContext(OptionValueType.NONE,"Must be supplied if the affected other node gets decoupled\n" + "after this operation so that it gets deleted."));
}
