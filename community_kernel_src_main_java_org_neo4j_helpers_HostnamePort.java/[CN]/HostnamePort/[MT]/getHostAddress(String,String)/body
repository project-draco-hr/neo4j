{
  if (host == null) {
    return defaultHost;
  }
  try {
    InetAddress ip=InetAddress.getByName(host);
    if (ip == null) {
      return defaultHost;
    }
    return ip.getHostAddress();
  }
 catch (  UnknownHostException e) {
    return host;
  }
}
