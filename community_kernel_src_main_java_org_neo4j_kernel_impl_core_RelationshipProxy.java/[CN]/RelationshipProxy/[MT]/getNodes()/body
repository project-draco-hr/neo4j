{
  assertInTransaction();
  RelationshipData data=relationshipLookups.getRelationshipData(relId);
  return new Node[]{relationshipLookups.newNodeProxy(data.getStartNode()),relationshipLookups.newNodeProxy(data.getEndNode())};
}
