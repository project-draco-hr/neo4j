{
  assertInUnterminatedTransaction();
  RelationshipData data=actions.getRelationshipData(relId);
  return new Node[]{actions.newNodeProxy(data.getStartNode()),actions.newNodeProxy(data.getEndNode())};
}
