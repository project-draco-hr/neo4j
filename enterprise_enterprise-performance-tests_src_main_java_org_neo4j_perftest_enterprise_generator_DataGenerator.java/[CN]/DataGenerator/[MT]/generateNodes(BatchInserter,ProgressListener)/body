{
  batchInserter.setNodeProperties(0,generate(nodeProperties));
  for (int i=1; i < nodeCount; i++) {
    batchInserter.createNode(generate(nodeProperties));
    progressListener.set(i);
  }
  progressListener.done();
}
