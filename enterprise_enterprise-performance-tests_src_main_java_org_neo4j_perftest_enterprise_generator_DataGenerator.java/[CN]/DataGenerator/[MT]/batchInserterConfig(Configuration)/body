{
  Map<String,String> config=new HashMap<>();
  config.put("dump_configuration","true");
  config.put(GraphDatabaseSettings.pagecache_memory.name(),configuration.get(pagecache_memory));
  return config;
}
