{
  Map<String,String> config=new HashMap<>();
  config.put("dump_configuration","true");
  config.put(GraphDatabaseSettings.mapped_memory_total_size.name(),configuration.get(mapped_memory_total_size));
  return config;
}
