{
  LuceneSchemaIndexBuilder builder=LuceneSchemaIndexBuilder.create();
  if (unique) {
    builder=builder.uniqueIndex();
  }
  return builder.withIndexRootFolder(new File("/graph.db")).withDirectoryFactory(dirFactory).withFileSystem(fs.get()).withIndexIdentifier("testIndex").build();
}
