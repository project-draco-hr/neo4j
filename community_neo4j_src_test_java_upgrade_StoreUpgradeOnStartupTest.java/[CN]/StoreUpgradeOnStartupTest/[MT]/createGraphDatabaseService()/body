{
  return new GraphDatabaseFactory().newEmbeddedDatabaseBuilder(workingDirectory.getPath()).setConfig(Collections.singletonMap(GraphDatabaseSettings.allow_store_upgrade.name(),"true")).newGraphDatabase();
}
