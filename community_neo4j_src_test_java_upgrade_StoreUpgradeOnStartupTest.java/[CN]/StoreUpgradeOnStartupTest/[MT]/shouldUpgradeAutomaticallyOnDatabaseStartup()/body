{
  GraphDatabaseService database=createGraphDatabaseService();
  database.shutdown();
  assertTrue("Some store files did not have the correct version",allStoreFilesHaveVersion(pageCache,workingDirectory,ALL_STORES_VERSION));
  assertConsistentStore(workingDirectory);
}
