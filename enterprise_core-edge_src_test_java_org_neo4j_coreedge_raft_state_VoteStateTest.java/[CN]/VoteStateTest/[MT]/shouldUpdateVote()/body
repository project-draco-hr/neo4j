{
  VoteState<CoreMember> voteState=new VoteState<>();
  CoreMember member1=new CoreMember(new AdvertisedSocketAddress("host1:1001"),new AdvertisedSocketAddress("host1:2001"),new AdvertisedSocketAddress("host1:3001"));
  CoreMember member2=new CoreMember(new AdvertisedSocketAddress("host2:1001"),new AdvertisedSocketAddress("host2:2001"),new AdvertisedSocketAddress("host2:3001"));
  voteState.update(member1,0);
  voteState.update(member2,1);
  assertEquals(member2,voteState.votedFor());
}
