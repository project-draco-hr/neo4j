{
  return new Predicate<StackTraceElement>(){
    @Override public boolean test(    StackTraceElement element){
      return element.getClassName().equals(klass.getName()) && element.getMethodName().equals(methodName);
    }
  }
;
}
