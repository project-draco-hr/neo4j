{
  assertValidName(username);
  User user=new User.Builder().withName(username).withGroup(group).withCredentials(Credential.forPassword(initialPassword)).withRequiredPasswordChange(requirePasswordChange).build();
  userRepository.create(user);
  return user;
}
