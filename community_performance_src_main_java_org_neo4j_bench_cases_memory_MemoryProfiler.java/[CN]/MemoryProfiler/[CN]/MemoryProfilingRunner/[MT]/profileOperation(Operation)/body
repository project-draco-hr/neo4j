{
  operaration.setUp();
  try {
    sampler.startProfiling();
    operaration.invoke();
    sampler.stopProfiling();
  }
  finally {
    operaration.tearDown();
  }
  return sampler.createReport();
}
