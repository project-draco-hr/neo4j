{
  String pluginsDirectory=model.getPluginsDirectory().getAbsolutePath();
  return withFlowLayout(withTitledBorder("Plugins and Extensions",createPanel(createUnmodifiableTextField(pluginsDirectory),openPluginsDirectoryButton)));
}
