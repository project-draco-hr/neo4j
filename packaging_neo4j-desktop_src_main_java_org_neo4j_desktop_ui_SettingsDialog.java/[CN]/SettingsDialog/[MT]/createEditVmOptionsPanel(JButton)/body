{
  File vmOptionsFile=model.getVmOptionsFile();
  String vmOptionsPath=vmOptionsFile == null ? "" : vmOptionsFile.getAbsolutePath();
  return withFlowLayout(withTitledBorder("Java VM Options",createPanel(createUnmodifiableTextField(vmOptionsPath),editVmOptionsButton)));
}
