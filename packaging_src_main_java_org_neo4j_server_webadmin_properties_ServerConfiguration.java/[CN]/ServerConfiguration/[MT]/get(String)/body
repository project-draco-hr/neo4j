{
  for (  ServerPropertyRepresentation prop : properties) {
    if (prop.getKey().equals(key)) {
      return prop;
    }
  }
  throw new NoSuchPropertyException("Property with key '" + key + "' does not exist.");
}
