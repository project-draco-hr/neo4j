{
  try {
    fileChannelLock.release();
  }
  finally {
    try {
      fileChannel.close();
    }
  finally {
      if (!FileUtils.deleteFile(lockFile)) {
        Files.delete(lockFile.toPath());
      }
    }
  }
}
