{
  if (lower == null || upper == null) {
    return false;
  }
 else {
    int cmp=compare(lower,upper);
    if (includeLower && includeUpper) {
      return cmp > 0;
    }
 else {
      return cmp >= 0;
    }
  }
}
