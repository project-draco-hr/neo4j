{
  while (true) {
    int refs=refCount.get();
    if (refs > 0 && refs < MAX_HOLDERS) {
      if (refCount.compareAndSet(refs,refs + 1)) {
        return true;
      }
    }
 else {
      return false;
    }
  }
}
