{
  while (null == (current=readTransaction(entries))) {
    if (entries != null) {
      entries.close();
    }
    if (null == (entries=openNext())) {
      return false;
    }
  }
  return true;
}
