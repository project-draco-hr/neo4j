{
  return new LogDeserializer().logEntries(new ReadAheadLogChannel(new PhysicalLogVersionedStoreChannel(channel,header.logVersion,header.logFormatVersion),NO_MORE_CHANNELS,DEFAULT_READ_AHEAD_SIZE));
}
