{
  Header instance=header;
  if (instance == null) {
synchronized (GraphStyle.class) {
      instance=header;
      if (instance == null) {
        header=instance=new Header();
      }
    }
  }
  return instance;
}
