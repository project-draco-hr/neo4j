{
  if (edgeFormat != null) {
    stream.append(edgeFormat.format(key,type,value) + "\\l");
  }
 else {
    stream.append(key + " = " + PropertyType.format(value)+ " : "+ type.typeName+ "\\l");
  }
}
