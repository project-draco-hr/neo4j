{
  IndexUpdatesValidator validator=mock(IndexUpdatesValidator.class);
  when(validator.validate(any(TransactionRepresentation.class),any(TransactionApplicationMode.class))).thenReturn(ValidatedIndexUpdates.NONE);
  return validator;
}
