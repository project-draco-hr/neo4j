{
  Monitors monitors=new Monitors();
  ThreadedSessions sessions=new ThreadedSessions(mock(Sessions.class),mock(JobScheduler.class),NullLogService.getInstance());
  Session session=sessions.newSession();
  session.run("hello",null,null,null);
}
