{
  GraphDatabaseQueryService queryService=mock(GraphDatabaseQueryService.class);
  return new TransactionHandle(kernel,executionEngine,queryService,registry,uriScheme,true,FULL,anyLong(),NullLogProvider.getInstance());
}
