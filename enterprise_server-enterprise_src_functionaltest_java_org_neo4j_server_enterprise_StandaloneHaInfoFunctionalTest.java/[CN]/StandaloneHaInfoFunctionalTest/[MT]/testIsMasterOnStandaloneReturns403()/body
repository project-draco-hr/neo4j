{
  FunctionalTestHelper helper=new FunctionalTestHelper(server());
  JaxRsResponse response=RestRequest.req().get(helper.managementUri() + MasterInfoService.BASE_PATH + MasterInfoService.ISMASTER_PATH);
  assertEquals(Response.SC_FORBIDDEN,response.getStatus());
}
