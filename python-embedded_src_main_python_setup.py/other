from distutils.core import setup, Command
from os.path import abspath, dirname, join as pathjoin
if (__name__ == '__main__'):
    pom = pathjoin(dirname(dirname(dirname(dirname(abspath(__file__))))), 'pom.xml')
    dependencies(pom)
