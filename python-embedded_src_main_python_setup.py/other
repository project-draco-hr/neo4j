from distutils.core import setup
if (__name__ == '__main__'):
    setup(name='neo4j-embedded', version='0.2', description='Bindings for the embedded version of the neo4j graph database.', author='Neo Technology', packages=['neo4j', 'neo4j.javalib'], package_data={'': ['*.jar'], })
