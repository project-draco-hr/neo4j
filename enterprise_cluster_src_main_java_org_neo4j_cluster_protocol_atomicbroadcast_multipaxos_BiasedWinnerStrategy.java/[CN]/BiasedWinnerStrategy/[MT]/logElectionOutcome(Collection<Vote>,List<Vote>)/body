{
  String electionOutcome=String.format("Election: received votes %s, eligible votes %s (Instance #%s has been %s)",votes,eligibleVotes,biasedNode,nodePromoted ? "promoted" : "demoted");
  electionContext.getLogger(getClass()).debug(electionOutcome);
}
