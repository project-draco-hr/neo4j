{
  try {
    checkPoint(graphdb);
    setSuccess(graphdb,true);
  }
 catch (  Exception e) {
    setSuccess(graphdb,false);
    throw Exceptions.launderedException(e);
  }
 finally {
    checkPointDone();
  }
}
