{
  if (changes.isEmpty()) {
    return store.dataProvider();
  }
 else {
    return new KeyValueMerger(store.dataProvider(),new UpdateProvider(sortedUpdates()),keys.keySize(),keys.valueSize());
  }
}
