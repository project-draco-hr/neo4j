{
  super(hostNameOrIp,port,logger,storeId,Protocol.DEFAULT_FRAME_LENGTH,SlaveServer.APPLICATION_PROTOCOL_VERSION,HaSettings.read_timeout.valueOf(getDefault(read_timeout,HaSettings.class),null),maxConcurrentChannels,maxConcurrentChannels,chunkSize);
  this.machineId=machineId;
}
