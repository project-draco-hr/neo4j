{
  try {
    webServer.removeJAXRSPackages(listFrom(new String[]{Configurator.REST_API_PACKAGE}),restApiUri().toString());
    tearDownRequestTimeLimit();
    unloadPlugins();
  }
 catch (  URISyntaxException e) {
    log.warn(e);
  }
}
