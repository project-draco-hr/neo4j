{
  StringBuilder result=new StringBuilder();
  for (  HighlyAvailableGraphDatabase member : getAllMembers())   result.append(result.length() > 0 ? "," : "").append(":" + member.getDependencyResolver().resolveDependency(ClusterClient.class).getServerUri().getPort());
  return result.toString();
}
