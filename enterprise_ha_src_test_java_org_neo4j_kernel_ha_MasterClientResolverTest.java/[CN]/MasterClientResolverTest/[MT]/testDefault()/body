{
  MasterClientResolver resolver=new MasterClientResolver(StringLogger.SYSTEM,0,0,0);
  assertEquals("default was not the latest one",MasterClientResolver.F18.class,resolver.getDefault().getClass());
}
