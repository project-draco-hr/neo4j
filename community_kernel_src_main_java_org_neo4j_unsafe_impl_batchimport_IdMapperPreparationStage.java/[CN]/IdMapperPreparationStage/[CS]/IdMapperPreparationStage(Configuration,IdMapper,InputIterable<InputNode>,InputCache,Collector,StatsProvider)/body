{
  super("Prepare node index",config,false);
  add(new IdMapperPreparationStep(control(),config,idMapper,idsOf(nodes.supportsMultiplePasses() ? nodes : inputCache.nodes()),collector,memoryUsageStats));
}
