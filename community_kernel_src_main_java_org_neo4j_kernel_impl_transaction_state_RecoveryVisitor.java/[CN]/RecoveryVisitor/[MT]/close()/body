{
  if (lastTransactionIdApplied != -1) {
    store.setLastCommittedAndClosedTransactionId(lastTransactionIdApplied,lastTransactionChecksum,lastTransactionCommitTimestamp,lastTransactionLogPosition.getByteOffset(),lastTransactionLogPosition.getLogVersion());
  }
}
