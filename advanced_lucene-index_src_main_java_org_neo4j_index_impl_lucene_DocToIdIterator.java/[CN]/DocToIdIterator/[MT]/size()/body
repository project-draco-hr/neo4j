{
  if (closed) {
    return served;
  }
  Long result=getNextOrNullFromSource();
  while (result != null) {
    spool.add(result);
    served++;
    result=getNextOrNullFromSource();
  }
  return served;
}
