{
  long time=System.currentTimeMillis();
  String name=quorumBean.getName();
  int size=quorumBean.getQuorumSize();
  return String.format("name=%s, size=%s, time=%s (+%sms)",name,size,format(time),System.currentTimeMillis() - time);
}
