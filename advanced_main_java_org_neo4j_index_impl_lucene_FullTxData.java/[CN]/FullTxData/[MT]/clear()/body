{
  try {
    if (writer != null) {
      writer.deleteAll();
    }
    invalidateSearcher();
    this.extraQueries=null;
    return this;
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
