{
  Role currentRole=raftMachine.currentRole();
  if (!currentRole.equals(Role.LEADER)) {
    throw new WriteOperationsNotAllowedException(String.format("No write operations are allowed on this database. Writes are only permitted on " + "the leader and the role of this server is: %s",currentRole),Status.Cluster.NotALeader);
  }
}
