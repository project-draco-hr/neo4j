{
  return life.add(new MasterClient201(hostNameOrIp,port,logging,storeId,readTimeoutSeconds,lockReadTimeout,maxConcurrentChannels,chunkSize,monitors.newMonitor(ByteCounterMonitor.class,MasterClient201.class),monitors.newMonitor(RequestMonitor.class,MasterClient201.class)));
}
