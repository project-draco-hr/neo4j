{
  Set<Long> addedLabels=state.getAddedLabels(nodeId,false);
  Set<Long> removedLabels=state.getRemovedLabels(nodeId,true);
  if (addedLabels != null)   addedLabels.remove(labelId);
  if (!removedLabels.add(labelId))   return;
}
