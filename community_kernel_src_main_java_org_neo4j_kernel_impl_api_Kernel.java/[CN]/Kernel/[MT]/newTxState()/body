{
  return new TxState(new OldTxStateBridgeImpl(transactionManager.getTransactionState()),persistenceManager,new TxState.IdGeneration(){
    @Override public long newSchemaRuleId(){
      return neoStore.getSchemaStore().nextId();
    }
  }
);
}
