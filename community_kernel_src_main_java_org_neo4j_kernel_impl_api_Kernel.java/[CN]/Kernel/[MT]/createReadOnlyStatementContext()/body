{
  StatementContext result=new StoreStatementContext(propertyIndexManager,nodeManager,neoStore,indexService,new IndexReaderFactory.Caching(indexService));
  result=new CachingStatementContext(result,persistenceCache,schemaCache);
  result=new ReadOnlyStatementContext(result);
  result=createSchemaStateStatementContext(result);
  return result;
}
