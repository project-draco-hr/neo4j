{
  int count=0;
  for (long i=files.getHighestHistoryLogVersion(); i >= 0; i--) {
    if (fs.fileExists(files.getHistoryFileName(i))) {
      count++;
    }
 else {
      break;
    }
  }
  return count;
}
