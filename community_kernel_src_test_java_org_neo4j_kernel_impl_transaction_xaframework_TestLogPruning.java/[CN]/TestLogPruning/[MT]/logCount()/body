{
  int count=0;
  for (long i=files.getHighestLogVersion(); i >= 0; i--) {
    if (fs.fileExists(files.getVersionFileName(i))) {
      count++;
    }
 else {
      break;
    }
  }
  return count;
}
