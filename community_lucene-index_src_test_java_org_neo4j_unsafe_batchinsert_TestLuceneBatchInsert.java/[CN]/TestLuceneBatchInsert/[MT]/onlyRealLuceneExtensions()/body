{
  return new Predicate<KernelExtensionFactory>(){
    @Override public boolean accept(    KernelExtensionFactory extension){
      if (extension instanceof InMemoryLabelScanStoreExtension || extension instanceof InMemoryIndexProviderFactory) {
        return false;
      }
      return true;
    }
  }
;
}
