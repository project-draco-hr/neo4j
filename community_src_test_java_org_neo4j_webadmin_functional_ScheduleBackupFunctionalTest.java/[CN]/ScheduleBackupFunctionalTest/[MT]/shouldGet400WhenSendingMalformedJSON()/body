{
  Client client=Client.create();
  WebResource createResource=client.resource(TestUtil.SERVER_BASE + BackupService.ROOT_PATH + BackupService.JOBS_PATH);
  String badJSON="this:::isNot::JSON}";
  ClientResponse scheduleResponse=createResource.type(MediaType.APPLICATION_JSON).accept(MediaType.APPLICATION_JSON).entity(badJSON).put(ClientResponse.class);
  System.out.println(scheduleResponse);
  assertEquals(400,scheduleResponse.getStatus());
}
