{
  setUpNode(42,1,2,3);
  PropertyLoader loader=new PropertyLoader(neoStores);
  loader.nodeLoadProperties(42,receiver);
  assertEquals(asList(intProperty(PROP_KEY_ID,1),intProperty(PROP_KEY_ID,2),intProperty(PROP_KEY_ID,3)),toList(receiver));
}
