{
  if (!accessMode.allowsWrites()) {
    throw new AuthorizationViolationException(String.format("Write operations are not allowed for `%s` transactions.",accessMode.name()));
  }
  transactionType=transactionType.enableDataWriteTransaction();
}
