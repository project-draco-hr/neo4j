{
  if (!accessMode.allowsWrites()) {
    throw new InvalidTransactionTypeKernelException(String.format("Write operations are not allowed for `%s` transactions.",accessMode.name()));
  }
  transactionType=transactionType.enableDataWriteTransaction();
}
