{
  if (!accessMode.allowsReads()) {
    throw new IllegalStateException(String.format("Read operations are not allowed for `%s` transactions.",accessMode.name()));
  }
  transactionType.enableReadTransaction();
}
