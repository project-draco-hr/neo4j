{
  assertTransactionOpen();
  if (currentStatement == null) {
    currentStatement=new KernelStatement(this,this,locks,operations,storageEngine.storeReadLayer().acquireStatement());
  }
  currentStatement.acquire();
  return currentStatement;
}
