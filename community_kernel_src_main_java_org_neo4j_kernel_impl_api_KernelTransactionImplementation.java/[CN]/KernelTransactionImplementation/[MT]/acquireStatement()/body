{
  assertTransactionOpen();
  if (currentStatement == null) {
    currentStatement=new KernelStatement(this,this,locks,operations,storeLayer.acquireStatement(),procedures);
  }
  currentStatement.acquire();
  return currentStatement;
}
