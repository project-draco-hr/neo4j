{
  assertOpen();
  if (currentStatement == null) {
    currentStatement=new KernelStatement(this,new IndexReaderFactory.Caching(indexService),labelScanStore,this,lockHolder,legacyKernelOperations,operations,persistenceManager.getResource());
  }
  currentStatement.acquire();
  return currentStatement;
}
