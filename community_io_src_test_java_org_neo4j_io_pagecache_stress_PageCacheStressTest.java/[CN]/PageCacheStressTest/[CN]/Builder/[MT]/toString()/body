{
  return format("Parameters:%n" + " - numberOfPages = %d%n" + " - recordsPerPage = %d%n"+ " - numberOfThreads = %d%n"+ " - numberOfCachePAges = %d%n"+ " - cachePagePadding = %d%n",numberOfPages,recordsPerPage,numberOfThreads,numberOfCachePages,cachePagePadding);
}
