{
  return new Register.ObjectRegister<T>(){
    private T value=initialValue;
    @Override public T read(){
      return value;
    }
    @Override public void write(    T value){
      this.value=value;
    }
  }
;
}
