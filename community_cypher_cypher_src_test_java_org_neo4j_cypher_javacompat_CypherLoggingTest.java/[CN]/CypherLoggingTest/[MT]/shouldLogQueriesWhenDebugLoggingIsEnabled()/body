{
  debugEnabled=true;
  engine.execute("CREATE (n:Reference) CREATE (foo {test:'me'}) RETURN n");
  engine.execute("START n=node(*) RETURN n");
  assertEquals("CREATE (n:Reference) CREATE (foo {test:'me'}) RETURN n" + LINE_SEPARATOR + "START n=node(*) RETURN n"+ LINE_SEPARATOR,logger.toString());
}
