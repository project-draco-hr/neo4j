{
  debugEnabled=true;
  engine.execute("CREATE (n:Reference) CREATE (foo {test:'me'}) RETURN n");
  engine.execute("MATCH n RETURN n");
  assertEquals("CREATE (n:Reference) CREATE (foo {test:'me'}) RETURN n" + LINE_SEPARATOR + "MATCH n RETURN n"+ LINE_SEPARATOR,logger.toString());
}
