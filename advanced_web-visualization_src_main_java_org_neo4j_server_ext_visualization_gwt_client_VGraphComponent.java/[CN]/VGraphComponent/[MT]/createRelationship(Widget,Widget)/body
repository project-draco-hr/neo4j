{
  String type=new String[]{"LIKES","EATS","FEARS"}[Random.nextInt(3)];
  Relationship edge=new Relationship(this,node1,node2,type);
  mapRelationship(node1,edge,edgesFrom);
  mapRelationship(node2,edge,edgesTo);
  return edge;
}
