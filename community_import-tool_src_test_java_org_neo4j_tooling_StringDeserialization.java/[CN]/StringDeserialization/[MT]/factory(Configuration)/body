{
  return new Function<SourceTraceability,Deserialization<String>>(){
    @Override public Deserialization<String> apply(    SourceTraceability from) throws RuntimeException {
      return new StringDeserialization(config);
    }
  }
;
}
