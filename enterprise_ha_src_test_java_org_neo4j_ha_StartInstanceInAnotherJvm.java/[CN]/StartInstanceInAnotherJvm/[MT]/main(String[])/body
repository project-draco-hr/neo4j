{
  String dir=args[0];
  GraphDatabaseAPI newSlave=(GraphDatabaseAPI)new TestHighlyAvailableGraphDatabaseFactory().newHighlyAvailableDatabaseBuilder(dir).setConfig(Args.parse(args).asMap()).newGraphDatabase();
}
