{
  LazyIndexUpdates updates=new LazyIndexUpdates(nodeStore,propertyStore,propertyCommands,nodeCommands,propertyLoader);
synchronized (indexingService) {
    indexingService.updateIndexes(updates,mode.needsIdempotencyChecks());
  }
}
