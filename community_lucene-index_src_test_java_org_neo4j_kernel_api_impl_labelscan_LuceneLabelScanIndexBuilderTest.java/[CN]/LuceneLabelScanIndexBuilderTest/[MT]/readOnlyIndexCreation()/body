{
  try (LabelScanIndex index=LuceneLabelScanIndexBuilder.create().withIndexRootFolder(testDir.graphDbDir()).withConfig(getReadOnlyConfig()).withOperationalMode(OperationalMode.single).build()){
    assertTrue("Builder should construct read only index.",index.isReadOnly());
  }
 }
