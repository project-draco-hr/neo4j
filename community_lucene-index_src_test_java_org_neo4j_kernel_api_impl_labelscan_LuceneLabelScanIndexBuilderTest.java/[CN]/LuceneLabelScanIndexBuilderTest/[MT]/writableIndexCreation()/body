{
  try (LabelScanIndex index=LuceneLabelScanIndexBuilder.create().withIndexRootFolder(testDir.graphDbDir()).withConfig(getDefaultConfig()).withOperationalMode(OperationalMode.single).build()){
    assertFalse("Builder should construct writable index.",index.isReadOnly());
  }
 }
