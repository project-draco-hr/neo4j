{
  boolean collect=collects(bit);
  if (collect) {
    out.println(exception.getMessage());
    badEntries++;
  }
  if (!collect || badEntries > tolerance) {
    throw collect ? withMessage(exception,format("Too many bad entries %d, where last one was: ",badEntries) + exception.getMessage()) : exception;
  }
}
