{
  String name="Mattias";
  Node node=createNodeAndLoadFresh(map("age",5,"name",name),0,0);
  assertEquals(withNodeOverhead(withArrayOverheadIncludingReferences(withObjectOverhead(8 + 8) + withObjectOverhead(8 + REFERENCE_SIZE + 4+ sizeOf(name)),2)),sizeOfNode(node));
}
