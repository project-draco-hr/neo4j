{
  int size=8 + 8 + 8+ sizeOf(type);
  for (  int rels : new int[]{nrOut,nrIn,nrLoop}) {
    if (rels > 0)     size+=withObjectOverhead(withReference(withArrayOverhead(4 * (rels + 1))));
  }
  if (nrLoop > 0)   size+=8;
  return withObjectOverhead(size);
}
