{
  try {
    monitor.recordStatus(storeName,brickCount,brickSize,availableMem,fileChannel.size());
  }
 catch (  IOException e) {
    throw new IOException("Unable to get file size for " + storeName,e);
  }
}
