{
  MadeUpCommunicationInterface implementation=new MadeUpImplementation(new StoreId(data.creationTime,data.storeId,data.storeVersion));
  server=new MadeUpServer(implementation,8888,data.internalProtocolVersion,data.applicationProtocolVersion);
}
