{
  Transaction tx=graphDb.beginTx();
  try {
    doFirst();
    latch.await();
    doSecond();
    tx.success();
  }
 catch (  InterruptedException e) {
    throw new RuntimeException(e);
  }
 finally {
    tx.finish();
  }
}
