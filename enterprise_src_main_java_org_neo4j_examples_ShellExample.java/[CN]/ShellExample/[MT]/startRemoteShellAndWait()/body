{
  neo.enableRemoteShell();
  waitForUserInput("Remote shell enabled, connect to it by running:\n" + "java -jar shell.jar\n" + "\nWhen you're done playing around, just press any key "+ "in this terminal");
}
