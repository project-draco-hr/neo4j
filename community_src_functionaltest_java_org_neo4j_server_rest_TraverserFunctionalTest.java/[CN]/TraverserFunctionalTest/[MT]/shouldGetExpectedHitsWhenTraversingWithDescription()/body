{
  String description=JsonHelper.createJsonFrom(MapUtil.map("prune evaluator",MapUtil.map("language","builtin","name","none"),"return filter",MapUtil.map("language","javascript","body","position.endNode().getProperty('name').toLowerCase().contains('t')")));
  ClientResponse response=traverse(startNode,description);
  String entity=response.getEntity(String.class);
  expectNodes(entity,startNode,child1_l1,child1_l3,child2_l3);
}
