{
  if (neoStores != null) {
    neoStores.close();
  }
  if (pageCache != null) {
    pageCache.close();
  }
  if (targetDirectory != null) {
    targetDirectory.cleanup();
  }
  if (efs != null) {
    efs.shutdown();
  }
}
