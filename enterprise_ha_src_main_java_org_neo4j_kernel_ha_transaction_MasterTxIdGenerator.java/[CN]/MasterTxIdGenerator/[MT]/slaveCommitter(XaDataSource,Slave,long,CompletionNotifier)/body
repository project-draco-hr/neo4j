{
  return new Callable<Void>(){
    @Override public Void call(){
      try {
        commitAtSlave(dataSource,slave,txId);
        return null;
      }
  finally {
        notifier.completed();
      }
    }
  }
;
}
