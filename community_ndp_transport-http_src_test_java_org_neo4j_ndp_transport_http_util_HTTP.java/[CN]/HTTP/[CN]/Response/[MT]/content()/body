{
  try {
    return (T)JsonHelper.readJson(new String(entity,Charsets.UTF_8));
  }
 catch (  JsonParseException e) {
    throw new RuntimeException("Unable to deserialize: " + BytePrinter.hex(entity),e);
  }
}
