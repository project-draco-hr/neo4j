{
  TestLogger logger=logging.getMessagesLog(IndexSamplingController.class);
  logger.assertAtLeastOnce(warn(format("Recovering index sampling for index :label[%d](property[%d])",label,property)));
}
