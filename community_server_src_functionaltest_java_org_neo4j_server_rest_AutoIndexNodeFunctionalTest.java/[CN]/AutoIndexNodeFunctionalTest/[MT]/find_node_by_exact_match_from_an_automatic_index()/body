{
  data.get();
  String entity=gen.get().expectedStatus(200).get(nodeAutoIndexUri() + "name/I").entity();
  Collection<?> hits=(Collection<?>)JsonHelper.jsonToSingleValue(entity);
  assertEquals(1,hits.size());
}
