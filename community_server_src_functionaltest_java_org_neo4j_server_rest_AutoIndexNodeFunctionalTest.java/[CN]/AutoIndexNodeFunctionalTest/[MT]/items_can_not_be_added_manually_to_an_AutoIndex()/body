{
  data.get();
  String indexName=graphdb().index().getNodeAutoIndexer().getAutoIndex().getName();
  gen.get().expectedStatus(405).payload(JsonHelper.createJsonFrom(getNodeUri(data.get().get("I")))).post(getNodeIndexUri(indexName,"name","I")).entity();
}
