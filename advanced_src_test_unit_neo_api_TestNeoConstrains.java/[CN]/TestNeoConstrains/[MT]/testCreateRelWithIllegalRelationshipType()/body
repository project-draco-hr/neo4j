{
  try {
    NodeManager nm=NodeManager.getManager();
    Node node1=nm.createNode();
    Node node2=nm.createNode();
    try {
      NodeManager.getManager().createRelationship(node1,node2,IllegalRelType.TEST_ILLEGAL);
    }
 catch (    IllegalArgumentException e) {
    }
    try {
      TransactionFactory.getUserTransaction().commit();
    }
 catch (    Exception e) {
    }
  }
 catch (  Exception e) {
    fail("" + e);
  }
}
