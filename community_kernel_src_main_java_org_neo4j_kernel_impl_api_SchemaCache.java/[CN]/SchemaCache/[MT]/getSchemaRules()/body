{
  return new NestingIterable<SchemaRule,Collection<SchemaRule>>(rulesMap.values()){
    @Override protected Iterator<SchemaRule> createNestedIterator(    Collection<SchemaRule> item){
      return item.iterator();
    }
  }
;
}
