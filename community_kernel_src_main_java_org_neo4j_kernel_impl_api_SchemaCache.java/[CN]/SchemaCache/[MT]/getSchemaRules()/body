{
  return new NestingIterable<SchemaRule,Map<Long,SchemaRule>>(rulesMap.values()){
    @Override protected Iterator<SchemaRule> createNestedIterator(    Map<Long,SchemaRule> item){
      return item.values().iterator();
    }
  }
;
}
