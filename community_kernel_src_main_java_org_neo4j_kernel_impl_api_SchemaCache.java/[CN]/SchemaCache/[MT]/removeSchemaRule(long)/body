{
  SchemaRule rule=ruleByIdMap.remove(id);
  if (rule == null)   return;
  Map<Long,SchemaRule> rules=rulesMap.get(rule.getLabel());
  if (rules.remove(id) != null)   if (rules.isEmpty())   rulesMap.remove(rule.getLabel());
}
