{
  SchemaRule rule=ruleByIdMap.remove(id);
  if (rule == null)   return;
  Map<Long,SchemaRule> rules=rulesByLabelMap.get(rule.getLabel());
  if (rules.remove(id) != null)   if (rules.isEmpty())   rulesByLabelMap.remove(rule.getLabel());
}
