{
  SchemaRule rule=ruleByIdMap.remove(id);
  if (rule == null)   return;
  Collection<SchemaRule> rules=rulesMap.get(rule.getLabel());
  if (rules.remove(rules))   if (rules.isEmpty())   rulesMap.remove(rule.getLabel());
}
