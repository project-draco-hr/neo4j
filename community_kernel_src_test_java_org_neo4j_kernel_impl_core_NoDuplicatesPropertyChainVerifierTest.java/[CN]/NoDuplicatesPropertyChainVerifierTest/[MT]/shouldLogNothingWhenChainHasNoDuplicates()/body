{
  DefinedProperty[] propertyChain=new DefinedProperty[]{Property.charProperty(1,'a'),Property.charProperty(2,'b')};
  chainVerifier.verifySortedPropertyChain(propertyChain,entity);
  assertThat(buffer.length(),is(0));
}
