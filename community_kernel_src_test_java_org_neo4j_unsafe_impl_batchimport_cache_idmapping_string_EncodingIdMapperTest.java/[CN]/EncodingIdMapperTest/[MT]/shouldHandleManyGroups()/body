{
  IdMapper mapper=new EncodingIdMapper(NumberArrayFactory.HEAP,new LongEncoder(),new Radix.String(),NO_MONITOR);
  int size=100;
  for (int i=0; i < size; i++) {
    mapper.put(i,i,new Group.Adapter(i,"" + i));
  }
  mapper.prepare(null,NONE);
  for (int i=0; i < size; i++) {
    assertEquals(i,mapper.get(i,new Group.Adapter(i,"" + i)));
  }
}
