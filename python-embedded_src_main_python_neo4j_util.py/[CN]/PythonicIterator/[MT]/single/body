def single(self):
    for item in self:
        break
    else:
        return None
    for item in self:
        raise ValueError('Too many items in the iterator')
    try:
        self.close()
    except:
        pass
    return item
