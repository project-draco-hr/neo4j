{
  return format("ExecutingQuery{queryId=%d, username='%s', queryText='%s', queryParameters=%s, startTime=%d}",queryId,username == null ? "<unavailable>" : username,queryText,queryParameters,startTime);
}
