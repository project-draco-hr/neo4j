{
  return new PrefetchingIterator<Triplet<String,String,String>>(){
    @Override protected Triplet<String,String,String> fetchNextOrNull(){
      return Triplet.of(nodeIds.get(random.nextInt(nodeIds.size())),nodeIds.get(random.nextInt(nodeIds.size())),randomType());
    }
  }
;
}
