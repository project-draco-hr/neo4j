{
  List<String> nodeIds=nodeIds();
  Configuration config=Configuration.COMMAS;
  ImportTool.main(arguments("--into",directory.absolutePath(),"--nodes",nodeHeader(config).getAbsolutePath() + MULTI_DELIMITER + nodeData(false,config,nodeIds,alwaysTrue()).getAbsolutePath(),"--relationships",relationshipHeader(config).getAbsolutePath() + MULTI_DELIMITER + relationshipData(false,config,nodeIds,alwaysTrue()).getAbsolutePath()));
  verifyData();
}
