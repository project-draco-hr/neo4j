{
  if (fileSystem.fileExists(base) && !fileSystem.isDirectory(base))   throw new IllegalStateException(base + " exists and is not a directory!");
  try {
    fileSystem.mkdirs(base);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return base;
}
