{
  logger.info("Logical log recovery complete, " + "all transactions have been resolved");
  try {
    neoStore.makeStoreOk();
  }
 catch (  IOException e) {
    throw new RuntimeException("Unable to make stores ok",e);
  }
}
