{
  calculate();
  if (matchNodes == null) {
    return null;
  }
  LinkedList<List<Node>> paths=new LinkedList<List<Node>>();
  for (  Node matchNode : matchNodes) {
    LinkedList<Node> pathNodes=new LinkedList<Node>();
    pathNodes.addAll(Util.constructSinglePathToNodeAsNodes(matchNode,predecessors1,true,false));
    pathNodes.addAll(Util.constructSinglePathToNodeAsNodes(matchNode,predecessors2,false,true));
    paths.add(pathNodes);
  }
  return paths;
}
