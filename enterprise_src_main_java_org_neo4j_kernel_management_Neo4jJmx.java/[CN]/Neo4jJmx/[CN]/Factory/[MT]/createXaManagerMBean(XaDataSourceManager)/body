{
  if (!register(new XaMonitor.MXBeanImplementation(instance.id,datasourceMananger))) {
    if (!register(new XaMonitor.XaManager(instance.id,datasourceMananger)))     failedToRegister("XaManagerMBean");
  }
}
