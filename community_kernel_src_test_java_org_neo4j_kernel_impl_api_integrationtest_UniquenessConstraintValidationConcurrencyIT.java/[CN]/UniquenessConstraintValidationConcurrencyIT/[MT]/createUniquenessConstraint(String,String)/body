{
  return new Function<GraphDatabaseService,Void>(){
    @Override public Void apply(    GraphDatabaseService db){
      db.schema().constraintFor(label(label)).unique().on(propertyKey).create();
      return null;
    }
  }
;
}
