{
  try {
    hazelcastInstance.getMap(HazelcastClusterTopology.EDGE_SERVERS).remove(config.get(ClusterSettings.server_id));
    hazelcastInstance.shutdown();
  }
 catch (  RuntimeException ignored) {
  }
}
