{
  exception.expect(ProcedureException.class);
  exception.expectMessage("Procedures must return a Stream of records, where a record is a concrete class that" + " you define, with public non-final fields defining the fields in the record. If " + "you'd like your procedure to return `String`, you could define a record class "+ "like:\n"+ "public class Output {\n"+ "    public String out;\n"+ "}");
  compile(ProcedureWithInvalidRecordOutput.class).get(0);
}
