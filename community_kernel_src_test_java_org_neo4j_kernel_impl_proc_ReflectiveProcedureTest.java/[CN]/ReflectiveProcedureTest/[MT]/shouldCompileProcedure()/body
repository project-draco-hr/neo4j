{
  List<Procedure> procedures=compile(SingleReadOnlyProcedure.class);
  TestCase.assertEquals(1,procedures.size());
  Assert.assertThat(procedures.get(0).signature(),Matchers.equalTo(procedureSignature("org","neo4j","kernel","impl","proc","listCoolPeople").out("name",Neo4jTypes.NTString).build()));
}
