{
  data.get();
  String query="START b=(node_auto_index,'name:B') " + "MATCH a-[:FOLLOW]->b, b-[:FOLLOW]->a RETURN a ";
  String result=engine.execute(parser.parse(query)).toString();
  assertTrue(result.contains("A"));
}
