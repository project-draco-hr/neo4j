{
  Collection<InputRelationship> result=new ArrayList<>();
  int count=100;
  Group group=Group.GLOBAL;
  boolean typeIds=random.nextBoolean();
  for (int i=0; i < count; i++) {
    int typeId=random.nextInt(5);
    Object node=(long)i;
    InputRelationship relationship=new InputRelationship("test",i,i,NO_PROPERTIES,null,group,node,group,node,typeIds ? null : String.valueOf(typeId),typeIds ? typeId : null);
    result.add(relationship);
  }
  return result;
}
