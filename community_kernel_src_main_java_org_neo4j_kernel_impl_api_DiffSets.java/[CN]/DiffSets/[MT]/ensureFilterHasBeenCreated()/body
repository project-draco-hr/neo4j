{
  if (filter == null) {
    filter=new Predicate<T>(){
      @Override public boolean accept(      T item){
        return (removedElements == null || !removedElements.contains(item)) && (addedElements == null || !addedElements.contains(item));
      }
    }
;
  }
}
