{
  ManagedCluster cluster=clusterRule.provider(clusterOfSize(memberCount)).config(HaSettings.tx_push_factor,"" + pushFactor).config(HaSettings.tx_push_strategy,pushStrategy.name()).startCluster();
  mapMachineIds(cluster);
  return cluster;
}
