{
  ManagedCluster cluster=clusterRule.provider(clusterOfSize(memberCount)).config(HaSettings.tx_push_factor,"" + pushFactor).config(HaSettings.tx_push_strategy,pushStrategy).availabilityChecks(Arrays.asList(allSeesAllAsAvailable())).startCluster();
  mapMachineIds(cluster);
  return cluster;
}
