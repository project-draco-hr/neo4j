{
  Traverser traverser=new TraversalDescription().traverse(referenceNode());
  int count=0;
  for (  Path path : traverser.paths()) {
    assertNotNull("returned paths should not be null. path #" + count,path);
    count++;
  }
  assertEquals(13,count);
}
