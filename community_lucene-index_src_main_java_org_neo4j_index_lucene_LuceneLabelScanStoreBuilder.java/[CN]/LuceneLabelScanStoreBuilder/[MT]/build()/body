{
  if (null == labelScanStore) {
    labelScanStore=new LuceneLabelScanStore(new NodeRangeDocumentLabelScanStorageStrategy(),DirectoryFactory.PERSISTENT,LabelScanStoreProvider.getStoreDirectory(storeDir),fileSystem,IndexWriterFactories.standard(),fullStoreStream,LuceneLabelScanStore.loggerMonitor(logProvider));
    try {
      labelScanStore.init();
      labelScanStore.start();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return labelScanStore;
}
