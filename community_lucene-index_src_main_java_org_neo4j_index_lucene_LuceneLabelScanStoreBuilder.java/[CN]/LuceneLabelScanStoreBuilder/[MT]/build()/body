{
  if (null == labelScanStore) {
    labelScanStore=new LuceneLabelScanStore(new NodeRangeDocumentLabelScanStorageStrategy(),DirectoryFactory.PERSISTENT,LabelScanStoreProvider.getStoreDirectory(storeDir),fileSystem,IndexWriterFactories.standard(),fullStoreLabelUpdateStream(neoStoresSupplier),LuceneLabelScanStore.loggerMonitor(logProvider));
    try {
      labelScanStore.init();
      labelScanStore.start();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return labelScanStore;
}
