{
  File file;
  if (DatabaseLocator.isLocalDatabase()) {
    file=new File(new File(DatabaseLocator.getDatabaseLocation()),BACKUP_CONFIG_PATH);
  }
 else {
    file=new File(new File(CONFIG_FOLDER),BACKUP_CONFIG_PATH);
  }
  return ensureFileExists(file);
}
