{
  super(map);
  try {
    xaContainer=XaContainer.create(resourceFile(),new DummyCommandFactory(),new DummyTransactionFactory(),null);
    xaContainer.openLogicalLog();
  }
 catch (  IOException e) {
    throw new InstantiationException("" + e);
  }
}
