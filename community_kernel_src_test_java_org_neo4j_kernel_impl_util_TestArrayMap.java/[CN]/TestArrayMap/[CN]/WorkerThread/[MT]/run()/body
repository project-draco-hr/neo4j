{
  try {
    for (int i=0; i < 10000; i++) {
      if (map.size() > 5) {
        for (int j=i; j < (i + 10); j++) {
          if (map.remove(j % 10) != null) {
            break;
          }
        }
      }
 else       if (map.size() <= 5) {
        map.put(i % 10,new Object());
      }
      yield();
    }
    success=true;
  }
 catch (  Throwable t) {
    this.t=t;
  }
 finally {
    done=true;
  }
}
