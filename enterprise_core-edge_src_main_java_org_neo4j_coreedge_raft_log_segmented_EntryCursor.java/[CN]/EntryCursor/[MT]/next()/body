{
  currentIndex++;
  if (segmentRange == null || currentIndex >= limit) {
    if (!nextSegment()) {
      return false;
    }
  }
  if (reader.next()) {
    currentRecord.set(reader.get());
    return true;
  }
  currentRecord.invalidate();
  return false;
}
