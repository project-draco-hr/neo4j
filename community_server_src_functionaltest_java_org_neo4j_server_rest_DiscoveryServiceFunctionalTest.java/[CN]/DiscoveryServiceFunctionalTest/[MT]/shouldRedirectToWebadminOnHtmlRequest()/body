{
  Client nonRedirectingClient=Client.create();
  nonRedirectingClient.setFollowRedirects(false);
  ClientResponse clientResponse=nonRedirectingClient.resource(server.baseUri()).accept(MediaType.TEXT_HTML).get(ClientResponse.class);
  assertEquals(303,clientResponse.getStatus());
}
