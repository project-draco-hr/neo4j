{
  LuceneBatchInserterIndex index=indexes.get(identifier);
  if (index == null) {
    index=new LuceneBatchInserterIndex(inserter.getStore(),identifier,config);
    indexes.put(identifier,index);
  }
  return index;
}
