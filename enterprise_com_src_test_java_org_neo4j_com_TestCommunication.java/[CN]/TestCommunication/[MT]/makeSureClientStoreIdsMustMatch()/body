{
  MadeUpImplementation serverImplementation=new MadeUpImplementation(storeIdToUse);
  MadeUpServer server=madeUpServer(serverImplementation);
  MadeUpClient client=new MadeUpClient(PORT,new StoreId(10,10,NeoStore.versionStringToLong(CommonAbstractStore.ALL_STORES_VERSION)),INTERNAL_PROTOCOL_VERSION,APPLICATION_PROTOCOL_VERSION);
  try {
    client.multiply(1,2);
    fail();
  }
 catch (  ComException e) {
  }
 finally {
    client.shutdown();
    server.shutdown();
  }
}
