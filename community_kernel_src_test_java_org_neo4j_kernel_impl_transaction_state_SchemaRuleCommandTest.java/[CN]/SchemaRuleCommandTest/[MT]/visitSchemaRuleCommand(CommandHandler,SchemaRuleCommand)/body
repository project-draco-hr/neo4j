{
  TransactionToApply tx=new TransactionToApply(new PhysicalTransactionRepresentation(Arrays.<Command>asList(command)),txId);
  tx.validatedIndexUpdates(mock(ValidatedIndexUpdates.class));
  CommandHandlerContract.apply(applier,tx);
}
