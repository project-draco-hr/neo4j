{
  gen.create().payload("{\"foo\" : {\"bar\" : \"baz\"}}").expectedStatus(Response.Status.BAD_REQUEST).post(functionalTestHelper.nodeUri());
  ClientResponse response=setNodePropertyOnServer("jim",new HashMap<String,Object>());
  assertEquals(400,response.getStatus());
  response.close();
}
