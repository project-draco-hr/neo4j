{
  DocsGenerator.create("Set property on node").payload(JsonHelper.createJsonFrom("bar")).expectedStatus(Response.Status.NO_CONTENT).put(getPropertyUri("foo").toString());
  ClientResponse response=setNodePropertyOnServer("foo","bar");
  assertEquals(204,response.getStatus());
  response.close();
}
