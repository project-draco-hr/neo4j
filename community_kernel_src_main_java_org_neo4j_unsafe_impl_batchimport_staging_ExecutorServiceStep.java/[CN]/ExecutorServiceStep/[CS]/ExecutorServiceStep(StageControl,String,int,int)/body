{
  super(control,name);
  this.workAheadSize=workAheadSize;
  this.numberOfExecutors=numberOfExecutors;
  NamedThreadFactory threadFactory=new NamedThreadFactory(name);
  this.executor=numberOfExecutors == 1 ? newSingleThreadExecutor(threadFactory) : newFixedThreadPool(numberOfExecutors,threadFactory);
}
