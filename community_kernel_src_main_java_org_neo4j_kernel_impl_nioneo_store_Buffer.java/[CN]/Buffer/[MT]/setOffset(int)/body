{
  try {
    buf.position(offset);
  }
 catch (  java.lang.IllegalArgumentException e) {
    throw new IllegalArgumentException("Illegal offset " + offset + " for buffer:"+ buf,e);
  }
  return this;
}
