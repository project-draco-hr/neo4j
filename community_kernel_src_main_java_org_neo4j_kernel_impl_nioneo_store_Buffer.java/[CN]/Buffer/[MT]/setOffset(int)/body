{
  try {
    buf.position(offset);
  }
 catch (  java.lang.IllegalArgumentException e) {
    throw new IllegalArgumentException("Illegal offset " + offset + " for window position:"+ position()+ ", buffer:"+ buf,e);
  }
  return this;
}
