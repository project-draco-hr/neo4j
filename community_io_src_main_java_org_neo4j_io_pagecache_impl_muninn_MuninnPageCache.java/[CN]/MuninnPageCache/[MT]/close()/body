{
  if (closed) {
    return;
  }
  closed=true;
  FileMapping file=mappedFiles;
  while (file != null) {
    file.pagedFile.close();
    file=file.next;
  }
  for (int i=0; i < pages.length; i++) {
    pages[i].freeBuffer();
  }
}
