{
  String dir=forTest(getClass()).directory(name,true).getAbsolutePath();
  return waitForExit(getRuntime().exec(new String[]{"java","-cp",getProperty("java.class.path"),mainClass.getName(),dir}),20);
}
