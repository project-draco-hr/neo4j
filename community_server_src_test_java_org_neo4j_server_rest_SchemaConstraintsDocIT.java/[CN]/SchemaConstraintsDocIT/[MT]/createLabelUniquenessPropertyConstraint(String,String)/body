{
  Transaction tx=graphdb().beginTx();
  try {
    ConstraintDefinition constraintDefinition=graphdb().schema().constraintFor(label(labelName)).unique().on(propertyKey).create();
    tx.success();
    return constraintDefinition;
  }
  finally {
    tx.finish();
  }
}
