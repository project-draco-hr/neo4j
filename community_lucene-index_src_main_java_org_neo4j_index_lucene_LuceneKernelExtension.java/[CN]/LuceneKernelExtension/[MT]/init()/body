{
  LuceneIndexImplementation indexImplementation=new LuceneIndexImplementation(config,indexStore,fileSystemAbstraction);
  indexProviders.registerIndexProvider(LuceneIndexImplementation.SERVICE_NAME,indexImplementation);
}
