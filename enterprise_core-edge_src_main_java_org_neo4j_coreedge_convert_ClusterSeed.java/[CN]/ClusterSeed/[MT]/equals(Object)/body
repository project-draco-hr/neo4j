{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ClusterSeed that=(ClusterSeed)o;
  return lastTxId == that.lastTxId && Objects.equals(before,that.before) && Objects.equals(after,that.after);
}
