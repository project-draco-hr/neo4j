def property_type(type):
    if (type is None):
        return DynamicProperty
    if issubclass(type, strings):
        return StringProperty
    if (type in integers):
        return NumberProperty
    if (type is float):
        return NumberProperty
    if (type is datetime.datetime):
        return DateProperty
