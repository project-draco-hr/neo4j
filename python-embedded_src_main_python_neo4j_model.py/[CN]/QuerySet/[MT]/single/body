def single(self):
    iterator = iter(self)
    for item in iterator:
        break
    else:
        return None
    for item in iterator:
        raise ValueError('Query yielded in more than one result.')
    try:
        iterator.close()
    except:
        pass
    return item
