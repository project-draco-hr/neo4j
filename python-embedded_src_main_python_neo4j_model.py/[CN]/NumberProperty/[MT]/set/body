def set(self, value):
    if (not (isinstance(value, integers) or isinstance(value, float))):
        raise ValueError(("Invalid property type: '%s' is not a number" % type(value).__name__))
    return value
