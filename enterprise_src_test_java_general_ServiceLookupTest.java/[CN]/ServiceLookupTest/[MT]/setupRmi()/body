{
  try {
    RmiTransport.register(new LocalGraphDatabase(PATH),RMI_RESOURCE);
  }
 catch (  ConnectException ex) {
    if (rmi) {
      throw ex;
    }
 else {
      return false;
    }
  }
  return true;
}
