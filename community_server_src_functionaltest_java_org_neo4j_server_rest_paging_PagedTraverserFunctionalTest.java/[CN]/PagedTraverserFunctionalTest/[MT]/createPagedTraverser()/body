{
  String description=traverserDescription();
  ClientResponse response=Client.create().resource(functionalTestHelper.nodeUri(theStartNode.getId()) + "/paged/traverse/node").accept(MediaType.APPLICATION_JSON_TYPE).entity(description).post(ClientResponse.class);
  return response;
}
