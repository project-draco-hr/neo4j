{
  ClientResponse response=CLIENT.resource(functionalTestHelper.nodeUri(theStartNode.getId()) + "/paged/traverse/node").accept(MediaType.APPLICATION_JSON_TYPE).entity(traverserDescription()).post(ClientResponse.class);
  return response;
}
