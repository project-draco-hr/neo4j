{
  Collection<Object[]> mappings=new ArrayList<>();
  for (  DocStruct struct : docs().read("dev/serialization.asciidoc","code[data-lang=\"bolt_value_struct\"]",struct_definition)) {
    mappings.add(new Object[]{struct});
  }
  return mappings;
}
