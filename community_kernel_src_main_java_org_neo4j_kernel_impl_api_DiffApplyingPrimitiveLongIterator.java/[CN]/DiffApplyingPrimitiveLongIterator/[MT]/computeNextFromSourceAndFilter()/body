{
  for (hasNext=source.hasNext(); hasNext; hasNext=source.hasNext()) {
    nextValue=source.next();
    if ((removedElements == null || !removedElements.contains(nextValue)) && (addedElements == null || !addedElements.contains(nextValue))) {
      return;
    }
  }
  transitionToAddedElements();
}
