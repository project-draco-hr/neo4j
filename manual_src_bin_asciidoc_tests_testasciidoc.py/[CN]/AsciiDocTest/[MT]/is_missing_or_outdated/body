def is_missing_or_outdated(self, backend):
    '\n        Returns True if the output test data file is missing or out of date.\n        '
    return (self.is_missing(backend) or (os.path.getmtime(self.source) > os.path.getmtime(self.backend_filename(backend))))
