def strip_end(lines):
    '\n    Strip blank strings from the end of list of strings.\n    '
    for i in range((len(lines) - 1), (-1), (-1)):
        if (not lines[i]):
            del lines[i]
        else:
            break
