{
  return new ReplicatedTransactionStateMachine<>(commitProcess,new GlobalSession<>(UUID.randomUUID(),RaftTestMember.member(1)),mock(ReplicatedLockTokenStateMachine.class,RETURNS_MOCKS),new CommittingTransactionsRegistry(),new InMemoryStateStorage<>(sessionTrackerState),NullLogProvider.getInstance(),recoverTransactionLogState);
}
