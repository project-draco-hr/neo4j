{
  File neoStore=new File(dataDir,NeoStore.DEFAULT_NAME);
  long logVersion=NeoStore.isStorePresent(pageCache,dataDir) ? NeoStore.getRecord(pageCache,neoStore,NeoStore.Position.LOG_VERSION) : 0;
  return recoveryNeededAt(dataDir,logVersion);
}
