{
  if (isRunning()) {
    throw new IllegalStateException("Already started");
  }
  server=new DesktopBootstrapper(model.getDatabaseDirectory(),model.getDatabaseConfigurationFile(),new ArrayList<String>());
  server.start();
}
