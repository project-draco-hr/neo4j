{
  if (isRunning()) {
    throw new IllegalStateException("Already started");
  }
  server=new DesktopBootstrapper(new File(path),getDatabaseConfigurationFile(path),extensionPackages);
  server.start();
}
