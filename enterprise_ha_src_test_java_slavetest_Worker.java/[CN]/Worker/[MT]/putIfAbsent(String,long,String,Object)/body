{
  return executeDontWait(new WorkerCommand<WorkerState,Boolean>(){
    @Override public Boolean doWork(    WorkerState state){
      return state.db.index().forNodes(index).putIfAbsent(state.db.getNodeById(node),key,value);
    }
  }
);
}
