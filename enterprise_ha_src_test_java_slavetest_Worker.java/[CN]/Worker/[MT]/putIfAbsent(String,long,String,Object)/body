{
  return executeDontWait(new WorkerCommand<WorkerState,Node>(){
    @Override public Node doWork(    WorkerState state){
      return state.db.index().forNodes(index).putIfAbsent(state.db.getNodeById(node),key,value);
    }
  }
);
}
