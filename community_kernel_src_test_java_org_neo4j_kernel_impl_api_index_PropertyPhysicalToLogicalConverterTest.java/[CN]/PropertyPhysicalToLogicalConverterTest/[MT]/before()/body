{
  Monitors monitors=new Monitors();
  StoreFactory storeFactory=new StoreFactory(new Config(),new DefaultIdGeneratorFactory(),new DefaultWindowPoolFactory(monitors,new Config()),fs.get(),StringLogger.DEV_NULL,new DefaultTxHook(),monitors);
  File storeFile=new File("propertystore");
  storeFactory.createPropertyStore(storeFile);
  store=storeFactory.newPropertyStore(storeFile);
  converter=new PropertyPhysicalToLogicalConverter(store);
}
