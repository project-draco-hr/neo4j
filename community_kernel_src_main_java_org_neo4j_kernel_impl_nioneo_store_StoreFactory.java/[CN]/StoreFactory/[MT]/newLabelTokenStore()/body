{
  DynamicStringStore nameStore=newDynamicStringStore(storeFileName(LABEL_TOKEN_NAMES_STORE_NAME),IdType.LABEL_TOKEN_NAME);
  return new LabelTokenStore(storeFileName(LABEL_TOKEN_STORE_NAME),config,idGeneratorFactory,pageCache,fileSystemAbstraction,stringLogger,nameStore,versionMismatchHandler,monitors);
}
