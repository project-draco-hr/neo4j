{
  DynamicStringStore nameStore=newDynamicStringStore(storeFileName(PROPERTY_KEY_TOKEN_NAMES_STORE_NAME),IdType.PROPERTY_KEY_TOKEN_NAME);
  return new PropertyKeyTokenStore(storeFileName(PROPERTY_KEY_TOKEN_STORE_NAME),config,idGeneratorFactory,pageCache,fileSystemAbstraction,stringLogger,nameStore,versionMismatchHandler,monitors);
}
