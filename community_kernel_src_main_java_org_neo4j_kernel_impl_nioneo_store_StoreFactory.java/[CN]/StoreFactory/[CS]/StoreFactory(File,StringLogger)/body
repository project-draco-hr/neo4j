{
  this(configForStoreDir(new Config(),storeDir),new DefaultIdGeneratorFactory(),new DefaultWindowPoolFactory(),new DefaultFileSystemAbstraction(),logger,StoreVersionMismatchHandler.THROW_EXCEPTION);
}
