{
  final List<LockInfo> locks=new ArrayList<LockInfo>();
  lockManager.eachLock(new Visitor<LockInfo>(){
    @Override public boolean visit(    LockInfo lock){
      locks.add(lock);
      return false;
    }
  }
);
  return locks.toArray(new LockInfo[locks.size()]);
}
