{
  while (!isCompleted()) {
    try {
      checkPointer.forceCheckPoint();
      Thread.sleep(10);
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
  completedLatch.countDown();
  return null;
}
