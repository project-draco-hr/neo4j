{
  return new MappingRepresentation(type){
    @Override protected void serialize(    final MappingSerializer serializer){
      for (      Map.Entry<String,String> pair : map.entrySet()) {
        serializer.putString(pair.getKey(),pair.getValue());
      }
    }
  }
;
}
