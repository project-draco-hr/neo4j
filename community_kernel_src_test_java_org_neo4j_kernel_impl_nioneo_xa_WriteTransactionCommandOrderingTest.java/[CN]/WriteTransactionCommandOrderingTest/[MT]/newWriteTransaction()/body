{
  WriteTransaction tx=new WriteTransaction(0,0l,mock(XaLogicalLog.class),TransactionState.NO_STATE,store,mock(CacheAccessBackDoor.class),mock(IndexingService.class),WriteTransactionTest.NO_LABEL_SCAN_STORE,mock(IntegrityValidator.class),mock(KernelTransactionImplementation.class));
  tx.setCommitTxId(store.getLastCommittedTx() + 1);
  return tx;
}
