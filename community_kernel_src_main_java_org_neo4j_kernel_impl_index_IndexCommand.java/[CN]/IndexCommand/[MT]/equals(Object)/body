{
  IndexCommand other=(IndexCommand)obj;
  boolean equals=getCommandType() == other.getCommandType() && entityType == other.entityType && indexNameId == other.indexNameId && keyId == other.keyId && getValueType() == other.getValueType();
  if (!equals) {
    return false;
  }
  return value == null ? other.value == null : value.equals(other.value);
}
