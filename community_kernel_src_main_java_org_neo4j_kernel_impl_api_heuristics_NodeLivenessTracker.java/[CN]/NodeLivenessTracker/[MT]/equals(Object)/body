{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  NodeLivenessTracker that=(NodeLivenessTracker)o;
  return liveNodesSeenNow == that.liveNodesSeenNow && maxNodes == that.maxNodes && round == that.round && skippedNodesSeenNow == that.skippedNodesSeenNow && deadNodes.equals(that.deadNodes) && liveNodes.equals(that.liveNodes);
}
