{
  if (pageCache != null) {
    try {
      pageCache.stop();
    }
 catch (    IOException e) {
      throw new AssertionError("Failed to stop existing PageCache prior to creating a new one",e);
    }
  }
  PageSwapperFactory swapperFactory=new SingleFilePageSwapperFactory(fs);
  pageCache=new LifecycledPageCache(swapperFactory,jobScheduler,config,new Monitors().newMonitor(PageCacheMonitor.class));
  pageCache.start();
  return pageCache;
}
