{
  if (pageCache != null) {
    try {
      pageCache.stop();
    }
 catch (    IOException e) {
      throw new AssertionError("Failed to stop PageCache after test",e);
    }
    pageCache=null;
  }
  jobScheduler.shutdown();
}
