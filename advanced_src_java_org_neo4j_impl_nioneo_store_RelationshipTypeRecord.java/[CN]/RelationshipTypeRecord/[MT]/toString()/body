{
  StringBuffer buf=new StringBuffer();
  buf.append("RelationshipTypeRecord[").append(getId()).append(",").append(inUse()).append(",").append(typeBlock);
  buf.append(", blocks[");
  for (  DynamicRecord record : typeRecords.values()) {
    buf.append(record);
  }
  buf.append("]]");
  return buf.toString();
}
