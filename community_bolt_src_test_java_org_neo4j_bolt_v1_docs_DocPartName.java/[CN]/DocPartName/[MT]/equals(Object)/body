{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  DocPartName that=(DocPartName)o;
  return fileName.equals(that.fileName) && title.equals(that.title) && !(detail != null ? !detail.equals(that.detail) : that.detail != null);
}
