{
  try {
    return new ScanResistantWindowPool(storageFileName,pageSize,targetBytesPerPage,new FileMapper(fileChannel),cart,reportingInterval,statisticsListener);
  }
 catch (  IOException e) {
    throw new UnderlyingStorageException(e);
  }
}
