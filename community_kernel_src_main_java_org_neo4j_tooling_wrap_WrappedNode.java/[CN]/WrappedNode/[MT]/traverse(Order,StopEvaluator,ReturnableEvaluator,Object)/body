{
  WrappedTraverser.Evaluator evaluator=new WrappedTraverser.Evaluator(graphdb,stopEvaluator,returnableEvaluator);
  return new WrappedTraverser(graphdb,actual().traverse(traversalOrder,evaluator,evaluator,relationshipTypesAndDirections));
}
