{
  transactionMetadataCache.cacheTransactionMetadata(element.getCommitEntry().getTxId(),element.getStartEntry().getStartPosition(),element.getStartEntry().getMasterId(),element.getStartEntry().getLocalId(),LogEntry.Start.checksum(element.getStartEntry()));
  return true;
}
