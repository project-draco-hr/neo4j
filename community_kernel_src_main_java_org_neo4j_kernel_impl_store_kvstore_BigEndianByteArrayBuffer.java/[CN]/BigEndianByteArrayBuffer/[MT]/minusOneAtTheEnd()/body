{
  for (int i=0; i < buffer.length / 2; i++) {
    if (buffer[i] != 0) {
      return false;
    }
  }
  for (int i=buffer.length / 2; i < buffer.length; i++) {
    if (buffer[i] != -1) {
      return false;
    }
  }
  return true;
}
