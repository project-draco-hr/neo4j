{
  for (int i=0; i < updaters.size(); ) {
    IndexUpdater updater=updaters.get(i);
    try {
      updater.close();
      i++;
    }
 catch (    Throwable t) {
      updaters.remove(i);
      multipleIndexPopulator.fail(i,t);
    }
  }
}
