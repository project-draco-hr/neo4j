{
  for (int i=0; i < populations.size(); ) {
    IndexPopulation populator=populations.get(i);
    try {
      populator.add(update);
      i++;
    }
 catch (    Throwable t) {
      fail(i,t);
    }
  }
}
