{
  return new KernelData(new DefaultFileSystemAbstraction(),new File("graph.db"),new Config()){
    @Override public Version version(){
      return version;
    }
    @Override public GraphDatabaseAPI graphDatabase(){
      return graphDatabaseAPI;
    }
  }
;
}
