{
  return cleanupService.resourceIterator(map(new Function<Long,Node>(){
    @Override public Node apply(    Long id){
      return getNodeById(id);
    }
  }
,input),state.closeable(kernelAPI.statementOperations().lifecycleOperations()));
}
