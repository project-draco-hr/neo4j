{
  breakNodeStore();
  ConsistencyCheckService service=new ConsistencyCheckService();
  File specificLogFile=new File(testDirectory.directory(),"specific_logfile.txt");
  service.runFullConsistencyCheck(fixture.directory().getPath(),new Config(stringMap(ConsistencyCheckSettings.consistency_check_report_file.name(),specificLogFile.getPath()),GraphDatabaseSettings.class,ConsistencyCheckSettings.class),ProgressMonitorFactory.NONE,StringLogger.DEV_NULL);
  assertTrue("Inconsistency report file " + specificLogFile + " not generated",specificLogFile.exists());
}
