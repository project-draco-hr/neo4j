{
  String uri=nodeUri.toString();
  ResponseEntity entity=DocsGenerator.create("Get node - compact").expectedType(CompactJsonFormat.MEDIA_TYPE).get(uri);
  assertTrue(entity.entity().contains("self"));
}
