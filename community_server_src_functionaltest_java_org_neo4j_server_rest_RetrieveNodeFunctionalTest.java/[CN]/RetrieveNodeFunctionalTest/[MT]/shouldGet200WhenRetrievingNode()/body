{
  String uri=nodeUri.toString();
  DocsGenerator.create("Get node","Note that the response contains URI/templates for " + "the available operations for getting properties and relationships.").get(uri);
}
