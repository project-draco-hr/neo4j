{
  boolean removed=allTransactions.remove(tx);
  if (!removed) {
    throw new IllegalStateException("Transaction: " + tx + " is not present in the "+ "set of known active transactions: "+ allTransactions);
  }
}
