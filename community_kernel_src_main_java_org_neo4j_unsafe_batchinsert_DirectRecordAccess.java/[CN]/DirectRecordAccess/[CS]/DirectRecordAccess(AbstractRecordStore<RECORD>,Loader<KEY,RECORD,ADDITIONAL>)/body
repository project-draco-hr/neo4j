{
  this.store=store;
  this.loader=loader;
  proxyFlyweightPool=new FlyweightPool<DirectRecordProxy>(100){
    @Override protected DirectRecordProxy create(){
      return new DirectRecordProxy();
    }
  }
;
}
