{
  this.store=store;
  this.loader=loader;
  proxyFlyweightPool=new LinkedQueuePool<>(100,new Factory<DirectRecordProxy>(){
    @Override public DirectRecordProxy newInstance(){
      return new DirectRecordProxy();
    }
  }
);
}
