{
  if (changeCounter.value() == 0) {
    return;
  }
  for (  DirectRecordProxy proxy : batch.values()) {
    proxy.store();
    proxyFlyweightPool.release(proxy);
  }
  changeCounter.clear();
  batch.clear();
}
