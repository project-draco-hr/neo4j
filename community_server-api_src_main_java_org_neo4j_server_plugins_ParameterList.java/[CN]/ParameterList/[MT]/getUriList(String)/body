{
  return getList(name,null,new Converter<URI>(){
    @Override URI convert(    GraphDatabaseSPI graphDb,    Object value) throws BadInputException {
      return convertURI(value);
    }
    @Override URI[] newArray(    int size){
      return new URI[size];
    }
  }
);
}
