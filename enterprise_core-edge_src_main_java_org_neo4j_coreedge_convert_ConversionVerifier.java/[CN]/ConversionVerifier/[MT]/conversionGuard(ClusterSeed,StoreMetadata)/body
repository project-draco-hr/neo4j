{
  StoreId sourceBefore=source.before();
  StoreId targetBefore=target.storeId();
  long sourceLastTxId=source.lastTxId();
  long targetLastTxId=target.lastTxId();
  if (!(sourceBefore.equals(targetBefore) && sourceLastTxId == targetLastTxId)) {
    throw new RuntimeException(String.format("Cannot convert store. Backup ID does not match on source and target databases. Source: %s, Target: %s",source,target));
  }
}
