{
  return new AbstractDynamicStore(fileName,new Config(),IdType.ARRAY_BLOCK,new DefaultIdGeneratorFactory(),new DefaultWindowPoolFactory(),fsr.get(),StringLogger.DEV_NULL,StoreVersionMismatchHandler.ACCEPT){
    @Override public void accept(    Processor processor,    DynamicRecord record){
    }
    @Override public String getTypeDescriptor(){
      return "TestDynamicStore";
    }
  }
;
}
