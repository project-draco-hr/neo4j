{
  KernelHealth kernelHealth=new KernelHealth(mock(KernelPanicEventGenerator.class),NullLogProvider.getInstance().getLog(KernelHealth.class));
  return getDataSource(dir,fs,pageCache,additionalConfig,kernelHealth);
}
