{
  String recordFormat=configuredRecordFormat(config);
  RecordFormats recordFormats=StringUtils.isNotEmpty(recordFormat) ? selectSpecificFormat(recordFormat,logService) : getFirstAvailableOrDefault(DEFAULT_AUTOSELECT_FORMAT);
  logSelectedFormat(logService,recordFormats);
  return recordFormats;
}
