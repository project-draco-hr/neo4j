{
  if (index < size) {
    try {
      return next(documentStructure.getNodeId(hits.doc(index++)));
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
  return false;
}
