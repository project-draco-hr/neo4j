{
  if (index < size) {
    try {
      next(documentStructure.getNodeId(hits.doc(index++)));
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
 else {
    endReached();
  }
}
