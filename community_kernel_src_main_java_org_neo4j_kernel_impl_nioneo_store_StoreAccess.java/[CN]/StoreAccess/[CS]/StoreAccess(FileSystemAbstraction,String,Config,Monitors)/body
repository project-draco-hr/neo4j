{
  this(new StoreFactory(config,new DefaultIdGeneratorFactory(),new DefaultWindowPoolFactory(monitors,config),fileSystem,StringLogger.DEV_NULL,new DefaultTxHook(),monitors).newNeoStore(new File(path,"neostore")));
  this.closeable=true;
}
