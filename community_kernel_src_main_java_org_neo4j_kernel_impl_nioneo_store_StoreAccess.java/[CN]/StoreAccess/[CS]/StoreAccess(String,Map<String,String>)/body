{
  this(new StoreFactory(new Config(requiredParams(params,path)),new DefaultIdGeneratorFactory(),new DefaultFileSystemAbstraction(),new DefaultLastCommittedTxIdSetter(),initLogger(path),new DefaultTxHook()).attemptNewNeoStore(new File(path,"neostore").getAbsolutePath()));
  this.closeable=true;
}
