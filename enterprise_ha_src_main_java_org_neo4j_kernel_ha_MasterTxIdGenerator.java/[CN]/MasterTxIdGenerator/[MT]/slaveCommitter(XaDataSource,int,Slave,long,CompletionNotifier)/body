{
  return new Callable<Void>(){
    @Override public Void call(){
      try {
        commitAtSlave(dataSource,identifier,slave,txId);
        return null;
      }
  finally {
        notifier.completed();
      }
    }
  }
;
}
