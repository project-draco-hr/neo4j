{
  super(type,node);
  this.src=src;
  if (src == null) {
    srcTraversed=true;
  }
  if (add == null) {
    addTraversed=true;
  }
  this.add=add;
  if (remove != null) {
    for (int i=0; i < remove.length(); i++) {
      this.remove.add(remove.get(i));
    }
  }
}
