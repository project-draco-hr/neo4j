{
  if (data == null) {
    return;
  }
  try (BufferedReader reader=new BufferedReader(new InputStreamReader(data));PrintWriter writer=new PrintWriter(file)){
    String input=reader.readLine();
    while (input != null) {
      writer.println(input);
      input=reader.readLine();
    }
  }
 }
