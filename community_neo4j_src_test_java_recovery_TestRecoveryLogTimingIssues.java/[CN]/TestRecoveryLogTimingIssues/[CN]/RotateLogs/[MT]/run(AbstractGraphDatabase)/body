{
  try {
    graphdb.getConfig().getTxModule().getXaDataSourceManager().getXaDataSource(DEFAULT_DATA_SOURCE_NAME).rotateLogicalLog();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
