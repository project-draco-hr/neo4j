{
  StatementTokenNameLookup lookup=new StatementTokenNameLookup(readOps);
  return new UniquenessConstraintDefinition(schemaActions,Label.label(lookup.labelGetName(labelId)),lookup.propertyKeyGetName(propertyKeyId));
}
