{
  StatementTokenNameLookup lookup=new StatementTokenNameLookup(readOps);
  return new UniquenessConstraintDefinition(schemaActions,DynamicLabel.label(lookup.labelGetName(labelId)),lookup.propertyKeyGetName(propertyKeyId));
}
