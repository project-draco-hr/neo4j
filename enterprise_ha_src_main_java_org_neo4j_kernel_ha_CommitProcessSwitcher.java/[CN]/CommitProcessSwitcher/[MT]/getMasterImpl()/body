{
  TransactionRepresentationCommitProcess transactionRepresentationCommitProcess=new TransactionRepresentationCommitProcess(logicalTransactionStore,kernelHealth,neoStore,storeApplier,false);
  return new MasterTransactionCommitProcess(transactionRepresentationCommitProcess,pusher,validator);
}
