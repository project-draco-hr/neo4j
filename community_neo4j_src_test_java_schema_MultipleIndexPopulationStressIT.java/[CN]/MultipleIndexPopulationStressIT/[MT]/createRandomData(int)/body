{
  BatchImporter importer=new ParallelBatchImporter(directory.directory(),fs,DEFAULT,NullLogService.getInstance(),ExecutionMonitors.invisible(),EMPTY,Config.empty());
  importer.doImport(new RandomDataInput(count));
}
