{
  String[] clusterRoles=asCollection(clusterConfiguration.getRolesOf(clusterUri)).toArray(new String[0]);
  return new ClusterMemberInfo(clusterUri.toString(),available,alive,haRole.name(),clusterRoles,new String[]{clusterUri.toString(),nullSafeUriToString(haUri)});
}
