{
  Map<URI,Member> newMembers=new HashMap<URI,Member>();
  for (  URI memberClusterUri : clusterConfiguration.getMembers())   newMembers.put(memberClusterUri,new Member(memberClusterUri));
  members.clear();
  members.putAll(newMembers);
}
