{
  int targetBytesPerPage=1;
  int bytesPerRecord=1;
  FileMapper fileMapper=mock(FileMapper.class);
  when(fileMapper.fileSizeInBytes()).thenReturn(Integer.MAX_VALUE * 2L);
  try {
    new ScanResistantWindowPool(new File("storeFileName"),bytesPerRecord,targetBytesPerPage,fileMapper,mock(PageReplacementStrategy.class),100000,mock(MappingStatisticsListener.class));
    fail("should have thrown exception");
  }
 catch (  IllegalArgumentException expected) {
  }
}
