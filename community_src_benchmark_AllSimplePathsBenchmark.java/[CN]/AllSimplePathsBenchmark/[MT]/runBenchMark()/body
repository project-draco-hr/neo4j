{
  Transaction tx=neo.beginTx();
  startNode=graph.getRandomNode(null);
  endNode=graph.getRandomNode(startNode);
  new AllSimplePaths(startNode,endNode,Integer.MAX_VALUE,Direction.BOTH,MyRelTypes.R1).calculate();
  tx.finish();
}
