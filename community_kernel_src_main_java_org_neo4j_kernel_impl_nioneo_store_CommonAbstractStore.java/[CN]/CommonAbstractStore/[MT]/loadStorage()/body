{
  try {
    readAndVerifyBlockSize();
    verifyFileSizeAndTruncate();
    loadIdGenerator();
    this.windowPool=windowPoolFactory.create(getStorageFileName(),getEffectiveRecordSize(),getFileChannel());
  }
 catch (  IOException e) {
    throw new UnderlyingStorageException("Unable to load storage " + getStorageFileName(),e);
  }
}
