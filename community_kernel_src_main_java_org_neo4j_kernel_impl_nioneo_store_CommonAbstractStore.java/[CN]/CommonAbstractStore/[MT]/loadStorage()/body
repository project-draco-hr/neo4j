{
  try {
    readAndVerifyBlockSize();
    verifyFileSizeAndTruncate();
    loadIdGenerator();
    this.windowPool=windowPoolFactory.create(getStorageFileName(),getEffectiveRecordSize(),getFileChannel(),configuration,getNumberOfReservedLowIds(),monitors);
  }
 catch (  IOException e) {
    throw new UnderlyingStorageException("Unable to load storage " + getStorageFileName(),e);
  }
}
