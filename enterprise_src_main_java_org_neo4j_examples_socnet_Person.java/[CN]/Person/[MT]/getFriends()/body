{
  return new IterableWrapper<Person,Relationship>(underlyingNode.getRelationships(FRIEND)){
    @Override protected Person underlyingObjectToObject(    Relationship friendRel){
      return new Person(friendRel.getOtherNode(underlyingNode));
    }
  }
;
}
