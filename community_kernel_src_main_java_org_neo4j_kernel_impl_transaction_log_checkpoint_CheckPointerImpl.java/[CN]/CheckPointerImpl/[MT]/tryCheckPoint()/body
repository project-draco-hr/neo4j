{
  if (lock.tryLock()) {
    try {
      return doCheckPoint(LogCheckPointEvent.NULL);
    }
  finally {
      lock.unlock();
    }
  }
 else {
    lock.lock();
    try {
      return lastCheckPointedTx;
    }
  finally {
      lock.unlock();
    }
  }
}
