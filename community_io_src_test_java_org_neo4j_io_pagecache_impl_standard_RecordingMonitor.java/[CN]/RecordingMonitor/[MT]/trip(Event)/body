{
  Trap trap=null;
synchronized (traps) {
    int index=traps.indexOf(new Trap(event,null));
    if (index != -1) {
      trap=traps.remove(index);
    }
  }
  if (trap != null) {
    trap.await();
  }
}
