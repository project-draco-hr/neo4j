{
  if (name == null) {
    return Group.GLOBAL;
  }
  Group group=groups.get(name);
  if (group == null) {
    throw new IllegalArgumentException("Unknown group '" + name + "'");
  }
  return group;
}
