{
  if (closed)   return null;
  try {
    String line=reader.readLine();
    if (line == null)     close();
    return line;
  }
 catch (  IOException e) {
    close();
    throw new RuntimeException(e);
  }
}
