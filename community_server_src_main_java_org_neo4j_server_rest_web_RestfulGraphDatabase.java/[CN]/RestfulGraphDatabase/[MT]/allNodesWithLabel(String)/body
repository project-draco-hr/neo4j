{
  try {
    if (labelName.isEmpty())     throw new BadInputException("Empty label name");
    return output.ok(actions.getNodesWithLabel(labelName));
  }
 catch (  BadInputException e) {
    return output.badRequest(e);
  }
}
