{
  try {
    ListRepresentation result=actions.pagedTraverse(traverserId,returnType);
    return Response.ok(uriInfo.getRequestUri()).entity(output.format(result)).build();
  }
 catch (  NotFoundException e) {
    return output.notFound(e);
  }
}
