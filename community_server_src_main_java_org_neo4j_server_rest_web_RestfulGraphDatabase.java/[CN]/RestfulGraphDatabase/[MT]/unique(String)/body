{
  boolean unique;
  if (uniqueParam == null) {
    unique=false;
  }
 else   if ("".equals(uniqueParam)) {
    unique=true;
  }
 else {
    unique=Boolean.parseBoolean(uniqueParam);
  }
  return unique;
}
