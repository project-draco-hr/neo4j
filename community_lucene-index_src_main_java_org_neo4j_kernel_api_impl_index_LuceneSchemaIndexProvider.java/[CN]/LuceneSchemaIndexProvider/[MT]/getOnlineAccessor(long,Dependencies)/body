{
  try {
    return new LuceneIndexAccessor(standard(directoryFactory),dependencies.getFileSystem(),dir(dependencies.getRootDirectory(),indexId),documentLogic,writerLogic);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
