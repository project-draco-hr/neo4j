{
  deleteFileOrDirectory(PATH);
  db=new HighlyConfigurableGraphDatabase(PATH,configForNoMemoryMapping(),new JumpingIdGeneratorFactory(SIZE_PER_JUMP),new JumpingFileSystemAbstraction(SIZE_PER_JUMP));
}
