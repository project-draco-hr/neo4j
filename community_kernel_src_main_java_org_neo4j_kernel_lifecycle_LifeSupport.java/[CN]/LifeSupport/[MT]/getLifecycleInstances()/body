{
  return Iterables.map(new Function<LifecycleInstance,Lifecycle>(){
    @Override public Lifecycle apply(    LifecycleInstance lifecycleInstance){
      return lifecycleInstance.instance;
    }
  }
,new ArrayList<LifecycleInstance>(instances));
}
