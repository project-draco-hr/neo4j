{
  if (this == o)   return true;
  if (o == null || getClass() != o.getClass())   return false;
  LogCall logCall=(LogCall)o;
  return flush == logCall.flush && level == logCall.level && message.equals(logCall.message) && !(cause != null ? !cause.equals(logCall.cause) : logCall.cause != null);
}
