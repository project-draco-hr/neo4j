{
  boolean found=false;
  for (  String line : IteratorUtil.asIterable(new File(slaveDir,StringLogger.DEFAULT_NAME)))   if (line.contains(expectingToFind))   found=true;
  assertTrue("Expected to find at least one log message including '" + expectingToFind + "' but didn't",found);
}
