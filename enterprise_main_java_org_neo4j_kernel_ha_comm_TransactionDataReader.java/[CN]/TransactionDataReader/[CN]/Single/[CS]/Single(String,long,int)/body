{
  this.resource=resource;
  this.txId=txId;
  if (chunkSize < 0) {
    moreChunks=true;
    chunkSize=-chunkSize;
  }
 else {
    moreChunks=false;
  }
  leftInChunk=chunkSize;
  current=new byte[chunkSize];
  data=new ArrayList<byte[]>();
}
