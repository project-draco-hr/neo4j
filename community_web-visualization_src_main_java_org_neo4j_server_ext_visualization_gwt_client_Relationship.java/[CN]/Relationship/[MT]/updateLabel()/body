{
  Style style=label.getElement().getStyle();
  double x=getLabelCenter(getCenterX(from),getCenterX(to)) - label.getOffsetWidth() / 2;
  style.setLeft(x,Unit.PX);
  double y=getLabelCenter(getCenterY(from),getCenterY(to)) - label.getOffsetHeight() / 2;
  style.setTop(y,Unit.PX);
  return style;
}
