{
  log.warn("Sending log compaction info. Log pruned? Status=%s, LeaderContext=%s",statusAsString(),leaderContext);
  outbound.send(follower,new RaftMessages.LogCompactionInfo(CURRENT_VERSION,leader,leaderContext.term,raftLog.prevIndex()));
}
