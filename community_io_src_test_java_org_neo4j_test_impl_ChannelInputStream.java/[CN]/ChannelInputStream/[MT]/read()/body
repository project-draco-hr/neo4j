{
  buffer.clear();
  buffer.limit(1);
  while (buffer.hasRemaining()) {
    int read=channel.read(buffer);
    if (read == -1) {
      return -1;
    }
  }
  buffer.flip();
  position++;
  return buffer.get();
}
