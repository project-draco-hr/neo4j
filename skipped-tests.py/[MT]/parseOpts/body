def parseOpts(args):
    (opts, args) = getopt.getopt(args, '', ['days=', 'fail'])
    result = {'days': 30, 'fail': False, }
    for (o, a) in opts:
        if (o == '--days'):
            result['days'] = int(a)
        elif (o == 'fail'):
            result['fail'] = True
    return result
