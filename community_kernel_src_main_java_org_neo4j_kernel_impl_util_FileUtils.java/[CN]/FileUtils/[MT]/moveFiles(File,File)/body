{
  if (!fromDirectory.isDirectory()) {
    throw new IllegalArgumentException("Move target must be a directory, not " + fromDirectory);
  }
  for (  File memberOf : fromDirectory.listFiles()) {
    moveFile(memberOf,toDirectory);
  }
}
