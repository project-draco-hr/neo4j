{
  boolean found=false;
  for (  String candidate : candidates) {
    if (candidate.startsWith(shouldStartWith)) {
      found=true;
    }
  }
  assertTrue("Should have found a candidate among " + candidates + " starting with '"+ shouldStartWith+ "'",found);
}
