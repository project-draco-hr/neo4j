{
  final NeoStoreDataSource.BufferedIdMaintenanceController idMaintenanceController=embeddedDatabase.getDependencyResolver().resolveDependency(NeoStoreDataSource.BufferedIdMaintenanceController.class);
  return executorService.submit(new idMaintainer(idMaintenanceController,stopFlag));
}
