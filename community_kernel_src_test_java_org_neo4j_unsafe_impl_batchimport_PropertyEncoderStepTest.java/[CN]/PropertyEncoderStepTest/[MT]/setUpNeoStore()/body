{
  File storeDir=new File("dir");
  pageCache=pageCacheRule.getPageCache(fsRule.get());
  StoreFactory storeFactory=new StoreFactory(fsRule.get(),storeDir,pageCache,LowLimitV3_0.RECORD_FORMATS,NullLogProvider.getInstance());
  neoStores=storeFactory.openAllNeoStores(true);
}
