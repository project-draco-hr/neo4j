{
  return new Predicate<StackTraceElement[]>(){
    @Override public boolean test(    StackTraceElement[] item){
      for (      StackTraceElement element : item)       if (element.getClassName().equals(cls.getName()))       return true;
      return false;
    }
  }
;
}
