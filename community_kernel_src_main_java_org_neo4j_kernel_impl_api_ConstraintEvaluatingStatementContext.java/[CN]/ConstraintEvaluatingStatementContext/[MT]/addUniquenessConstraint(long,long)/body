{
  if (getConstraints(labelId,propertyKey).hasNext()) {
    throw new ConstraintViolationKernelException("Property " + propertyKey + " already has a uniqueness constraint for label "+ labelId+ ".");
  }
  return delegate.addUniquenessConstraint(labelId,propertyKey);
}
