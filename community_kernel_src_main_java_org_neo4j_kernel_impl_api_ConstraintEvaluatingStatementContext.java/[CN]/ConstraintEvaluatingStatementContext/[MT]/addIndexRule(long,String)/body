{
  for (  String existingRule : getIndexRules(labelId)) {
    if (existingRule.equals(propertyKey))     throw new ConstraintViolationKernelException("Property " + propertyKey + " is already indexed for label "+ labelId+ ".");
  }
  delegate.addIndexRule(labelId,propertyKey);
}
