{
  bootstrapper=newBootstrapper();
  File configFile=tempDir.newFile("neo4j.config");
  store(stringMap(forced_kernel_id.name(),"ourcustomvalue"),configFile);
  start(bootstrapper,array("-C",configFile.getAbsolutePath(),"-c",configOption(legacy_db_location.name(),tempDir.getRoot().getAbsolutePath())));
  assertThat(bootstrapper.getServer().getConfig().get(forced_kernel_id),equalTo("ourcustomvalue"));
}
