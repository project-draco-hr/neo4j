{
  ArrayList<String> config=new ArrayList<>();
  Collections.addAll(config,params);
  config.addAll(asList("-c","dbms.connector.1.type=HTTP","-c","dbms.connector.1.enabled=true"));
  return config.toArray(new String[config.size()]);
}
