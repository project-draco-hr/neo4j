{
  int resultCode=start(bootstrapper,commandLineConfig("-c",configOption(ServerSettings.data_directory.name(),tempDir.getRoot().getAbsolutePath())));
  assertEquals(Bootstrapper.OK,resultCode);
  assertNotNull(bootstrapper.getServer());
}
