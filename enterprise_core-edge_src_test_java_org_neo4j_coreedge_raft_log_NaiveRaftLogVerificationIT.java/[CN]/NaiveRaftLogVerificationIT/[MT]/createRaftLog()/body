{
  FileSystemAbstraction fsa=fsRule.get();
  File directory=new File(NAIVE_LOG_DIRECTORY_NAME);
  fsa.mkdir(directory);
  return new NaiveDurableRaftLog(fsa,directory,new DummyRaftableContentSerializer(),NullLogProvider.getInstance());
}
