{
  try {
    Response<Long> response=broker.getMaster().commitSingleResourceTransaction(broker.getSlaveContext(),identifier,dataSource.getName(),new TransactionStream(dataSource.getCommittedTransaction(identifier)));
    return receiver.receive(response);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
