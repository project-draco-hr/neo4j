{
  Collection<Object[]> mappings=new ArrayList<>();
  for (  DocExchangeExample ex : docs().read("dev/transport.asciidoc","code[data-lang=\"ndp_exchange\"]",exchange_example)) {
    mappings.add(new Object[]{ex});
  }
  for (  DocExchangeExample ex : docs().read("dev/examples.asciidoc","code[data-lang=\"ndp_exchange\"]",exchange_example)) {
    mappings.add(new Object[]{ex});
  }
  return mappings;
}
