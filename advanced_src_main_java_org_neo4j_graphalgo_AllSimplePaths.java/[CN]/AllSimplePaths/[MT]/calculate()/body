{
  if (doneCalculation) {
    return;
  }
  doneCalculation=true;
  traverser1(node2,maximumTotalDepth / 2,new Path(node2,null,null));
  traverser2(node1,maximumTotalDepth / 2 + maximumTotalDepth % 2,new LinkedList<Object>());
}
