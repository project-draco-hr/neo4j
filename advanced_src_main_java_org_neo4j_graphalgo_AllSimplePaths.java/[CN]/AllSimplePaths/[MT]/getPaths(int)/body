{
  reset();
  totalDepth++;
  traverser1(node2,totalDepth / 2,new Path(node2,null,null));
  traverser2(node1,totalDepth / 2 + totalDepth % 2,new LinkedList<PropertyContainer>());
  return foundPaths;
}
