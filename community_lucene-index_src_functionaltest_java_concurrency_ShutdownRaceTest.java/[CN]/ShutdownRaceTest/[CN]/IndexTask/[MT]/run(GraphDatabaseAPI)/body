{
  try {
    Transaction tx=graphdb.beginTx();
    try {
      index(graphdb.index().forNodes("name"),graphdb.getReferenceNode());
      tx.success();
    }
  finally {
      tx.finish();
    }
  }
  finally {
    done();
  }
}
