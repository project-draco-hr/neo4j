{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  LogEntryStart start=(LogEntryStart)o;
  return authorId == start.authorId && lastCommittedTxWhenTransactionStarted == start.lastCommittedTxWhenTransactionStarted && masterId == start.masterId && timeWritten == start.timeWritten && Arrays.equals(additionalHeader,start.additionalHeader) && startPosition.equals(start.startPosition);
}
