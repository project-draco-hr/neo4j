{
  String oldMembers=members.values().toString();
  members.put(instanceId,getMember(instanceId).availableAs(role,roleUri,storeId));
  eventOccurred();
  log.debug("Member is available. Role: " + role + ", Instance: "+ instanceId+ ". OldMembers: "+ oldMembers+ ". NewMembers: "+ members.values());
}
