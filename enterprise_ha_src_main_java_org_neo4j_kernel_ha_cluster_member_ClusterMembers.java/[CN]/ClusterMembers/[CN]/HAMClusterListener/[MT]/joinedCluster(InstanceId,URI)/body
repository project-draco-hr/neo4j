{
  String oldMembers=members.values().toString();
  members.put(member,new ClusterMember(member));
  log.debug("Joined cluster. Instance: " + member + ", URI: "+ memberUri+ ". OldMembers: "+ oldMembers+ ". NewMembers: "+ members.values());
}
