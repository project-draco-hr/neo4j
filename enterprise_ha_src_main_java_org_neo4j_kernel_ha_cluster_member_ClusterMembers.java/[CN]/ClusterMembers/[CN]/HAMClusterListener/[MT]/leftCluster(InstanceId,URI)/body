{
  String oldMembers=members.values().toString();
  members.remove(instanceId);
  log.debug("Left cluster. Instance: " + instanceId + ", URI: "+ member+ ". OldMembers: "+ oldMembers+ ". NewMembers: "+ members.values());
}
