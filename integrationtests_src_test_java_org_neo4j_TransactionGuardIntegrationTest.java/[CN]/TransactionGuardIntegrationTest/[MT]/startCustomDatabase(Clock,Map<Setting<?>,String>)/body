{
  GuardCommunityFacadeFactory guardCommunityFacadeFactory=new GuardCommunityFacadeFactory(clock);
  GraphDatabaseAPI database=(GraphDatabaseAPI)new GuardTestGraphDatabaseFactory(guardCommunityFacadeFactory).newImpermanentDatabase(configMap);
  cleanupRule.add(database);
  return database;
}
