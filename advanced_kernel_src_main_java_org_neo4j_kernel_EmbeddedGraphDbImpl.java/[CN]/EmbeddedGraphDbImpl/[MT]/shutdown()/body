{
  if (inShutdown)   return;
  inShutdown=true;
  try {
    if (graphDbInstance.started()) {
      sendShutdownEvent();
      extensions.shutdown(msgLog);
    }
    graphDbInstance.shutdown();
  }
  finally {
    inShutdown=false;
  }
}
