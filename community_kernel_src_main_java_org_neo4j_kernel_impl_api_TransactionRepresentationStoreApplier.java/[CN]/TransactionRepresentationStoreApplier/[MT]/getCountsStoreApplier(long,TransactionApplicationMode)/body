{
  Optional<CommandHandler> handlerOption=storageEngine.neoStores().getCounts().apply(transactionId).map(CountsStoreApplier.FACTORY);
  if (mode == TransactionApplicationMode.RECOVERY) {
    handlerOption=handlerOption.or(CommandHandler.EMPTY);
  }
  return handlerOption.get();
}
