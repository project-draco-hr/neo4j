{
  try {
    RawPropertyData properties[]=persistenceManager.loadProperties(relationship);
    return properties;
  }
 catch (  Exception e) {
    log.severe("Failed loading properties for relationship[" + relationship.getId() + "]");
    throw new RuntimeException(e);
  }
}
