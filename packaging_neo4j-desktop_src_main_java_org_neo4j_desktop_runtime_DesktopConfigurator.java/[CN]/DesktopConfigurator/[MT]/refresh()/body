{
  config=new ConfigLoader(CommunityBootstrapper.settingsClasses).loadConfig(null,installation.getConfigurationsFile(),FormattedLog.toOutputStream(System.out),pair(auth_store.name(),new File(dbDir,"./dbms/auth").getAbsolutePath()),pair(tls_certificate_file.name(),new File(dbDir,"./certificates/snakeoil.cert").getAbsolutePath()),pair(tls_key_file.name(),new File(dbDir,"./certificates/snakeoil.key").getAbsolutePath()),pair(DatabaseManagementSystemSettings.database_path.name(),dbDir.getAbsolutePath()));
}
