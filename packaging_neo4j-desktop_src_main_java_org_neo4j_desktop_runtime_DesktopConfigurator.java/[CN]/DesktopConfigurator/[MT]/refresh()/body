{
  config=new BaseServerConfigLoader().loadConfig(null,installation.getConfigurationsFile(),FormattedLog.toOutputStream(System.out),pair(auth_store.name(),new File(dbDir,"./dbms/auth").getAbsolutePath()),pair(tls_certificate_file.name(),new File(dbDir,"./dbms/ssl/snakeoil.cert").getAbsolutePath()),pair(tls_key_file.name(),new File(dbDir,"./dbms/ssl/snakeoil.key").getAbsolutePath()),pair(ServerSettings.database_path.name(),dbDir.getAbsolutePath()));
}
