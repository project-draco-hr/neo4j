{
  Map<String,String> newMap=new HashMap<>(map);
  ConfigurationBuilder propertyFileConfig=new PropertyFileConfigurator(installation.getServerConfigurationsFile(),FormattedLog.toOutputStream(System.out));
  newMap.putAll(propertyFileConfig.configuration().getParams());
  this.compositeConfig.applyChanges(newMap);
}
