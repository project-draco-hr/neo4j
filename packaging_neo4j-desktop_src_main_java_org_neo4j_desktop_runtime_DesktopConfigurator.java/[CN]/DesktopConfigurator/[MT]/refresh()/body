{
  config=new ConfigLoader(CommunityBootstrapper.settingsClasses).loadConfig(dbDir.getAbsoluteFile(),Optional.of(getConfigurationsFile()),FormattedLog.toOutputStream(System.out),pair(DatabaseManagementSystemSettings.database_path.name(),dbDir.getAbsolutePath()));
}
