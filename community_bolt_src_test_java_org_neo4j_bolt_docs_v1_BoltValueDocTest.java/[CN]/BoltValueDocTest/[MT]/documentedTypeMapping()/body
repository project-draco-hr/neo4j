{
  Collection<Object[]> mappings=new ArrayList<>();
  for (  DocTable.Row row : docs().read("dev/serialization.asciidoc","#bolt-type-system-mapping",table).get(0)) {
    mappings.add(new Object[]{row.get(0)});
  }
  return mappings;
}
