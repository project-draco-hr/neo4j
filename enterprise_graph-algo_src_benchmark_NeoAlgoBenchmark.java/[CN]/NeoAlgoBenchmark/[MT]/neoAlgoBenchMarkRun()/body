{
  if (internalIds == null) {
    neoAlgoBenchMarkRunInternal();
  }
 else {
    for (    Object id : internalIds) {
      currentInternalId=id;
      neoAlgoBenchMarkRunInternal();
    }
    System.out.println(" --- Results ---");
    for (    Object object : internalIds) {
      System.out.println(object + ": " + results.get(object)+ "s");
    }
  }
}
