{
  return new CountingIndexUpdater.IndexUpdateCountVisitor(){
    @Override public void visitIndexUpdateCount(    long indexUpdates){
      view.updateIndexCount(descriptor,indexUpdates);
    }
  }
;
}
