{
  db=new TestGraphDatabaseFactory().newEmbeddedDatabaseBuilder(tmpDir.getRoot().getAbsolutePath()).setConfig(BoltKernelExtension.Settings.bolt_enabled,"true").newGraphDatabase();
  assertEventuallyServerResponds("localhost",7687);
}
