{
  protocolToFactoryMapping=new HashMap<ProtocolCombo,MasterClientFactory>();
  protocolToFactoryMapping.put(ProtocolCombo.PC_153,new F153(messageLogger,readTimeout,lockReadTimeout,channels));
  protocolToFactoryMapping.put(ProtocolCombo.PC_18,new F18(messageLogger,readTimeout,lockReadTimeout,channels));
}
