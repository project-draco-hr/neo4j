{
  protocolToFactoryMapping=new HashMap<ProtocolVersionCombo,MasterClientFactory>();
  protocolToFactoryMapping.put(ProtocolVersionCombo.PC_153,new F153(messageLogger,readTimeout,lockReadTimeout,channels,chunkSize));
  protocolToFactoryMapping.put(ProtocolVersionCombo.PC_17,new F17(messageLogger,readTimeout,lockReadTimeout,channels,chunkSize));
  protocolToFactoryMapping.put(ProtocolVersionCombo.PC_18,new F18(messageLogger,readTimeout,lockReadTimeout,channels,chunkSize));
}
