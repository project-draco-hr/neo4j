{
  Config config=config();
  return new DynamicArrayStore(dynamicStoreFile(),config,IdType.ARRAY_BLOCK,ID_GENERATOR_FACTORY,pageCache,fs.get(),StringLogger.DEV_NULL,StoreVersionMismatchHandler.THROW_EXCEPTION,new Monitors());
}
