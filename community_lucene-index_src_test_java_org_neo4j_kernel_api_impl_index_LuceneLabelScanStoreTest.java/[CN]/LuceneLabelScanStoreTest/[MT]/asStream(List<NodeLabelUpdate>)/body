{
  return new FullStoreChangeStream(){
    @Override public Iterator<NodeLabelUpdate> iterator(){
      return existingData.iterator();
    }
    @Override public long numberOfNodes(){
      return existingData.size();
    }
  }
;
}
