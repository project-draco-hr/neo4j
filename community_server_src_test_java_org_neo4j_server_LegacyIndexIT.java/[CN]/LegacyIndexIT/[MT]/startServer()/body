{
  server=server().withHttpsEnabled().withProperty("remote_shell_enabled","false").withProperty("dbms.security.auth_enabled","false").usingDatabaseDir(folder.cleanDirectory(name.getMethodName()).getAbsolutePath()).build();
}
