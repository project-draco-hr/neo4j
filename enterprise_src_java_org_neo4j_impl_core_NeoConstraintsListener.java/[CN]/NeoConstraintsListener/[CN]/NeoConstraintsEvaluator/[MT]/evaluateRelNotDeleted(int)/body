{
  if (deletedRelationships != null && deletedRelationships.contains(relId)) {
    throw new IllegalStateException("Property operation on rel[" + relId + "] illegal since it has already been deleted "+ "(in this tx)");
  }
}
