{
  super(null,new DefaultSchemaIndexProviderMap(NO_INDEX_PROVIDER),new NeoStoreIndexStoreView(locks,neoStore),null,new KernelSchemaStateStore(),IteratorUtil.<IndexRule>emptyIterator(),new SingleLoggingService(DEV_NULL),IndexingService.NO_MONITOR);
}
