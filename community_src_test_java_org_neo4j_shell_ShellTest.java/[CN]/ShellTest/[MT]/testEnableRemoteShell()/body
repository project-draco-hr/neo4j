{
  int port=8085;
  GraphDatabaseService graphDb=new EmbeddedGraphDatabase("target/shell-neo",stringMap(ENABLE_REMOTE_SHELL,"port=" + port));
  ShellLobby.newClient(port,AbstractServer.DEFAULT_NAME);
  graphDb.shutdown();
}
