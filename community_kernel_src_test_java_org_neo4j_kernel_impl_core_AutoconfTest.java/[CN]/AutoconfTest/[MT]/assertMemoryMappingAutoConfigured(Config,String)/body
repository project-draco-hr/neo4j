{
  Long configuredValue=config.get(memoryMappingSetting("neostore." + store));
  assertTrue(format("Memory mapping for '%s' should be greater than 0, was %s",store,configuredValue),configuredValue > 0);
}
