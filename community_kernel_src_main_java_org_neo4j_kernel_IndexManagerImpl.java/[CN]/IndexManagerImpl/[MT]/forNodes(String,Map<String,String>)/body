{
  if (NodeAutoIndexerImpl.NODE_AUTO_INDEX.equals(indexName)) {
    throw new IllegalArgumentException("The node auto index should not be accessed by name");
  }
  return getOrCreateNodeIndex(indexName,customConfiguration);
}
