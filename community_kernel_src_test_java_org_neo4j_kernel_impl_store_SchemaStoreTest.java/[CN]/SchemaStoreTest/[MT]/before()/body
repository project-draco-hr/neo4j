{
  File storeDir=new File("dir");
  fs.get().mkdirs(storeDir);
  config=new Config();
  DefaultIdGeneratorFactory idGeneratorFactory=new DefaultIdGeneratorFactory(fs.get());
  Monitors monitors=new Monitors();
  storeFactory=new StoreFactory(storeDir,config,idGeneratorFactory,pageCacheRule.getPageCache(fs.get()),fs.get(),NullLogProvider.getInstance(),monitors);
  storeFactory.createSchemaStore();
  store=storeFactory.newSchemaStore();
}
