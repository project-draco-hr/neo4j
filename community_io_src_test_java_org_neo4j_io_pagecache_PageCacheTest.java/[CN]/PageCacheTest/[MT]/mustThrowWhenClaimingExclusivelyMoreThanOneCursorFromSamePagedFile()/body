{
  getPageCache(fs,maxPages,pageCachePageSize,PageCacheMonitor.NULL);
  try (PagedFile pf=pageCache.map(file,filePageSize);PageCursor a=pf.io(0,PF_EXCLUSIVE_LOCK);PageCursor b=pf.io(0,PF_EXCLUSIVE_LOCK)){
    fail("The second io() call should have thrown");
  }
 }
