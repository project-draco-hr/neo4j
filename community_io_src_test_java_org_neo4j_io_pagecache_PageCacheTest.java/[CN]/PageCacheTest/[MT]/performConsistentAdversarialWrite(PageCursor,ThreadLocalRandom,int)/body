{
  for (int j=0; j < 3; j++) {
    assertTrue(cursor.next());
    byte b=(byte)rng.nextInt();
    for (int k=0; k < pageSize; k++) {
      cursor.putByte(b);
    }
    assertFalse(cursor.shouldRetry());
  }
}
