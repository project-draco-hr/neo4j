{
  getPageCache(fs,maxPages,pageCachePageSize,PageCacheTracer.NULL);
  File file=file("a");
  try (PagedFile oldMapping=pageCache.map(file,filePageSize);PagedFile newMapping=pageCache.map(file,0,PageCacheOpenOptions.ANY_PAGE_SIZE)){
  }
 }
