{
  getPageCache(fs,maxPages,pageCachePageSize,PageCacheTracer.NULL);
  try (PagedFile pf=pageCache.map(file,filePageSize,StandardOpenOption.CREATE);PageCursor cursor=pf.io(0,PF_EXCLUSIVE_LOCK)){
    assertTrue(cursor.next());
  }
 }
