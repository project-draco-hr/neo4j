{
  return new Runnable(){
    @Override public void run(){
      try {
        pageCache.unmap(file);
      }
 catch (      IOException e) {
        throw new AssertionError(e);
      }
    }
  }
;
}
