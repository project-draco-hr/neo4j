{
  return new Function<OutputStream,Collector>(){
    @Override public Collector apply(    OutputStream out) throws RuntimeException {
      return badCollector(out,tolerance,collect);
    }
  }
;
}
