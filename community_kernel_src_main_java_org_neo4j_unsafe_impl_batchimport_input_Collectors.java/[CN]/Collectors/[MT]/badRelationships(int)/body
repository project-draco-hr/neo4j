{
  return new Function<OutputStream,Collector<InputRelationship>>(){
    @Override public Collector<InputRelationship> apply(    OutputStream out) throws RuntimeException {
      return badRelationshipsCollector(out,tolerance);
    }
  }
;
}
