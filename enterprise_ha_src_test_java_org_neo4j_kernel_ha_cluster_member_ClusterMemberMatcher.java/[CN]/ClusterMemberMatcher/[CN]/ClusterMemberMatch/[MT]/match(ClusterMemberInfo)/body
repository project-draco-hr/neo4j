{
  if (!member.toString().equals(toMatch.getInstanceId()))   return false;
  if (available != null && toMatch.isAvailable() != available)   return false;
  if (alive != null && toMatch.isAlive() != alive)   return false;
  if (haRole != null && !haRole.equals(toMatch.getHaRole()))   return false;
  if (uris != null && !uris.equals(new HashSet<String>(asList(toMatch.getURIs()))))   return false;
  return true;
}
