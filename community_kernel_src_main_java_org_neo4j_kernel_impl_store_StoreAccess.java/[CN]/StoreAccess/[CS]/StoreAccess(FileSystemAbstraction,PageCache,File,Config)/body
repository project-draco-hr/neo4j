{
  this(new StoreFactory(storeDir,config,new DefaultIdGeneratorFactory(fileSystem),pageCache,fileSystem,RecordFormatSelector.autoSelectFormat(config,NullLogService.getInstance()),NullLogProvider.getInstance()).openAllNeoStores());
  this.closeable=true;
}
