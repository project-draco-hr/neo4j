{
  Block block=Block.getBlock(Arrays.asList("[source, cypher]","----","CREATE (n:Person {name:\"Ad\" + \"am\"})","RETURN n;","----","Adam"));
  String output=block.process(engine,database);
  assertThat(output,not(containsString("Adam")));
}
