{
  Block block=Block.getBlock(Arrays.asList("[source, cypher, includeresult]","----","CREATE (n:Person {name:\"Ad\" + \"am\"})","RETURN n;","----","Adam"));
  String output=block.process(engine,database);
  assertThat(output,containsString("Adam"));
}
