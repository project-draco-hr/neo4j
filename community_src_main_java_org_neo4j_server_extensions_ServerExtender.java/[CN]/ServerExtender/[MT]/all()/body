{
  return new NestingIterable<ExtensionPoint,Map<String,ExtensionPoint>>(extensions.values()){
    @Override protected Iterator<ExtensionPoint> createNestedIterator(    Map<String,ExtensionPoint> item){
      return item.values().iterator();
    }
  }
;
}
