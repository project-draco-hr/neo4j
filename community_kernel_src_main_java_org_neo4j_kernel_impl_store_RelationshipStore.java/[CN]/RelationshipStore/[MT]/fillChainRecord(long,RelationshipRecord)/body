{
  try {
    assertIdExists(id);
  }
 catch (  InvalidRecordException e) {
    return false;
  }
  if (fillRecord(id,record,RecordLoad.NORMAL)) {
    return true;
  }
 else {
    return false;
  }
}
