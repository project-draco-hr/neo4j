{
  return new ResourcePool<Something>(minSize,new ResourcePool.CheckStrategy.TimeoutCheckStrategy(100,clock),stateMonitor){
    @Override protected Something create(){
      return new Something();
    }
    @Override protected boolean isAlive(    Something resource){
      return !resource.closed;
    }
  }
;
}
