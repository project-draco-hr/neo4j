{
  this.release.set(true);
  latch.release();
  try {
    released.await();
    Thread runner;
    do {
      runner=this.runner;
    }
 while (runner == null);
    runner.join();
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
}
