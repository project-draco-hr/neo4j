{
  int k=stackTrace.length;
  if (k == 0)   return null;
  StringBuilder out=new StringBuilder();
  out.append(stackTrace[0]);
  for (int x=1; x < k; ++x)   out.append("\n").append(stackTrace[x]);
  return out.toString();
}
