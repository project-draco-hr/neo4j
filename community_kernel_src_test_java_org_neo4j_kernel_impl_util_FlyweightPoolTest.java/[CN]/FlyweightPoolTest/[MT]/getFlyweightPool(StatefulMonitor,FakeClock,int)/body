{
  return new FlyweightPool<Object>(minSize,new FlyweightPool.CheckStrategy.TimeoutCheckStrategy(100,clock),stateMonitor){
    @Override protected Object create(){
      return new Object();
    }
  }
;
}
