{
  String haServer=config.get(HaConfig.CONFIG_KEY_SERVER);
  if (haServer == null) {
    InetAddress host=null;
    try {
      host=InetAddress.getLocalHost();
    }
 catch (    UnknownHostException hostBecomesNull) {
    }
    if (host == null) {
      throw new IllegalStateException("Could not auto configure host name, please supply " + HaConfig.CONFIG_KEY_SERVER);
    }
    haServer=host.getHostAddress() + ":" + CONFIG_DEFAULT_PORT;
  }
  return haServer;
}
