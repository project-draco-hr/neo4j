{
  try {
    RawPropertyData properties[]=persistenceManager.loadRelProperties((int)relationship.getId());
    return properties;
  }
 catch (  Exception e) {
    log.severe("Failed loading properties for relationship[" + relationship.getId() + "]");
    throw new RuntimeException(e);
  }
}
