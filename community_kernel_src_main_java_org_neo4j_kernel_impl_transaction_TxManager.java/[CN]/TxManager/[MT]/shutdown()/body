{
  if (txLog != null) {
    try {
      txLog.close();
    }
 catch (    IOException e) {
      log.logMessage("Unable to close tx log[" + txLog.getName() + "]");
    }
  }
  log.logMessage("TM shutting down",true);
}
