{
  GraphDatabaseService db=new GraphDatabaseFactory().newEmbeddedDatabaseBuilder("target/test-data/shell-db").setConfig(ShellSettings.remote_shell_enabled,GraphDatabaseSetting.TRUE).newGraphDatabase();
  System.out.println("ENTER to quit");
  System.in.read();
  db.shutdown();
}
