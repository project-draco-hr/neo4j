{
  return new IterableWrapper<Slave,SlaveContext>(new FilteringIterable<SlaveContext>(slaves.values(),AVAILABLE)){
    @Override protected Slave underlyingObjectToObject(    SlaveContext context){
      return context.slave;
    }
  }
;
}
