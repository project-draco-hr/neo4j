{
  Iterator<String> keys=oldRelationship.getPropertyKeys().iterator();
  while (keys.hasNext()) {
    String key=keys.next();
    newRelationship.setProperty(key,oldRelationship.getProperty(key));
  }
}
