{
  testCallEmpty(db,adminSubject,"CALL dbms.createUser('craig', '1234', true)");
  assertNotNull("User craig should exist",manager.getUser("craig"));
  testCallFail(db,adminSubject,"CALL dbms.createUser('craig', '1234', true)",QueryExecutionException.class,"The specified user already exists");
}
