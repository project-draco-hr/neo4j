{
  try {
    testSuccessfulSchema(subject);
    fail("Expected exception to be thrown");
  }
 catch (  AuthorizationViolationException e) {
    assertTrue("Exception should contain 'Schema operations are not allowed'",e.getMessage().contains("Schema operations are not allowed"));
  }
}
