{
  try {
    testSuccessfulRead(subject,count);
    fail("Expected exception to be thrown");
  }
 catch (  AuthorizationViolationException e) {
    assertTrue("Exception should contain 'Read operations are not allowed'",e.getMessage().contains("Read operations are not allowed"));
  }
}
