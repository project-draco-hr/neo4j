{
  try {
    testSuccessfulWriteAction(subject);
    fail("Expected exception to be thrown");
  }
 catch (  AuthorizationViolationException e) {
    assertTrue("Exception should contain 'Write operations are not allowed'",e.getMessage().contains("Write operations are not allowed"));
  }
}
