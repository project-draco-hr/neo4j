{
  List<RelationshipType> types=new ArrayList<RelationshipType>();
  for (  RelationshipType type : graphdb.getRelationshipTypes()) {
    types.add(type);
  }
  return new Walker(graphdb.getAllNodes(),types.toArray(new RelationshipType[types.size()]));
}
