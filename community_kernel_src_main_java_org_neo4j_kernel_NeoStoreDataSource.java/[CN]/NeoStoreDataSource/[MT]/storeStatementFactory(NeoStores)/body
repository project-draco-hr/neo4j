{
  final LockService lockService=FeatureToggles.flag(getClass(),"propertyReadLocks",false) ? this.lockService : NO_LOCK_SERVICE;
  return new Factory<StoreStatement>(){
    @Override public StoreStatement newInstance(){
      return new StoreStatement(neoStores,lockService);
    }
  }
;
}
