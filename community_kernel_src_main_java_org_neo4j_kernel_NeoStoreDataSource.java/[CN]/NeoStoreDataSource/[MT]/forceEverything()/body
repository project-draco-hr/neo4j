{
  indexingModule.indexingService().flushAll();
  indexingModule.labelScanStore().force();
  for (  IndexImplementation index : indexProviders.values()) {
    index.force();
  }
  neoStoreModule.neoStore().flush();
}
